---
phase: 22-engagement-and-visualization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/advisorContext.ts
autonomous: true
requirements: [PROT-05]
must_haves:
  truths:
    - "The AI Advisor context includes a per-supplement dose bucket (high/standard/low) for protocol supplements"
    - "A supplement with no personalDose is bucketed as 'standard'"
    - "A supplement whose personalDose >= 1.25x the DB default is bucketed 'high'; <= 0.75x is 'low'"
    - "A supplement with non-numeric dose units omits the dose bucket entirely (no NaN leaks)"
    - "The existing supplements: string[] field is preserved for backward compatibility"
  artifacts:
    - path: "src/lib/advisorContext.ts"
      provides: "supplementDetails field with dose bucketing in AdvisorContext"
      contains: "supplementDetails"
  key_links:
    - from: "src/lib/advisorContext.ts"
      to: "src/data/supplementTimings.ts"
      via: "SUPPLEMENT_DATABASE defaultDose lookup for ratio"
      pattern: "SUPPLEMENT_DATABASE"
    - from: "src/lib/advisorContext.ts assembleAdvisorContext"
      to: "AdvisorContext.supplementDetails"
      via: "context object assembly"
      pattern: "supplementDetails"
---

<objective>
Add personal-dose bucketing to the AI Advisor context. For each protocol supplement, compute a "high" / "standard" / "low" bucket from the ratio of the user's personalDose to the SUPPLEMENT_DATABASE defaultDose, and expose it via a new parallel `supplementDetails` field on AdvisorContext — preserving the existing `supplements: string[]` field unchanged.

Purpose: Satisfies PROT-05 — gives the Claude Edge Function dose context (bucketed, never raw, per pharmacist-liability decision) without breaking the existing prompt shape.
Output: `supplementDetails` field added to AdvisorContext type and populated in assembleAdvisorContext().
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-engagement-and-visualization/22-CONTEXT.md
@.planning/phases/22-engagement-and-visualization/22-RESEARCH.md
@.planning/phases/22-engagement-and-visualization/22-PATTERNS.md

<interfaces>
<!-- src/lib/advisorContext.ts current state -->
AdvisorContext interface (line ~26) has: supplements: string[]   // names only (D-09)
ProtocolItem already imported (line ~20); has: name, timing?, personalDose?, dose
Existing supplement block (lines ~195-204):
  const suppNames = protocolState?.supplements
    ? protocolState.supplements.map((s) => s.name)
    : [ ...(protocolState?.addedSupplements ?? []), ...(protocolState?.customSupplements ?? []).map((s) => s.name) ];
  const supplements = Array.from(new Set(suppNames));
context assembled at line ~228; `supplements` added at line ~234.

<!-- src/data/supplementTimings.ts -->
SUPPLEMENT_DATABASE: SupplementInfo[]; SupplementInfo has defaultDose: string

<!-- Edge Function (supabase/functions/ai-advisor/index.ts line 119) serializes the whole context via
     JSON.stringify(context, null, 2) — adding a parallel field is safe; it does not read `supplements` by key. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dose bucketing + supplementDetails field in advisorContext</name>
  <files>src/lib/advisorContext.ts</files>
  <read_first>
    - src/lib/advisorContext.ts (full file — focus: AdvisorContext interface lines ~26-42, ProtocolItem import line ~20, internal ProtocolState lines ~71-80, supplement block lines ~195-204, context assembly lines ~228-266)
    - src/data/supplementTimings.ts (confirm SUPPLEMENT_DATABASE export and SupplementInfo.defaultDose: string)
    - .planning/phases/22-engagement-and-visualization/22-PATTERNS.md (advisorContext.ts section — exact dose-bucket loop and type change; RESEARCH Pattern 5 + Pitfall 5/6)
  </read_first>
  <behavior>
    - Supplement with no personalDose → supplementDetails entry has doseBucket: 'standard'
    - personalDose '1000mg' vs defaultDose '500mg' (ratio 2.0) → doseBucket: 'high'
    - personalDose '250mg' vs defaultDose '500mg' (ratio 0.5) → doseBucket: 'low'
    - personalDose '500mg' vs defaultDose '500mg' (ratio 1.0) → doseBucket: 'standard'
    - defaultDose 'as directed' (parseFloat NaN) with a personalDose set → doseBucket omitted from that entry
    - context.supplements (string[] of unique names) still present and unchanged
  </behavior>
  <action>
    Add `import { SUPPLEMENT_DATABASE } from '../data/supplementTimings';` at the top (alongside existing imports). In the AdvisorContext interface (lines ~26-42), add a new OPTIONAL parallel field `supplementDetails?: Array<{ name: string; timing?: string; doseBucket?: 'high' | 'standard' | 'low' }>;` — do NOT change or remove the existing `supplements: string[]` field (RESEARCH Pitfall 6: keep backward compat; the Edge Function serializes the whole blob so a parallel field is additive and safe).

    In assembleAdvisorContext(), replace the existing `suppNames`/`supplements` block (lines ~195-204) with a version that builds `supplementDetails` first, then derives the backward-compat `supplements` string[] from it. For the new-schema branch (`protocolState?.supplements` present), map each `ProtocolItem` to `{ name, timing?, doseBucket? }`: look up `dbEntry = SUPPLEMENT_DATABASE.find(db => db.name.toLowerCase() === item.name.toLowerCase())`. Determine doseBucket per D-07: if `!item.personalDose` → `'standard'` (DB default is by definition standard); else if `dbEntry?.defaultDose` exists, `personal = parseFloat(item.personalDose)` and `standard = parseFloat(dbEntry.defaultDose)`, and ONLY if `!isNaN(personal) && !isNaN(standard) && standard > 0` compute `ratio = personal / standard` and set `ratio >= 1.25 ? 'high' : ratio <= 0.75 ? 'low' : 'standard'`; otherwise leave doseBucket undefined so it is omitted (RESEARCH Pitfall 5 — never emit NaN). Build the returned object spreading `timing` only when present and `doseBucket` only when defined. For the legacy fallback branch, map `addedSupplements`/`customSupplements` to `{ name }` objects (no doseBucket). Then derive `const supplements = Array.from(new Set(supplementDetails.map(s => s.name)));` to keep the existing string[] shape.

    In the context object assembly (line ~228), add `supplementDetails,` alongside the existing `supplements,` line (line ~234). Ensure the empty-state context path (line ~266, `supplements: []`) also sets `supplementDetails: []` so the type is satisfied on the no-data branch.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "advisorContext.ts" | grep -qx 0 && echo "OK: advisorContext type-clean"</automated>
  </verify>
  <acceptance_criteria>
    - advisorContext.ts contains `import { SUPPLEMENT_DATABASE } from '../data/supplementTimings'`
    - advisorContext.ts AdvisorContext interface contains `supplementDetails?:` with a `doseBucket?: 'high' | 'standard' | 'low'` member
    - advisorContext.ts still contains `supplements: string[]` in the interface (not removed)
    - advisorContext.ts contains `ratio >= 1.25 ? 'high' : ratio <= 0.75 ? 'low' : 'standard'`
    - advisorContext.ts contains the guard `!isNaN(personal) && !isNaN(standard) && standard > 0`
    - advisorContext.ts context assembly contains `supplementDetails,`
    - The no-data context branch sets `supplementDetails: []`
    - `npx tsc --noEmit` reports no errors originating in advisorContext.ts
  </acceptance_criteria>
  <done>supplementDetails with dose buckets is populated and added to the context; supplements: string[] is preserved; NaN never leaks; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| advisorContext → Supabase Edge Function → Claude | Anonymized health context leaves the device for AI processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06 | Information Disclosure | Raw personal dose strings leaving the device | mitigate | Only the bucketed value (high/standard/low) is added — raw `personalDose` string is never placed in supplementDetails (pharmacist-liability decision, REQUIREMENTS Out of Scope) |
| T-22-07 | Tampering | Non-numeric/NaN dose ratios reaching Claude | mitigate | `!isNaN && standard > 0` guard; doseBucket omitted when parse fails — no `NaN`/`undefined` literal serialized |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — SUPPLEMENT_DATABASE is existing local data |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 project-wide
- Source audit: `grep -n "personalDose" src/lib/advisorContext.ts` shows personalDose used only inside parseFloat for ratio math, never assigned into the returned context object
</verification>

<success_criteria>
- PROT-05: AI Advisor context includes each supplement's personal dose bucketed as high/standard/low relative to the DB recommended range, with non-numeric doses gracefully omitted and raw dose never transmitted
</success_criteria>

<output>
Create `.planning/phases/22-engagement-and-visualization/22-03-SUMMARY.md` when done
</output>
