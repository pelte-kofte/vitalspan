---
phase: 22-engagement-and-visualization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/BiomarkerDetailScreen.tsx
autonomous: true
requirements: [TRND-01, TRND-02, TRND-03, DLIM-01, DLIM-02]
must_haves:
  truths:
    - "BiomarkerDetailScreen shows a sparkline LineChart of lab values for the selected biomarker"
    - "A 30D | 90D | 365D segmented pill above the chart filters the charted entries"
    - "With fewer than 2 data points in the window, a placeholder 'Add at least 2 entries to see your trend.' renders instead of the chart"
    - "The biomarker's optimal range (optMin..optMax) renders as a translucent green band behind the line"
    - "A non-premium user with >30 days of history sees only the last 30 days in chart and history list"
    - "A non-premium user with hidden entries sees an upgrade banner with the hidden count that navigates to Paywall"
    - "A premium user sees full history with no upgrade banner"
  artifacts:
    - path: "src/screens/BiomarkerDetailScreen.tsx"
      provides: "Chart + range band + time-window toggle + premium gate + upgrade banner"
      contains: "timeWindow"
  key_links:
    - from: "src/screens/BiomarkerDetailScreen.tsx"
      to: "react-native-chart-kit LineChart"
      via: "chart render of windowed values"
      pattern: "LineChart"
    - from: "src/screens/BiomarkerDetailScreen.tsx"
      to: "src/context/PremiumContext.tsx"
      via: "usePremiumContext isPremium gate"
      pattern: "usePremiumContext"
    - from: "src/screens/BiomarkerDetailScreen.tsx"
      to: "Paywall route"
      via: "nav.navigate('Paywall') from upgrade banner"
      pattern: "nav.navigate\\('Paywall'\\)"
---

<objective>
Add a biomarker trend visualization to BiomarkerDetailScreen's detail branch: a 30/90/365-day segmented pill, a react-native-chart-kit LineChart of lab values with an SVG optimal-range band behind it, a <2-point placeholder, and a free-tier 30-day history cap with a conditional upgrade banner.

Purpose: Satisfies TRND-01/02/03 (trend chart + toggle + range band) and DLIM-01/02 (non-premium 30-day cap + upgrade banner) within the existing detail view — no new screens.
Output: All chart, toggle, range band, premium-gate, and banner logic inside the `if (selectedId)` branch of BiomarkerDetailScreen.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-engagement-and-visualization/22-CONTEXT.md
@.planning/phases/22-engagement-and-visualization/22-RESEARCH.md
@.planning/phases/22-engagement-and-visualization/22-PATTERNS.md

@src/screens/ExerciseDetailScreen.tsx
@src/screens/ExerciseScreen.tsx
@src/context/PremiumContext.tsx

<interfaces>
<!-- BiomarkerDetailScreen detail branch (lines ~114-270): bm = biomarkers.find(b => b.id === selectedId) -->
<!-- bm.optMin / bm.optMax are numbers; history = historyFor(selectedId): StoredEntry[] sorted date DESC -->
StoredEntry { id: string; biomarkerId: string; value: number; date: string /* YYYY-MM-DD */; ... }
<!-- nav available as: const nav = useNavigation<Nav>();  Paywall route confirmed in AppNavigator (lines 47,217) -->
<!-- Insert chart/toggle/band BETWEEN insightCard (line ~204) and <Text style={s.sectionLabel}>History</Text> (line ~209) -->
<!-- History list maps `history` at line ~225 — switch to the premium-filtered array -->

<!-- PremiumContext: import { usePremiumContext } from '../context/PremiumContext'; const { isPremium } = usePremiumContext(); -->

<!-- Segmented pill style (copy from ExerciseScreen lines 757-775): segmentedControl, segment, segmentActive, segmentTxt, segmentTxtActive -->
<!-- LineChart chartConfig (copy from ExerciseDetailScreen lines 209-222): all three background keys = Colors.surface -->
<!-- SVG absoluteFill overlay pattern: LongevityScoreScreen line ~625 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Premium gate + time-window state + filtered/charted history derivation</name>
  <files>src/screens/BiomarkerDetailScreen.tsx</files>
  <read_first>
    - src/screens/BiomarkerDetailScreen.tsx (full file — focus: component state lines ~40-49, historyFor line ~109, detail branch start line ~114, `const history = historyFor(selectedId)` line ~122, history list map line ~225)
    - src/context/PremiumContext.tsx (confirm hook name usePremiumContext and isPremium field)
    - .planning/phases/22-engagement-and-visualization/22-PATTERNS.md (BiomarkerDetailScreen sub-tasks A, B, C — exact imports, state, filter derivation)
  </read_first>
  <behavior>
    - isPremium true → premiumFilteredHistory equals full history; hiddenCount 0
    - isPremium false → premiumFilteredHistory keeps only entries with date >= (today - 30 days); hiddenCount = history.length - that count
    - chartHistory = premiumFilteredHistory further filtered to the selected window (30/90/365 days), premium filter applied FIRST then window on top
    - chartValues / chartLabels are in chronological (ascending date) order
  </behavior>
  <action>
    Add imports: `Dimensions` and `StyleSheet` from 'react-native' (add only if not already imported — confirm in read_first), `LineChart` from 'react-native-chart-kit', `Svg, { Rect }` from 'react-native-svg', and `usePremiumContext` from '../context/PremiumContext'. `Haptics` is already imported — reuse it. At the top of the component body add `const { isPremium } = usePremiumContext();` and `const [timeWindow, setTimeWindow] = useState<TimeWindow>('90D');` with a `type TimeWindow = '30D' | '90D' | '365D';`. Default 90D is the Claude's-Discretion choice from D-05 (covers a typical quarterly lab cycle).

    In the detail branch, after `const history = historyFor(selectedId)` (line ~122): compute `cutoff30ISO` = (new Date, minus 30 days).toISOString().slice(0,10). Build `premiumFilteredHistory = isPremium ? history : history.filter(e => e.date >= cutoff30ISO)` per D-06 (premium cap applied FIRST). Compute `hiddenCount = history.length - premiumFilteredHistory.length`. Then map timeWindow to windowDays (30/90/365), compute `windowCutoffISO`, and `chartHistory = premiumFilteredHistory.filter(e => e.date >= windowCutoffISO)` — window filter applied on top of the premium filter, never the reverse (RESEARCH anti-pattern). Derive `chartValues = [...chartHistory].reverse().map(e => e.value)` and `chartLabels = [...chartHistory].reverse().map(e => { const d = new Date(e.date); return \`${d.getMonth()+1}/${d.getDate()}\`; })` (reverse because history is date-DESC and the chart needs ascending). Keep these derivations co-located so Task 2 can consume them. Do NOT yet render the chart — that is Task 2. Replace the History list's `history.map(...)` at line ~225 with `premiumFilteredHistory.map(...)` so the list also respects the 30-day cap, and update the adjacent `history.length` references in that block to `premiumFilteredHistory.length`.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "BiomarkerDetailScreen.tsx" | grep -qx 0 && echo "OK: type-clean after task 1"</automated>
  </verify>
  <acceptance_criteria>
    - BiomarkerDetailScreen.tsx contains `usePremiumContext` and `const { isPremium }`
    - BiomarkerDetailScreen.tsx contains `type TimeWindow = '30D' | '90D' | '365D'` and `useState<TimeWindow>('90D')`
    - BiomarkerDetailScreen.tsx contains `premiumFilteredHistory` and `hiddenCount`
    - BiomarkerDetailScreen.tsx contains `chartHistory`, `chartValues`, and `chartLabels`
    - History list renders from `premiumFilteredHistory` (grep shows `premiumFilteredHistory.map`)
    - `npx tsc --noEmit` reports no errors originating in BiomarkerDetailScreen.tsx
  </acceptance_criteria>
  <done>Premium gate, time-window state, and the premium-then-window filtered arrays exist; the history list reflects the 30-day cap for non-premium users.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Segmented pill + LineChart with SVG range band + placeholder + upgrade banner</name>
  <files>src/screens/BiomarkerDetailScreen.tsx</files>
  <read_first>
    - src/screens/BiomarkerDetailScreen.tsx (full file — focus: insightCard line ~204, sectionLabel History line ~209, StyleSheet `s` starting line ~399)
    - src/screens/ExerciseDetailScreen.tsx (LineChart + chartConfig + nonZeroCount>=2 guard, lines ~198-231 — copy chartConfig with all three background keys = Colors.surface)
    - src/screens/ExerciseScreen.tsx (segmented control render lines ~231-245 and styles lines ~757-775)
    - .planning/phases/22-engagement-and-visualization/22-PATTERNS.md (BiomarkerDetailScreen sub-tasks D, F — exact chart JSX, coordinate math, and styles; and RESEARCH Pitfalls 3 & 4)
  </read_first>
  <behavior>
    - With chartValues.length >= 2 the LineChart renders; with < 2 the placeholder text renders instead (no chart)
    - The SVG Rect range band is positioned so higher optMax maps to a smaller y pixel (chart y-axis is inverted)
    - The chartConfig sets backgroundColor, backgroundGradientFrom, and backgroundGradientTo all to Colors.surface so the band is not obscured
    - Upgrade banner renders only when !isPremium && hiddenCount > 0 and tapping it calls nav.navigate('Paywall')
  </behavior>
  <action>
    Add module-level constants before the component: `CHART_HEIGHT = 160`, `CHART_TOP_PAD = 16`, `CHART_BOTTOM_PAD = 32`, and two rgba helper functions `PRIMARY_RGBA = (o:number) => \`rgba(45, 106, 79, ${o})\`` and `SURFACE_MUTED_RGBA = (o:number) => \`rgba(107, 107, 100, ${o})\`` (these RGBA literals mirror Colors.primary and a muted label color for chart-kit's color callbacks, which require rgba functions not hex tokens).

    Between the insightCard (line ~204) and the History sectionLabel (line ~209), insert: (1) the segmented pill `<View style={s.segmentedControl}>` mapping over `(['30D','90D','365D'] as TimeWindow[])`, each a TouchableOpacity styled `[s.segment, timeWindow === w && s.segmentActive]` with onPress `() => { setTimeWindow(w); Haptics.selectionAsync().catch(() => null); }` and inner Text `[s.segmentTxt, timeWindow === w && s.segmentTxtActive]`. (2) An IIFE that computes `chartWidth = Dimensions.get('window').width - Spacing.base * 2 - Spacing.md * 2`; if `chartValues.length < 2` return the placeholder `<View style={s.chartPlaceholder}><Text style={s.chartPlaceholderTxt}>Add at least 2 entries to see your trend.</Text></View>`. Otherwise compute the range-band coordinates per RESEARCH Pattern 2 / Pitfall 3: `plotH = CHART_HEIGHT - CHART_TOP_PAD - CHART_BOTTOM_PAD`, `dataMin/dataMax` from chartValues, `dataRange = dataMax - dataMin || 1`, `clampedOptMin = Math.max(bm.optMin, dataMin)`, `clampedOptMax = Math.min(bm.optMax, dataMax)`, `yTop = CHART_TOP_PAD + plotH * (1 - (clampedOptMax - dataMin) / dataRange)`, `yBot = CHART_TOP_PAD + plotH * (1 - (clampedOptMin - dataMin) / dataRange)`. Render a relative-positioned `<View style={{ width: chartWidth, height: CHART_HEIGHT, alignSelf: 'center' }}>` containing first an `<Svg width={chartWidth} height={CHART_HEIGHT} style={StyleSheet.absoluteFill}>` with a `<Rect x={0} y={yTop} width={chartWidth} height={Math.max(yBot - yTop, 0)} fill={Colors.status.optimalBg} fillOpacity={0.15} />` (theme green at 15% per D-04/specifics), then the `<LineChart>` on top with `data={{ labels: chartLabels, datasets: [{ data: chartValues }] }}`, width chartWidth, height CHART_HEIGHT, the chartConfig copied from ExerciseDetailScreen (all three background keys set to Colors.surface — RESEARCH Pitfall 4), `bezier`, `withShadow={false}`, `withOuterLines={false}`, style `{ borderRadius: Radius.lg }`. (3) The upgrade banner: `{!isPremium && hiddenCount > 0 && (...)}` rendering a TouchableOpacity `style={s.upgradeBanner}` `onPress={() => nav.navigate('Paywall')}` with a left View (lock emoji + Text `{hiddenCount} entries hidden — upgrade to see your full history.`) and a right View `s.upgradeCta` with Text "Upgrade".

    Add the styles from 22-PATTERNS.md sub-task F to the `s` StyleSheet: `segmentedControl`, `segment`, `segmentActive`, `segmentTxt`, `segmentTxtActive` (copied verbatim from ExerciseScreen), plus `chartPlaceholder`, `chartPlaceholderTxt`, `upgradeBanner`, `upgradeBannerLeft`, `upgradeBannerIcon`, `upgradeBannerTxt`, `upgradeCta`, `upgradeCtaTxt`. Use only theme tokens (Colors.*, Spacing.*, Radius.*, Typography.*) — no hardcoded hex or numeric spacing except the chart-kit padding constants and the `padding: 3` inside segmentedControl (which has no Spacing.* equivalent, matching ExerciseScreen).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "BiomarkerDetailScreen.tsx" | grep -qx 0 && echo "OK: type-clean after task 2"</automated>
  </verify>
  <acceptance_criteria>
    - BiomarkerDetailScreen.tsx contains `<LineChart` and `<Rect`
    - BiomarkerDetailScreen.tsx contains the literal `Add at least 2 entries to see your trend.`
    - BiomarkerDetailScreen.tsx contains `chartValues.length < 2` (the placeholder guard)
    - BiomarkerDetailScreen.tsx contains `fill={Colors.status.optimalBg}` and `fillOpacity={0.15}`
    - BiomarkerDetailScreen.tsx chartConfig sets `backgroundColor: Colors.surface`, `backgroundGradientFrom: Colors.surface`, `backgroundGradientTo: Colors.surface`
    - BiomarkerDetailScreen.tsx contains `!isPremium && hiddenCount > 0` and `nav.navigate('Paywall')`
    - BiomarkerDetailScreen.tsx StyleSheet contains `segmentedControl:`, `upgradeBanner:`, and `chartPlaceholder:`
    - `grep -v '^#' src/screens/BiomarkerDetailScreen.tsx | grep -cE "#[0-9A-Fa-f]{6}"` returns 0 (no hardcoded hex; rgba helpers and Colors tokens only)
    - `npx tsc --noEmit` reports no errors originating in BiomarkerDetailScreen.tsx
  </acceptance_criteria>
  <done>Chart with range band renders for >=2 points, placeholder for <2, segmented pill switches windows, and the upgrade banner appears only for gated non-premium users and routes to Paywall.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PremiumContext → BiomarkerDetailScreen | Subscription state from Adapty-backed context gates history visibility |
| AsyncStorage → BiomarkerDetailScreen | Locally-stored biomarker entries parsed into chart data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Elevation of Privilege | Free-tier 30-day data cap | mitigate | isPremium read ONLY from usePremiumContext (Adapty source of truth, v4.0 decision) — never from AsyncStorage which has no subscription data; filter applied before render |
| T-22-04 | Information Disclosure | Chart renders biomarker values (health PII) | accept | Data already local + already displayed in the existing history list; chart adds no new exposure surface, stays on-device |
| T-22-05 | Denial of Service | LineChart crash on single data point | mitigate | `chartValues.length < 2` placeholder guard before any LineChart render (RESEARCH anti-pattern) |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No package installs — react-native-chart-kit and react-native-svg already in package.json (RESEARCH Package Legitimacy Audit: no new packages) |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 project-wide
- Manual (non-premium, biomarker with >30 days history): chart shows last 30 days, banner shows hidden count, tapping banner opens Paywall
- Manual (biomarker with 1 entry): placeholder text shows, no chart crash
- Manual: 30D/90D/365D taps re-filter the chart; green band sits over the optimal range
</verification>

<success_criteria>
- TRND-01: sparkline chart with 30/90/365-day toggle present
- TRND-02: ≥2 points render chart; <2 shows placeholder
- TRND-03: optimal range band overlays the chart
- DLIM-01: non-premium limited to last 30 days (chart + list); premium sees all
- DLIM-02: upgrade banner shows hidden count and routes to Paywall
</success_criteria>

<output>
Create `.planning/phases/22-engagement-and-visualization/22-02-SUMMARY.md` when done
</output>
