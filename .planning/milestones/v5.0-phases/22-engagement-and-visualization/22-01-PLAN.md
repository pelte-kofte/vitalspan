---
phase: 22-engagement-and-visualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/protocol.ts
  - src/screens/ProtocolScreen.tsx
autonomous: true
requirements: [STRK-01, STRK-02, STRK-03]
must_haves:
  truths:
    - "Protocol screen shows the user's current consecutive-day streak below the progress pill"
    - "Protocol screen shows the all-time best streak in the same row"
    - "Completing all visible items, then opening the app the next day, increments the current streak"
    - "Missing an item before the next-day open resets the current streak to 0 while best is preserved"
    - "A day with zero visible items leaves the current streak unchanged (pause)"
  artifacts:
    - path: "src/types/protocol.ts"
      provides: "ProtocolState streak fields + EMPTY_PROTOCOL defaults"
      contains: "currentStreak"
    - path: "src/screens/ProtocolScreen.tsx"
      provides: "Streak evaluation in loadData + streak stat row UI"
      contains: "streakRow"
  key_links:
    - from: "src/screens/ProtocolScreen.tsx"
      to: "src/types/protocol.ts"
      via: "ProtocolState type import with streak fields"
      pattern: "currentStreak"
    - from: "src/screens/ProtocolScreen.tsx loadData()"
      to: "@vitalspan_protocol"
      via: "AsyncStorage.setItem write of streak fields before daily reset"
      pattern: "@vitalspan_protocol"
---

<objective>
Add adherence streak tracking to the Protocol screen: three optional fields on ProtocolState, streak evaluation slotted into the existing daily-reset block in ProtocolScreen.loadData(), and a two-stat row rendered below the existing progress pill.

Purpose: Satisfies STRK-01/02/03 — users see current and best streaks, with correct increment/reset/pause semantics, giving them a daily engagement loop.
Output: Extended ProtocolState type + EMPTY_PROTOCOL defaults; streak evaluation + persistence + UI in ProtocolScreen.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-engagement-and-visualization/22-CONTEXT.md
@.planning/phases/22-engagement-and-visualization/22-RESEARCH.md
@.planning/phases/22-engagement-and-visualization/22-PATTERNS.md

<interfaces>
<!-- Current ProtocolState (src/types/protocol.ts) — extend additively, do NOT remove fields -->
ProtocolState { supplements: ProtocolItem[]; medTimes: Record<string, TimeSlot>; hiddenMeds: string[]; taken: string[]; takenDate: string; }
EMPTY_PROTOCOL = { supplements: [], medTimes: {}, hiddenMeds: [], taken: [], takenDate: '' }

<!-- ProtocolScreen helpers already defined (reuse, do not redefine) -->
parseDoseCount(dose: string): number   // line ~63
doseId(name: string, n: number): string   // line ~79
visibleMeds = medications.filter(m => !protocol.hiddenMeds.includes(m))   // render-time, line ~714
totalItems / takenCount dose-ID logic   // lines ~770-785

<!-- Existing daily reset block in loadData() (lines ~589-601) — streak eval inserts BEFORE finalState -->
const today = new Date().toISOString().slice(0,10);
const finalState = { ...migrated, taken: migrated.takenDate === today ? migrated.taken : [], takenDate: today };
setProtocol(finalState);

<!-- profileRaw is loaded in the same Promise.all (line ~575) — parse medications from it for streak eval -->

<!-- Existing progress pill (lines ~791-800) — streak row goes immediately after the header </View> -->
progressTxt style (line ~1077): { fontSize: Typography.sizes.xs, fontWeight: '600', color: Colors.primary }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add streak fields to ProtocolState + EMPTY_PROTOCOL</name>
  <files>src/types/protocol.ts</files>
  <read_first>
    - src/types/protocol.ts (full file — see current ProtocolState interface lines ~48-59 and EMPTY_PROTOCOL lines ~79-85)
  </read_first>
  <behavior>
    - ProtocolState accepts objects with no streak fields (legacy data) without type error — fields are optional
    - ProtocolState accepts currentStreak/bestStreak as numbers and lastCompleteDate as string
    - EMPTY_PROTOCOL has currentStreak: 0, bestStreak: 0, lastCompleteDate: ''
  </behavior>
  <action>
    In the ProtocolState interface, after the `takenDate: string;` field, add three OPTIONAL fields: `currentStreak?: number` (consecutive days all visible items taken), `bestStreak?: number` (all-time high), and `lastCompleteDate?: string` (ISO YYYY-MM-DD of the last fully-completed day, '' when never completed). Per D-01, they MUST be optional (`?`) so old serialised AsyncStorage data without these keys deserialises cleanly with no migration step. In EMPTY_PROTOCOL, add `currentStreak: 0`, `bestStreak: 0`, `lastCompleteDate: ''`. Do not remove or rename any existing field. Do not touch the CustomSupplement interface (migration-detection only).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "protocol.ts" | grep -qx 0 && echo "OK: protocol.ts type-clean"</automated>
  </verify>
  <acceptance_criteria>
    - src/types/protocol.ts contains `currentStreak?: number`
    - src/types/protocol.ts contains `bestStreak?: number`
    - src/types/protocol.ts contains `lastCompleteDate?: string`
    - EMPTY_PROTOCOL contains `currentStreak: 0`, `bestStreak: 0`, and `lastCompleteDate: ''`
    - `supplements:` field still present in ProtocolState (no fields removed)
    - `npx tsc --noEmit` reports no errors originating in protocol.ts
  </acceptance_criteria>
  <done>ProtocolState has three optional streak fields; EMPTY_PROTOCOL sets defaults; tsc clean for this file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Streak evaluation in loadData() + streak stat row UI</name>
  <files>src/screens/ProtocolScreen.tsx</files>
  <read_first>
    - src/screens/ProtocolScreen.tsx (full file — focus: loadData daily reset block lines ~585-601, profileRaw load line ~575, parseDoseCount/doseId lines ~63-81, visibleMeds line ~714, totalItems/takenCount lines ~770-785, header render lines ~791-800, progressPill/progressTxt styles lines ~1072-1077)
    - src/types/protocol.ts (streak fields added in Task 1)
    - .planning/phases/22-engagement-and-visualization/22-PATTERNS.md (ProtocolScreen sub-tasks A & B — exact streak eval block and UI block)
  </read_first>
  <behavior>
    - Given takenDate is a real past date (not '' and not today) and every visible item ID is in taken[], on next load currentStreak increments by 1 and bestStreak = max(bestStreak, currentStreak), lastCompleteDate = takenDate
    - Given takenDate is a past date and NOT all visible items are in taken[], currentStreak resets to 0 (bestStreak unchanged)
    - Given visible item count is 0 for the elapsed day, currentStreak is unchanged (pause)
    - Given takenDate === '' (first launch), streak evaluation block does NOT run
    - The evaluated streak fields persist to @vitalspan_protocol before taken[] is wiped
    - The header renders "🔥 N-day streak" and either "Best: N days" (bestStreak>0) or "Start your streak today!" (currentStreak 0 and bestStreak 0)
  </behavior>
  <action>
    Insert a streak-evaluation block inside loadData() immediately BEFORE the existing `finalState` construction in the daily-reset path (the block that sets `taken: migrated.takenDate === today ? ... : []`). Gate the whole block with `if (migrated.takenDate !== '' && migrated.takenDate !== today)` — per RESEARCH Pitfall 1, the empty-string guard is mandatory or first launch wrongly fires the streak. Inside the block: parse medications from the already-loaded `profileRaw` (JSON.parse, default to `[]` when null), filter out `migrated.hiddenMeds` to get the streak-eligible meds. Build `visibleItemIds` exactly mirroring the totalItems/takenCount logic: spread the eligible med names, then for each supplement in `migrated.supplements` use `parseDoseCount(s.personalDose ?? s.dose)` — count 1 yields `[s.id]`, count N yields `doseId(s.name, i)` for i in 0..N-1. Reuse the existing `parseDoseCount` and `doseId` helpers — do NOT redefine them. If `visibleItemIds.length === 0`, do nothing (pause). Otherwise compute `allTaken = visibleItemIds.every(id => new Set(migrated.taken).has(id))`; if allTaken set `migrated.currentStreak = (migrated.currentStreak ?? 0) + 1`, `migrated.bestStreak = Math.max(migrated.bestStreak ?? 0, migrated.currentStreak)`, `migrated.lastCompleteDate = migrated.takenDate`; else set `migrated.currentStreak = 0`. After mutating, persist the streak fields to `@vitalspan_protocol` via AsyncStorage.setItem with the post-reset shape (`{ ...migrated, taken: [], takenDate: today }`) so the streak survives even though finalState then wipes taken[]. The existing finalState construction and setProtocol call remain unchanged below.

    For the UI: immediately after the closing `</View>` of the header block (the View containing the heading + progressPill), add a streak stat row `<View style={s.streakRow}>`. Left Text: `{'🔥'} {protocol.currentStreak ?? 0}-day streak` styled `[s.streakTxt, (protocol.currentStreak ?? 0) > 0 ? s.streakActive : s.streakMuted]`. Right side: if `(protocol.bestStreak ?? 0) > 0` render `<Text style={s.streakBest}>Best: {protocol.bestStreak} days</Text>`; else if `(protocol.currentStreak ?? 0) === 0` render `<Text style={s.streakHint}>Start your streak today!</Text>`; else null. Add styles to the `s` StyleSheet near progressTxt (line ~1077): `streakRow` (flexDirection row, alignItems center, justifyContent space-between, paddingHorizontal Spacing.base, paddingBottom Spacing.sm), `streakTxt` (fontSize Typography.sizes.xs, fontWeight '600'), `streakActive` (color Colors.status.optimalText), `streakMuted` (color Colors.onSurfaceMuted), `streakBest` (fontSize Typography.sizes.xs, color Colors.onSurfaceMuted), `streakHint` (fontSize Typography.sizes.xs, color Colors.onSurfaceMuted, fontStyle italic). Use only theme tokens — no hardcoded hex or numeric spacing.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "ProtocolScreen.tsx" | grep -qx 0 && echo "OK: ProtocolScreen type-clean"</automated>
  </verify>
  <acceptance_criteria>
    - ProtocolScreen.tsx contains the guard `migrated.takenDate !== '' && migrated.takenDate !== today` in loadData
    - ProtocolScreen.tsx contains `migrated.currentStreak = (migrated.currentStreak ?? 0) + 1`
    - ProtocolScreen.tsx contains `Math.max(migrated.bestStreak ?? 0, migrated.currentStreak)`
    - ProtocolScreen.tsx contains an `AsyncStorage.setItem('@vitalspan_protocol'` call inside the streak-evaluation block
    - ProtocolScreen.tsx contains `s.streakRow` in JSX and `streakRow:` in the StyleSheet
    - ProtocolScreen.tsx contains the literal `-day streak` and `Start your streak today!`
    - The streak eval reuses `parseDoseCount` and `doseId` (no new `function parseDoseCount` / `function doseId` definitions added)
    - `grep -v '^#' src/screens/ProtocolScreen.tsx | grep -cE "#[0-9A-Fa-f]{6}"` returns 0 (no new hardcoded hex in modified file)
    - `npx tsc --noEmit` reports no errors originating in ProtocolScreen.tsx
  </acceptance_criteria>
  <done>Streak increments/resets/pauses correctly at daily reset, persists to AsyncStorage, and renders as a stat row below the progress pill using theme tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage → ProtocolScreen | Locally-stored, user-owned protocol JSON parsed on load (no remote/untrusted source) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | Malformed/legacy @vitalspan_protocol JSON missing streak keys | mitigate | Streak fields optional + `?? 0` / `?? ''` defaults at every read; existing JSON.parse already wrapped in try/catch in loadData |
| T-22-02 | Denial of Service | Streak eval runs on every screen focus | accept | O(visibleItems) array work on already-loaded local data; negligible cost, no network/disk loop |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — no supply-chain surface |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 project-wide (no errors in protocol.ts or ProtocolScreen.tsx)
- Manual: complete all items today, change device date forward one day, reopen app → "🔥 1-day streak" appears
- Manual: leave an item untaken, advance a day, reopen → "0-day streak" with Best preserved
</verification>

<success_criteria>
- STRK-01: current streak visible on Protocol screen
- STRK-02: best streak visible on Protocol screen
- STRK-03: increments only when all visible items taken; missed day resets to 0; zero-item day pauses
</success_criteria>

<output>
Create `.planning/phases/22-engagement-and-visualization/22-01-SUMMARY.md` when done
</output>
