---
phase: "23"
plan: "04"
type: execute
wave: 3
depends_on:
  - "23-02"
  - "23-03"
autonomous: false
files_modified: []
requirements:
  - PROD-02

must_haves:
  truths:
    - "eas build --platform ios --profile production completes without entitlement errors"
    - "The production build installs on a physical device via TestFlight"
    - "The full user flow (onboarding → protocol → notifications → AI Advisor) passes on device"
    - "No Supabase URL or anon key appears in source files"
  artifacts: []
  key_links:
    - from: "eas build"
      to: "app.json ios.entitlements aps-environment: production"
      via: "EAS syncs entitlement to Apple Developer Portal provisioning profile"
      pattern: "aps-environment"

user_setup:
  - service: apple-developer-eas
    why: "EAS production build requires Apple distribution certificate and provisioning profile with push notification entitlement"
    env_vars: []
    dashboard_config:
      - task: "Run eas credentials to verify/generate distribution certificate and App Store provisioning profile"
        location: "Terminal — eas credentials command (interactive)"
      - task: "Run eas build --platform ios --profile production"
        location: "Terminal — initiates cloud build on EAS servers"
      - task: "Submit to App Store Connect via eas submit --platform ios (enter appleId and ascAppId interactively)"
        location: "Terminal — eas submit command (interactive)"
      - task: "Install the TestFlight build on a physical device and run the full flow: onboarding → protocol → toggle a reminder → AI Advisor"
        location: "TestFlight app on device"
---

<objective>
Run the EAS production build, verify the build succeeds without entitlement errors, submit to App Store Connect, install via TestFlight on a physical device, and run the full user flow verification.

Purpose: PROD-02 delivers the milestone — a working production build on device closes v5.0. This plan also performs the final source audit (no secrets in source) and tsc clean-build verification before initiating the build.

Output: A TestFlight-distributed production build that passes the full flow test on a physical iOS device.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/23-notifications-and-production-build/23-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/23-notifications-and-production-build/23-RESEARCH.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/23-notifications-and-production-build/23-01-SUMMARY.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/23-notifications-and-production-build/23-02-SUMMARY.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/23-notifications-and-production-build/23-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-build audit — tsc + secret grep + entitlement verification</name>
  <files></files>
  <read_first>
    - /Users/bekircemkusdemir/Downloads/vitalspan/app.json — verify aps-environment: production is present before initiating build
    - /Users/bekircemkusdemir/Downloads/vitalspan/eas.json — confirm production profile has autoIncrement: true and env vars; no changes needed per RESEARCH.md
  </read_first>
  <action>
Run three pre-build verification checks in order:

**Check 1 — TypeScript clean build:**
```
npx tsc --noEmit
```
Must exit 0. If there are errors, fix them before proceeding. Do not initiate eas build with TypeScript errors.

**Check 2 — Secret audit:**
```
grep -r "supabase.co\|eyJhbGci" src/ App.tsx --include="*.ts" --include="*.tsx"
```
Must return 0 matches. The Supabase URL and anon key must appear ONLY in .env (not committed) and be read via `process.env.EXPO_PUBLIC_*`. If any match is found, report the file location and halt — do not proceed to eas build.

**Check 3 — Entitlement verification:**
```
node -e "const j=require('./app.json'); console.log('aps-env:', j.expo.ios.entitlements['aps-environment']); console.log('hk:', j.expo.ios.entitlements['com.apple.developer.healthkit'])"
```
Must output `aps-env: production` and `hk: true`. If aps-env is missing or shows "development", fix app.json before proceeding.

Report all three check results before marking this task done.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit && echo "TSC_PASS" && grep -rn "supabase.co\|eyJhbGci" src/ App.tsx --include="*.ts" --include="*.tsx" | wc -l | xargs -I{} sh -c 'if [ "{}" = "0" ]; then echo "SECRET_AUDIT_PASS"; else echo "SECRET_AUDIT_FAIL: {} matches found"; fi'</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0 (no TypeScript errors project-wide)
    - Secret grep returns 0 matches across src/ and App.tsx for "supabase.co" and "eyJhbGci"
    - node -e check outputs `aps-env: production` (string, not undefined or "development")
    - node -e check outputs `hk: true` (HealthKit entitlement preserved)
  </acceptance_criteria>
  <done>All three pre-build checks pass: tsc clean, no secrets in source, aps-environment: production confirmed.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: EAS production build + TestFlight submission + device verification</name>
  <what-built>Pre-build audit passed (Task 1). All notification infrastructure is in place: expo-notifications installed, app.json has aps-environment: production, App.tsx reschedules on launch, ProtocolScreen has the 4-slot Reminders section.</what-built>
  <how-to-verify>
Run these commands in order:

**Step 1 — Verify credentials (run once if not already provisioned):**
```
eas credentials
```
Select iOS → Production. Confirm a distribution certificate and App Store provisioning profile exist. If not, let EAS generate them (it will prompt for Apple ID and team).

**Step 2 — Initiate production build:**
```
eas build --platform ios --profile production
```
Wait for the build to complete on EAS servers (typically 15-30 min). The build URL will be printed.

Expected: Build status shows "Finished" with no entitlement errors in the log. If the log shows "aps-environment entitlement not found" or a codesign error, check app.json ios.entitlements and re-run.

**Step 3 — Submit to App Store Connect:**
```
eas submit --platform ios
```
Enter your Apple ID and ASC App ID when prompted (interactive). The build from Step 2 will be auto-selected.

**Step 4 — TestFlight install and full flow verification:**
1. Open TestFlight on your physical iPhone and install the new build
2. Launch the app fresh (or delete + reinstall to simulate a clean install)
3. Complete onboarding (or navigate directly to Protocol tab if already onboarded)
4. Open Protocol tab → Reminders section is visible below the streak row
5. Toggle "Morning" ON → iOS permission dialog appears → Grant → time chip appears → slot is enabled
6. Tap the time chip → DateTimePicker (spinner wheel) opens → change to a different time → picker closes → new time shown
7. Force-quit the app and relaunch → notification persists (check with `Notifications.getAllScheduledNotificationsAsync` or wait for the notification to fire)
8. Navigate to AI Advisor → report generates without error
9. Toggle Morning OFF → notification is cancelled (no duplicate banners after 24h)

All 9 steps must pass without crash or broken UI.
  </how-to-verify>
  <resume-signal>Type "build-passed" if the EAS build succeeded, TestFlight installed correctly, and the full flow passed. Describe any issues encountered.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EAS build environment → Apple Developer Portal | EAS syncs aps-environment entitlement to provisioning profile; misconfiguration causes runtime push failures |
| Source code → EAS build | Secrets must not be in source — only EXPO_PUBLIC_* env vars from EAS secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-10 | Information Disclosure | Supabase URL / anon key in source code | mitigate | Pre-build secret audit grep (Task 1 Check 2) blocks build if any match found; env vars sourced from EAS secrets via $EXPO_PUBLIC_* pattern already established in all previous phases |
| T-23-11 | Tampering | aps-environment entitlement value | mitigate | Pre-build entitlement check (Task 1 Check 3) confirms value is "production" before eas build runs; "development" value would cause TestFlight push failures |
| T-23-12 | Elevation of Privilege | Distribution certificate scope | accept | EAS manages certificate rotation; developer never commits .p12 or .p8 files; certificate is scoped to the Vitalspan bundle ID only |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; eas CLI is already installed at version 20.0.0 and pre-audited |
</threat_model>

<verification>
Phase 23 is complete when:
1. `npx tsc --noEmit` exits 0 (Task 1)
2. Secret grep returns 0 matches (Task 1)
3. EAS production build status is "Finished" with no entitlement errors (Task 2)
4. TestFlight build installs on physical device (Task 2)
5. Full flow passes: onboarding → protocol → Reminders toggle (permission + time picker) → AI Advisor (Task 2)
6. app.json has aps-environment: production and expo-notifications plugin (Plan 01)
7. App.tsx has setNotificationHandler (module scope) and reschedule useEffect (Plan 02)
8. ProtocolScreen Reminders section has 4 slots, working toggle, time picker, inline permission denial (Plan 03)
</verification>

<success_criteria>
- EAS production build succeeds: `eas build --platform ios --profile production` status = Finished with no entitlement errors
- TestFlight build installs and launches on physical device
- Full flow (onboarding → protocol → notifications → AI Advisor) passes without crash
- Notifications reschedule automatically on relaunch after update (launch → cancel-all → reschedule from prefs)
- No secrets in source (grep confirms)
- tsc --noEmit exits 0
</success_criteria>

<output>
Create `.planning/phases/23-notifications-and-production-build/23-04-SUMMARY.md` when done
</output>
