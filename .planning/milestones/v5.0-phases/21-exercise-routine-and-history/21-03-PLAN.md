---
phase: 21-exercise-routine-and-history
plan: 03
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - src/components/SwipeableLogRow.tsx
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03

must_haves:
  truths:
    - "History log entries display the full date (day + month + year) instead of just the date string"
    - "Tapping a log row body calls onEdit(log) to open the edit sheet"
    - "Swiping left still triggers onDelete as before"
    - "onEdit prop is optional so existing callsites without onEdit still compile"
    - "tsc --noEmit exits 0"
  artifacts:
    - path: "src/components/SwipeableLogRow.tsx"
      provides: "onEdit prop + full-date display + date-read-only behavior on row"
      contains: "onEdit"
  key_links:
    - from: "SwipeableLogRow row body"
      to: "onEdit(log)"
      via: "TouchableOpacity wrapping Animated.View row content"
      pattern: "onEdit"
---

<objective>
Add `onEdit` prop to SwipeableLogRow and upgrade date display from raw ISO string to full human-readable date.

Purpose: HIST-01 requires full dates visible on every log row. HIST-02 requires an edit entry point — SwipeableLogRow is the visible row; tapping the body opens the edit sheet (the edit sheet itself is built in Plan 21-04). HIST-03 (delete) is already implemented; this plan wires the edit path.
Output: Updated `SwipeableLogRow.tsx` with full-date rendering, `onEdit` prop, and touch on row body that invokes it.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/21-exercise-routine-and-history/21-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/21-exercise-routine-and-history/21-01-SUMMARY.md

<interfaces>
<!-- Current SwipeableLogRowProps (src/components/SwipeableLogRow.tsx lines 22-26) -->
interface SwipeableLogRowProps {
  log: ExerciseLogEntry;
  onDelete: (id: string) => void;
  showBorder: boolean;
}

<!-- After this plan, props become: -->
interface SwipeableLogRowProps {
  log: ExerciseLogEntry;
  onDelete: (id: string) => void;
  onEdit?: (log: ExerciseLogEntry) => void;   // optional — callers without onEdit still compile
  showBorder: boolean;
}

<!-- Current date display (lines 71-75): -->
<Text style={s.meta}>
  {log.category}
  {log.sets ? ` · ${log.sets}×${log.reps}` : ''}
  {log.durationMin ? ` · ${log.durationMin}min` : ''}
</Text>

<!-- Current row body structure: -->
<GestureDetector gesture={pan}>
  <Animated.View style={[s.row, rowStyle]}>
    <View style={s.left}>
      <Text style={s.name}>{log.exerciseName}</Text>
      <Text style={s.meta}>...</Text>
    </View>
    <View style={[s.dot, { backgroundColor: dotColor }]} />
  </Animated.View>
</GestureDetector>

<!-- log.date is an ISO date string "YYYY-MM-DD" -->
<!-- Full-date format target: "16 Jun 2026" (day month-abbrev year) -->
<!-- Date parsing: new Date(log.date + 'T00:00:00') to avoid UTC-offset shifting -->
<!-- Formatter: date.toLocaleDateString('en-GB', { day: 'numeric', month: 'short', year: 'numeric' }) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add onEdit prop, full-date display, and tap-to-edit to SwipeableLogRow</name>
  <files>src/components/SwipeableLogRow.tsx</files>
  <read_first>
    - src/components/SwipeableLogRow.tsx — read fully; understand pan gesture, rowStyle, deleteZone, current JSX tree and all StyleSheet entries
    - src/data/exercises.ts — confirm ExerciseLogEntry type (especially the `date` field: string "YYYY-MM-DD")
    - src/theme/index.ts — confirm Colors, Spacing, Typography tokens already imported in this file
  </read_first>
  <action>
    Make the following targeted changes to SwipeableLogRow.tsx:

    1. PROPS: Add `onEdit?: (log: ExerciseLogEntry) => void` to `SwipeableLogRowProps`. Mark it optional (?) so existing callsites in ExerciseScreen.tsx that don't pass onEdit still compile without changes.

    2. DATE HELPER: Add a pure helper function inside the file (above the component, not exported):
       `function formatLogDate(dateStr: string): string`
       Implementation: parse with `new Date(dateStr + 'T00:00:00')` (the +T00:00:00 prevents UTC offset shifting the day), then return `date.toLocaleDateString('en-GB', { day: 'numeric', month: 'short', year: 'numeric' })`. Example output: "16 Jun 2026".

    3. DISPLAY META: Update the meta Text to include the formatted date as the first element:
       Format: `{formatLogDate(log.date)} · {log.category}`
       Then append sets/reps: `{log.setsData && log.setsData.length > 0 ? ` · ${log.setsData.length}×${log.setsData[0]?.reps}${log.setsData[0]?.weightKg ? ` @ ${log.setsData[0].weightKg}kg` : ''}` : (log.sets ? ` · ${log.sets}×${log.reps}` : '')}`.
       Then duration: `{log.durationMin ? ` · ${log.durationMin}min` : ''}`.
       This ensures: new entries (setsData) show "3×10 @ 80kg", legacy entries (sets/reps) show "3×12", and all entries show the full date.

    4. TAP-TO-EDIT: Wrap the inner content of `<Animated.View style={[s.row, rowStyle]}>` in a `<TouchableOpacity activeOpacity={0.7} onPress={() => onEdit && onEdit(log)} style={{ flex: 1, flexDirection: 'row', alignItems: 'center', gap: Spacing.sm }}>`. Move the `<View style={s.left}>` and the intensity dot `<View>` inside this TouchableOpacity. The outer `<Animated.View style={[s.row, rowStyle]}>` loses its padding (padding moves to the inner TouchableOpacity to keep tap target full-width).

       Alternatively (simpler): Keep the existing Animated.View row with its padding. Add `onPress={() => onEdit?.(log)}` directly on the `<Animated.View>` by wrapping it in a `<TouchableOpacity>` that sits inside `<GestureDetector>`. The TouchableOpacity must not interfere with the pan gesture — use `activeOpacity={0.8}` and rely on the fact that pan requires horizontal movement while tap is a stationary touch. This is the pattern used in the app's existing code.

       Use the simpler approach: wrap the contents of `<Animated.View style={[s.row, rowStyle]}>` in a `<TouchableOpacity style={{ flexDirection: 'row', alignItems: 'center', flex: 1 }} onPress={() => { if (onEdit) onEdit(log); }} activeOpacity={0.75}>`. The outer Animated.View keeps its style but its padding moves to the inner TouchableOpacity (or keep on outer for hit area).

    5. STYLE: The `row` style's `flexDirection: 'row'` and `alignItems: 'center'` can move to the inner TouchableOpacity if needed. Keep `gap: Spacing.sm` and `padding: Spacing.md` on the TouchableOpacity. The outer `Animated.View` needs no padding or gap of its own to avoid double-spacing.

    6. No new style tokens needed — use existing `s.row`, `s.left`, `s.name`, `s.meta`, `s.dot` unchanged.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "onEdit" src/components/SwipeableLogRow.tsx` returns at least 3 matches (prop definition, prop in component signature, onPress invocation)
    - `grep "formatLogDate\|toLocaleDateString" src/components/SwipeableLogRow.tsx` returns a match
    - `grep "T00:00:00" src/components/SwipeableLogRow.tsx` returns a match (UTC-offset prevention)
    - `grep "setsData" src/components/SwipeableLogRow.tsx` returns a match (new setsData-aware meta display)
    - All callers of SwipeableLogRow in ExerciseScreen.tsx still compile without adding onEdit (prop is optional)
    - `npx tsc --noEmit` exits 0
    - Swipe-left-to-delete behavior (pan gesture, SWIPE_THRESHOLD, triggerDelete) is unchanged
  </acceptance_criteria>
  <done>SwipeableLogRow shows full dates, accepts onEdit prop, tap on row body calls onEdit; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| log.date from AsyncStorage | User-supplied date string; malformed dates must not crash the component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Tampering | formatLogDate parsing invalid date string | accept | `new Date(dateStr + 'T00:00:00')` on an invalid string produces Invalid Date; toLocaleDateString returns "Invalid Date" string — visually benign, not a crash |
| T-21-05 | Tampering | setsData[0] access on empty array | mitigate | guard with `log.setsData && log.setsData.length > 0` before accessing [0] |
</threat_model>

<verification>
- Run: `grep "onEdit" src/components/SwipeableLogRow.tsx` — must match
- Run: `grep "formatLogDate" src/components/SwipeableLogRow.tsx` — must match
- Run: `npx tsc --noEmit` — must exit 0
- Confirm ExerciseScreen.tsx still passes tsc (SwipeableLogRow callers don't need to change)
</verification>

<success_criteria>
- Full date "16 Jun 2026" appears in meta line of every log row
- setsData-aware secondary line: "3×10 @ 80kg" for weighted; "3×10" for bodyweight (no weightKg); legacy "3×12" for old entries
- Tapping the row body invokes onEdit(log) when prop is provided
- Swipe-left-to-delete still works (pan gesture unchanged)
- tsc --noEmit clean
</success_criteria>

<output>
Create `.planning/phases/21-exercise-routine-and-history/21-03-SUMMARY.md` when done
</output>
