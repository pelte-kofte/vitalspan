---
phase: 20-protocol-schema-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 20-02
files_modified:
  - src/screens/InteractionCheckerScreen.tsx
  - src/lib/advisorContext.ts
autonomous: true
requirements:
  - PROT-02
  - PROT-03

must_haves:
  truths:
    - "InteractionCheckerScreen auto-populates from supplements[] (not addedSupplements)"
    - "InteractionCheckerScreen includes both 'db' and 'manual' source items in its stack"
    - "advisorContext.ts assembles supplement names from protocol.supplements[].map(s => s.name)"
    - "advisorContext.ts no longer references addedSupplements or customSupplements"
    - "tsc --noEmit exits 0 with no errors across the full project"
  artifacts:
    - path: "src/screens/InteractionCheckerScreen.tsx"
      provides: "Updated autoPopulate reading from supplements[]"
      contains: "protocol.supplements"
    - path: "src/lib/advisorContext.ts"
      provides: "Updated supplement assembly reading from supplements[]"
      contains: "supplements.map"
  key_links:
    - from: "InteractionCheckerScreen autoPopulate"
      to: "AsyncStorage @vitalspan_protocol"
      via: "const protocol: { supplements?: ProtocolItem[] } = JSON.parse(protocolRaw)"
      pattern: "protocol.supplements"
    - from: "advisorContext assembleAdvisorContext"
      to: "AsyncStorage @vitalspan_protocol"
      via: "protocolState?.supplements ?? []"
      pattern: "protocolState.supplements"
---

<objective>
Update the two downstream consumers of ProtocolState to read from the new supplements[] schema. Both files currently read addedSupplements (string[]) from AsyncStorage — they must switch to supplements[].map(s => s.name).

Purpose: After Plans 01 and 02, AsyncStorage no longer stores addedSupplements. These consumers would read undefined and silently produce empty arrays. This plan makes them schema-correct.

Output: InteractionCheckerScreen.tsx and advisorContext.ts reading from supplements[]; project-wide tsc clean.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/20-protocol-schema-migration/20-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/20-protocol-schema-migration/20-02-SUMMARY.md

<interfaces>
<!-- Types available from src/types/protocol.ts -->

From src/types/protocol.ts:
```typescript
export type TimeSlot = 'morning' | 'afternoon' | 'evening' | 'night';

export interface ProtocolItem {
  id: string;
  name: string;
  dose: string;
  personalDose?: string;
  timing?: TimeSlot;
  source: 'db' | 'manual';
  addedAt: string;
}

export interface ProtocolState {
  supplements: ProtocolItem[];
  medTimes: Record<string, TimeSlot>;
  hiddenMeds: string[];
  taken: string[];
  takenDate: string;
}
```

Current InteractionCheckerScreen.tsx autoPopulate (lines 87–133):
```typescript
if (protocolRaw) {
  const protocol: { addedSupplements?: string[] } = JSON.parse(protocolRaw);
  for (const suppName of (protocol.addedSupplements ?? [])) {
    newItems.push({ name: suppName, type: 'supp' });
  }
}
```
Must become: read protocol.supplements (ProtocolItem[]), push item.name for each.

Current advisorContext.ts supplement assembly (lines 192–195):
```typescript
const addedSups = protocolState?.addedSupplements ?? [];
const customSups = (protocolState?.customSupplements ?? []).map((s) => s.name);
const supplements = Array.from(new Set([...addedSups, ...customSups]));
```
Must become: protocolState?.supplements?.map(s => s.name) ?? []
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update InteractionCheckerScreen to read from supplements[]</name>
  <files>src/screens/InteractionCheckerScreen.tsx</files>
  <read_first>
    - src/screens/InteractionCheckerScreen.tsx (full file — specifically the autoPopulate function and its ProtocolState type annotation)
    - src/types/protocol.ts (ProtocolItem, ProtocolState exports)
    - .planning/phases/20-protocol-schema-migration/20-CONTEXT.md (D-05 migration note, D-01 supplements[] shape)
  </read_first>
  <action>
    In src/screens/InteractionCheckerScreen.tsx, make the following targeted changes:

    1. ADD import at top of file: import { ProtocolItem } from '../types/protocol'

    2. In the autoPopulate function (inside useFocusEffect), locate the protocolRaw parsing block:
       Current: const protocol: { addedSupplements?: string[] } = JSON.parse(protocolRaw);
       Replace with: const protocol: { supplements?: ProtocolItem[]; addedSupplements?: string[] } = JSON.parse(protocolRaw);

    3. Replace the for..of loop that reads addedSupplements:
       Current:
         for (const suppName of (protocol.addedSupplements ?? [])) {
           newItems.push({ name: suppName, type: 'supp' });
         }
       Replace with:
         const suppItems = protocol.supplements ?? (protocol.addedSupplements ?? []).map(name => ({ name } as ProtocolItem));
         for (const item of suppItems) {
           newItems.push({ name: item.name, type: 'supp' });
         }

       This handles both: post-migration data (supplements[]) and any edge case where old schema is somehow still in storage (falls back to addedSupplements string[] mapped to name-only objects). The fallback is a safety net only — migration in ProtocolScreen will have already run by the time this screen is opened.

    4. No other changes to InteractionCheckerScreen.tsx — the medication auto-population from profileRaw, the MedicationSearch, INTERACTIONS logic, SAFE_COMBOS, and all styles remain unchanged.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | grep -i "InteractionChecker" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - src/screens/InteractionCheckerScreen.tsx imports ProtocolItem from '../types/protocol'
    - src/screens/InteractionCheckerScreen.tsx contains "protocol.supplements" in the autoPopulate function
    - src/screens/InteractionCheckerScreen.tsx does NOT contain "protocol.addedSupplements" as the primary read path (may remain only as fallback in the ?? expression)
    - npx tsc --noEmit reports no errors for InteractionCheckerScreen.tsx
  </acceptance_criteria>
  <done>InteractionCheckerScreen reads supplement names from protocol.supplements[]; backward-compatible fallback included; tsc clean</done>
</task>

<task type="auto">
  <name>Task 2: Update advisorContext.ts and run full tsc audit</name>
  <files>src/lib/advisorContext.ts</files>
  <read_first>
    - src/lib/advisorContext.ts (full file — specifically the internal ProtocolState interface and supplement assembly lines 192–195)
    - src/types/protocol.ts (ProtocolState, ProtocolItem exports)
    - .planning/phases/20-protocol-schema-migration/20-CONTEXT.md (D-04 states all consumers import from src/types/protocol.ts)
  </read_first>
  <action>
    In src/lib/advisorContext.ts, make the following targeted changes:

    1. ADD import at top of file (after existing imports): import { ProtocolItem } from '../types/protocol'
       Note: The file has its own internal ProtocolState interface for AsyncStorage parsing. This local interface must be updated to match the new schema.

    2. UPDATE the internal ProtocolState interface (currently at lines 70–76):
       Current:
         interface ProtocolState {
           medTimes: Record<string, string>;
           addedSupplements: string[];
           customSupplements: CustomSupplement[];
           taken: string[];
           takenDate: string;
         }
       Replace with:
         interface ProtocolState {
           supplements?: ProtocolItem[];
           addedSupplements?: string[];           // legacy — for backward compat during migration window
           customSupplements?: CustomSupplement[];  // legacy — for backward compat
           medTimes: Record<string, string>;
           taken: string[];
           takenDate: string;
           hiddenMeds?: string[];
         }

    3. UPDATE the supplement assembly block (currently lines 192–195):
       Current:
         const addedSups = protocolState?.addedSupplements ?? [];
         const customSups = (protocolState?.customSupplements ?? []).map((s) => s.name);
         const supplements = Array.from(new Set([...addedSups, ...customSups]));
       Replace with:
         // New schema: read from supplements[]
         // Backward compat: fall back to addedSupplements + customSupplements if migration hasn't run yet
         const suppNames = protocolState?.supplements
           ? protocolState.supplements.map((s) => s.name)
           : [
               ...(protocolState?.addedSupplements ?? []),
               ...(protocolState?.customSupplements ?? []).map((s) => s.name),
             ];
         const supplements = Array.from(new Set(suppNames));

    4. The internal CustomSupplement interface (currently at lines 61–68) stays unchanged — it's still needed for the backward-compat fallback path above.

    5. After making these changes, run the full TypeScript audit:
       npx tsc --noEmit
       Fix any errors that surface in files not yet updated (unlikely, but catch and fix any stray addedSupplements references in files not covered by Plans 01–03).
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/advisorContext.ts imports ProtocolItem from '../types/protocol'
    - src/lib/advisorContext.ts internal ProtocolState interface contains supplements?: ProtocolItem[]
    - src/lib/advisorContext.ts contains "protocolState.supplements" (or "protocolState?.supplements") in the supplement assembly block
    - src/lib/advisorContext.ts does NOT have addedSupplements as the primary (non-fallback) read path
    - npx tsc --noEmit exits 0 with zero errors across the full project
  </acceptance_criteria>
  <done>advisorContext.ts reads from supplements[]; full tsc audit passes clean; Phase 20 schema migration complete and stable for Phase 22/23 consumers</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage → autoPopulate | JSON.parse; backward-compat fallback prevents silent empty results during migration window |
| AsyncStorage → assembleAdvisorContext | JSON.parse; internal ProtocolState type updated to reflect new schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Information Disclosure | advisorContext.ts | accept | supplement names already included in context per Phase 17 design; no new data exposure in Phase 20 |
| T-20-06 | Tampering | advisorContext.ts ProtocolState parse | mitigate | Outer try/catch in assembleAdvisorContext already returns minimal context on any error (unchanged) |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- grep -c "protocol.supplements" src/screens/InteractionCheckerScreen.tsx → at least 1
- grep -c "protocolState?.supplements" src/lib/advisorContext.ts → at least 1
- grep -c "addedSupplements" src/lib/advisorContext.ts → at most 2 (only in backward-compat fallback path)
- grep -c "addedSupplements" src/screens/InteractionCheckerScreen.tsx → at most 1 (only in fallback expression)
- npx tsc --noEmit exits 0
</verification>

<success_criteria>
Both downstream consumers read from protocol.supplements[]; both include backward-compat fallback for any edge case where migration hasn't run; full project tsc audit exits 0; Phase 20 is complete and the schema is stable for Phase 22 (adherence streaks + dose bucketing) and Phase 23 (notification consumers).
</success_criteria>

<output>
Create `.planning/phases/20-protocol-schema-migration/20-03-SUMMARY.md` when done.
</output>
