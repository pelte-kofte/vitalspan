---
phase: 10-apple-health-and-articles
plan: 05
type: execute
wave: 3
depends_on:
  - "10-03"
  - "10-04"
files_modified: []
autonomous: false
requirements:
  - HK-01
  - HK-02
  - HK-03
  - HK-04
  - ART-01
  - ART-02
  - ART-03
  - ART-04

must_haves:
  truths:
    - "LongevityScoreScreen shows a 'Connect Apple Health' prompt card on a clean install (pre-request state) — not demo orbital values"
    - "Tapping the Dashboard 'Longevity Research' card navigates to ArticlesScreen which shows article cards with title, journal, date, and abstract snippet"
    - "Articles tagged to a user's out-of-range biomarkers appear higher in the list than untagged articles (personalization working)"
    - "Tapping an article card opens its PubMed URL in the in-app SafariViewController browser"
    - "tsc --noEmit exits 0 with no TypeScript errors across all modified files"
    - "grep confirms zero occurrences of expo-health import in any src/ file"
    - "grep confirms zero hardcoded hex values in ArticlesScreen.tsx and ArticleCard.tsx"
    - "grep confirms generateMockData does not exist in healthkit.ts"
    - "EAS preview build triggers successfully (build initiated, not necessarily complete)"
    - "Supabase articles table creation SQL run confirmed by developer"
  artifacts:
    - path: "src/lib/healthkit.ts"
      provides: "Real HealthKit reads — no mock"
      contains: "AppleHealthKit.initHealthKit"
    - path: "src/lib/articleService.ts"
      provides: "Full NCBI + Supabase service"
      exports: ["loadCachedArticles", "refreshArticlesIfStale", "Article"]
    - path: "src/screens/ArticlesScreen.tsx"
      provides: "Article feed UI"
      contains: "FlatList"
    - path: "src/screens/LongevityScoreScreen.tsx"
      provides: "Three-state permission flow"
      contains: "permissionState"
  key_links:
    - from: "Phase 10 as a whole"
      to: "tsc --noEmit exits 0"
      via: "TypeScript compiler"
      pattern: "zero errors"
---

<objective>
Final verification pass for Phase 10: TypeScript compilation clean, automated source audits, Supabase table creation confirmed, EAS preview build initiated, and human visual verification of all three HealthKit states and the Articles flow.

Purpose: EAS build is required because react-native-health contains native code that cannot be tested in Expo Go (Pitfall 7 from RESEARCH.md). This plan verifies everything before the build lands.
Output: Confirmed clean codebase, initiated EAS preview build, developer-verified UI flows.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/10-apple-health-and-articles/10-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/10-apple-health-and-articles/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated source audits and TypeScript compilation check</name>
  <files></files>
  <read_first>
    - /Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/10-apple-health-and-articles/10-RESEARCH.md — Common Pitfalls list (all 7 pitfalls) to know what to grep for
  </read_first>
  <action>
    Run each of the following checks and report results. Fix any failures before proceeding to the checkpoint tasks.

    Check 1 — TypeScript: `cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit`. Must exit 0.

    Check 2 — No expo-health import: `grep -r "from 'expo-health'" /Users/bekircemkusdemir/Downloads/vitalspan/src/ --include="*.ts" --include="*.tsx"`. Must return no matches.

    Check 3 — No generateMockData: `grep -r "generateMockData" /Users/bekircemkusdemir/Downloads/vitalspan/src/ --include="*.ts" --include="*.tsx"`. Must return no matches.

    Check 4 — No isDemoMode: true: `grep -rn "isDemoMode: true" /Users/bekircemkusdemir/Downloads/vitalspan/src/ --include="*.ts" --include="*.tsx"`. Must return no matches.

    Check 5 — Articles AsyncStorage key: `grep -c "vitalspan_articles_last_fetched" /Users/bekircemkusdemir/Downloads/vitalspan/src/lib/articleService.ts`. Must output 1 or more.

    Check 6 — No hardcoded hex in new files: `grep -En "#[0-9A-Fa-f]{6}" /Users/bekircemkusdemir/Downloads/vitalspan/src/screens/ArticlesScreen.tsx /Users/bekircemkusdemir/Downloads/vitalspan/src/components/ArticleCard.tsx`. Must return no matches.

    Check 7 — No any type in articleService: `grep -v '^//' /Users/bekircemkusdemir/Downloads/vitalspan/src/lib/articleService.ts | grep -c ": any"`. Must output 0.

    Check 8 — AppNavigator has Articles route: `grep -c "Articles: undefined" /Users/bekircemkusdemir/Downloads/vitalspan/src/navigation/AppNavigator.tsx`. Must output 1.

    Check 9 — Security audit (no secrets): `grep -rn "EXPO_PUBLIC_SUPABASE" /Users/bekircemkusdemir/Downloads/vitalspan/src/ --include="*.ts" --include="*.tsx"`. Must return no matches (keys stay in .env only).

    If any check fails, diagnose and fix before proceeding. Do NOT skip failures.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - All 9 checks pass (zero failures)
    - tsc --noEmit exits 0
    - No expo-health imports anywhere in src/
    - No generateMockData or isDemoMode: true in src/
    - ArticlesScreen.tsx and ArticleCard.tsx have no hardcoded hex values
    - articleService.ts has no `: any` type
    - Articles route registered in AppNavigator
    - No Supabase keys embedded in source files
  </acceptance_criteria>
  <done>All automated source audits pass. Codebase is clean for EAS build.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Developer runs Supabase table creation SQL</name>
  <what-built>Claude created src/db/create_articles_table.sql with the articles table schema and RLS policies in Plan 02.</what-built>
  <how-to-verify>
    1. Open your Supabase project dashboard at https://supabase.com/dashboard.
    2. Navigate to SQL Editor (left sidebar).
    3. Open the file /Users/bekircemkusdemir/Downloads/vitalspan/src/db/create_articles_table.sql in your editor to copy its contents.
    4. Paste the SQL into the Supabase SQL Editor and click "Run".
    5. Confirm: No errors. Navigate to Table Editor → you should see an "articles" table with columns: pmid, title, journal, pub_date, abstract, biomarker_tags, fetched_at.
    6. Navigate to Authentication → Policies → articles table. You should see 3 policies: "public read articles", "anon insert articles", "anon update articles".
    7. Type "confirmed" once the table and policies are created.
  </how-to-verify>
  <resume-signal>Type "confirmed" once the articles table is created in Supabase with RLS policies</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: EAS preview build and UI verification</name>
  <what-built>Claude upgraded healthkit.ts, LongevityScoreScreen, ProfileScreen, ArticlesScreen, ArticleCard, AppNavigator, and DashboardScreen across Plans 01-04.</what-built>
  <how-to-verify>
    1. Initiate an EAS preview build: run `eas build --profile preview --platform ios` from the project root. This is required because react-native-health contains native code that cannot run in Expo Go (Pitfall 7 from RESEARCH.md — "Any runtime error mentioning native module not found" indicates Expo Go was used). Wait for the build to complete or proceed with a simulator build if available.

    2. Install the build on a physical iOS device or iOS simulator.

    3. Test the Articles flow (simulator-safe — no HealthKit needed):
       a. Open the app and navigate to Dashboard.
       b. Scroll down — confirm you see a "Longevity Research" card with subtitle "Personalised PubMed articles for your biomarker profile" and a → arrow. Background: green-tinted (Colors.primaryBg).
       c. Tap the card — confirm it navigates to the Articles screen.
       d. Articles screen shows "Research" title centred at top with back chevron ←.
       e. Either a loading indicator (ActivityIndicator in green) or a list of ArticleCards appears.
       f. If articles load: confirm each card shows title, journal, date, abstract snippet, and "Read article →" link.
       g. Pull down to refresh — confirm RefreshControl spinner appears in green and list refreshes.
       h. Tap an article card — confirm PubMed URL opens in SafariViewController (in-app browser).

    4. Test the HealthKit flow (physical device required for real HealthKit; simulator shows prompt UI only):
       a. Navigate to LongevityScore (tap bio age card on Dashboard).
       b. Confirm the permission prompt card appears at the bottom with "Connect Apple Health" headline, body text about HRV/sleep/VO₂/glucose, and a green "Connect Apple Health" CTA button.
       c. On simulator: confirm the UI renders correctly (note: initHealthKit will fail on simulator — that's expected; you should see the denied state appear after tapping Connect).
       d. Navigate to Profile tab — confirm NO "Disconnect Apple Health" row appears (permissions not yet granted in this state).
       e. On physical device only: tap "Connect Apple Health", grant permissions in the iOS system prompt, and confirm orbitals populate with real values within the same session.

    5. Type "approved" if the Articles flow works correctly in steps 3a-3h and the HealthKit UI shows the permission prompt card in step 4b. Note any issues for fixing.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EAS build pipeline → app binary | Build runs in Expo's cloud; no app secrets in source code (verified by Check 9) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Information Disclosure | EAS build logs | accept | No secrets in source; Supabase keys passed as EAS secrets (already configured in Phase 4) |
| T-10-12 | Tampering | app.json HealthKit entitlement | mitigate | Verified in Check 8 audit; EAS build auto-syncs capability to Apple Developer Portal per RESEARCH.md docs.expo.dev/build-reference/ios-capabilities/ source |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No new installs in this plan — verification only |
</threat_model>

<verification>
This plan IS the verification step for Phase 10. All checks in Task 1 must pass before the human verification checkpoint.

Final confirmation checklist:
- [ ] tsc --noEmit exits 0
- [ ] No expo-health imports anywhere in src/
- [ ] No generateMockData in src/
- [ ] No isDemoMode: true in src/
- [ ] articles table created in Supabase with 3 RLS policies
- [ ] EAS preview build initiated
- [ ] Dashboard Research CTA card visible and tappable
- [ ] ArticlesScreen loads and displays article cards
- [ ] LongevityScoreScreen shows pre-request permission prompt
</verification>

<success_criteria>
All 9 automated source audits pass. Supabase articles table and RLS policies confirmed. EAS preview build completes without native module errors. Dashboard shows Research CTA navigating to Articles. ArticlesScreen displays PubMed articles with title/journal/date/abstract. LongevityScoreScreen shows Connect Apple Health prompt on first visit. ProfileScreen shows Disconnect row when health is connected.
</success_criteria>

<output>
Create `.planning/phases/10-apple-health-and-articles/10-05-SUMMARY.md` when done
</output>
