---
phase: 10-apple-health-and-articles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - app.json
  - src/lib/healthkit.ts
autonomous: false
requirements:
  - HK-01
  - HK-02

must_haves:
  truths:
    - "react-native-health v1.19.0 is installed and listed in package.json dependencies"
    - "app.json has react-native-health config plugin with healthSharePermission string"
    - "app.json ios.entitlements contains com.apple.developer.healthkit: true"
    - "healthkit.ts imports AppleHealthKit from react-native-health (not expo-health)"
    - "requestHealthKitPermissions() calls AppleHealthKit.initHealthKit with 8 permission constants"
    - "syncHealthData() reads HRV, resting HR, VO2max, sleep, respiratory rate, steps, mindful minutes, glucose from HealthKit callbacks"
    - "HRV values multiplied by 1000 to convert seconds to milliseconds"
    - "generateMockData() is removed — no mock data path in production reads"
    - "isInitialized module-level flag prevents reads before initHealthKit callback fires"
  artifacts:
    - path: "src/lib/healthkit.ts"
      provides: "Real HealthKit reads via react-native-health"
      contains: "import AppleHealthKit"
    - path: "app.json"
      provides: "HealthKit entitlement + plugin config"
      contains: "com.apple.developer.healthkit"
  key_links:
    - from: "src/lib/healthkit.ts"
      to: "AppleHealthKit.initHealthKit"
      via: "react-native-health import"
      pattern: "AppleHealthKit\\.initHealthKit"
    - from: "app.json plugins"
      to: "react-native-health config plugin"
      via: "plugins array"
      pattern: "react-native-health"

user_setup:
  - service: react-native-health
    why: "HealthKit requires Apple Developer account with HealthKit capability enabled on the App ID"
    dashboard_config:
      - task: "Verify HealthKit capability is enabled on your App ID in Apple Developer Portal"
        location: "developer.apple.com → Certificates, Identifiers & Profiles → Identifiers → your App ID → Capabilities"
---

<objective>
Install react-native-health v1.19.0 and replace the mock HealthKit implementation in src/lib/healthkit.ts with real HealthKit reads using AppleHealthKit callbacks.

Purpose: The mock was designed as a clean swap — real implementation comments are already present in the file. This plan performs that swap.
Output: healthkit.ts that calls real HealthKit APIs for HRV, sleep, resting HR, VO2max, respiratory rate, steps, mindful minutes, and glucose. app.json updated with the plugin and entitlement.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/10-apple-health-and-articles/10-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/10-apple-health-and-articles/10-RESEARCH.md

<interfaces>
<!-- From src/lib/healthkit.ts — existing exports the executor is replacing -->
export interface HealthData {
  hrv?: number;               // ms, rmssd
  restingHeartRate?: number;  // bpm
  vo2max?: number;            // mL/kg/min
  sleepHours?: number;        // last night total
  sleepDeep?: number;         // deep sleep hours
  sleepRem?: number;          // REM hours
  respiratoryRate?: number;   // breaths/min
  steps?: number;             // last 7 days average
  mindfulMinutes?: number;    // last 7 days total
  glucose?: number;           // mg/dL, last reading
  recovery?: number;          // 0–100 composite score
  lastSynced?: string;        // ISO date
  isDemoMode?: boolean;       // true when using mock data
}

export interface PermissionStatus {
  granted: boolean;
  categories: { heart: boolean; sleep: boolean; activity: boolean; glucose: boolean };
  requestedAt?: string;
}
export interface SyncResult { success: boolean; data?: HealthData; error?: string; isDemoMode?: boolean; }

<!-- Existing functions whose signatures must NOT change (consumers depend on them) -->
export function isHealthKitAvailable(): boolean
export async function loadPermissionStatus(): Promise<PermissionStatus | null>
export async function requestHealthKitPermissions(): Promise<PermissionStatus>
export async function syncHealthData(): Promise<SyncResult>
export async function connectAndSync(): Promise<SyncResult>
export async function loadHealthData(): Promise<HealthData | null>
export function isHealthDataStale(data: HealthData): boolean
export type HealthState = 'neutral' | 'good' | 'poor' | 'stressed'
export function deriveHealthState(data: HealthData | null): HealthState
export function formatSyncTime(iso: string | undefined): string
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify react-native-health package legitimacy before install</name>
  <what-built>Nothing yet — this gate must clear before the install runs.</what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/react-native-health — confirm version 1.19.0, published by agencyenterprise, MIT license, >3M downloads.
    2. Visit https://github.com/agencyenterprise/react-native-health — confirm it is a real, maintained repository with >4 years of history and recent commits.
    3. Confirm the package is NOT expo-health (0.0.0 placeholder stub — do not install that).
    4. Visit https://www.npmjs.com/package/expo-web-browser — confirm it is an official Expo SDK package.
    5. Type "approved" once both packages are confirmed legitimate.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with installation</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Install react-native-health, update app.json with HealthKit plugin and entitlement, upgrade healthkit.ts to real reads</name>
  <files>package.json, app.json, src/lib/healthkit.ts</files>
  <read_first>
    - /Users/bekircemkusdemir/Downloads/vitalspan/src/lib/healthkit.ts — full file; understand the mock structure and all existing exports before replacing internals
    - /Users/bekircemkusdemir/Downloads/vitalspan/app.json — current plugin array and ios.infoPlist to avoid duplicate NSHealthShareUsageDescription
    - /Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/10-apple-health-and-articles/10-RESEARCH.md — Pattern 1 (initHealthKit), Pattern 2 (individual reads), app.json config example, Pitfall 2 (init must precede reads), Pitfall 6 (HRV unit conversion)
  </read_first>
  <action>
    Install the package: run `npx expo install react-native-health expo-web-browser` from the project root. This sets peer-compatible versions.

    Update app.json: In the `plugins` array, add the react-native-health plugin entry: `["react-native-health", { "healthSharePermission": "Vitalspan reads your health data to display longevity metrics on your dashboard." }]`. Add to `expo.ios.entitlements`: `"com.apple.developer.healthkit": true`. Remove `NSHealthShareUsageDescription` from `ios.infoPlist` if it exists (the plugin sets it; having both causes a conflict — per A4 in RESEARCH.md assumptions). expo-web-browser requires no special entitlement.

    Rewrite src/lib/healthkit.ts internals. Keep ALL exported function signatures, the HealthData interface, PermissionStatus interface, SyncResult interface, and HealthState type exactly as they are. Change only the implementations:

    Add at top of file (after existing imports): `import AppleHealthKit, { HealthKitPermissions } from 'react-native-health';`

    Add a module-level `let _isInitialized = false;` flag to guard reads.

    Replace `requestHealthKitPermissions()` implementation: Remove the Alert-based mock. Instead, define `PERMISSIONS: HealthKitPermissions` with `permissions.read` containing `AppleHealthKit.Constants.Permissions.HeartRateVariability`, `AppleHealthKit.Constants.Permissions.RestingHeartRate`, `AppleHealthKit.Constants.Permissions.Vo2Max`, `AppleHealthKit.Constants.Permissions.SleepAnalysis`, `AppleHealthKit.Constants.Permissions.RespiratoryRate`, `AppleHealthKit.Constants.Permissions.Steps`, `AppleHealthKit.Constants.Permissions.MindfulSession`, `AppleHealthKit.Constants.Permissions.BloodGlucose`, and `permissions.write: []`. Call `AppleHealthKit.initHealthKit(PERMISSIONS, callback)` inside the returned Promise. In the callback: if error, set `_isInitialized = false`, save `{ granted: false, ... }` status and resolve with it. If no error, set `_isInitialized = true`, save `{ granted: true, categories: { heart: true, sleep: true, activity: true, glucose: true }, requestedAt: new Date().toISOString() }` and resolve with it. Per Pitfall 2 in RESEARCH.md: all sample reads must only execute inside the success callback path (guarded by `_isInitialized`).

    Replace `syncHealthData()` implementation with real reads. Structure as an async wrapper that returns a Promise resolving to SyncResult. All 8 AppleHealthKit sample methods run concurrently using Promise-wrapping of the callback APIs. Date ranges: `sevenDaysAgo` (7 days back), `yesterday` (1 day back), `thirtyDaysAgo` (30 days back) computed from `new Date()`. Read each metric:
    - HRV: `getHeartRateVariabilitySamples({ startDate: sevenDaysAgo, limit: 1, ascending: false })` — multiply result[0].value by 1000 for ms (per Pitfall 6 in RESEARCH.md and Pattern 2)
    - Resting HR: `getRestingHeartRateSamples({ startDate: sevenDaysAgo, limit: 1, ascending: false })` — value in bpm
    - VO2max: `getVo2MaxSamples({ startDate: thirtyDaysAgo, limit: 1, ascending: false, unit: 'ml/(kg * min)' })` — value directly
    - Sleep: `getSleepSamples({ startDate: yesterday })` — aggregate deep (filter 'DEEP'), REM (filter 'REM'), total (filter 'ASLEEP' or 'CORE') by summing (endDate - startDate) in ms then dividing by 3600000 for hours
    - Respiratory rate: `getRespiratoryRateSamples({ startDate: sevenDaysAgo, limit: 1, ascending: false })` — value in breaths/min
    - Steps: `getDailyStepCountSamples({ startDate: sevenDaysAgo })` — average across results
    - Mindful minutes: `getMindfulSession({ startDate: sevenDaysAgo })` — sum durations in ms, divide by 60000
    - Glucose: `getBloodGlucoseSamples({ startDate: sevenDaysAgo, limit: 1, ascending: false })` — value in mg/dL
    Compute `recovery` as a composite: `Math.min(100, Math.round(55 + ((hrv ?? 50) - 48) * 1.2))` — same formula as the existing mock. Set `isDemoMode: undefined` (not false, not true — absent) on real data. Set `lastSynced: new Date().toISOString()`. Save to `@vitalspan_health_data` via `AsyncStorage.setItem`. Return `{ success: true, data }`.

    Remove `generateMockData()` entirely — it must not exist in the file.

    Keep `loadHealthData()`, `isHealthDataStale()`, `deriveHealthState()`, `formatSyncTime()`, `isHealthKitAvailable()`, `loadPermissionStatus()`, and `savePermissionStatus()` exactly as they are.

    Do NOT add console.log. Use `console.error(e)` in catch blocks per CLAUDE.md.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies contains "react-native-health" (any version >=1.19.0)
    - app.json plugins array contains an entry matching the string "react-native-health"
    - app.json contains "com.apple.developer.healthkit": true under ios.entitlements
    - src/lib/healthkit.ts contains `import AppleHealthKit` (not `import Health` from expo-health)
    - src/lib/healthkit.ts does NOT contain the string "generateMockData"
    - src/lib/healthkit.ts does NOT contain the string "isDemoMode: true"
    - src/lib/healthkit.ts contains `AppleHealthKit.initHealthKit`
    - src/lib/healthkit.ts contains `getHeartRateVariabilitySamples`
    - src/lib/healthkit.ts contains `* 1000` (HRV unit conversion)
    - src/lib/healthkit.ts contains `getSleepSamples`
    - src/lib/healthkit.ts contains `getDailyStepCountSamples`
    - `npx tsc --noEmit` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>react-native-health is installed, app.json has the HealthKit entitlement and plugin, healthkit.ts reads real HealthKit data for all 8 metrics via AppleHealthKit callbacks with HRV unit conversion, no mock data path exists, and tsc passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS HealthKit → healthkit.ts | HealthKit data enters the app via AppleHealthKit callbacks — untrusted raw numeric values |
| npm registry → project | react-native-health installed from npm — supply chain risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | react-native-health npm package | mitigate | Human legitimacy checkpoint (Task 1) before install; verify npmjs.com + GitHub repo |
| T-10-02 | Information Disclosure | HealthKit data in AsyncStorage | accept | iOS sandboxes AsyncStorage per-app; no server transmission; no PII beyond health metrics |
| T-10-03 | Denial of Service | initHealthKit called before install resolves | mitigate | _isInitialized flag prevents reads before callback fires (Pitfall 2 mitigation) |
| T-10-SC | Tampering | npm/expo install | mitigate | slopcheck + blocking human checkpoint for [ASSUMED] packages |
</threat_model>

<verification>
Run after plan completes:
1. `npx tsc --noEmit` — must exit 0
2. Grep check: `grep -c "generateMockData" /Users/bekircemkusdemir/Downloads/vitalspan/src/lib/healthkit.ts` must output 0
3. Grep check: `grep -c "expo-health" /Users/bekircemkusdemir/Downloads/vitalspan/src/lib/healthkit.ts` must output 0
4. Grep check: `grep -v '^//' /Users/bekircemkusdemir/Downloads/vitalspan/src/lib/healthkit.ts | grep -c "AppleHealthKit"` must output > 0
</verification>

<success_criteria>
react-native-health installed and recognized by TypeScript, app.json has HealthKit entitlement (com.apple.developer.healthkit: true) and react-native-health plugin with usage description string, healthkit.ts implements real HealthKit reads for all 8 data types with correct HRV unit conversion (seconds × 1000 = ms), generateMockData removed, isDemoMode never set to true, tsc --noEmit exits 0.
</success_criteria>

<output>
Create `.planning/phases/10-apple-health-and-articles/10-01-SUMMARY.md` when done
</output>
