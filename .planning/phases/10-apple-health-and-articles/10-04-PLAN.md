---
phase: 10-apple-health-and-articles
plan: 04
type: execute
wave: 2
depends_on:
  - "10-02"
files_modified:
  - src/screens/ArticlesScreen.tsx
  - src/components/ArticleCard.tsx
  - src/navigation/AppNavigator.tsx
  - src/screens/DashboardScreen.tsx
autonomous: true
requirements:
  - ART-02
  - ART-03
  - ART-04

must_haves:
  truths:
    - "ArticlesScreen exists as a stack screen navigable from Dashboard via nav.navigate('Articles')"
    - "ArticlesScreen FlatList renders ArticleCard for each article"
    - "ArticleCard shows title (2 lines max), journal name, publication date, abstract snippet (3 lines max), and Read article link"
    - "ArticleCard tap calls WebBrowser.openBrowserAsync with the PubMed URL for the article's pmid"
    - "ArticlesScreen calls loadCachedArticles on mount (show immediately) then refreshArticlesIfStale in background"
    - "Pull-to-refresh triggers forceRefreshArticles"
    - "DashboardScreen has Research CTA card matching uploadCard pattern, navigates to Articles"
    - "AppNavigator has Articles in RootStackParamList and as a Stack.Screen"
    - "All colors from Colors.*, all spacing from Spacing.*, StyleSheet named s, no hardcoded hex values"
    - "Components under 200 lines each — ArticleCard extracted to src/components/ArticleCard.tsx"
  artifacts:
    - path: "src/screens/ArticlesScreen.tsx"
      provides: "Article list with load sequence and pull-to-refresh"
      contains: "FlatList"
    - path: "src/components/ArticleCard.tsx"
      provides: "Single article card component"
      contains: "WebBrowser.openBrowserAsync"
    - path: "src/navigation/AppNavigator.tsx"
      provides: "Articles route in stack"
      contains: "Articles"
    - path: "src/screens/DashboardScreen.tsx"
      provides: "Research CTA card"
      contains: "Longevity Research"
  key_links:
    - from: "DashboardScreen Research CTA"
      to: "ArticlesScreen"
      via: "nav.navigate('Articles')"
      pattern: "navigate\\('Articles'\\)"
    - from: "ArticleCard tap"
      to: "PubMed URL"
      via: "WebBrowser.openBrowserAsync"
      pattern: "openBrowserAsync.*pubmed"
    - from: "ArticlesScreen mount"
      to: "articleService.loadCachedArticles"
      via: "useEffect"
      pattern: "loadCachedArticles"
---

<objective>
Create ArticlesScreen and ArticleCard, wire Articles into AppNavigator, and add the Research CTA card to DashboardScreen. This completes the article feed UI over the service layer built in Plan 02.

Purpose: Users can navigate from Dashboard → Articles and read personalized PubMed content in SafariViewController.
Output: Full articles UI: ArticleCard component, ArticlesScreen with stale-while-revalidate load, AppNavigator route, Dashboard CTA.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/10-apple-health-and-articles/10-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/10-apple-health-and-articles/10-UI-SPEC.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/10-apple-health-and-articles/10-02-SUMMARY.md

<interfaces>
<!-- From src/lib/articleService.ts (Plan 02 output) — consumed by ArticlesScreen -->
export interface Article {
  pmid: string;
  title: string;
  journal: string;
  pub_date: string;
  abstract: string | null;
  biomarker_tags: string[];
  fetched_at: string;
}
export async function loadCachedArticles(entries: StoredEntry[]): Promise<Article[]>
export async function refreshArticlesIfStale(entries: StoredEntry[]): Promise<Article[] | null>
export async function forceRefreshArticles(entries: StoredEntry[]): Promise<Article[] | null>

<!-- From src/navigation/AppNavigator.tsx — current RootStackParamList (add Articles) -->
export type RootStackParamList = {
  Landing: undefined;
  Onboarding: undefined;
  Main: undefined;
  BiomarkerDetail: { biomarkerId?: string };
  BiomarkerEntry: { biomarkerId?: string };
  InteractionChecker: undefined;
  LabUpload: undefined;
  LongevityScore: undefined;
  GuidedFirstRun: undefined;
  Settings: undefined;
  About: undefined;
  // ADD: Articles: undefined;
};

<!-- From src/theme/index.ts — tokens for ArticlesScreen and ArticleCard -->
Colors.bg              // #EDE8DC — ArticlesScreen background
Colors.bgCard          // #FFFFFF — ArticleCard background
Colors.textPrimary     // #1A1A18 — article title
Colors.textSecondary   // #4A4A45 — abstract snippet
Colors.textMuted       // #8A8A82 — journal name, date, back chevron title
Colors.Beige.textMuted // #6B6B64 — eyebrow row (journal per UI-SPEC)
Colors.primary         // #2D6A4F — Read article link, back chevron, pull-to-refresh tint
Colors.primaryBg       // #E8F5EE — Research CTA card background (uploadCard pattern)
Colors.border          // #D4CFC4 — separator, card border
Colors.status.reviewBg     // #FEF9E7 — relevance tag background
Colors.status.reviewBorder // #F0D88A — relevance tag border
Colors.status.reviewText   // #7A5B10 — relevance tag text
Spacing.xs = 4, Spacing.sm = 8, Spacing.md = 12, Spacing.base = 16, Spacing.lg = 20, Spacing.xl = 24, Spacing.xxl = 32
Radius.xl = 20, Radius.full = 9999
Elevation.sm = { shadowColor: '#000', shadowOffset: { width: 0, height: 2 }, shadowOpacity: 0.06, shadowRadius: 8, elevation: 2 }
Typography.sizes.h2 = 22, Typography.sizes.body = 15, Typography.sizes.base = 14, Typography.sizes.xs = 11
Typography.letterSpacing.widest = 3

<!-- DashboardScreen existing uploadCard style (line 527) — Research CTA must match this pattern -->
uploadCard: { marginHorizontal: Spacing.base, marginBottom: Spacing.base, backgroundColor: Colors.bgCard, borderRadius: Radius.xl, padding: Spacing.md, flexDirection: 'row', alignItems: 'center', gap: Spacing.md, shadowColor: '#000', shadowOffset: { width: 0, height: 2 }, shadowOpacity: 0.06, shadowRadius: 12, elevation: 2 }

<!-- DashboardScreen JSX insertion point: after exerciseCard block (~line 416), before sectionHdr 'Today's protocol' (~line 418) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ArticleCard component and ArticlesScreen</name>
  <files>src/components/ArticleCard.tsx, src/screens/ArticlesScreen.tsx</files>
  <read_first>
    - /Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/10-apple-health-and-articles/10-UI-SPEC.md — Component Inventory section 1 (ArticleCard full visual spec), New Screens section (ArticlesScreen layout, loading state, empty state, pull-to-refresh), Interaction Contracts (ArticlesScreen load sequence), Copywriting Contract (all strings), Accessibility section
    - /Users/bekircemkusdemir/Downloads/vitalspan/src/screens/DashboardScreen.tsx — lines 1-25 (imports pattern) and lines 461-534 (StyleSheet pattern) to understand how this codebase builds screens
    - /Users/bekircemkusdemir/Downloads/vitalspan/src/theme/index.ts — first 120 lines (Colors, Spacing, Typography, Radius, Elevation tokens)
  </read_first>
  <action>
    Create src/components/ArticleCard.tsx. Props interface: `{ article: Article; onPress: () => void; isRelevant?: boolean; relevantBiomarkerName?: string }`. Max 200 lines.

    Import: `Article` interface from '../lib/articleService'. Import `* as WebBrowser from 'expo-web-browser'` is NOT needed here — the onPress callback is provided by ArticlesScreen. Import `* as Haptics from 'expo-haptics'`.

    Layout (per UI-SPEC): `TouchableOpacity` wrapper with `activeOpacity: 0.82`, `accessibilityRole="button"`, `accessibilityLabel={`${article.title}, ${article.journal}, published ${article.pub_date}`}`, `onPress`. Internal: `View` with card container style.

    Styles (exact per UI-SPEC): Card container: `backgroundColor: Colors.bgCard`, `borderRadius: Radius.xl`, `marginHorizontal: Spacing.base`, `marginBottom: Spacing.sm`, `padding: Spacing.base`, plus `...Elevation.sm` spread for shadow.

    Eyebrow row: `flexDirection: 'row', justifyContent: 'space-between'`. Journal name: `Typography.sizes.xs` (11px) weight 400, `Colors.Beige.textMuted`. Publication date: same size/color, right-aligned.

    Title: `Typography.sizes.base` (14px) weight 600, `Colors.textPrimary`, `lineHeight: 20`, `marginTop: Spacing.xs`, `numberOfLines: 2`, `ellipsizeMode: 'tail'`.

    Abstract snippet: render only if `article.abstract` is not null. `Typography.sizes.body` (15px) weight 400, `Colors.textSecondary`, `lineHeight: 22`, `marginTop: Spacing.sm`, `numberOfLines: 3`, `ellipsizeMode: 'tail'`.

    Meta row: `marginTop: Spacing.sm`, `flexDirection: 'row, alignItems: 'center', justifyContent: 'space-between'`. Left: relevance tag if `isRelevant === true`. Right: "Read article →" in `Colors.primary`, `Typography.sizes.xs` (11px) weight 600.

    Relevance tag: pill with `backgroundColor: Colors.status.reviewBg`, `borderColor: Colors.status.reviewBorder`, `borderWidth: 0.5`, `borderRadius: Radius.full`, `paddingHorizontal: Spacing.sm`, `paddingVertical: Spacing.xs`. Text: `relevantBiomarkerName` at `Typography.sizes.xs` weight 600, `Colors.status.reviewText`. Show only when `isRelevant === true && relevantBiomarkerName` is truthy.

    StyleSheet named `s` at bottom of file.

    Create src/screens/ArticlesScreen.tsx. Import `Article, loadCachedArticles, refreshArticlesIfStale, forceRefreshArticles` from '../lib/articleService'. Import `ArticleCard` from '../components/ArticleCard'. Import `* as WebBrowser from 'expo-web-browser'`. Import `BIOMARKERS` from '../data/biomarkers'. Import `StoredEntry` shape via inline local interface (same shape: `{ biomarkerId: string; value: number; date: string; unit: string }`) — do not import from BiomarkerEntryScreen. Import `AsyncStorage` for reading `@vitalspan_biomarkers`. Import `useNavigation` from '@react-navigation/native'. Import `NativeStackNavigationProp` for type. Import `RootStackParamList` from '../navigation/AppNavigator'.

    Navigation type: `type Nav = NativeStackNavigationProp<RootStackParamList>`.

    State: `articles: Article[]`, `loading: boolean` (true only on initial empty-cache load), `refreshing: boolean` (for RefreshControl spinner), `entries: StoredEntry[]` (biomarker entries for ranking — initialized to `[]`).

    Load sequence on mount (useEffect, runs once): (1) `setLoading(true)` only if no articles yet. (2) Read `@vitalspan_biomarkers` from AsyncStorage, parse as `StoredEntry[]`, call `setEntries(parsed)`. (3) `const cached = await loadCachedArticles(parsed)`. If cached.length > 0: `setArticles(cached)`, `setLoading(false)`. If empty: `setLoading(true)`. (4) Background: `const fresh = await refreshArticlesIfStale(parsed)`. If fresh !== null: `setArticles(fresh)`. `setLoading(false)`. Note: use `parsed` (the local variable) not `entries` state in this effect — state updates are async and the state value would still be `[]` within the same synchronous effect closure.

    Build `outOfRangeSet: Set<string>` in a `useMemo([entries])` — iterate `entries`, look up each biomarkerId in `BIOMARKERS`, compare the latest value for each id against `optMin`/`optMax` range fields, and add the biomarkerId to the set if out of range. Deps array: `[entries]`. For each article in the FlatList renderItem, compute `isRelevant = article.biomarker_tags.some(t => outOfRangeSet.has(t))` and `relevantBiomarkerName = BIOMARKERS.find(b => article.biomarker_tags.includes(b.id) && outOfRangeSet.has(b.id))?.name ?? undefined`.

    Pull-to-refresh: `onRefresh` = `setRefreshing(true)`, read entries, call `forceRefreshArticles(entries)`, setArticles if result is not null, `setRefreshing(false)`.

    Layout (per UI-SPEC): `SafeAreaView` with `backgroundColor: Colors.bg`. `StatusBar` style `dark-content`. Top bar: back chevron `←` at left, `TouchableOpacity onPress={() => nav.goBack()}`, `accessibilityLabel="Go back"`. Screen title "Research" centred: `Typography.sizes.xs` (11px) weight 600, `Colors.textMuted`, `letterSpacing: Typography.letterSpacing.widest` (3).

    FlatList: `data={articles}`, `keyExtractor={(item) => item.pmid}`, `renderItem` renders `ArticleCard` passing `article`, `onPress: () => { Haptics.impactAsync(Light); WebBrowser.openBrowserAsync('https://pubmed.ncbi.nlm.nih.gov/' + item.pmid + '/') }`, `isRelevant`, `relevantBiomarkerName`. `contentContainerStyle: { paddingTop: Spacing.base, paddingBottom: Spacing.xxl }`. `ItemSeparatorComponent: null` (ArticleCard uses marginBottom). `RefreshControl` with `refreshing`, `onRefresh`, `tintColor: Colors.primary`.

    Loading state (when `loading === true` and `articles.length === 0`): render `ActivityIndicator` centered, `color={Colors.primary}`.

    Empty state (when `loading === false` and `articles.length === 0`): centred View with body text "No articles available. Check your connection and pull to refresh." in `Typography.sizes.body`, `Colors.textMuted`, `textAlign: 'center'`, `lineHeight: 22`, `paddingHorizontal: Spacing.xxl`.

    StyleSheet named `s` at bottom. No hardcoded hex values. No hardcoded spacing numbers. TypeScript strict — no `any`.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ArticleCard.tsx exists with exported ArticleCard component
    - ArticleCard.tsx contains `WebBrowser` import is NOT here — onPress passed from parent (verify no WebBrowser import in ArticleCard.tsx)
    - ArticleCard.tsx contains `numberOfLines: 2` on title and `numberOfLines: 3` on abstract
    - ArticleCard.tsx StyleSheet named `s` at bottom of file
    - src/screens/ArticlesScreen.tsx exists
    - ArticlesScreen.tsx contains `loadCachedArticles` call
    - ArticlesScreen.tsx contains `forceRefreshArticles` (pull-to-refresh)
    - ArticlesScreen.tsx contains `WebBrowser.openBrowserAsync`
    - ArticlesScreen.tsx contains `pubmed.ncbi.nlm.nih.gov`
    - ArticlesScreen.tsx contains `RefreshControl` with `tintColor: Colors.primary`
    - Neither file contains hardcoded hex values (grep for `#[0-9A-Fa-f]{6}` should return 0 matches)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ArticleCard renders eyebrow/title/abstract/meta layout per UI-SPEC with relevance tag for out-of-range articles. ArticlesScreen loads cached data immediately, refreshes in background, supports pull-to-refresh, opens PubMed URLs via WebBrowser, shows loading/empty states. Both files use Colors/Spacing tokens, StyleSheet named s. tsc passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Articles into AppNavigator and add Research CTA card to DashboardScreen</name>
  <files>src/navigation/AppNavigator.tsx, src/screens/DashboardScreen.tsx</files>
  <read_first>
    - /Users/bekircemkusdemir/Downloads/vitalspan/src/navigation/AppNavigator.tsx — full file; identify RootStackParamList (lines 26-37) and Stack.Screen list (lines 129-184) for exact insertion points
    - /Users/bekircemkusdemir/Downloads/vitalspan/src/screens/DashboardScreen.tsx — lines 370-418 (uploadCard and exerciseCard blocks) to find the exact insertion point for Research CTA; lines 527-534 (uploadCard StyleSheet style) to replicate for researchCard
    - /Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/10-apple-health-and-articles/10-UI-SPEC.md — Component Inventory section 3 (Research CTA card visual spec), Copywriting Contract (Articles Screen section)
  </read_first>
  <action>
    In AppNavigator.tsx:
    - Add `Articles: undefined` to `RootStackParamList` type (after the `About: undefined` entry).
    - Add `import ArticlesScreen from '../screens/ArticlesScreen'` at the top with the other screen imports.
    - Add a `Stack.Screen` entry after the About screen entry: `<Stack.Screen name="Articles" component={ArticlesScreen} options={{ presentation: 'card' }} />`. The `presentation: 'card'` matches the BiomarkerDetail pattern for full-screen stack push from Dashboard.

    In DashboardScreen.tsx:
    - Add `import * as WebBrowser from 'expo-web-browser'` — NOT needed here actually. Do not import. WebBrowser is used in ArticlesScreen only.
    - Between the exerciseCard block and the `<View style={s.sectionHdr}>` that contains 'Today's protocol' (around line 418), insert the Research CTA card. This is a `TouchableOpacity` that matches the existing `s.uploadCard` pattern exactly (per UI-SPEC: "Matches the s.uploadCard pattern exactly").

    Research CTA card: Use `style={[s.uploadCard, s.researchCard]}` where `researchCard` overrides only `backgroundColor: Colors.primaryBg`. Internal content: `<Text style={s.researchIcon}>📄</Text>`, a View with body content (style `s.uploadCardBody`): title "Longevity Research" using `s.uploadCardTitle`, subtitle "Personalised PubMed articles for your biomarker profile" using `s.uploadCardSub`, then `<Text style={s.uploadCardArrow}>→</Text>`.

    onPress: `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light).catch(() => null); nav.navigate('Articles')`.

    Add to StyleSheet `s`: `researchCard: { backgroundColor: Colors.primaryBg }` and `researchIcon: { fontSize: 28 }`.

    Do NOT modify any existing DashboardScreen logic, state, or other UI.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - AppNavigator.tsx RootStackParamList contains `Articles: undefined`
    - AppNavigator.tsx contains `Stack.Screen name="Articles"`
    - AppNavigator.tsx imports ArticlesScreen
    - DashboardScreen.tsx contains `nav.navigate('Articles')`
    - DashboardScreen.tsx contains "Longevity Research" string
    - DashboardScreen.tsx contains "Personalised PubMed articles for your biomarker profile" string
    - DashboardScreen.tsx does NOT contain any new hardcoded hex values
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>AppNavigator registers Articles as a card-presentation stack screen. DashboardScreen has a Research CTA card (matching uploadCard pattern, backgroundColor Colors.primaryBg) between the exercise card and the protocol section. Tapping navigates to ArticlesScreen. tsc passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| articleService → ArticlesScreen | Article data from Supabase cache enters UI — typed, no PII |
| ArticleCard tap → WebBrowser | Opens external URL in SafariViewController — URL is constructed from pmid stored in Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Tampering | PubMed URL construction from pmid | mitigate | URL constructed as `https://pubmed.ncbi.nlm.nih.gov/${pmid}/` — pmid is a NCBI-assigned numeric string; no user input in URL |
| T-10-10 | Information Disclosure | WebBrowser opens in SFSafariViewController | accept | SFSafariViewController is sandboxed; does not expose app data to the external site |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | expo-web-browser was installed in Plan 01 (gated by legitimacy checkpoint); no new installs here |
</threat_model>

<verification>
Run after plan completes:
1. `npx tsc --noEmit` — must exit 0
2. `grep -c "Articles: undefined" /Users/bekircemkusdemir/Downloads/vitalspan/src/navigation/AppNavigator.tsx` must output 1
3. `grep -c "navigate('Articles')" /Users/bekircemkusdemir/Downloads/vitalspan/src/screens/DashboardScreen.tsx` must output > 0
4. `grep -c "openBrowserAsync" /Users/bekircemkusdemir/Downloads/vitalspan/src/screens/ArticlesScreen.tsx` must output > 0
5. `grep -c "pubmed.ncbi.nlm.nih.gov" /Users/bekircemkusdemir/Downloads/vitalspan/src/screens/ArticlesScreen.tsx` must output > 0
</verification>

<success_criteria>
Articles route registered in AppNavigator RootStackParamList and Stack.Navigator. DashboardScreen has Research CTA card navigating to Articles. ArticlesScreen loads cached articles immediately on mount and refreshes stale data in background. ArticleCard displays title (2 lines), journal, date, abstract snippet (3 lines), relevance tag for out-of-range biomarker articles. Tapping an article opens its PubMed URL in SafariViewController. tsc exits 0.
</success_criteria>

<output>
Create `.planning/phases/10-apple-health-and-articles/10-04-SUMMARY.md` when done
</output>
