---
phase: 10-apple-health-and-articles
plan: 03
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - src/screens/LongevityScoreScreen.tsx
  - src/screens/ProfileScreen.tsx
autonomous: true
requirements:
  - HK-01
  - HK-02
  - HK-03
  - HK-04

must_haves:
  truths:
    - "LongevityScoreScreen reads @vitalspan_health_permissions on mount and derives permissionState: 'pre-request' | 'granted' | 'denied'"
    - "State A (pre-request): empty orbitals + Connect Apple Health prompt card with CTA — no demo data"
    - "State B (granted): loadHealthData() called, real data populates orbitals, isDemoMode badge absent"
    - "State C (denied): Apple Health access needed card with Open Settings CTA calling Linking.openURL('app-settings:') and secondary 'Continue without Health data' link"
    - "Permission request fires initHealthKit (react-native-health) — NOT the old Alert-based mock"
    - "hasRequestedHealthKit flag in AsyncStorage gates the first-visit permission request"
    - "Empty HRV result after initHealthKit → denied state (per iOS privacy design)"
    - "ProfileScreen shows Disconnect Apple Health row in Colors.danger when permissions are granted"
    - "Disconnect clears @vitalspan_health_permissions and @vitalspan_health_data via Alert confirmation"
    - "All new UI text matches copywriting contract from 10-UI-SPEC.md exactly"
  artifacts:
    - path: "src/screens/LongevityScoreScreen.tsx"
      provides: "Three-state permission flow (pre-request / granted / denied)"
      contains: "permissionState"
    - path: "src/screens/ProfileScreen.tsx"
      provides: "Disconnect Apple Health row with confirmation Alert"
      contains: "Disconnect Apple Health"
  key_links:
    - from: "LongevityScoreScreen permissionState === 'pre-request'"
      to: "initHealthKit via requestHealthKitPermissions()"
      via: "CTA button onPress"
      pattern: "requestHealthKitPermissions"
    - from: "LongevityScoreScreen permissionState === 'denied'"
      to: "Linking.openURL('app-settings:')"
      via: "Open Settings CTA"
      pattern: "app-settings:"
    - from: "ProfileScreen Disconnect row"
      to: "AsyncStorage.removeItem / setItem"
      via: "handleDisconnect"
      pattern: "vitalspan_health_permissions"
---

<objective>
Upgrade LongevityScoreScreen to show three distinct permission states (pre-request, granted, denied) driven by the real react-native-health initHealthKit call. Add the Disconnect Apple Health row to ProfileScreen.

Purpose: Completes the HealthKit user-facing integration. After Plan 01 upgraded the library layer, this plan upgrades the UI consumers.
Output: LongevityScoreScreen with permission-gated orbital display per D-05, D-06, D-07, D-08. ProfileScreen with HK-04 disconnect capability.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/10-apple-health-and-articles/10-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/10-apple-health-and-articles/10-UI-SPEC.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/10-apple-health-and-articles/10-01-SUMMARY.md

<interfaces>
<!-- From src/lib/healthkit.ts (post Plan 01) — key signatures -->
export interface PermissionStatus {
  granted: boolean;
  categories: { heart: boolean; sleep: boolean; activity: boolean; glucose: boolean };
  requestedAt?: string;
  hasRequestedHealthKit?: boolean;  // set true after first initHealthKit attempt
}
export async function requestHealthKitPermissions(): Promise<PermissionStatus>
export async function loadPermissionStatus(): Promise<PermissionStatus | null>
export async function connectAndSync(): Promise<SyncResult>
export async function loadHealthData(): Promise<HealthData | null>
export function formatSyncTime(iso: string | undefined): string

<!-- From src/theme/index.ts — tokens used in new UI elements (per UI-SPEC) -->
Colors.primaryLight  // #52B788 — CTA button background on dark
Colors.dark.bg       // #0C0F0D — CTA button text on dark
Colors.dark.text     // #E8F5EE — prompt headline
Colors.dark.textMuted  // rgba(232,245,238,0.5) — prompt body, secondary link
Colors.dark.border   // rgba(255,255,255,0.08) — prompt card border
Colors.danger        // #B13126 — disconnect row label
Spacing.base         // 16 — card horizontal padding
Spacing.lg           // 20 — card paddingVertical
Spacing.md           // 12 — button marginTop
Spacing.sm           // 8 — secondary link marginTop
Spacing.xs           // 4 — body marginTop
Radius.xl            // 20 — card borderRadius

<!-- LongevityScoreScreen existing state variables (lines 266-274) -->
const [healthData, setHealthData] = useState<HealthData>({});
const [connecting, setConnecting] = useState(false);
const [isConnected, setIsConnected] = useState(false);
// ADD: const [permissionState, setPermissionState] = useState<'pre-request' | 'granted' | 'denied' | 'loading'>('loading');

<!-- LongevityScoreScreen existing HealthKit card (lines 603-621) — replace this entire block -->
<TouchableOpacity style={[s.healthKitCard, isConnected && s.healthKitCardConnected]} ...>

<!-- ProfileScreen existing structure: card rows ending at s.settingsCard + s.aboutCard + height 32 spacer -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade LongevityScoreScreen with three-state permission flow</name>
  <files>src/screens/LongevityScoreScreen.tsx</files>
  <read_first>
    - /Users/bekircemkusdemir/Downloads/vitalspan/src/screens/LongevityScoreScreen.tsx — full file; understand state variables (lines 264-274), loadAll() (lines 275-300), handleConnectHealth() (lines 308-323), and the current HealthKit card block (lines 603-621) which is being replaced
    - /Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/10-apple-health-and-articles/10-UI-SPEC.md — Component Inventory section 2 (permission prompt card), all three states, Interaction Contracts (LongevityScoreScreen state machine), Copywriting Contract (exact strings), Animation Contracts (fade-in with withTiming(1.0, { duration: 400 }))
    - /Users/bekircemkusdemir/Downloads/vitalspan/src/lib/healthkit.ts — loadPermissionStatus(), requestHealthKitPermissions() signatures after Plan 01
  </read_first>
  <action>
    Add `permissionState` state variable: `const [permissionState, setPermissionState] = useState<'pre-request' | 'granted' | 'denied' | 'loading'>('loading')`.

    Add `promptOpacity` shared value: `const promptOpacity = useSharedValue(0)`. Add animated style: `const promptStyle = useAnimatedStyle(() => ({ opacity: promptOpacity.value }))`. When permissionState transitions to 'pre-request' or 'denied', trigger: `promptOpacity.value = withTiming(1.0, { duration: 400 })`.

    Modify `loadAll()`: After loading `perms` from `loadPermissionStatus()`, derive permissionState:
    - If `perms` is null or `!perms.hasRequestedHealthKit`: set `permissionState('pre-request')`
    - If `perms.hasRequestedHealthKit && perms.granted`: set `permissionState('granted')`, call `loadHealthData()` and set healthData
    - If `perms.hasRequestedHealthKit && !perms.granted`: set `permissionState('denied')`
    Remove the existing `setIsConnected(perms?.granted ?? false)` — replace with the permissionState logic above.

    Modify `handleConnectHealth()` (renamed to `handleRequestPermission()`): Call `requestHealthKitPermissions()` which now calls `initHealthKit` internally (per Plan 01). After the call resolves: attempt to read HRV via `loadHealthData()`. If the result has a non-null `hrv` value, treat as granted: save `{ hasRequestedHealthKit: true, granted: true }` via `AsyncStorage.setItem`, call `connectAndSync()`, set healthData with result, `setPermissionState('granted')`. If HRV is null (empty result — iOS denied), save `{ hasRequestedHealthKit: true, granted: false }` via `AsyncStorage.setItem`, `setPermissionState('denied')`. The `connecting` state wraps this flow (set true before, false in finally).

    Add `handleOpenSettings()`: `await Linking.openURL('app-settings:')`. Add `Linking` import from 'react-native' if not already imported.

    Add `handleDismissPrompt()`: `setPermissionState('granted')` — user chose to continue without Health data; shows empty orbitals (no data = no orbital values displayed, which is the existing behavior when healthData is {}).

    Replace the existing HealthKit card block (lines 603-621) with a conditional render:
    - When `permissionState === 'loading'`: render nothing (null) at that position — loadAll is in progress
    - When `permissionState === 'granted'`: render the existing connected card (s.healthKitCard + s.healthKitCardConnected) unchanged for the "resync" experience — or simply nothing if no card is desired in connected state (per UI-SPEC: State B preserves existing s.healthKitCard / s.healthKitCardConnected pattern)
    - When `permissionState === 'pre-request'`: render the Animated.View (using promptStyle) containing the pre-request prompt card. Card: `backgroundColor: 'rgba(20,25,22,0.85)'`, `borderWidth: 0.5`, `borderColor: Colors.dark.border`, `borderRadius: Radius.xl`, `marginHorizontal: Spacing.base`, `paddingVertical: Spacing.lg`, `paddingHorizontal: Spacing.base`, `marginBottom: Spacing.base`. Icon: `'⌚'` at fontSize 28. Headline: "Connect Apple Health" at Typography.sizes.base weight 600, Colors.dark.text. Body: "Sync HRV, sleep, VO₂ max and glucose to power your longevity orbitals." at Typography.sizes.body weight 400, Colors.dark.textMuted, lineHeight 22, marginTop Spacing.xs. CTA button: full-width, `backgroundColor: Colors.primaryLight`, `borderRadius: Radius.xl`, `height: 44`, `marginTop: Spacing.md`. Label: "Connect Apple Health" at Typography.sizes.base weight 600, Colors.dark.bg. `onPress: () => { Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium); handleRequestPermission(); }`. `disabled={connecting}`. When `connecting === true`, replace label with "Connecting…".
    - When `permissionState === 'denied'`: render Animated.View (using promptStyle) with denied card. Same container style as pre-request. Icon: `'⚠'` text (not emoji) at fontSize 20, `color: Colors.warning`, marginBottom Spacing.sm. Headline: "Apple Health access needed" — same style as pre-request headline. Body: "Enable Health access in Settings to see live HRV, sleep and recovery data." — same style as pre-request body. CTA button: same dimensions/colors. Label: "Open Settings". `onPress: handleOpenSettings`. Secondary link below: "Continue without Health data" in Colors.dark.textMuted, Typography.sizes.xs, textAlign center, marginTop Spacing.sm. `onPress: handleDismissPrompt`. `accessibilityRole="button"`.

    Keep the `isConnected` state variable for backward compat with `bioConfidence` calculation (line ~415). Set it to `permissionState === 'granted'`.

    The `isDemoMode` badge references at lines ~529 and ~573 disappear automatically because real HealthData from Plan 01 never sets `isDemoMode: true`.

    Do NOT modify the orbital rendering logic, the SVG sphere, DATA_POINTS, or any animation outside the prompt card fade.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File contains `permissionState` state variable typed as `'pre-request' | 'granted' | 'denied' | 'loading'`
    - File contains "Connect Apple Health" as string literal (State A headline and CTA)
    - File contains "Apple Health access needed" (State C headline)
    - File contains "app-settings:" (denied state Settings deep-link per D-07)
    - File contains "Continue without Health data" (secondary dismiss link per UI-SPEC)
    - File contains `Linking.openURL` import or usage
    - File does NOT contain `isDemoMode: true` (no mock data)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>LongevityScoreScreen shows State A (Connect Apple Health prompt) on first visit before permissions, State B (live orbitals) after grant, State C (denied card with Settings deep-link) when HealthKit reads are empty. Permission request calls real initHealthKit. Fade-in animation on prompt cards.</done>
</task>

<task type="auto">
  <name>Task 2: Add Disconnect Apple Health row to ProfileScreen</name>
  <files>src/screens/ProfileScreen.tsx</files>
  <read_first>
    - /Users/bekircemkusdemir/Downloads/vitalspan/src/screens/ProfileScreen.tsx — full file; understand the read view JSX (lines ~230+), the Settings and About card rows (s.settingsCard, s.aboutCard around lines 335-343), and the StyleSheet at line 351+
    - /Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/10-apple-health-and-articles/10-UI-SPEC.md — Interaction Contracts: Profile/Settings — Disconnect Health (HK-04) section; Copywriting Contract: Disconnecting Health table
  </read_first>
  <action>
    Add required imports: `import AsyncStorage from '@react-native-async-storage/async-storage'` if not already imported. Import `loadPermissionStatus` from '../lib/healthkit'.

    Add state: `const [healthConnected, setHealthConnected] = useState(false)`.

    In `handleRefresh()` (or the existing useFocusEffect load), add: `const perms = await loadPermissionStatus(); setHealthConnected(perms?.granted ?? false)`.

    Add `handleDisconnect()` async function:
    ```
    await AsyncStorage.multiRemove(['@vitalspan_health_permissions', '@vitalspan_health_data']);
    setHealthConnected(false);
    ```

    In the read view JSX, between the s.aboutCard row and the `<View style={{ height: 32 }} />` bottom spacer, add a conditional block: when `healthConnected === true`, render a TouchableOpacity using the same style pattern as s.settingsCard. This row shows label "Disconnect Apple Health" in `Colors.danger` (not Colors.textPrimary). Arrow right is `Colors.danger`. On press: show Alert with title "Disconnect Apple Health", body "Orbitals will revert to manual-entry data. You can reconnect anytime.", two buttons: `{ text: 'Keep Connected', style: 'cancel' }` and `{ text: 'Disconnect Health', style: 'destructive', onPress: handleDisconnect }`. Add haptic `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium)` before showing the Alert.

    Add to StyleSheet `s`: a `disconnectCard` style that matches `s.settingsCard` dimensions/padding/borderRadius/backgroundColor but is otherwise identical — the text color difference is handled inline with `{ color: Colors.danger }`.

    Do NOT change any existing ProfileScreen functionality, state, or UI outside this addition.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File contains `healthConnected` state variable
    - File contains "Disconnect Apple Health" as string literal
    - File contains Alert.alert with "Disconnect Health" destructive button
    - File contains `multiRemove(['@vitalspan_health_permissions', '@vitalspan_health_data'])` or sequential removeItem calls
    - File contains `Colors.danger` usage on the disconnect row text
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ProfileScreen shows "Disconnect Apple Health" row (in danger color) only when health permissions are granted. Tapping shows a confirmation Alert. Confirming clears both health AsyncStorage keys and hides the row. tsc passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS permission callback → permissionState | initHealthKit result drives UI state; empty read probed to infer denial |
| User tap → Linking.openURL | Opens iOS Settings — no data exposed, no risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Information Disclosure | HealthData in AsyncStorage after disconnect | mitigate | handleDisconnect removes both @vitalspan_health_permissions and @vitalspan_health_data keys |
| T-10-08 | Spoofing | iOS cannot report true denial status | accept | Empty HRV probe heuristic is Apple's recommended pattern; false positives (new device, no data) are a UX edge case, not a security issue |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan — uses existing react-native-health from Plan 01 |
</threat_model>

<verification>
Run after plan completes:
1. `npx tsc --noEmit` — must exit 0
2. `grep -c "permissionState" /Users/bekircemkusdemir/Downloads/vitalspan/src/screens/LongevityScoreScreen.tsx` must output > 0
3. `grep -c "app-settings:" /Users/bekircemkusdemir/Downloads/vitalspan/src/screens/LongevityScoreScreen.tsx` must output > 0
4. `grep -c "Disconnect Apple Health" /Users/bekircemkusdemir/Downloads/vitalspan/src/screens/ProfileScreen.tsx` must output > 0
</verification>

<success_criteria>
LongevityScoreScreen renders three permission states: State A (pre-request prompt with Connect CTA calling initHealthKit), State B (live orbitals when granted), State C (denied card with Open Settings CTA + dismiss link). ProfileScreen shows Disconnect Apple Health row in danger color when health is connected, with Alert confirmation that clears both health storage keys. tsc exits 0.
</success_criteria>

<output>
Create `.planning/phases/10-apple-health-and-articles/10-03-SUMMARY.md` when done
</output>
