---
phase: 19
plan: "19-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/OrbitalInfoModal.tsx
autonomous: true
requirements:
  - UX-04

must_haves:
  truths:
    - "OrbitalInfoModal renders correctly when visible=true with title, body, and optional ctaLabel"
    - "Tapping the dismiss button calls onDismiss"
    - "Tapping the optional CTA button calls onCta"
    - "Modal is dismissible via backdrop tap (calls onDismiss)"
  artifacts:
    - path: "src/components/OrbitalInfoModal.tsx"
      provides: "Reusable modal component for LongevityScore orbital empty-state info"
      exports: ["OrbitalInfoModal", "OrbitalInfoModalProps"]
  key_links:
    - from: "src/screens/LongevityScoreScreen.tsx"
      to: "src/components/OrbitalInfoModal.tsx"
      via: "import OrbitalInfoModal from '../components/OrbitalInfoModal'"
      pattern: "OrbitalInfoModal"
---

<objective>
Create the OrbitalInfoModal component that LongevityScoreScreen will use in Wave 2 to explain Sleep and HRV/Fitness empty states with optional CTA routing.

Purpose: Decouples the modal presentation from LongevityScoreScreen logic and provides a reusable bottom-sheet style modal consistent with ExplainerModal already in LongevityScoreScreen.
Output: src/components/OrbitalInfoModal.tsx — a typed, self-contained modal component.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/19-global-ux-fixes/19-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/src/theme/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OrbitalInfoModal component</name>
  <files>src/components/OrbitalInfoModal.tsx</files>
  <read_first>
    - src/screens/LongevityScoreScreen.tsx (lines 116–181 — ExplainerModal is the style reference; match backdrop, sheet, sheetHandle, sheetTitle, sheetBody, and button styles exactly)
    - src/theme/index.ts (color tokens and spacing)
  </read_first>
  <action>
    Create src/components/OrbitalInfoModal.tsx as a named export. Export OrbitalInfoModalProps interface and OrbitalInfoModal function component.

    Props interface:
    - visible: boolean
    - title: string
    - body: string
    - ctaLabel?: string (defaults to "Got it" when undefined)
    - onCta?: () => void (called when CTA button tapped; if undefined, button still renders with "Got it" label and calls onDismiss)
    - onDismiss: () => void (called on backdrop tap and on dismiss-only button tap)

    JSX structure mirrors ExplainerModal in LongevityScoreScreen:
    - Root: Modal (visible, transparent, animationType="slide", onRequestClose={onDismiss})
    - Backdrop: TouchableOpacity with style matching s.modalOverlay (flex:1, backgroundColor:'rgba(0,0,0,0.5)', justifyContent:'flex-end'), activeOpacity=1, onPress={onDismiss}
    - Sheet: View with style matching s.explainerSheet (backgroundColor: Colors.dark.bgCard, borderTopLeftRadius: Radius.xl, borderTopRightRadius: Radius.xl, padding: Spacing.base, paddingBottom: 40)
      - Render sheet's inner View with onStartShouldSetResponder={() => true} to block backdrop tap propagation through the sheet
      - Handle bar: View (width:36, height:4, borderRadius:2, backgroundColor: Colors.dark.border, alignSelf:'center', marginBottom: Spacing.base)
      - Title: Text (fontSize: Typography.sizes.h3, fontWeight:'600', color: Colors.dark.text, marginBottom: Spacing.md)
      - Body: Text (fontSize: Typography.sizes.base, color: Colors.dark.textMuted, lineHeight: 22, marginBottom: Spacing.xl)
      - CTA button: TouchableOpacity (backgroundColor: Colors.viz.bioGreen, borderRadius: Radius.xl, paddingVertical: Spacing.md, alignItems:'center') calling onCta ?? onDismiss
        - Button label text: ctaLabel ?? "Got it" (color: Colors.dark.bg, fontWeight:'700', fontSize: Typography.sizes.base)

    StyleSheet named `s` at bottom of file per project conventions. No hardcoded hex values — use theme tokens only. Import Modal, TouchableOpacity, View, Text, StyleSheet from 'react-native'. Import Colors, Spacing, Radius, Typography from '../theme'.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | grep -E "OrbitalInfoModal|components/OrbitalInfoModal" | head -20</automated>
  </verify>
  <done>
    - src/components/OrbitalInfoModal.tsx exists with exported OrbitalInfoModalProps interface and OrbitalInfoModal component
    - Component accepts visible, title, body, ctaLabel?, onCta?, onDismiss props (TypeScript strict — no 'any')
    - tsc reports zero errors in OrbitalInfoModal.tsx
    - StyleSheet named s at bottom, no hardcoded hex values
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI component | Pure presentational component; no network calls, no storage access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-SC | Tampering | No new npm installs | accept | No package-manager installs in this plan |
</threat_model>

<verification>
Run: npx tsc --noEmit (must exit 0, zero new errors)
Confirm: grep -r "OrbitalInfoModal" src/components/OrbitalInfoModal.tsx shows export of both OrbitalInfoModalProps and OrbitalInfoModal
</verification>

<success_criteria>
- OrbitalInfoModal.tsx created in src/components/ with correct TypeScript props interface
- No tsc errors introduced
- Component is importable by LongevityScoreScreen in Wave 2
</success_criteria>

<output>
Create .planning/phases/19-global-ux-fixes/19-01-SUMMARY.md when done
</output>
