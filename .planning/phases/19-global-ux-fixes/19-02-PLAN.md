---
phase: 19
plan: "19-02"
type: execute
wave: 2
depends_on: []
files_modified:
  - src/screens/ExerciseDetailScreen.tsx
autonomous: true
requirements:
  - UX-02
  - UX-05

must_haves:
  truths:
    - "ExerciseDetailScreen back button and title are fully visible below the Dynamic Island on iPhone 15 Pro and iPhone 16 Plus"
    - "ExerciseDetailScreen no longer renders MuscleMapView or the section card containing it"
    - "The muscleView state variable is removed"
    - "The MuscleMapView import is removed"
  artifacts:
    - path: "src/screens/ExerciseDetailScreen.tsx"
      provides: "Fixed ExerciseDetailScreen with safe-area inset header and muscle diagram removed"
  key_links:
    - from: "src/screens/ExerciseDetailScreen.tsx header View"
      to: "useSafeAreaInsets().top"
      via: "inline paddingTop: insets.top on the header View"
      pattern: "insets\\.top"
---

<objective>
Apply two independent fixes to ExerciseDetailScreen in a single file pass: (1) add safe-area inset handling to the custom header so it clears the Dynamic Island on iPhone 15 Pro / 16 Plus; (2) remove the MuscleMapView muscle diagram section entirely.

Purpose: Eliminates two post-v4.0 visual bugs in a single file. The header clipping is a layout defect on modern iPhones; the muscle diagram was identified as low-quality and adds no clinical value.
Output: Modified src/screens/ExerciseDetailScreen.tsx.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/19-global-ux-fixes/19-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/src/theme/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply Dynamic Island inset fix to custom header</name>
  <files>src/screens/ExerciseDetailScreen.tsx</files>
  <read_first>
    - src/screens/ExerciseDetailScreen.tsx (full file — understand current header at line ~79 and StyleSheet entry `header` near line 173)
  </read_first>
  <action>
    In ExerciseDetailScreen.tsx:

    1. Add import: import { useSafeAreaInsets } from 'react-native-safe-area-context'; (place after existing react-navigation imports)

    2. Also ensure Spacing is imported from '../theme' — it will be used in the inline dynamic style. If the theme import already imports Spacing, no change needed; otherwise add Spacing to the existing theme import.

    3. Inside the ExerciseDetailScreen function body, immediately after the existing useState declarations, add:
       const insets = useSafeAreaInsets();

    4. Find the JSX `<View style={s.header}>` element (the header View containing the back button). Change it to:
       `<View style={[s.header, { paddingTop: insets.top + Spacing.md }]}>`

       This replaces the static paddingVertical: Spacing.md from the StyleSheet with a device-aware top that accounts for the Dynamic Island / notch. The + Spacing.md preserves visual spacing below the inset.

    5. In the StyleSheet `s.header` entry (near line 173), remove the `paddingVertical: Spacing.md` property and replace with `paddingBottom: Spacing.md` only. The top padding is now set dynamically via the inline style override. The existing paddingHorizontal: Spacing.base remains unchanged.

    Do NOT wrap the root View in SafeAreaView — the root is already `<View style={s.root}>` with flex:1 and this pattern is correct per D-02.
  </action>
  <acceptance_criteria>
    - src/screens/ExerciseDetailScreen.tsx imports useSafeAreaInsets from 'react-native-safe-area-context'
    - Spacing is imported from '../theme'
    - const insets = useSafeAreaInsets() appears in the function body
    - The header View uses style={[s.header, { paddingTop: insets.top + Spacing.md }]}
    - StyleSheet s.header contains paddingBottom: Spacing.md (not paddingVertical)
    - No hardcoded numeric padding values in the dynamic style
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep -n "useSafeAreaInsets\|insets\.top" src/screens/ExerciseDetailScreen.tsx</automated>
  </verify>
  <done>
    useSafeAreaInsets is imported and used; Spacing is imported; header View has paddingTop: insets.top + Spacing.md applied inline.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove MuscleMapView section and state</name>
  <files>src/screens/ExerciseDetailScreen.tsx</files>
  <read_first>
    - src/screens/ExerciseDetailScreen.tsx (already read in Task 1 — focus on lines 13, 35, 109–119)
  </read_first>
  <action>
    In ExerciseDetailScreen.tsx (continuing the same file edit from Task 1):

    1. Remove the import line: `import MuscleMapView from '../components/MuscleMapView';`
       (The MuscleMapView.tsx component file itself is NOT deleted — only remove the import from this screen per D-05.)

    2. Remove the state declaration: `const [muscleView, setMuscleView] = useState<'front' | 'back'>('front');`

    3. Remove the entire muscle map section block in JSX, which spans:
       ```
       {/* 2. Muscle Map */}
       <View style={s.sectionCard}>
         <Text style={s.sectionLabel}>MUSCLES</Text>
         <MuscleMapView
           primaryMuscles={[exercise.muscleGroup]}
           secondaryMuscles={exercise.secondaryMuscles}
           interactive={false}
           view={muscleView}
           onViewToggle={() => setMuscleView(v => v === 'front' ? 'back' : 'front')}
         />
       </View>
       ```
       Remove this entire block including the comment line.

    The section comments (1. Illustration, 3. Metadata chips, 4. Form Cue, etc.) can be left as-is or renumbered — leave them as-is for minimal diff.

    After both tasks are applied, run tsc to confirm no errors.
  </action>
  <acceptance_criteria>
    - grep -n "MuscleMapView" src/screens/ExerciseDetailScreen.tsx returns no results
    - grep -n "muscleView" src/screens/ExerciseDetailScreen.tsx returns no results
    - The sectionCard View with sectionLabel "MUSCLES" and MuscleMapView child is absent from the file
    - src/components/MuscleMapView.tsx still exists (not deleted)
    - npx tsc --noEmit exits 0 with no errors in ExerciseDetailScreen.tsx
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "MuscleMapView\|muscleView" src/screens/ExerciseDetailScreen.tsx && test -f src/components/MuscleMapView.tsx && echo "muscle diagram removed, MuscleMapView.tsx still exists"</automated>
  </verify>
  <done>
    MuscleMapView import and muscleView state removed; muscle diagram section absent from JSX; MuscleMapView.tsx component file still present; tsc clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries | Pure UI fix — no network calls, no user input changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-SC | Tampering | No new npm installs | accept | react-native-safe-area-context already in package.json; no install needed |
</threat_model>

<verification>
1. npx tsc --noEmit — must exit 0
2. grep -n "useSafeAreaInsets\|insets\.top" src/screens/ExerciseDetailScreen.tsx — must show import and usage
3. ! grep -q "MuscleMapView\|muscleView" src/screens/ExerciseDetailScreen.tsx — must succeed (no matches)
4. test -f src/components/MuscleMapView.tsx — must exist
</verification>

<success_criteria>
- ExerciseDetailScreen header accounts for Dynamic Island via insets.top
- Muscle diagram section completely removed from JSX and its import/state cleaned up
- TypeScript compiles clean
</success_criteria>

<output>
Create .planning/phases/19-global-ux-fixes/19-02-SUMMARY.md when done
</output>
