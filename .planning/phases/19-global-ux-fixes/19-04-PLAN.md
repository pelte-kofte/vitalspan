---
phase: 19
plan: "19-04"
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - src/screens/LongevityScoreScreen.tsx
autonomous: true
requirements:
  - UX-04

must_haves:
  truths:
    - "Sleep dataOrb and metricCell are tappable; onPress routing follows permissionState: pre-request→handleRequestPermission, granted+no-data→OrbitalInfoModal with sleep message, denied→handleOpenSettings() called directly (no modal)"
    - "HRV dataOrb and metricCell are tappable; onPress routing follows permissionState: pre-request→handleRequestPermission, granted+no-data→OrbitalInfoModal with HRV/Watch message + Connect Health CTA, denied→same modal with handleOpenSettings as CTA"
    - "Fitness dataOrb and metricCell are tappable with same routing as HRV"
    - "Inflammation and Glucose dataOrbs and metricCells remain unchanged (existing onPress routing intact)"
    - "Recovery dataOrb/metricCell with val!=null has no onPress (static display)"
    - "OrbitalInfoModal renders with correct title, body, ctaLabel, and callback per metric and permissionState"
  artifacts:
    - path: "src/screens/LongevityScoreScreen.tsx"
      provides: "LongevityScoreScreen with orbital onPress handlers for Sleep, HRV, Fitness"
  key_links:
    - from: "src/screens/LongevityScoreScreen.tsx dataOrb/metricCell"
      to: "src/components/OrbitalInfoModal.tsx"
      via: "import OrbitalInfoModal + orbitalModalConfig state"
      pattern: "OrbitalInfoModal"
    - from: "src/screens/LongevityScoreScreen.tsx orbital onPress"
      to: "handleRequestPermission / handleOpenSettings"
      via: "permissionState branch in handleOrbitalPress"
      pattern: "handleOrbitalPress"
---

<objective>
Wire onPress handlers to the Sleep, HRV, and Fitness data orbs and metric grid cells in LongevityScoreScreen. Each handler checks permissionState and either triggers HealthKit permission request, shows an OrbitalInfoModal with contextual copy, or opens iOS Settings.

Purpose: The three orbs currently render as non-interactive Views. Users who tap them get no feedback. Fixing this closes the only missing interaction in the app's core health dashboard.
Output: Modified src/screens/LongevityScoreScreen.tsx.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/19-global-ux-fixes/19-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/src/theme/index.ts
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/19-global-ux-fixes/19-01-SUMMARY.md
</context>

<interfaces>
From src/components/OrbitalInfoModal.tsx (created in Plan 19-01):

  export interface OrbitalInfoModalProps {
    visible: boolean;
    title: string;
    body: string;
    ctaLabel?: string;
    onCta?: () => void;
    onDismiss: () => void;
  }
  export function OrbitalInfoModal(props: OrbitalInfoModalProps): JSX.Element
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add OrbitalInfoModal state and handler function</name>
  <files>src/screens/LongevityScoreScreen.tsx</files>
  <read_first>
    - src/screens/LongevityScoreScreen.tsx (lines 1–120 for imports and METRIC_EMPTY; lines 265–370 for permissionState, handleRequestPermission, handleOpenSettings; lines 640–710 for dataOrb and metricCell rendering)
  </read_first>
  <action>
    In LongevityScoreScreen.tsx:

    1. Add import at top of imports section:
       import OrbitalInfoModal from '../components/OrbitalInfoModal';

    2. Add state for the orbital modal config inside the LongevityScoreScreen function component, after the existing useState declarations:
       const [orbitalModal, setOrbitalModal] = useState<{ title: string; body: string; ctaLabel?: string; onCta?: () => void } | null>(null);

    3. Add a handleOrbitalPress function inside the component (after handleDismissPrompt):

       function handleOrbitalPress(metricKey: 'sleep' | 'hrv' | 'fitness') {
         if (permissionState === 'pre-request') {
           Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium).catch(() => null);
           handleRequestPermission();
           return;
         }
         if (metricKey === 'sleep') {
           if (permissionState === 'denied') {
             // D-04: denied → call handleOpenSettings() directly, no modal
             handleOpenSettings();
             return;
           }
           // permissionState === 'granted' but no data
           setOrbitalModal({
             title: 'Sleep Score Unavailable',
             body: 'Open Apple Health, record sleep for at least 3 nights, then return here to see your score.',
           });
         } else {
           // hrv or fitness
           const ctaAction = permissionState === 'denied'
             ? () => { setOrbitalModal(null); handleOpenSettings(); }
             : () => { setOrbitalModal(null); handleRequestPermission(); };
           setOrbitalModal({
             title: metricKey === 'hrv' ? 'HRV Score Unavailable' : 'Fitness Score Unavailable',
             body: 'HRV and VO₂ max require Apple Watch paired to this iPhone. Ensure your Watch syncs with the Health app, then tap Connect Health below.',
             ctaLabel: 'Connect Health',
             onCta: ctaAction,
           });
         }
       }

    Note: handleOrbitalPress should only be called when val == null (the orb/cell is in empty state). When val != null, no onPress is needed.
  </action>
  <acceptance_criteria>
    - OrbitalInfoModal is imported from '../components/OrbitalInfoModal'
    - orbitalModal state exists (type: { title: string; body: string; ctaLabel?: string; onCta?: () => void } | null)
    - handleOrbitalPress function exists, accepting 'sleep' | 'hrv' | 'fitness' as parameter
    - handleOrbitalPress branches correctly on permissionState ('pre-request', 'granted', 'denied')
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep -n "OrbitalInfoModal\|orbitalModal\|handleOrbitalPress" src/screens/LongevityScoreScreen.tsx | head -20</automated>
  </verify>
  <done>
    OrbitalInfoModal imported, orbitalModal state declared, handleOrbitalPress function defined with correct branches.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire onPress to dataOrb and metricCell renders + render OrbitalInfoModal</name>
  <files>src/screens/LongevityScoreScreen.tsx</files>
  <read_first>
    - src/screens/LongevityScoreScreen.tsx (lines 640–710 — dataOrb and metricGrid rendering; lines 760–782 — closing tags before StyleSheet)
  </read_first>
  <action>
    In the dataOrb render block (inside the Animated.View for DATA_POINTS.map, approximately lines 642–661):

    For each orb, the current code renders `<View key={dp.key} style={[s.dataOrb, ...]} >`.

    Wrap the empty-state orbs in TouchableOpacity for 'sleep', 'hrv', 'fitness' only. The condition is: val == null AND dp.key is one of 'sleep', 'hrv', 'fitness'.

    Change the render to: when val == null AND dp.key is in ['sleep', 'hrv', 'fitness'], wrap the orb View in a TouchableOpacity with:
    - activeOpacity={0.75}
    - onPress={() => handleOrbitalPress(dp.key as 'sleep' | 'hrv' | 'fitness')}
    - style={{ position: 'absolute', left: x - 30, top: y - 22 }} (same positioning as the current View)
    - The inner View gets the s.dataOrb style and s.dataOrbEmpty (remove position:absolute, left, top from the inner View since the outer TouchableOpacity now holds position)

    When val != null OR dp.key is 'inflammation' | 'glucose' | 'recovery', keep the current plain View with full style={[s.dataOrb, { left: x - 30, top: y - 22 }, val == null && s.dataOrbEmpty]} rendering unchanged.

    In the metricGrid render block (DATA_POINTS.map, approximately lines 685–706):

    Wrap the metricCell View in a TouchableOpacity for empty-state cells of 'sleep', 'hrv', 'fitness'. Condition: val == null AND dp.key in ['sleep', 'hrv', 'fitness'].

    Change: `<View key={dp.key} style={[s.metricCell, val == null && s.metricCellEmpty]}>` to:
    - If dp.key in ['sleep','hrv','fitness'] AND val == null:
      `<TouchableOpacity key={dp.key} style={[s.metricCell, val == null && s.metricCellEmpty]} activeOpacity={0.75} onPress={() => handleOrbitalPress(dp.key as 'sleep' | 'hrv' | 'fitness')}>`
      Closing tag becomes `</TouchableOpacity>`
    - Otherwise: keep plain View as is.

    After the TransparencyModal closing tag (near line 779), before `</SafeAreaView>`, add:
    ```
    {orbitalModal && (
      <OrbitalInfoModal
        visible={true}
        title={orbitalModal.title}
        body={orbitalModal.body}
        ctaLabel={orbitalModal.ctaLabel}
        onCta={orbitalModal.onCta}
        onDismiss={() => setOrbitalModal(null)}
      />
    )}
    ```

    Do NOT modify the inflammation or glucose orbs/cells — they already have onPress wiring handled elsewhere. Check carefully: if inflammation/glucose currently use plain View, leave them as View. Only wrap sleep/hrv/fitness in TouchableOpacity.

    After making all changes, run: npx tsc --noEmit — must exit 0.
  </action>
  <acceptance_criteria>
    - In the dataOrb map, sleep/hrv/fitness empty-state orbs (val == null) are wrapped in TouchableOpacity with onPress calling handleOrbitalPress
    - In the metricGrid map, sleep/hrv/fitness empty-state cells (val == null) are wrapped in TouchableOpacity with onPress calling handleOrbitalPress
    - OrbitalInfoModal renders when orbitalModal state is non-null
    - OrbitalInfoModal onDismiss sets orbitalModal to null
    - inflammation and glucose orbs/cells are not modified
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | grep -E "LongevityScoreScreen|OrbitalInfoModal" | head -20</automated>
  </verify>
  <done>
    Sleep/HRV/Fitness dataOrbs and metricCells are tappable when empty; OrbitalInfoModal renders with correct config; tsc clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → HealthKit | handleRequestPermission triggers iOS permission dialog — already existing; no new trust boundary |
| UI → iOS Settings | handleOpenSettings calls Linking.openURL('app-settings:') — already existing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04-01 | Tampering | No new npm installs | accept | No package-manager installs in this plan |
| T-19-04-SC | Tampering | OrbitalInfoModal state | accept | Pure UI state; no user data stored or transmitted |
</threat_model>

<verification>
1. npx tsc --noEmit — exits 0
2. grep -n "handleOrbitalPress\|OrbitalInfoModal\|orbitalModal" src/screens/LongevityScoreScreen.tsx — shows import, state, function definition, JSX usage
3. grep -n "TouchableOpacity" src/screens/LongevityScoreScreen.tsx — new entries visible for dataOrb and metricCell wrapping
</verification>

<success_criteria>
- Sleep, HRV, Fitness orbs and grid cells are tappable when val == null
- Tapping routes correctly per permissionState (pre-request → permission, granted+no-data → info modal, denied → settings)
- OrbitalInfoModal shows correct copy for Sleep vs HRV/Fitness
- Inflammation and Glucose unchanged
- TypeScript compiles clean
</success_criteria>

<output>
Create .planning/phases/19-global-ux-fixes/19-04-SUMMARY.md when done
</output>
