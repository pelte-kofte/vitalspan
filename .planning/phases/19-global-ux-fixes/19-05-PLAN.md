---
phase: 19
plan: "19-05"
type: execute
wave: 2
depends_on: []
files_modified:
  - src/screens/ProtocolScreen.tsx
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - "Tapping any TextInput in AddCustomSupplementModal raises the keyboard without obscuring the input field"
    - "Tapping outside the modal sheet (on the overlay background) dismisses the keyboard and closes the modal"
    - "Tapping a supplement suggestion row inside the modal while keyboard is open does not dismiss the keyboard unexpectedly"
    - "The modal sheet content is wrapped in KeyboardAvoidingView with behavior='padding' on iOS"
  artifacts:
    - path: "src/screens/ProtocolScreen.tsx"
      provides: "AddCustomSupplementModal with keyboard-safe layout and tap-outside dismiss"
  key_links:
    - from: "AddCustomSupplementModal overlay View"
      to: "Keyboard.dismiss() + onClose()"
      via: "TouchableWithoutFeedback wrapping overlay"
      pattern: "TouchableWithoutFeedback"
    - from: "AddCustomSupplementModal sheet content"
      to: "KeyboardAvoidingView"
      via: "KeyboardAvoidingView wrapping ms.sheet View"
      pattern: "KeyboardAvoidingView"
---

<objective>
Fix keyboard overlap and tap-outside dismiss in the ProtocolScreen AddCustomSupplementModal. The modal sheet content is wrapped in KeyboardAvoidingView so the keyboard pushes the sheet up rather than covering inputs. The overlay background becomes a TouchableWithoutFeedback so tapping outside the sheet dismisses the keyboard and closes the modal.

Purpose: Without KeyboardAvoidingView, tapping the Name/Dose/Notes TextInputs on iPhone 15 Pro causes the keyboard to slide up and cover the inputs. Users cannot see what they are typing.
Output: Modified AddCustomSupplementModal function component in src/screens/ProtocolScreen.tsx.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/19-global-ux-fixes/19-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/src/theme/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap AddCustomSupplementModal sheet in KeyboardAvoidingView with tap-outside dismiss</name>
  <files>src/screens/ProtocolScreen.tsx</files>
  <read_first>
    - src/screens/ProtocolScreen.tsx (lines 1–10 for current imports; lines 125–284 for AddCustomSupplementModal JSX and modal styles ms.overlay, ms.sheet)
  </read_first>
  <action>
    In ProtocolScreen.tsx:

    1. Update the import from 'react-native' to add: KeyboardAvoidingView, TouchableWithoutFeedback, Keyboard, Platform.
       The existing import already has: View, Text, ScrollView, TouchableOpacity, StyleSheet, SafeAreaView, RefreshControl, Modal, TextInput, Alert.
       Add the four new names to that same import statement.

    2. In the AddCustomSupplementModal JSX return (lines ~185–283), restructure the content as follows:

       Current structure:
         <Modal ...>
           <View style={ms.overlay}>
             <View style={ms.sheet}>
               {/* sheet content */}
             </View>
           </View>
         </Modal>

       New structure:
         <Modal ...>
           <TouchableWithoutFeedback onPress={() => { Keyboard.dismiss(); onClose(); resetForm(); }}>
             <View style={ms.overlay}>
               <KeyboardAvoidingView behavior={Platform.OS === 'ios' ? 'padding' : undefined}>
                 <TouchableWithoutFeedback onPress={() => { /* absorbs tap — prevents propagation to outer overlay */ }}>
                   <View style={ms.sheet}>
                     {/* all existing sheet content unchanged */}
                   </View>
                 </TouchableWithoutFeedback>
               </KeyboardAvoidingView>
             </View>
           </TouchableWithoutFeedback>
         </Modal>

       The outer TouchableWithoutFeedback wraps the entire overlay View — tapping anywhere outside the sheet calls Keyboard.dismiss() + onClose() + resetForm() (matching existing cancel behavior).

       The inner TouchableWithoutFeedback wraps only the sheet View — it stops the tap event from propagating to the outer overlay dismisser, so tapping inside the sheet does not close the modal.

       The KeyboardAvoidingView sits between the overlay View and the sheet View. It uses behavior='padding' on iOS (undefined on Android) — this is the same pattern used in AIAdvisorScreen per D-01.

    3. Add keyboardShouldPersistTaps="handled" to any ScrollView inside the modal sheet. The current sheet does NOT use a ScrollView — instead, dbResults are rendered as a plain View with TouchableOpacity rows. The existing TouchableOpacity for supplement suggestion rows (selectFromDb) will work correctly with this structure since they are inside the inner TouchableWithoutFeedback boundary.

       If a ScrollView is present in the sheet (e.g., wrapping the content for overflow), add keyboardShouldPersistTaps="handled" to it. If no ScrollView exists in the modal sheet, skip this step.

    4. Do NOT change ms.overlay, ms.sheet, or any other modal styles. Do NOT change any TextInput or field behavior. Do NOT change the ProtocolScreen main ScrollView.
  </action>
  <acceptance_criteria>
    - src/screens/ProtocolScreen.tsx imports KeyboardAvoidingView, TouchableWithoutFeedback, Keyboard, Platform from 'react-native'
    - AddCustomSupplementModal return JSX wraps the overlay View in TouchableWithoutFeedback calling Keyboard.dismiss() + onClose() + resetForm()
    - The ms.sheet View is wrapped in an inner TouchableWithoutFeedback to prevent propagation
    - KeyboardAvoidingView with behavior={Platform.OS === 'ios' ? 'padding' : undefined} wraps the sheet View
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep -n "KeyboardAvoidingView\|TouchableWithoutFeedback\|Keyboard\.dismiss\|Platform\.OS" src/screens/ProtocolScreen.tsx | head -20</automated>
  </verify>
  <done>
    KeyboardAvoidingView and TouchableWithoutFeedback present in AddCustomSupplementModal JSX; Keyboard.dismiss called on overlay tap; tsc clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries | Keyboard management only; no new data flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05-SC | Tampering | No new npm installs | accept | All APIs (KeyboardAvoidingView, TouchableWithoutFeedback, Keyboard, Platform) are from 'react-native' core; no package install needed |
</threat_model>

<verification>
1. npx tsc --noEmit — exits 0
2. grep -n "KeyboardAvoidingView\|TouchableWithoutFeedback\|Keyboard\.dismiss" src/screens/ProtocolScreen.tsx — confirms all three are present
3. Manual smoke-test on device/simulator: open Protocol → tap "Add custom supplement" → tap Name field → keyboard appears without covering the field
</verification>

<success_criteria>
- AddCustomSupplementModal wraps sheet in KeyboardAvoidingView behavior='padding' on iOS
- Tapping overlay background dismisses keyboard and closes modal
- Inner sheet taps do not propagate to overlay dismiss handler
- TypeScript compiles clean
</success_criteria>

<output>
Create .planning/phases/19-global-ux-fixes/19-05-SUMMARY.md when done
</output>
