---
phase: 19
plan: "19-03"
type: execute
wave: 2
depends_on: []
files_modified:
  - src/screens/DashboardScreen.tsx
autonomous: true
requirements:
  - UX-03

must_haves:
  truths:
    - "Dashboard 'Movement Today' card background is Colors.bgCard (white) instead of Colors.status.optimalBg (green tint)"
    - "Movement Today card is visually indistinct from adjacent upload/research/AI Advisor cards (matching background)"
    - "No other exerciseCard style properties are changed"
  artifacts:
    - path: "src/screens/DashboardScreen.tsx"
      provides: "Fixed exerciseCard background color for AA contrast"
  key_links:
    - from: "DashboardScreen exerciseCard StyleSheet entry"
      to: "Colors.bgCard"
      via: "backgroundColor: Colors.bgCard"
      pattern: "exerciseCard.*bgCard"
---

<objective>
Change the Dashboard "Movement Today" card background from Colors.status.optimalBg (#E8F5EE green tint) to Colors.bgCard (#FFFFFF white), matching the uploadCard, researchCard backdrop, and AI Advisor card so the section passes AA contrast and is visually consistent with adjacent cards.

Purpose: The green tint background reduced text/value contrast against the subtitle text (Colors.textMuted #8A8A82 on #E8F5EE fails AA). Switching to white (#FFFFFF) restores correct contrast and makes the card visually consistent with the three cards above it.
Output: One-line change to the exerciseCard StyleSheet entry in DashboardScreen.tsx.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/19-global-ux-fixes/19-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/src/theme/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change exerciseCard backgroundColor to Colors.bgCard</name>
  <files>src/screens/DashboardScreen.tsx</files>
  <read_first>
    - src/screens/DashboardScreen.tsx (lines 693–700 — exerciseCard StyleSheet entry; confirm exact property name and current value Colors.status.optimalBg)
  </read_first>
  <action>
    In the DashboardScreen.tsx StyleSheet (near line 696), find the exerciseCard entry:

      exerciseCard: { marginHorizontal: Spacing.base, marginBottom: Spacing.base, backgroundColor: Colors.status.optimalBg, borderRadius: Radius.xl, padding: Spacing.md, flexDirection: 'row', alignItems: 'center', gap: Spacing.md, shadowColor: '#000', shadowOffset: { width: 0, height: 2 }, shadowOpacity: 0.06, shadowRadius: 12, elevation: 2 },

    Change ONLY the backgroundColor property from Colors.status.optimalBg to Colors.bgCard.

    All other properties (marginHorizontal, marginBottom, borderRadius, padding, flexDirection, alignItems, gap, shadow, elevation) remain unchanged.

    Do NOT change uploadCardTitle, uploadCardSub, uploadCardArrow, or any other style. Do NOT add a border or change elevation.
  </action>
  <acceptance_criteria>
    - grep -n "exerciseCard" src/screens/DashboardScreen.tsx shows backgroundColor: Colors.bgCard
    - grep -n "exerciseCard" src/screens/DashboardScreen.tsx does NOT show Colors.status.optimalBg
    - No other style properties in the exerciseCard entry are changed
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep -A2 "exerciseCard:" src/screens/DashboardScreen.tsx | grep "backgroundColor"</automated>
  </verify>
  <done>
    exerciseCard.backgroundColor is Colors.bgCard; tsc clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries | Pure StyleSheet value change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-SC | Tampering | No npm installs | accept | No new packages; single value change |
</threat_model>

<verification>
1. npx tsc --noEmit — exits 0
2. grep -A5 "exerciseCard:" src/screens/DashboardScreen.tsx — shows Colors.bgCard, not Colors.status.optimalBg
</verification>

<success_criteria>
- exerciseCard.backgroundColor changed from Colors.status.optimalBg to Colors.bgCard
- TypeScript compiles clean
- No other style properties altered
</success_criteria>

<output>
Create .planning/phases/19-global-ux-fixes/19-03-SUMMARY.md when done
</output>
