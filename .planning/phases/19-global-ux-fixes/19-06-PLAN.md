---
phase: 19
plan: "19-06"
type: execute
wave: 3
depends_on:
  - "19-01"
  - "19-02"
  - "19-03"
  - "19-04"
  - "19-05"
files_modified: []
autonomous: false
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - UX-05

must_haves:
  truths:
    - "npx tsc --noEmit exits 0 with zero new errors across the entire project"
    - "All 5 UX bugs are verified fixed per their success criteria"
    - "AIAdvisorScreen keyboard behavior is confirmed working (smoke-test)"
    - "No regression in SettingsScreen, AboutScreen, or BiomarkerDetailScreen layouts"
  artifacts:
    - path: ".planning/phases/19-global-ux-fixes/19-06-SUMMARY.md"
      provides: "Phase 19 completion record"
  key_links:
    - from: "TypeScript compiler"
      to: "All modified screens and new OrbitalInfoModal component"
      via: "npx tsc --noEmit"
      pattern: "0 errors"
---

<objective>
Run the full TypeScript audit across all Phase 19 changes, verify AIAdvisorScreen keyboard behavior has not regressed, and perform a final cross-screen smoke-test confirming all 5 UX success criteria are met. This is a human verification checkpoint.

Purpose: All Wave 2 plans modify different files independently. This Wave 3 plan provides the integration gate — tsc must pass, and a human must confirm the 5 success criteria on device or simulator before Phase 19 is marked complete.
Output: tsc clean confirmation + human visual verification of all 5 fixes.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/19-global-ux-fixes/19-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Run full TypeScript audit</name>
  <files><!-- audit only — no files created --></files>
  <read_first>
    - None — this is a verification task only
  </read_first>
  <action>
    Run the TypeScript compiler across the full project:
      cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit

    If errors are found in Phase 19 modified files (ExerciseDetailScreen.tsx, DashboardScreen.tsx, LongevityScoreScreen.tsx, ProtocolScreen.tsx, OrbitalInfoModal.tsx), fix them before proceeding.

    Common issues to watch for:
    - OrbitalInfoModal: missing onCta when ctaLabel is provided (optional prop — acceptable)
    - LongevityScoreScreen: dp.key cast to 'sleep' | 'hrv' | 'fitness' must not cause type error — if DATA_POINTS key union does not accept the cast cleanly, use a type assertion (dp.key as 'sleep' | 'hrv' | 'fitness') only inside the conditional block already gated by that key check
    - ProtocolScreen: Keyboard and Platform imports must be in the 'react-native' import statement (not separate import statements)
    - ExerciseDetailScreen: insets type from useSafeAreaInsets() is EdgeInsets — insets.top is number, valid for paddingTop arithmetic

    After fixing any errors, re-run tsc to confirm 0 errors.

    Also run a source audit for hardcoded hex values in modified files:
      grep -n "#[0-9A-Fa-f]\{6\}" src/components/OrbitalInfoModal.tsx src/screens/ExerciseDetailScreen.tsx src/screens/DashboardScreen.tsx src/screens/LongevityScoreScreen.tsx src/screens/ProtocolScreen.tsx

    Any hit that is NOT inside a comment or a string that is already a token reference must be replaced with a theme token from src/theme/index.ts.
  </action>
  <acceptance_criteria>
    - npx tsc --noEmit exits with code 0 and produces no error output
    - grep for hardcoded hex values in the 5 modified files returns 0 new hits (hits in pre-existing untouched lines are acceptable)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit && echo "TSC CLEAN"</automated>
  </verify>
  <done>
    tsc exits 0; "TSC CLEAN" printed; no hardcoded hex values introduced in modified files.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 5 Phase 19 UX fixes applied and TypeScript-verified:
    1. ProtocolScreen AddCustomSupplementModal: KeyboardAvoidingView + tap-outside dismiss (D-01, D-06)
    2. ExerciseDetailScreen: Dynamic Island inset on custom header + muscle diagram removed (D-02, D-05)
    3. DashboardScreen: Movement Today card background changed to white (D-03)
    4. LongevityScoreScreen: Sleep/HRV/Fitness orbital CTAs wired with OrbitalInfoModal (D-04)
    5. AIAdvisorScreen: Keyboard behavior smoke-test (D-01 verification)
  </what-built>
  <how-to-verify>
    Run the app on iPhone 15 Pro simulator or physical device (npx expo start → press i):

    Fix 1 — Keyboard in ProtocolScreen (UX-01):
    - Navigate to Protocol tab
    - Tap the "+" button or "Add custom supplement" to open AddCustomSupplementModal
    - Tap the "Name" text field
    - Expected: keyboard slides up and the Name field remains visible above it
    - Tap outside the sheet (on the dark overlay)
    - Expected: keyboard dismisses AND modal closes

    Fix 2 — Dynamic Island header on ExerciseDetailScreen (UX-02):
    - Navigate to Exercise tab → tap any exercise → tap to open ExerciseDetailScreen
    - Expected: back button "← [Exercise Name]" is fully visible below the Dynamic Island notch with no clipping
    - Confirm: muscle diagram (front/back silhouette) is ABSENT from the screen

    Fix 3 — Dashboard Movement Today contrast (UX-03):
    - Navigate to Dashboard tab
    - Scroll to the "Movement Today" card
    - Expected: card background is white (matching "Upload lab results", "Longevity Research", "AI Advisor" cards above it)
    - Text and values are clearly readable

    Fix 4 — LongevityScore orbital CTAs (UX-04):
    - Navigate to LongevityScore (tap the sphere on Dashboard, or tap the Longevity tab)
    - If HealthKit not connected: tap the Sleep orb (top of sphere) or the Sleep cell in the grid
      Expected: HealthKit permission dialog appears (pre-request state)
    - If HealthKit connected but no sleep data: tap Sleep orb
      Expected: OrbitalInfoModal appears with "Sleep Score Unavailable" title and "3 nights required" body; tapping "Got it" dismisses it
    - Tap Inflammation or Glucose orbs/cells
      Expected: still routes to BiomarkerEntry as before (unchanged)

    Fix 5 — AIAdvisorScreen keyboard smoke-test (UX-01):
    - Navigate to AI Advisor (via Dashboard Intelligence section, requires premium or premium mock)
    - If a report is generated, tap the chat input at the bottom
    - Expected: keyboard appears without obscuring the input field (pre-existing KeyboardAvoidingView behavior intact)

    If all 5 checks pass, type "approved". If any check fails, describe the issue.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 19 complete, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new trust boundaries | Audit and verification only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06-SC | Tampering | No npm installs in this plan | accept | Audit plan only; no package changes |
</threat_model>

<verification>
1. npx tsc --noEmit exits 0 (automated)
2. Human verification of all 5 success criteria on device/simulator (checkpoint)
</verification>

<success_criteria>
- TypeScript compiles with 0 errors across entire project
- All 5 Phase 19 UX bugs confirmed fixed by human verification
- Phase 19 marked complete in STATE.md and ROADMAP.md
</success_criteria>

<output>
Create .planning/phases/19-global-ux-fixes/19-06-SUMMARY.md when done
</output>
