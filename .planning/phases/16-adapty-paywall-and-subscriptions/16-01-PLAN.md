---
phase: 16-adapty-paywall-and-subscriptions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app.json
  - .env.example
  - src/lib/adapty.ts
autonomous: false
requirements: [PAY-01]
must_haves:
  truths:
    - "react-native-adapty and tslib are installed and in package.json"
    - "app.json plugins array contains react-native-adapty"
    - "EXPO_PUBLIC_ADAPTY_API_KEY is documented in .env.example"
    - "src/lib/adapty.ts exports activationPromise, fetchPremiumStatus, identifyAdaptyUser"
  artifacts:
    - path: "src/lib/adapty.ts"
      provides: "Adapty singleton activation + helpers"
      exports: ["activationPromise", "fetchPremiumStatus", "identifyAdaptyUser"]
    - path: ".env.example"
      provides: "API key documentation"
      contains: "EXPO_PUBLIC_ADAPTY_API_KEY="
  key_links:
    - from: "App.tsx"
      to: "src/lib/adapty.ts"
      via: "await activationPromise"
      pattern: "activationPromise"
    - from: "src/lib/adapty.ts"
      to: "react-native-adapty"
      via: "adapty.activate()"
      pattern: "adapty\\.activate"
---

<objective>
Install the react-native-adapty SDK behind a package legitimacy checkpoint, register the Expo config plugin in app.json, document the environment variable, and create the adapty.ts singleton with activation promise and helper functions.

Purpose: Establishes the Adapty SDK foundation that PremiumContext, PaywallScreen, and App.tsx all depend on. Nothing Adapty-related can be built until the SDK is installed and the singleton exists.
Output: adapty.ts singleton, updated app.json plugins, .env.example entry, installed packages.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/STATE.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/16-adapty-paywall-and-subscriptions/16-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/16-adapty-paywall-and-subscriptions/16-RESEARCH.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/16-adapty-paywall-and-subscriptions/16-PATTERNS.md

<interfaces>
From src/lib/supabase.ts (singleton pattern to mirror in adapty.ts):
- Module-level singleton created at import time
- EXPO_PUBLIC_* env var guard pattern: read env var, guard missing value with console.error, never throw
- Never-throws async helpers: try/catch, console.warn on error, return safe fallback
- AppState listener pattern used for JWT refresh (mirrors PremiumContext AppState refresh)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Package Legitimacy Gate</name>
  <what-built>
    The research phase flagged all four packages as [ASSUMED] because slopcheck was unavailable. Before any npm install, you must verify each package is legitimate.
  </what-built>
  <how-to-verify>
    Verify each package manually:
    1. Visit https://www.npmjs.com/package/react-native-adapty — confirm publisher is "adaptyteam", version 3.17.1 exists, weekly downloads are significant (>10k), source links to github.com/adaptyteam/AdaptySDK-React-Native
    2. Visit https://www.npmjs.com/package/tslib — confirm publisher is "typescript-team" or "microsoft", weekly downloads are in the hundreds of millions
    3. Confirm react-native-adapty has no suspicious postinstall scripts: run `npm view react-native-adapty scripts` and verify no postinstall is listed
    All three checks must pass before proceeding.
    4. Verify Expo SDK 54 compatibility: run `npm view react-native-adapty peerDependencies` and confirm the react-native peer dependency range includes >=0.74.0 (Expo SDK 54 ships React Native 0.74).
  </how-to-verify>
  <resume-signal>Type "verified" if all three checks pass, or describe any concern found</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install Packages + Config Plugin + Singleton</name>
  <files>app.json, .env.example, src/lib/adapty.ts</files>
  <read_first>
    - app.json — current plugins array (must not break existing @kingstinct/react-native-healthkit entry)
    - .env.example — existing env vars to understand format; add below existing entries
    - src/lib/supabase.ts — singleton pattern, env var guard pattern, never-throws helper pattern to mirror exactly
  </read_first>
  <action>
    Step 1 — Install packages (run from project root):
    npx expo install react-native-adapty tslib
    This uses expo install for Expo SDK compatibility resolution. Do NOT use npm install directly.

    Step 2 — Add react-native-adapty to app.json plugins array. The plugins array currently contains "expo-font" and the @kingstinct/react-native-healthkit array entry. Append "react-native-adapty" as a plain string (no options needed for coded paywall — no fallbackFile config required). The resulting plugins array must be:
    ["expo-font", ["@kingstinct/react-native-healthkit", {...}], "react-native-adapty"]
    Do NOT modify the @kingstinct/react-native-healthkit entry in any way — HealthKit entitlements depend on it.

    Step 3 — Add to .env.example (append after existing entries):
    EXPO_PUBLIC_ADAPTY_API_KEY=your_adapty_public_sdk_key_here
    Note: This is the public SDK key from the Adapty dashboard (App Settings → API Keys → Public SDK key). It is safe to commit to .env.example — it is not a secret key.

    Step 4 — Create src/lib/adapty.ts. This file mirrors the supabase.ts singleton pattern exactly. It must:
    - Import adapty and AdaptyPaywallProduct from 'react-native-adapty'
    - Read ADAPTY_API_KEY from process.env.EXPO_PUBLIC_ADAPTY_API_KEY ?? ''
    - Guard missing key with console.error (matching supabase.ts guard style — never throw)
    - Export activationPromise: Promise<void> — a module-level IIFE that calls adapty.activate(ADAPTY_API_KEY) and catches errors with console.warn
    - Export identifyAdaptyUser(userId: string): Promise<void> — awaits activationPromise first, then adapty.identify(userId), wrapped in try/catch with console.warn
    - Export fetchPremiumStatus(): Promise<boolean> — awaits activationPromise, calls adapty.getProfile(), returns profile.accessLevels?.['premium']?.isActive ?? false, returns false on any error
    - No other exports. No AppState listener in this file (that belongs in PremiumContext.tsx).
    - Per CLAUDE.md: StyleSheet rule does not apply to lib files. No inline styles.
    - Strict TypeScript: no 'any' types. All catch variables typed as unknown or omitted.

    Do NOT run expo prebuild in this task — that is a manual step because it clears the ios/ folder and the developer must run it locally with Xcode available.
  </action>
  <verify>
    <automated>grep -c "react-native-adapty" /Users/bekircemkusdemir/Downloads/vitalspan/package.json && grep -c "react-native-adapty" /Users/bekircemkusdemir/Downloads/vitalspan/app.json && grep -c "EXPO_PUBLIC_ADAPTY_API_KEY" /Users/bekircemkusdemir/Downloads/vitalspan/.env.example && npx tsc --noEmit --project /Users/bekircemkusdemir/Downloads/vitalspan/tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies contains "react-native-adapty" with version ~3.17.1
    - package.json dependencies contains "tslib"
    - app.json plugins array contains "react-native-adapty" as a string entry
    - app.json plugins array still contains the @kingstinct/react-native-healthkit array entry unchanged
    - .env.example contains the line EXPO_PUBLIC_ADAPTY_API_KEY=your_adapty_public_sdk_key_here
    - src/lib/adapty.ts exists and exports activationPromise, identifyAdaptyUser, fetchPremiumStatus
    - npx tsc --noEmit exits 0 (no TypeScript errors introduced)
  </acceptance_criteria>
  <done>Adapty SDK installed, config plugin registered, env var documented, singleton file with all three exports created, tsc clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Adapty SDK | App calls Adapty SDK methods; SDK communicates with Adapty backend for receipt validation |
| env var → bundle | EXPO_PUBLIC_ADAPTY_API_KEY is compiled into the app bundle — it is a public SDK key by design |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | EXPO_PUBLIC_ADAPTY_API_KEY in bundle | accept | Adapty public SDK keys are designed to be bundled — they identify the app, not authorize privileged actions. Server-side receipt validation is Adapty's backend responsibility. Do NOT use the secret API key (admin key) — only the public SDK key. |
| T-16-SC | Tampering | react-native-adapty, tslib npm install | mitigate | Package legitimacy gate (Task 1, blocking human checkpoint) verifies npm publisher, download count, and absence of postinstall scripts before install proceeds. |
</threat_model>

<verification>
- `grep "react-native-adapty" package.json` returns a match
- `grep "react-native-adapty" app.json` returns a match
- `grep "EXPO_PUBLIC_ADAPTY_API_KEY" .env.example` returns a match
- `cat src/lib/adapty.ts | grep -c "export"` returns 3 or more
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
react-native-adapty is installed and registered as a config plugin. adapty.ts singleton exports the three required functions. TypeScript compiles clean. Developer knows to run `npx expo prebuild --clean && cd ios && pod install && cd ..` locally before building the dev client.
</success_criteria>

<output>
Create `.planning/phases/16-adapty-paywall-and-subscriptions/16-01-SUMMARY.md` when done
</output>
