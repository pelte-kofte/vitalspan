---
phase: 16-adapty-paywall-and-subscriptions
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/context/PremiumContext.tsx
  - src/navigation/AppNavigator.tsx
  - src/screens/AIAdvisorScreen.tsx
autonomous: true
requirements: [PAY-04, AI-06]
must_haves:
  truths:
    - "PremiumContext exports PremiumProvider and usePremiumContext hook"
    - "PremiumContext has isPremium, isPremiumLoading, and refreshPremium"
    - "AppNavigator RootStackParamList includes Paywall and AIAdvisor routes"
    - "Both routes use fullScreenModal presentation matching LongevityScore config"
    - "AIAdvisorScreen renders a valid stub fullScreenModal with back button"
  artifacts:
    - path: "src/context/PremiumContext.tsx"
      provides: "isPremium boolean state + refreshPremium + AppState listener"
      exports: ["PremiumProvider", "usePremiumContext"]
    - path: "src/screens/AIAdvisorScreen.tsx"
      provides: "Stub fullScreenModal for AI Advisor route"
    - path: "src/navigation/AppNavigator.tsx"
      provides: "Paywall and AIAdvisor routes in RootStack"
      contains: "Paywall: undefined"
  key_links:
    - from: "src/context/PremiumContext.tsx"
      to: "src/lib/adapty.ts"
      via: "fetchPremiumStatus import"
      pattern: "fetchPremiumStatus"
    - from: "src/navigation/AppNavigator.tsx"
      to: "src/screens/AIAdvisorScreen.tsx"
      via: "Stack.Screen component prop"
      pattern: "AIAdvisorScreen"
---

<objective>
Create PremiumContext.tsx (the premium state provider), add the Paywall and AIAdvisor routes to AppNavigator, and create the AIAdvisorScreen stub. These three pieces give downstream plans the React context and navigation routes they depend on.

Purpose: PremiumContext is consumed by DashboardScreen, PaywallScreen, and any future gated screen. The route declarations in AppNavigator are required before any screen can call nav.navigate('Paywall') or nav.navigate('AIAdvisor'). This plan runs as soon as 16-01 completes.
Output: PremiumContext.tsx with provider + hook, AppNavigator extended with two routes, AIAdvisorScreen.tsx stub.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/STATE.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/16-adapty-paywall-and-subscriptions/16-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/16-adapty-paywall-and-subscriptions/16-PATTERNS.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/16-adapty-paywall-and-subscriptions/16-01-SUMMARY.md

<interfaces>
From src/lib/adapty.ts (created in 16-01):
- export const activationPromise: Promise<void>
- export async function fetchPremiumStatus(): Promise<boolean>
- export async function identifyAdaptyUser(userId: string): Promise<void>

From src/lib/supabase.ts (AppState listener pattern to mirror):
- AppState.addEventListener('change', (nextAppState) => { if (nextAppState === 'active') ... })
- Subscription cleanup: sub.remove() in return of useEffect

From src/navigation/AppNavigator.tsx (existing fullScreenModal pattern, line 183-186):
- presentation: 'fullScreenModal', animation: 'fade_from_bottom'
- Import pattern: import ScreenName from '../screens/ScreenName'
- RootStackParamList: route name as key, params type as value (undefined for no params)

From src/screens/LongevityScoreScreen.tsx (stub shell pattern):
- LinearGradient colors={['#080D09', '#0C1410', '#0F1C14']}
- SafeAreaView wraps all content
- topBar with back button (nav.goBack()), screen title, width-38 spacer
- StyleSheet named s at bottom of file
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PremiumContext.tsx</name>
  <files>src/context/PremiumContext.tsx</files>
  <read_first>
    - src/lib/supabase.ts — AppState.addEventListener pattern, subscription cleanup pattern in module scope (lines 47-61)
    - src/lib/adapty.ts (from 16-01 output) — fetchPremiumStatus signature to import
  </read_first>
  <action>
    Create the directory src/context/ (it does not exist — this is the first React context in the project).

    Create src/context/PremiumContext.tsx with:

    Imports: React (createContext, useContext, useEffect, useState), AppState and AppStateStatus from 'react-native', adapty from 'react-native-adapty', fetchPremiumStatus from '../lib/adapty'.

    PremiumContextValue interface with three fields:
    - isPremium: boolean
    - isPremiumLoading: boolean (prevents locked-UI flash on first load — pitfall 6 from RESEARCH.md)
    - refreshPremium: () => Promise<void>

    createContext with defaults: isPremium false, isPremiumLoading true, refreshPremium async no-op.

    PremiumProvider component:
    - useState for isPremium (false) and isPremiumLoading (true)
    - refreshPremium async function: calls fetchPremiumStatus(), setIsPremium(result), setIsPremiumLoading(false)
    - useEffect (runs once on mount):
      1. Call refreshPremium() for initial load
      2. Add adapty.addEventListener('onLatestProfileLoad', callback) — callback sets isPremium from profile.accessLevels?.['premium']?.isActive ?? false and setIsPremiumLoading(false)
      3. Add AppState.addEventListener('change', handler) — handler calls refreshPremium() when state === 'active'
      4. Return cleanup: listener.remove(), sub.remove()
    - Return PremiumContext.Provider with value={{ isPremium, isPremiumLoading, refreshPremium }}

    Export usePremiumContext = () => useContext(PremiumContext) as a named export.

    TypeScript strict: no 'any'. AppState handler param typed as AppStateStatus. adapty.addEventListener callback param: type it as whatever the SDK provides — check the installed types after 16-01 install (it will be an AdaptyProfile or similar type). Use optional chaining for all Adapty profile property access.

    No StyleSheet (this is a context file, not a screen). Per CLAUDE.md: no inline styles (not applicable here).
  </action>
  <verify>
    <automated>npx tsc --noEmit --project /Users/bekircemkusdemir/Downloads/vitalspan/tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/context/PremiumContext.tsx exists
    - File exports PremiumProvider (named export) and usePremiumContext (named export)
    - PremiumContextValue interface has isPremium: boolean, isPremiumLoading: boolean, refreshPremium: () => Promise&lt;void&gt;
    - useEffect cleanup returns both listener.remove() and sub.remove() calls
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>PremiumContext.tsx exists with provider, loading flag, AppState refresh listener, and usePremiumContext hook. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extend AppNavigator + Create AIAdvisorScreen Stub</name>
  <files>src/navigation/AppNavigator.tsx, src/screens/AIAdvisorScreen.tsx</files>
  <read_first>
    - src/navigation/AppNavigator.tsx — full file to understand RootStackParamList (lines 29-44), existing fullScreenModal config (lines 183-186 for LongevityScore), import block (lines 1-27)
    - src/screens/LongevityScoreScreen.tsx lines 1-60 — import pattern, Nav type alias, Dimensions usage
    - src/screens/LongevityScoreScreen.tsx lines 556-590 — fullScreenModal shell: LinearGradient, SafeAreaView, topBar with back button and screen title
  </read_first>
  <action>
    STEP A — Create src/screens/AIAdvisorScreen.tsx (stub only; Phase 18 fills the content):

    This is a minimal fullScreenModal shell matching the LongevityScoreScreen pattern exactly. It must:
    - Import React, View, Text, StyleSheet, TouchableOpacity, SafeAreaView from 'react-native'
    - Import useNavigation from '@react-navigation/native'
    - Import NativeStackNavigationProp from '@react-navigation/native-stack'
    - Import LinearGradient from 'expo-linear-gradient'
    - Import Colors, Spacing from '../theme'
    - Import RootStackParamList from '../navigation/AppNavigator'
    - Define type Nav = NativeStackNavigationProp<RootStackParamList>
    - Render LinearGradient with colors={['#080D09', '#0C1410', '#0F1C14']} and style={s.gradient}
    - Render SafeAreaView with style={s.safe} inside the gradient
    - Render a topBar View with: back button (nav.goBack()), centered Text "AI ADVISOR", width-38 spacer View
    - The body is empty — no "Coming soon" label, no placeholder text (per D-04: stub screen with no content; Phase 18 fills it)
    - StyleSheet named s at bottom: gradient (flex 1), safe (flex 1), topBar (flexDirection row, justifyContent space-between, alignItems center, padding based on Spacing tokens), backBtn (padding Spacing.sm), backArrow (color Colors.dark?.text or Colors.surface, fontSize 22), screenTitle (color Colors.dark?.text or Colors.surface, fontWeight '600', letterSpacing 1.5, fontSize from Typography)
    - No hardcoded hex values — all from Colors.*

    STEP B — Extend src/navigation/AppNavigator.tsx:

    1. Add to the import block (after the existing screen imports, before the theme/icon imports):
       import PaywallScreen from '../screens/PaywallScreen';
       import AIAdvisorScreen from '../screens/AIAdvisorScreen';
       Note: PaywallScreen does not exist yet (created in 16-04), but the TypeScript import will cause a compile error. Add the import with a comment: "// PaywallScreen created in 16-04-PLAN" and temporarily point it at AIAdvisorScreen OR use a TODO comment and skip PaywallScreen import until 16-04. Since TypeScript will fail if the file doesn't exist, use only the AIAdvisorScreen import now. The PaywallScreen import will be added in 16-04.

    2. Extend RootStackParamList (after 'ExerciseDetail: { exerciseId: string }' on line 44):
       Paywall: undefined;
       AIAdvisor: undefined;

    3. Add two Stack.Screen entries in the Stack.Navigator, after the ExerciseDetail screen entry:
       <Stack.Screen name="Paywall" component={/* placeholder until 16-04 */} options={{ presentation: 'fullScreenModal', animation: 'fade_from_bottom' }} />
       <Stack.Screen name="AIAdvisor" component={AIAdvisorScreen} options={{ presentation: 'fullScreenModal', animation: 'fade_from_bottom' }} />

    For the Paywall route: since PaywallScreen doesn't exist yet, temporarily set component={AIAdvisorScreen} with a comment "// temporary until PaywallScreen created in 16-04". The 16-04 plan will update this to the real PaywallScreen component and add the real import.

    TypeScript strict: RootStackParamList must compile — the two new route entries suffice.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project /Users/bekircemkusdemir/Downloads/vitalspan/tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/screens/AIAdvisorScreen.tsx exists
    - AIAdvisorScreen renders LinearGradient + SafeAreaView + topBar with nav.goBack() back button
    - AIAdvisorScreen has no body content (no "Coming soon" or placeholder text)
    - AppNavigator.tsx RootStackParamList contains "Paywall: undefined" and "AIAdvisor: undefined"
    - AppNavigator.tsx Stack.Navigator contains Stack.Screen entries for both Paywall and AIAdvisor with presentation: 'fullScreenModal'
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>AIAdvisorScreen stub exists. RootStackParamList has Paywall and AIAdvisor routes. Both routes configured as fullScreenModal. TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PremiumContext → Adapty SDK | getProfile() is called on app focus; Adapty is the source of truth for premium state |
| PremiumContext → React tree | isPremium value propagated to all consumers; no AsyncStorage persistence (D-11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | Tampering | PremiumContext isPremium default | mitigate | Default is false (not true) — no user starts as premium without Adapty confirming. isPremiumLoading guard prevents content flash before status resolves. |
| T-16-03 | Tampering | AsyncStorage premium cache | mitigate | D-11 decision: no AsyncStorage key for premium status. PremiumContext holds in-memory state only. App restart calls getProfile() fresh — no stale cache can grant premium access. |
</threat_model>

<verification>
- src/context/PremiumContext.tsx exists with PremiumProvider and usePremiumContext exports
- src/screens/AIAdvisorScreen.tsx exists with LinearGradient shell and empty body
- AppNavigator.tsx RootStackParamList contains Paywall: undefined and AIAdvisor: undefined
- Both Stack.Screen entries have presentation: 'fullScreenModal'
- npx tsc --noEmit exits 0
</verification>

<success_criteria>
PremiumContext provides isPremium, isPremiumLoading, and refreshPremium. AppNavigator has both routes. AIAdvisorScreen stub navigable with back button. TypeScript compiles clean.
</success_criteria>

<output>
Create `.planning/phases/16-adapty-paywall-and-subscriptions/16-02-SUMMARY.md` when done
</output>
