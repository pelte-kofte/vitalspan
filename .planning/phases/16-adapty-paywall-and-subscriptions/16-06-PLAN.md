---
phase: 16-adapty-paywall-and-subscriptions
plan: 06
type: execute
wave: 4
depends_on: [16-04, 16-05]
files_modified: []
autonomous: false
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, PAY-05, AI-06]
must_haves:
  truths:
    - "tsc --noEmit exits 0 across the whole project"
    - "No Supabase URL or anon key exists in any source file (security audit)"
    - "No EXPO_PUBLIC_ADAPTY_API_KEY value hardcoded in any source file (only in .env)"
    - "Free user tapping Research card sees Paywall, not Articles screen"
    - "Free user tapping AI Advisor card sees Paywall, not AIAdvisor screen"
    - "Premium user tapping Research card sees Articles screen directly"
    - "Premium user tapping AI Advisor card sees AIAdvisorScreen stub"
    - "PaywallScreen Restore Purchases flow shows correct alert when no subscription found"
    - "App cold-start does not crash after Phase 16 changes"
  artifacts:
    - path: "src/lib/adapty.ts"
      provides: "Adapty singleton"
      exports: ["activationPromise", "fetchPremiumStatus", "identifyAdaptyUser"]
    - path: "src/context/PremiumContext.tsx"
      provides: "Premium state provider"
      exports: ["PremiumProvider", "usePremiumContext"]
    - path: "src/screens/PaywallScreen.tsx"
      provides: "Compliant paywall screen"
    - path: "src/screens/AIAdvisorScreen.tsx"
      provides: "AI Advisor stub"
    - path: "App.tsx"
      provides: "Adapty activation + PremiumProvider wrap"
    - path: "src/screens/DashboardScreen.tsx"
      provides: "Intelligence section with gated cards"
  key_links:
    - from: "App.tsx"
      to: "PremiumProvider"
      via: "JSX wrapping AppNavigator"
      pattern: "PremiumProvider"
    - from: "DashboardScreen.tsx"
      to: "Paywall or Articles/AIAdvisor"
      via: "isPremium ternary in onPress"
      pattern: "isPremium"
---

<objective>
Run final integration checks: TypeScript compilation, security source audit, and manual on-device verification of all five phase success criteria. This plan does not modify any files — it confirms the entire phase is correct.

Purpose: Gate-check before marking Phase 16 complete. The human checkpoint verifies the purchase and restore flows on device with a sandbox Apple ID, confirming the paywall works end-to-end.
Output: No new files. Verification report captured in SUMMARY.md.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/16-adapty-paywall-and-subscriptions/16-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/16-adapty-paywall-and-subscriptions/16-04-SUMMARY.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/16-adapty-paywall-and-subscriptions/16-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript + Security Audit</name>
  <files></files>
  <read_first>
    - No files to read — this task runs CLI commands only
  </read_first>
  <action>
    Run the following audits and report results:

    AUDIT 1 — TypeScript compilation:
    npx tsc --noEmit --project /Users/bekircemkusdemir/Downloads/vitalspan/tsconfig.json
    Must exit 0. If errors exist, fix them before proceeding (they indicate a wiring bug from 16-01 through 16-05).

    AUDIT 2 — No secrets in source:
    grep -r "EXPO_PUBLIC_SUPABASE_URL\s*=" src/ App.tsx --include="*.ts" --include="*.tsx" 2>/dev/null | grep -v ".env"
    grep -r "eyJ" src/ App.tsx --include="*.ts" --include="*.tsx" 2>/dev/null
    Both must return no matches (zero lines of output).

    AUDIT 3 — No hardcoded Adapty key in source:
    grep -r "EXPO_PUBLIC_ADAPTY_API_KEY\s*=" src/ App.tsx --include="*.ts" --include="*.tsx" 2>/dev/null | grep -v ".env"
    The grep should return only the process.env.EXPO_PUBLIC_ADAPTY_API_KEY ?? '' pattern in src/lib/adapty.ts — NOT an actual key value. If any match contains a real key (starting with "public_live_" or similar), that is a critical security issue — remove it and use process.env.

    AUDIT 4 — No AsyncStorage premium cache:
    grep -r "vitalspan_is_premium\|isPremium.*AsyncStorage\|AsyncStorage.*isPremium" src/ App.tsx --include="*.ts" --include="*.tsx" 2>/dev/null
    Must return no matches (D-11 compliance).

    AUDIT 5 — adapty.logShowPaywall present in PaywallScreen:
    grep -c "logShowPaywall" /Users/bekircemkusdemir/Downloads/vitalspan/src/screens/PaywallScreen.tsx
    Must return 1 (exactly one call).

    AUDIT 6 — Restore Purchases button present:
    grep -c "restorePurchases" /Users/bekircemkusdemir/Downloads/vitalspan/src/screens/PaywallScreen.tsx
    Must return at least 1.

    AUDIT 7 — Intelligence section in DashboardScreen:
    grep -c "Intelligence" /Users/bekircemkusdemir/Downloads/vitalspan/src/screens/DashboardScreen.tsx
    Must return at least 1.

    AUDIT 8 — PremiumProvider in App.tsx:
    grep -c "PremiumProvider" /Users/bekircemkusdemir/Downloads/vitalspan/App.tsx
    Must return at least 2 (import + usage).

    Report all results inline. If any audit fails, fix the issue before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>npx tsc --noEmit --project /Users/bekircemkusdemir/Downloads/vitalspan/tsconfig.json 2>&1 | tail -3 && grep -c "logShowPaywall" /Users/bekircemkusdemir/Downloads/vitalspan/src/screens/PaywallScreen.tsx && grep -c "restorePurchases" /Users/bekircemkusdemir/Downloads/vitalspan/src/screens/PaywallScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits 0
    - No Supabase URL or anon key literal in any .ts/.tsx file
    - No EXPO_PUBLIC_ADAPTY_API_KEY value (only process.env reference) in source files
    - No AsyncStorage key for premium status in source files
    - grep -c "logShowPaywall" PaywallScreen.tsx returns 1
    - grep -c "restorePurchases" PaywallScreen.tsx returns at least 1
    - grep -c "Intelligence" DashboardScreen.tsx returns at least 1
    - grep -c "PremiumProvider" App.tsx returns at least 2
  </acceptance_criteria>
  <done>All automated audits pass. TypeScript clean. No secrets in source. All required Adapty calls present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-Device Integration Verification</name>
  <what-built>
    Phase 16 has built and wired:
    - react-native-adapty SDK with activation + identify + getProfile helpers
    - PremiumContext providing isPremium state with AppState refresh
    - PaywallScreen: hybrid dark hero + white price card with Day 1-7 trial timeline, annual/monthly plans, restore purchases
    - AIAdvisorScreen: stub fullScreenModal
    - AppNavigator: Paywall and AIAdvisor routes as fullScreenModal
    - App.tsx: Adapty activation in init(), PremiumProvider wrapping AppNavigator
    - DashboardScreen: Intelligence section with gated Research and AI Advisor cards
  </what-built>
  <how-to-verify>
    Run the dev client on a real iOS device (simulator cannot test IAP). Use a sandbox Apple ID (created in 16-03).

    SCENARIO 1 — Free user gate check (no active subscription):
    1. Open the app → go to Dashboard tab
    2. Confirm you see an "Intelligence" section header above the protocol section
    3. Confirm you see two cards: "Longevity Research" and "AI Advisor"
    4. Tap "Longevity Research" — confirm the PaywallScreen opens (not the Articles screen)
    5. On the PaywallScreen: confirm the dark hero with orbital animation is visible in the top half
    6. Confirm the white price card is in the bottom half with: Subscribe Annually button, monthly link, Day 1-8 timeline (Days 1-7 green, Day 8 neutral), Restore Purchases link
    7. Confirm prices display real values (not "..." placeholder) — if "..." shows, the Adapty placement is not published or the dev client hasn't resolved SDK activation yet
    8. Tap the close button — confirm you return to Dashboard
    9. Tap "AI Advisor" — confirm PaywallScreen opens
    10. Close PaywallScreen

    SCENARIO 2 — Restore Purchases with no subscription:
    1. Open PaywallScreen via any Intelligence card
    2. Tap "Restore Purchases"
    3. Confirm an Alert appears with "No active subscription was found for your Apple ID." message

    SCENARIO 3 — Sandbox purchase (if sandbox Apple ID is set up and Adapty dashboard is published):
    1. Open PaywallScreen
    2. Sign into your sandbox Apple ID on device (Settings → App Store → Sandbox Account)
    3. Tap "Subscribe Annually" — Apple's native payment sheet should appear
    4. Confirm purchase in the payment sheet
    5. Confirm the PaywallScreen dismisses and you return to Dashboard
    6. Now tap "Longevity Research" on Dashboard — confirm it navigates to the Articles screen (not Paywall)
    7. Tap "AI Advisor" on Dashboard — confirm it navigates to the AIAdvisor stub screen (shows "AI ADVISOR" title and back button)
    8. Background and reopen the app — confirm still premium (AppState refresh)

    SCENARIO 4 — Sandbox restore:
    1. Delete the app → reinstall → open
    2. Navigate to Dashboard → tap "Longevity Research" → PaywallScreen opens
    3. Tap "Restore Purchases" — confirm premium is restored and PaywallScreen dismisses
    4. Confirm Dashboard now shows Articles card routing to Articles

    Note: Scenarios 3 and 4 require sandbox Apple ID and published Adapty placement. If Adapty dashboard is not yet fully configured, mark Scenarios 3-4 as "deferred to final QA before TestFlight submission" and approve with Scenarios 1-2 verified.
  </how-to-verify>
  <resume-signal>
    Type "verified" if Scenarios 1 and 2 pass (Scenarios 3-4 can be verified before TestFlight submission).
    Or describe any issue found with its scenario number and screen location.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit → source | Grep audits confirm no secrets leaked into source files |
| device → Adapty backend | On-device test confirms server-side purchase validation is working |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-12 | Information Disclosure | Console logs of purchase data | mitigate | Audit 3 confirms no API key in source. All catch blocks use console.warn for errors only — no purchase receipts, user IDs, or subscription status details logged to console in any Adapty helper. |
| T-16-13 | Spoofing | Sandbox Apple ID during testing | accept | Sandbox purchases are isolated from production by Apple. Sandbox environment does not affect production subscription state. |
</threat_model>

<verification>
All 8 automated audits pass.
Human confirms:
- Intelligence section visible on Dashboard
- Paywall opens when free user taps either Intelligence card
- PaywallScreen renders dark hero + orbital + white card with Day 1-8 timeline
- Restore Purchases shows correct Alert when no subscription found
- Phase 16 success criteria 1-4 from ROADMAP.md verified
</verification>

<success_criteria>
All six Phase 16 requirements verified: PAY-01 (subscribe via IAP), PAY-02 (paywall UI with trial timeline and restore button), PAY-03 (restore purchases), PAY-04 (free tier gated), PAY-05 (Articles gated), AI-06 (AI Advisor gated). TypeScript clean. No secrets in source. Phase 16 complete.
</success_criteria>

<output>
Create `.planning/phases/16-adapty-paywall-and-subscriptions/16-06-SUMMARY.md` when done
</output>
