---
phase: 16-adapty-paywall-and-subscriptions
plan: 05
type: execute
wave: 3
depends_on: [16-02]
files_modified:
  - App.tsx
  - src/screens/DashboardScreen.tsx
autonomous: true
requirements: [PAY-04, PAY-05, AI-06]
must_haves:
  truths:
    - "App.tsx awaits activationPromise after initSupabaseSession() per D-09"
    - "App.tsx calls identifyAdaptyUser(user.id) fire-and-forget after supabase.auth.getUser() resolves"
    - "App.tsx wraps AppNavigator with PremiumProvider"
    - "DashboardScreen has an Intelligence section header grouping Research and AI Advisor cards"
    - "Research card onPress checks isPremium — premium routes to Articles, free routes to Paywall"
    - "AI Advisor card onPress checks isPremium — premium routes to AIAdvisor, free routes to Paywall"
  artifacts:
    - path: "App.tsx"
      provides: "Adapty activation + identify + PremiumProvider wrap"
      contains: "PremiumProvider"
    - path: "src/screens/DashboardScreen.tsx"
      provides: "Intelligence section replacing standalone Research CTA"
      contains: "Intelligence"
  key_links:
    - from: "App.tsx"
      to: "src/lib/adapty.ts"
      via: "await activationPromise (after initSupabaseSession)"
      pattern: "activationPromise"
    - from: "App.tsx"
      to: "src/context/PremiumContext.tsx"
      via: "PremiumProvider wrapping AppNavigator"
      pattern: "PremiumProvider"
    - from: "src/screens/DashboardScreen.tsx"
      to: "src/context/PremiumContext.tsx"
      via: "usePremiumContext() hook"
      pattern: "usePremiumContext"
---

<objective>
Wire Adapty activation into App.tsx startup and wrap AppNavigator with PremiumProvider. Replace the standalone Research CTA in DashboardScreen with the Intelligence section (per D-05) containing gated Research and AI Advisor cards.

Purpose: App.tsx wiring makes Adapty activate on every app launch. PremiumProvider makes isPremium available to all screens. The Intelligence section implements PAY-05 (Articles gated) and AI-06 (AI Advisor gated). Both wiring changes depend only on 16-02 (PremiumContext + routes), making this plan runnable in parallel with 16-04.
Output: App.tsx with Adapty init and PremiumProvider. DashboardScreen with Intelligence section replacing the existing Research CTA block at lines 543-559.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/STATE.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/16-adapty-paywall-and-subscriptions/16-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/16-adapty-paywall-and-subscriptions/16-PATTERNS.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/16-adapty-paywall-and-subscriptions/16-02-SUMMARY.md

<interfaces>
From src/lib/adapty.ts (16-01):
- export const activationPromise: Promise<void>
- export async function identifyAdaptyUser(userId: string): Promise<void>

From src/context/PremiumContext.tsx (16-02):
- export function PremiumProvider({ children }: { children: React.ReactNode })
- export const usePremiumContext: () => { isPremium: boolean; isPremiumLoading: boolean; refreshPremium: () => Promise<void> }

From App.tsx (current state — confirmed from file read):
- Line 21: await initSupabaseSession() — Adapty activation inserts after this line
- Line 22: await supabase.auth.getUser() — identifyAdaptyUser called after this resolves
- Lines 58-64: return with GestureHandlerRootView > StatusBar > AppNavigator > MedicalDisclaimer
  PremiumProvider wraps AppNavigator (only AppNavigator, not MedicalDisclaimer)
- Existing imports at lines 1-13: add activationPromise, identifyAdaptyUser, PremiumProvider here

From src/screens/DashboardScreen.tsx (current state):
- Lines 543-559: standalone Research CTA (the exact block to REPLACE with Intelligence section)
- Line 561: sectionHdr View for "Today's protocol" — Intelligence section goes BEFORE this
- Line 549-550: Haptics.impactAsync pattern to reuse in Intelligence card onPress handlers
- s.sectionHdr, s.sectionTitle, s.uploadCard, s.researchCard styles already exist — reuse them
- ClipboardIcon already imported at line 13 — use for Research card icon
- s.uploadCardBody, s.uploadCardTitle, s.uploadCardSub, s.uploadCardArrow styles already exist
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire App.tsx — Adapty Activation + PremiumProvider</name>
  <files>App.tsx</files>
  <read_first>
    - App.tsx — full file (75 lines total) to understand exact init() sequence and return JSX before making changes
    - src/lib/supabase.ts lines 72-86 — initSupabaseSession pattern (to understand what line 21 does)
  </read_first>
  <action>
    Make three targeted changes to App.tsx:

    CHANGE 1 — Add two new imports in the import block (after existing imports, before the end):
    - import { activationPromise, identifyAdaptyUser } from './src/lib/adapty';
    - import { PremiumProvider } from './src/context/PremiumContext';

    CHANGE 2 — Modify the init() function inside useEffect. The current init() is:
      await initSupabaseSession();                         // line 21
      const { data: { user } } = await supabase.auth.getUser();  // line 22
      if (user && !user.is_anonymous) { ... }             // lines 23-27
      else { setInitialRoute('Welcome'); }                 // lines 28-29

    Change it to:
      await initSupabaseSession();                         // existing line 21, unchanged
      await activationPromise;                             // NEW — Adapty activate (IIFE already started at module load; this awaits completion)
      const { data: { user } } = await supabase.auth.getUser();  // existing line 22, unchanged
      if (user && user.id) {                               // NEW — fire-and-forget identify
        identifyAdaptyUser(user.id).catch(() => null);     // only after getUser() resolves (STATE.md race rule)
      }
      if (user && !user.is_anonymous) { ... }             // existing routing, unchanged
      else { setInitialRoute('Welcome'); }                 // existing, unchanged

    The activation race decision from STATE.md: activate with no customerUserId (activationPromise handles this at module load); call identify only after getUser() confirms the user ID. identifyAdaptyUser is fire-and-forget (.catch(() => null)) — same pattern as existing fire-and-forget tasks at lines 34-45.

    CHANGE 3 — Wrap AppNavigator with PremiumProvider in the return JSX.
    Current return (lines 58-64):
      <GestureHandlerRootView style={{ flex: 1 }}>
        <StatusBar style="auto" />
        <AppNavigator initialRoute={initialRoute} />
        <MedicalDisclaimer />
      </GestureHandlerRootView>

    New return:
      <GestureHandlerRootView style={{ flex: 1 }}>
        <StatusBar style="auto" />
        <PremiumProvider>
          <AppNavigator initialRoute={initialRoute} />
        </PremiumProvider>
        <MedicalDisclaimer />
      </GestureHandlerRootView>

    PremiumProvider wraps only AppNavigator, not MedicalDisclaimer — MedicalDisclaimer does not need premium context.

    No other changes to App.tsx.
  </action>
  <verify>
    <automated>grep -n "activationPromise\|PremiumProvider\|identifyAdaptyUser" /Users/bekircemkusdemir/Downloads/vitalspan/App.tsx && npx tsc --noEmit --project /Users/bekircemkusdemir/Downloads/vitalspan/tsconfig.json 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx import block contains "activationPromise" and "identifyAdaptyUser" from './src/lib/adapty'
    - App.tsx import block contains "PremiumProvider" from './src/context/PremiumContext'
    - App.tsx init() contains "await activationPromise" on a line between initSupabaseSession() and supabase.auth.getUser()
    - App.tsx init() contains "identifyAdaptyUser(user.id).catch(() => null)" after supabase.auth.getUser() resolves
    - App.tsx JSX contains <PremiumProvider> wrapping <AppNavigator>
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>App.tsx activates Adapty on startup, identifies user after Supabase resolves, and provides PremiumContext to all screens via PremiumProvider.</done>
</task>

<task type="auto">
  <name>Task 2: DashboardScreen Intelligence Section</name>
  <files>src/screens/DashboardScreen.tsx</files>
  <read_first>
    - src/screens/DashboardScreen.tsx lines 1-30 — import block (to add usePremiumContext import)
    - src/screens/DashboardScreen.tsx lines 540-570 — the Research CTA block (lines 543-559) to REPLACE and the Today's protocol sectionHdr that follows (line 561)
    - src/screens/DashboardScreen.tsx lines 620-680 — existing StyleSheet s to find sectionHdr, uploadCard, researchCard styles (do NOT duplicate them, reuse them)
  </read_first>
  <action>
    Make three targeted changes to DashboardScreen.tsx:

    CHANGE 1 — Add import at the top of the file (after existing imports):
    import { usePremiumContext } from '../context/PremiumContext';

    CHANGE 2 — Inside the component function, add near the top (after existing hook calls, before the return):
    const { isPremium } = usePremiumContext();

    CHANGE 3 — Replace the Research CTA block (lines 543-559) with the Intelligence section.
    The block to remove starts at the comment "{/* Research CTA */}" and ends at the closing </TouchableOpacity> before the sectionHdr "Today's protocol" View.

    Replace it with:

    {/* Intelligence section — D-05: groups Articles + AI Advisor */}
    <View style={s.sectionHdr}>
      <Text style={s.sectionTitle}>Intelligence</Text>
    </View>

    {/* Longevity Research — now gated per PAY-05 */}
    <TouchableOpacity
      style={[s.uploadCard, s.researchCard]}
      activeOpacity={0.82}
      accessibilityRole="button"
      onPress={() => {
        Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light).catch(() => null);
        isPremium ? nav.navigate('Articles') : nav.navigate('Paywall');
      }}
    >
      <ClipboardIcon color={Colors.onSurface} size={20} />
      <View style={s.uploadCardBody}>
        <Text style={s.uploadCardTitle}>Longevity Research</Text>
        <Text style={s.uploadCardSub}>Personalised PubMed articles for your biomarker profile</Text>
      </View>
      <Text style={s.uploadCardArrow}>→</Text>
    </TouchableOpacity>

    {/* AI Advisor — new, per D-04 and AI-06 */}
    <TouchableOpacity
      style={[s.uploadCard, s.aiAdvisorCard]}
      activeOpacity={0.82}
      accessibilityRole="button"
      onPress={() => {
        Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light).catch(() => null);
        isPremium ? nav.navigate('AIAdvisor') : nav.navigate('Paywall');
      }}
    >
      <DnaHelixIcon color={Colors.onSurface} size={20} />
      <View style={s.uploadCardBody}>
        <Text style={s.uploadCardTitle}>AI Advisor</Text>
        <Text style={s.uploadCardSub}>Personalised longevity insights powered by AI</Text>
      </View>
      <Text style={s.uploadCardArrow}>→</Text>
    </TouchableOpacity>

    Note on DnaHelixIcon: it is already imported at line 13. If DnaHelixIcon is not the right semantic icon for AI Advisor, use BellIcon or RunnerIcon from the same import — but prefer DnaHelixIcon as it relates to biology/health.

    CHANGE 4 — Add aiAdvisorCard style to the StyleSheet s at the bottom of the file.
    Read the existing researchCard style to understand the pattern, then add:
    aiAdvisorCard: { backgroundColor: Colors.primaryBg ?? Colors.Beige?.surface ?? Colors.surface }
    The exact token depends on what exists in src/theme/index.ts — use the most appropriate warm/distinct background token that differentiates this card from the researchCard. If Colors.primaryBg does not exist, use Colors.Beige.surface or Colors.surface with opacity. Never hardcode hex.

    Do NOT add isPremiumLoading gate to the Intelligence section — the section should always render. Free users see both cards, but tapping them routes to Paywall. This is correct behavior.
  </action>
  <verify>
    <automated>grep -n "Intelligence\|isPremium\|aiAdvisorCard\|usePremiumContext" /Users/bekircemkusdemir/Downloads/vitalspan/src/screens/DashboardScreen.tsx && npx tsc --noEmit --project /Users/bekircemkusdemir/Downloads/vitalspan/tsconfig.json 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - DashboardScreen.tsx contains "usePremiumContext" import
    - DashboardScreen.tsx contains "Intelligence" section header text
    - DashboardScreen.tsx contains both nav.navigate('Articles') and nav.navigate('Paywall') guarded by isPremium check
    - DashboardScreen.tsx contains nav.navigate('AIAdvisor') guarded by isPremium check
    - DashboardScreen.tsx does NOT contain "nav.navigate('Articles')" without an isPremium check (the unguarded old navigate call is removed)
    - s.aiAdvisorCard style exists in StyleSheet s at bottom of file
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>DashboardScreen has Intelligence section. Research card and AI Advisor card both check isPremium — free users route to Paywall, premium users route to Articles/AIAdvisor. Old unguarded Research CTA removed. TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DashboardScreen → navigation | isPremium gate is enforced at tap handler level — free users cannot navigate past the gate by any normal interaction |
| App.tsx → Adapty | activationPromise fire at module load; awaited in init() before routing — ensures Adapty is ready before any screen renders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | Elevation of Privilege | isPremium gate in DashboardScreen | mitigate | Gate is in onPress handler — user cannot navigate('Articles') or navigate('AIAdvisor') without passing isPremium check. PremiumContext.isPremium is derived from adapty.getProfile() (server-side Adapty state), not from any client-writable value. |
| T-16-11 | Information Disclosure | Logging in App.tsx init | mitigate | identifyAdaptyUser is fire-and-forget (.catch(() => null)) — no user ID or subscription status is logged to console in production paths. console.warn in adapty.ts helpers is only triggered on actual errors. |
</threat_model>

<verification>
- App.tsx import block has activationPromise, identifyAdaptyUser, PremiumProvider
- App.tsx init() has await activationPromise between initSupabaseSession and getUser
- App.tsx JSX has PremiumProvider wrapping AppNavigator
- DashboardScreen has Intelligence section header
- Both Research and AI Advisor onPress handlers have isPremium ternary routing to Paywall
- npx tsc --noEmit exits 0
</verification>

<success_criteria>
App.tsx activates Adapty, identifies user, provides PremiumContext. DashboardScreen Intelligence section gates both Articles and AI Advisor behind premium check. TypeScript clean.
</success_criteria>

<output>
Create `.planning/phases/16-adapty-paywall-and-subscriptions/16-05-SUMMARY.md` when done
</output>
