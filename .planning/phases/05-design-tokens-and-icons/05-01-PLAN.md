---
phase: 05-design-tokens-and-icons
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/theme/index.ts
autonomous: true
requirements:
  - THEME-01

must_haves:
  truths:
    - "Colors.Beige.bg through Colors.Beige.headerBg are accessible by importing Colors from src/theme/index.ts"
    - "No existing Colors.* key is renamed or removed"
    - "tsc --noEmit passes after the change"
  artifacts:
    - path: "src/theme/index.ts"
      provides: "Colors.Beige nested object with 11 semantic tokens"
      contains: "Beige: {"
  key_links:
    - from: "src/theme/index.ts"
      to: "Colors const"
      via: "Beige nested object appended before closing brace of Colors"
      pattern: "Beige:\\s*\\{"
---

<objective>
Append the `Colors.Beige` token block to `src/theme/index.ts`, giving Phase 6 all warm-screen color tokens it needs without modifying any existing key.

Purpose: Phase 6 applies warm Beige tokens to six screens. Every token must exist before Phase 6 executes — this plan delivers that foundation per D-08, D-09, D-10, D-11.
Output: `src/theme/index.ts` with `Colors.Beige` nested object containing 11 semantic tokens; all existing keys untouched; `tsc --noEmit` passes.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/05-design-tokens-and-icons/05-CONTEXT.md

<interfaces>
<!-- Existing Colors const structure extracted from src/theme/index.ts -->
<!-- Colors is a plain object exported as `export const Colors = { ... }` -->
<!-- Current last nested block before closing brace is `viz: { ... }` (lines 79-90) -->
<!-- Append Beige block after viz block, before the closing `}` on line 91 -->

Existing top-level keys in Colors (MUST NOT be removed or renamed):
  primary, primaryDark, primaryLight, primaryBg, primaryBorder,
  bg, bgCard, bgSecondary, bgShade,
  textPrimary, textSecondary, textMuted,
  border, borderLight,
  status (nested), warning, warningBg, warningBorder, warningText, warningTextDark,
  danger, dangerBg,
  accent, accentBg, accentBorder, accentDark,
  chartGreen, chartBlue, chartOrange, chartPurple, chartRed,
  darkCard, darkCardText,
  dark (nested), viz (nested)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append Colors.Beige token block to theme</name>
  <files>src/theme/index.ts</files>
  <read_first>
    - src/theme/index.ts — read the full file to see the current closing structure of the Colors const; the Beige block is inserted after the `viz` nested object and before the `};` that closes Colors
  </read_first>
  <action>
    Open src/theme/index.ts. Locate the `viz: { ... }` block (currently the last entry in Colors, ending around line 90). After the closing `},` of the `viz` block and before the `};` that closes the Colors const, insert the following `Beige` nested object. The property order within Beige must match D-10 exactly:

    Key names and values to add (per D-10):
    - bg: '#EDE8DC'
    - bgSecondary: '#EDE8DC'
    - bgShade: '#E4E0D4'
    - card: '#FFFFFF'
    - border: '#D4CFC4'
    - borderLight: '#E2DED6'
    - text: '#1A1A18'
    - textSecondary: '#4A4A45'
    - textMuted: '#8A8A82'
    - divider: '#C8C0B0'
    - headerBg: '#F5F0E8'

    Property name in the Colors object: `Beige` (capital B, per D-11).

    Do NOT add a TypeScript type annotation or `as const` to the Beige object — keep it consistent with the surrounding plain-object style of the Colors const (no `as const` on nested blocks).

    Do NOT rename, reorder, or remove any existing key in Colors. The edit is an additive insertion only.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep -c "Beige:" src/theme/index.ts && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/theme/index.ts contains `Beige: {` (grep returns count >= 1)
    - All 11 keys present: bg, bgSecondary, bgShade, card, border, borderLight, text, textSecondary, textMuted, divider, headerBg — each on its own line inside the Beige block
    - Beige.divider value is '#C8C0B0' and Beige.headerBg value is '#F5F0E8' (the two NEW tokens per D-10)
    - The viz block still exists and its keys are unchanged
    - `npx tsc --noEmit` exits with zero errors
  </acceptance_criteria>
  <done>Colors.Beige object with 11 tokens exists in src/theme/index.ts; all existing Colors keys untouched; tsc --noEmit exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source edit | Manual theme-file edit with no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | src/theme/index.ts | accept | Pure additive edit; no runtime behavior change; TypeScript compile gate catches structural errors |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; react-native-svg already in node_modules |
</threat_model>

<verification>
After task 1 completes:
- `grep "Beige:" src/theme/index.ts` returns a match
- `grep "divider:" src/theme/index.ts` returns a match (the new warm tan token)
- `grep "headerBg:" src/theme/index.ts` returns a match (the new warm cream token)
- `npx tsc --noEmit` exits 0
- No existing Colors key is missing: spot-check `grep "primary:" src/theme/index.ts` and `grep "viz:" src/theme/index.ts` both return matches
</verification>

<success_criteria>
1. `Colors.Beige.bg`, `Colors.Beige.divider`, `Colors.Beige.headerBg` and all 11 tokens are importable from src/theme/index.ts
2. `npx tsc --noEmit` passes with zero errors
3. Zero existing Colors.* keys removed or renamed — confirmed by grep spot-checks
</success_criteria>

<output>
Create `.planning/phases/05-design-tokens-and-icons/05-01-SUMMARY.md` when done
</output>
