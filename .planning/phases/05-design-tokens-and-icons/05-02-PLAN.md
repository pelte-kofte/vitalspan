---
phase: 05-design-tokens-and-icons
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/TabIcons.tsx
autonomous: true
requirements:
  - ICON-01
  - ICON-02

must_haves:
  truths:
    - "HomeIcon, BiomarkersIcon, ProtocolIcon, ExerciseIcon, ProfileIcon are named exports from src/components/TabIcons.tsx"
    - "Every icon renders stroke-based SVG paths with strokeWidth 1.5 inside a 24x24 viewBox"
    - "When focused=true the icon's focal node renders filled (solid circle); when focused=false it renders as stroke-only circle"
    - "The color prop value is used as the stroke color throughout — no hardcoded hex in JSX"
  artifacts:
    - path: "src/components/TabIcons.tsx"
      provides: "Five named SVG tab icon components"
      exports:
        - HomeIcon
        - BiomarkersIcon
        - ProtocolIcon
        - ExerciseIcon
        - ProfileIcon
  key_links:
    - from: "src/components/TabIcons.tsx"
      to: "react-native-svg"
      via: "import { Svg, Circle, Line, Path, G } from 'react-native-svg'"
      pattern: "from 'react-native-svg'"
    - from: "TabIcons.tsx"
      to: "React Navigation tabBarIcon callback"
      via: "color and focused props"
      pattern: "focused.*boolean.*color.*string"
---

<objective>
Create `src/components/TabIcons.tsx` with five named SVG tab icon components following the neural-dots aesthetic established by NeuralGrid.tsx.

Purpose: Replace the five emoji `TabIcon` placeholders in AppNavigator.tsx with scientifically aesthetic stroke-based SVG icons (ICON-01, ICON-02). Wiring happens in Plan 03; this plan only creates the component file.
Output: `src/components/TabIcons.tsx` exporting HomeIcon, BiomarkersIcon, ProtocolIcon, ExerciseIcon, ProfileIcon — each a stroke-based 24×24 SVG accepting `{ color: string; focused: boolean; size?: number }` props.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/05-design-tokens-and-icons/05-CONTEXT.md

<interfaces>
<!-- react-native-svg usage pattern extracted from src/components/NeuralGrid.tsx -->
<!-- Import statement: import Svg, { Circle, Line } from 'react-native-svg' -->
<!-- Svg element: <Svg width={W} height={H}> -->
<!-- Circle element: <Circle cx={n.x} cy={n.y} r={n.r} fill={color} fillOpacity={n.baseOpacity} /> -->
<!-- Line element: <Line x1={...} y1={...} x2={...} y2={...} stroke={color} strokeWidth={0.4} strokeOpacity={...} /> -->
<!-- Path is available from react-native-svg for arcs — same import pattern -->

<!-- React Navigation tabBarIcon callback signature -->
<!-- ({ color, focused, size }) => React.ReactNode -->
<!-- color: string — active = tabBarActiveTintColor, inactive = tabBarInactiveTintColor -->
<!-- focused: boolean — true when this tab is selected -->
<!-- size: number — React Navigation's recommended icon size (typically 24) -->

<!-- Existing component file conventions (CLAUDE.md) -->
<!-- - StyleSheet named `s` at bottom of file -->
<!-- - Import order: React -> React Native -> Expo -> third-party -> internal theme -->
<!-- - Components max 200 lines — each icon is ~15-20 lines, all 5 fit in one file -->
<!-- - TypeScript strict — no `any` -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TabIcons.tsx with five neural-dots SVG icons</name>
  <files>src/components/TabIcons.tsx</files>
  <read_first>
    - src/components/NeuralGrid.tsx — read for react-native-svg API patterns: how Svg, Circle, Line are used; stroke vs fill props; the node-and-connection visual vocabulary
    - src/navigation/AppNavigator.tsx (lines 42-48) — read the TabIcon component being replaced; understand that React Navigation passes `color` (active/inactive tint) and `focused` (boolean) to tabBarIcon callbacks
  </read_first>
  <action>
    Create src/components/TabIcons.tsx as a new file. Follow the import order convention: React first, then react-native-svg, then internal theme if needed.

    Props interface (shared by all five icons, per D-15):
    - color: string — stroke color passed by React Navigation (active = Colors.primary, inactive = Colors.textMuted)
    - focused: boolean — when true, fill the focal node with the color value; when false, focal node is stroke-only
    - size?: number — defaults to 24

    SVG shared constants for every icon (per D-16, D-02 to D-07, ICON-02):
    - viewBox="0 0 24 24"
    - All strokes at strokeWidth={1.5}
    - No hardcoded hex values in JSX — use the `color` prop for all stroke and fill color values
    - The focal node (one primary Circle) renders as: fill={focused ? color : 'none'} stroke={color} strokeWidth={1.5}
    - Secondary/orbiting nodes render as stroke-only circles: fill="none" stroke={color} strokeWidth={1.5}
    - Lines/connections render as: stroke={color} strokeWidth={1.5}

    Icon designs (per D-02 through D-06) — coordinate values for a 24×24 viewport:

    HomeIcon (D-02) — central node + 3 orbiting nodes:
    - Focal node: Circle cx=12 cy=12 r=2.5 (center)
    - Orbit node 1: Circle cx=5 cy=7 r=1.2
    - Orbit node 2: Circle cx=19 cy=7 r=1.2
    - Orbit node 3: Circle cx=12 cy=3 r=1.2
    - Connection lines: Line from focal center (12,12) to each orbit node center

    BiomarkersIcon (D-03) — DNA double helix, two strands of nodes connected diagonally:
    - Left strand nodes: Circle cx=7 cy=4 r=1.5 (focal — top-left), Circle cx=9 cy=12 r=1.2, Circle cx=7 cy=20 r=1.2
    - Right strand nodes: Circle cx=17 cy=4 r=1.2, Circle cx=15 cy=12 r=1.2, Circle cx=17 cy=20 r=1.2
    - Left strand spine: Line x1=7 y1=4 x2=9 y2=12, Line x1=9 y1=12 x2=7 y2=20
    - Right strand spine: Line x1=17 y1=4 x2=15 y2=12, Line x1=15 y1=12 x2=17 y2=20
    - Cross-connections (rungs): Line x1=7 y1=4 x2=17 y2=4, Line x1=9 y1=12 x2=15 y2=12, Line x1=7 y1=20 x2=17 y2=20
    - Focal node: the top-left Circle cx=7 cy=4

    ProtocolIcon (D-04) — molecule chain: 3 nodes linked in a horizontal chain with gentle vertical offset:
    - Node 1 (focal): Circle cx=5 cy=14 r=2.5
    - Node 2: Circle cx=12 cy=10 r=1.8
    - Node 3: Circle cx=19 cy=14 r=1.5
    - Bond line 1: Line x1=5 y1=14 x2=12 y2=10
    - Bond line 2: Line x1=12 y1=10 x2=19 y2=14

    ExerciseIcon (D-05) — figure-8 orbital: two nodes on a figure-8 path with a center crossing point:
    - Focal node: Circle cx=12 cy=8 r=2 (top loop)
    - Second node: Circle cx=12 cy=16 r=1.5 (bottom loop)
    - Top ellipse (upper loop): Path d="M 6 8 A 6 5 0 1 1 18 8 A 6 5 0 1 1 6 8"
    - Bottom ellipse (lower loop): Path d="M 6 16 A 6 5 0 1 0 18 16 A 6 5 0 1 0 6 16"
    - Both Path elements: fill="none" stroke={color} strokeWidth={1.5}

    ProfileIcon (D-06) — node-head: one circle node head + arc of shoulder nodes:
    - Head (focal node): Circle cx=12 cy=7 r=3
    - Shoulder node left: Circle cx=6 cy=16 r=1.2
    - Shoulder node center: Circle cx=12 cy=18 r=1.2
    - Shoulder node right: Circle cx=18 cy=16 r=1.2
    - Shoulder arc connections: Line x1=6 y1=16 x2=12 y2=18, Line x1=12 y1=18 x2=18 y2=16
    - Neck connection: Line x1=12 y1=10 x2=12 y2=15 (connecting head bottom to shoulder arc)

    File structure: export each icon as a named function component. No default export. No StyleSheet needed (all styling is SVG props). File must be under 200 lines total.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep -c "export function" src/components/TabIcons.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/components/TabIcons.tsx
    - Contains exactly 5 `export function` declarations: HomeIcon, BiomarkersIcon, ProtocolIcon, ExerciseIcon, ProfileIcon
    - Each function accepts `{ color, focused, size }` props — `color: string`, `focused: boolean`, `size?: number`
    - Each Svg element has viewBox="0 0 24 24" — grep "viewBox" returns 5 matches
    - Each icon has exactly one Circle whose fill prop evaluates to color when focused=true and 'none' when focused=false (the focal node pattern)
    - No hardcoded hex color strings in JSX (e.g. '#2D6A4F', '#8A8A82') — all color values reference the `color` prop variable
    - `npx tsc --noEmit` exits with zero errors
    - File is under 200 lines
  </acceptance_criteria>
  <done>src/components/TabIcons.tsx created with 5 named SVG icon exports; all icons use viewBox 0 0 24 24, strokeWidth 1.5, focal node fill toggle on focused; tsc --noEmit exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| New component file | Pure UI component; no user input, no network, no storage access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering | src/components/TabIcons.tsx | accept | Static SVG render only; no user input accepted; no external data; zero attack surface |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages; react-native-svg already in node_modules per CONTEXT.md canonical_refs |
</threat_model>

<verification>
After task 1 completes:
- `grep -c "export function" src/components/TabIcons.tsx` returns 5
- `grep "HomeIcon\|BiomarkersIcon\|ProtocolIcon\|ExerciseIcon\|ProfileIcon" src/components/TabIcons.tsx` returns 5 matches
- `grep -c 'viewBox="0 0 24 24"' src/components/TabIcons.tsx` returns 5
- `grep "focused ? color" src/components/TabIcons.tsx` returns matches (focal node fill pattern)
- No hardcoded hex: `grep "#[0-9A-Fa-f]\{6\}" src/components/TabIcons.tsx` returns 0
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
1. Five named icon exports exist and compile cleanly
2. Each icon is stroke-based at strokeWidth 1.5 within viewBox 0 0 24 24 — satisfies ICON-02
3. Focal node fill toggles on the focused prop — active state is visually distinct without custom focus logic
4. No hardcoded hex colors — all color derives from the `color` prop from React Navigation
</success_criteria>

<output>
Create `.planning/phases/05-design-tokens-and-icons/05-02-SUMMARY.md` when done
</output>
