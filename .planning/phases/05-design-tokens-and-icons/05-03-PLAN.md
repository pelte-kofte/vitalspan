---
phase: 05-design-tokens-and-icons
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - src/navigation/AppNavigator.tsx
autonomous: false
requirements:
  - ICON-01
  - ICON-02
  - THEME-01

must_haves:
  truths:
    - "Tab bar shows five SVG icons — no emoji characters visible anywhere in the tab bar"
    - "Active tab icon renders in Colors.primary (#2D6A4F); inactive renders in Colors.textMuted (#8A8A82)"
    - "Tab bar background is Colors.Beige.bg (#EDE8DC)"
    - "tsc --noEmit passes with zero errors across the full project"
  artifacts:
    - path: "src/navigation/AppNavigator.tsx"
      provides: "Tab bar wired with SVG icons and Beige.bg background"
      contains: "TabIcons"
  key_links:
    - from: "src/navigation/AppNavigator.tsx"
      to: "src/components/TabIcons.tsx"
      via: "named imports: HomeIcon, BiomarkersIcon, ProtocolIcon, ExerciseIcon, ProfileIcon"
      pattern: "from.*TabIcons"
    - from: "tabBarStyle.backgroundColor"
      to: "Colors.Beige.bg"
      via: "Colors import from theme"
      pattern: "Beige\\.bg"
---

<objective>
Wire the five SVG tab icons into AppNavigator.tsx and switch the tab bar background from `Colors.bg` to `Colors.Beige.bg`, then verify the full TypeScript project compiles cleanly.

Purpose: This plan connects the two Wave 1 deliverables (Beige tokens + TabIcons.tsx) to the navigation layer, completing Phase 5. The human-verify checkpoint confirms the tab bar renders correctly on device/simulator before the phase is closed.
Output: AppNavigator.tsx imports TabIcons and uses them in all five Tab.Screen tabBarIcon props; tab bar backgroundColor is Colors.Beige.bg; tsc --noEmit exits 0; human confirms no emoji visible.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/05-design-tokens-and-icons/05-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/05-design-tokens-and-icons/05-01-SUMMARY.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/05-design-tokens-and-icons/05-02-SUMMARY.md

<interfaces>
<!-- Existing AppNavigator.tsx structure (extracted from src/navigation/AppNavigator.tsx) -->

<!-- TabIcon component to DELETE (lines 42-48): -->
<!--   function TabIcon({ emoji, focused }: { emoji: string; focused: boolean }) { -->
<!--     return ( -->
<!--       <Text style={{ fontSize: 20, color: focused ? Colors.primary : Colors.textMuted }}> -->
<!--         {emoji} -->
<!--       </Text> -->
<!--     ); -->
<!--   } -->

<!-- Tab.Navigator screenOptions block (lines 53-72): -->
<!--   tabBarStyle: { -->
<!--     backgroundColor: Colors.bg,  <-- change to Colors.Beige.bg per D-12 -->
<!--     borderTopColor: 'rgba(0, 0, 0, 0.06)', -->
<!--     borderTopWidth: 0.5, -->
<!--     paddingBottom: Math.max(insets.bottom, 8), -->
<!--     paddingTop: 8, -->
<!--     height: Math.max(insets.bottom, 0) + 56, -->
<!--   }, -->
<!--   tabBarActiveTintColor: Colors.primary,       <-- keep unchanged -->
<!--   tabBarInactiveTintColor: Colors.textMuted,   <-- keep unchanged -->

<!-- Five Tab.Screen tabBarIcon callbacks to replace: -->
<!--   Home:       ({ focused }) => <TabIcon emoji="🏠" focused={focused} /> -->
<!--   Biomarkers: ({ focused }) => <TabIcon emoji="📊" focused={focused} /> -->
<!--   Protocol:   ({ focused }) => <TabIcon emoji="💊" focused={focused} /> -->
<!--   Exercise:   ({ focused }) => <TabIcon emoji="🏃" focused={focused} /> -->
<!--   Profile:    ({ focused }) => <TabIcon emoji="👤" focused={focused} /> -->

<!-- New tabBarIcon callbacks signature (React Navigation passes { color, focused, size }): -->
<!--   Home:       ({ color, focused }) => <HomeIcon color={color} focused={focused} /> -->
<!--   Biomarkers: ({ color, focused }) => <BiomarkersIcon color={color} focused={focused} /> -->
<!--   Protocol:   ({ color, focused }) => <ProtocolIcon color={color} focused={focused} /> -->
<!--   Exercise:   ({ color, focused }) => <ExerciseIcon color={color} focused={focused} /> -->
<!--   Profile:    ({ color, focused }) => <ProfileIcon color={color} focused={focused} /> -->

<!-- Colors.Beige.bg value: '#EDE8DC' (same as current Colors.bg — visual change is preparatory for Phase 6) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace emoji TabIcon with SVG icon imports in AppNavigator.tsx</name>
  <files>src/navigation/AppNavigator.tsx</files>
  <read_first>
    - src/navigation/AppNavigator.tsx — read the full file; identify: (a) the TabIcon function (lines 42-48) to delete, (b) tabBarStyle.backgroundColor line to change, (c) all five tabBarIcon callback props to update
    - src/components/TabIcons.tsx — read to confirm the exact named export identifiers and props interface before writing the import statement
    - src/theme/index.ts — read to confirm `Colors.Beige.bg` exists before referencing it (verifies Plan 01 completed)
  </read_first>
  <action>
    Make three targeted edits to src/navigation/AppNavigator.tsx:

    Edit 1 — Add import for SVG icons:
    After the existing `import { Colors } from '../theme';` line, add a new import line:
    `import { HomeIcon, BiomarkersIcon, ProtocolIcon, ExerciseIcon, ProfileIcon } from '../components/TabIcons';`

    Edit 2 — Delete the TabIcon component:
    Remove the entire TabIcon function (the `function TabIcon(...)` block with its return statement, approximately lines 42-48). This component is no longer used. Also remove the `Text` import from 'react-native' if TabIcon was its only consumer — check if Text is used elsewhere in the file first; if it is used elsewhere, keep the import.

    Edit 3 — Update Tab.Navigator screenOptions.tabBarStyle.backgroundColor:
    Change `backgroundColor: Colors.bg` to `backgroundColor: Colors.Beige.bg` (per D-12).
    Leave all other tabBarStyle properties unchanged: borderTopColor, borderTopWidth, paddingBottom, paddingTop, height.

    Edit 4 — Replace the five tabBarIcon callback props:
    For each Tab.Screen, replace the tabBarIcon prop value. Pass both `color` and `focused` from the destructured callback argument. New callbacks (per D-07, the `color` prop carries active/inactive tint automatically from tabBarActiveTintColor/tabBarInactiveTintColor):
    - Home:       `({ color, focused }) => <HomeIcon color={color} focused={focused} />`
    - Biomarkers: `({ color, focused }) => <BiomarkersIcon color={color} focused={focused} />`
    - Protocol:   `({ color, focused }) => <ProtocolIcon color={color} focused={focused} />`
    - Exercise:   `({ color, focused }) => <ExerciseIcon color={color} focused={focused} />`
    - Profile:    `({ color, focused }) => <ProfileIcon color={color} focused={focused} />`

    Do NOT change: tabBarLabel values, tabBarActiveTintColor, tabBarInactiveTintColor, tabBarLabelStyle, or any Stack.Screen entries.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep -c "TabIcon" src/navigation/AppNavigator.tsx && grep "Beige.bg" src/navigation/AppNavigator.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "TabIcon" src/navigation/AppNavigator.tsx` returns 0 (old emoji component gone)
    - `grep "TabIcons" src/navigation/AppNavigator.tsx` returns at least 1 match (import line present)
    - `grep "HomeIcon\|BiomarkersIcon\|ProtocolIcon\|ExerciseIcon\|ProfileIcon" src/navigation/AppNavigator.tsx` returns 10 matches (5 in import + 5 in tabBarIcon props)
    - `grep "Beige.bg" src/navigation/AppNavigator.tsx` returns 1 match (backgroundColor line)
    - `grep 'emoji' src/navigation/AppNavigator.tsx` returns 0 (no emoji props remaining)
    - `npx tsc --noEmit` exits with zero errors
  </acceptance_criteria>
  <done>AppNavigator.tsx imports and uses SVG icons for all 5 tabs; tab bar backgroundColor is Colors.Beige.bg; emoji TabIcon component removed; tsc --noEmit exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Wave 1 delivered Colors.Beige tokens (Plan 01) and TabIcons.tsx with 5 SVG neural-dots icons (Plan 02). This plan wired them into AppNavigator.tsx, replacing the emoji tab bar icons with SVG icons and updating the tab bar background to Colors.Beige.bg. tsc --noEmit was verified passing by the automated task above.
  </what-built>
  <how-to-verify>
    1. Run the Expo dev server: `npx expo start` (or `npx expo run:ios` for full simulator test)
    2. Open the app on iOS simulator or device — navigate to the Main tabs screen
    3. Verify: the tab bar shows 5 SVG icons — no emoji characters visible in any tab
    4. Verify: tapping each tab renders the icon in the green active color (#2D6A4F) for the selected tab and muted gray (#8A8A82) for inactive tabs
    5. Verify: the active tab icon's focal node appears filled (solid circle); inactive tabs show stroke-only circles
    6. Verify: all 5 tab labels (Home, Biomarkers, Protocol, Exercise, Profile) are still visible — no labels removed
    7. Verify: the tab bar background appears warm cream (visually identical to before since Colors.Beige.bg = Colors.bg = #EDE8DC, but the token change is preparatory)
    8. Confirm no runtime errors in the Expo console related to TabIcons or react-native-svg
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 SVG icons render correctly with no emoji visible. Describe any issues (wrong icon shape, color not switching on tab press, tsc errors, render crash) if something needs fixing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Navigation wiring | Pure code wiring; no user input crosses this boundary |
| SVG render | SVG rendered client-side with static props from React Navigation; no external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | src/navigation/AppNavigator.tsx | accept | Additive wiring change only; no auth flow, no data flow, no PII involved |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; all dependencies already present |
</threat_model>

<verification>
After Task 1 automated checks pass and human checkpoint is approved:
- Tab bar displays 5 SVG icons, zero emoji
- Active icon: filled focal node + Colors.primary stroke
- Inactive icons: stroke-only focal node + Colors.textMuted stroke
- Tab bar background: Colors.Beige.bg (#EDE8DC)
- All 5 tab labels present and unchanged
- `npx tsc --noEmit` exits 0 across the full project
- No Expo console errors related to SVG rendering
</verification>

<success_criteria>
1. Tab bar shows 5 custom SVG icons — no emoji (satisfies ICON-01)
2. Icons use color prop for active/inactive tint — no manual focused-state logic needed (satisfies ICON-02)
3. Colors.Beige.bg is the tab bar backgroundColor — preparatory token in place (satisfies THEME-01 integration)
4. Human confirms visual correctness on device or simulator
5. tsc --noEmit exits 0 across the entire project
</success_criteria>

<output>
Create `.planning/phases/05-design-tokens-and-icons/05-03-SUMMARY.md` when done
</output>
