---
phase: 18-ai-advisor-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-02
files_modified: []
autonomous: false
requirements:
  - AI-04
  - AI-05
  - AI-06

must_haves:
  truths:
    - "tsc --noEmit exits 0 — no TypeScript errors anywhere in the project"
    - "No hardcoded hex values in AIAdvisorScreen.tsx or advisor/ components (grep audit)"
    - "No inline styles with hardcoded numbers in AIAdvisorScreen.tsx or advisor/ components (grep audit)"
    - "AI-06 verified: free user gate at DashboardScreen.tsx:575 is intact"
    - "Human can visually verify: CTA state, loading state, report with 6 sections, and chat interface"
  artifacts:
    - path: "src/screens/AIAdvisorScreen.tsx"
      provides: "Complete AI Advisor screen — verified TypeScript clean"
    - path: "src/components/advisor/ScoreSummaryCard.tsx"
      provides: "Score summary card — verified TypeScript clean"
    - path: "src/components/advisor/ReportCard.tsx"
      provides: "Report section card with grade badges — verified TypeScript clean"
    - path: "src/components/advisor/ChatThread.tsx"
      provides: "Chat thread renderer — verified TypeScript clean"
  key_links:
    - from: "DashboardScreen.tsx line 575"
      to: "AIAdvisor route"
      via: "isPremium ? nav.navigate('AIAdvisor') : nav.navigate('Paywall')"
      pattern: "isPremium.*AIAdvisor"
---

<objective>
TypeScript audit, source code quality checks, and human visual verification of the
complete AI Advisor UI. This plan validates that Phase 18 deliverables are correct
and ready before marking the phase complete.

Purpose: Catch type errors, coding rule violations, and UI regressions before the
phase is declared done. The human visual checkpoint ensures the 6-section report and
chat interface render correctly on device.

Output: Clean type audit, source audit pass, and human sign-off on the full flow.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/18-ai-advisor-ui/18-01-SUMMARY.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/18-ai-advisor-ui/18-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript audit and source code quality checks</name>
  <files></files>
  <read_first>
    - src/screens/AIAdvisorScreen.tsx (verify line count under 200)
    - src/components/advisor/ScoreSummaryCard.tsx (verify no hardcoded hex)
    - src/components/advisor/ReportCard.tsx (verify no hardcoded hex, verify lookupGrade null returns)
    - src/components/advisor/ChatThread.tsx (verify no hardcoded hex)
  </read_first>
  <action>
    Run the following audits and fix any issues found before proceeding to the human checkpoint:

    1. TypeScript: run `npx tsc --noEmit` from the project root. If errors exist in any advisor file, fix the root cause — do NOT add `// @ts-ignore` or `any` casts. Re-run until exit code 0.

    2. Line count check: count lines in AIAdvisorScreen.tsx. If over 200 lines, extract the largest block (likely the report rendering or the hero CTA state) into a new component in src/components/advisor/ and import it.

    3. Hardcoded hex grep: run `grep -n '#[0-9a-fA-F]\{3,6\}' src/screens/AIAdvisorScreen.tsx src/components/advisor/*.tsx`. The only allowed hex values are the LinearGradient gradient array colors in AIAdvisorScreen.tsx (['#080D09', '#0C1410', '#0F1C14']) and the BackgroundView override '#080D09' for the chat input bar background (these are the screen's own gradient stops, not theme tokens). Any other hex values must be replaced with the appropriate theme token from Colors.dark.*, Colors.viz.*, Colors.primary, or Colors.status.*.

    4. Inline style check: run `grep -n "margin[A-Z].*[0-9]\|padding[A-Z].*[0-9]" src/screens/AIAdvisorScreen.tsx src/components/advisor/*.tsx | grep -v "Spacing\."`. Any hardcoded pixel numbers must be replaced with Spacing.* tokens.

    5. AI-06 integrity: run `grep -n "isPremium.*AIAdvisor\|AIAdvisor.*isPremium" src/screens/DashboardScreen.tsx`. Must find at least one match confirming the premium gate is intact at line ~575.

    6. No `@anthropic-ai/sdk` in Expo project: run `grep -r "anthropic" src/ --include="*.ts" --include="*.tsx"`. Must return zero matches.

    Report results of each check. Fix any failures before marking Task 1 done.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit && echo "TSC_PASS" && grep -c "isPremium.*AIAdvisor\|AIAdvisor.*isPremium" src/screens/DashboardScreen.tsx && grep -rn "anthropic" src/ --include="*.ts" --include="*.tsx" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (TSC_PASS printed)
    - `grep -c "isPremium.*AIAdvisor\|AIAdvisor.*isPremium" src/screens/DashboardScreen.tsx` returns 1 or more
    - `grep -rn "anthropic" src/` returns 0 matches
    - AIAdvisorScreen.tsx is 200 lines or fewer
    - No hardcoded hex values outside the gradient stops in AIAdvisorScreen.tsx
  </acceptance_criteria>
  <done>
    All audits pass with zero violations. TypeScript exits 0, AI-06 gate confirmed, no Anthropic SDK import in Expo source.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of the full AI Advisor flow</name>
  <read_first>
    - src/screens/AIAdvisorScreen.tsx (to understand what was built)
  </read_first>
  <what-built>
    Task 1 (Plan 01) created three sub-components:
    - ScoreSummaryCard: large centered biological age, ageBand, headline, trend
    - ReportCard: section cards with priority dots, status pills, and A/B/C evidence grade badges
    - ChatThread: alternating user/assistant bubbles with typing indicator

    Task 1 (Plan 02) rewrote AIAdvisorScreen.tsx with:
    - CTA state: dark hero with NeuralGrid background, "Generate My Report" button
    - Loading state: full-screen NeuralGrid + "Analyzing your health snapshot…" + ActivityIndicator
    - Report state: ScrollView with 6 sections + pinned chat input bar at bottom
    - Regenerate button in top bar visible when report is loaded
    - Inline error display for rate limit and other errors (no Alert dialogs)
  </what-built>
  <how-to-verify>
    Run the app: `npx expo start --ios` then navigate to the AI Advisor screen via Dashboard (requires premium account or sandbox purchase).

    Check these flows:

    1. CTA state (screen opens):
       - Screen shows dark neural background with NeuralGrid animation
       - "Your AI Longevity Advisor" headline visible, centered
       - "Generate My Report" green button centered below the subtitle
       - No report sections visible yet
       - No Regenerate button in the top bar

    2. Loading state (tap "Generate My Report"):
       - Full-screen NeuralGrid animation appears
       - "Analyzing your health snapshot…" text centered on screen
       - ActivityIndicator visible (loading spinner)
       - Loading lasts ~10-30 seconds (real Claude call)

    3. Report state (after generation):
       - 6 sections scroll vertically: SCORE SUMMARY, PRIORITY FINDINGS, BIOMARKER ANALYSIS, SUPPLEMENT & MEDICATION REVIEW, RECOMMENDATIONS, FOLLOW-UP CHAT
       - SCORE SUMMARY: large number (biological age) centered at top. If no biomarkers logged: shows "—" with "Add more biomarkers for your biological age"
       - PRIORITY FINDINGS: items with colored priority dots (red=high, amber=medium, green=low)
       - BIOMARKER ANALYSIS: items with status pill (Optimal/Suboptimal/Critical) and insight text
       - SUPPLEMENT & MEDICATION REVIEW: items with name + type tag + assessment
       - RECOMMENDATIONS: items with A/B/C evidence grade badge (only when name matches supplement/medication DB); items with no DB match show no badge
       - FOLLOW-UP CHAT header below recommendations
       - Chat input bar pinned to bottom (does not scroll away)
       - Regenerate button visible in top bar

    4. Chat flow:
       - Type a question in the chat input (e.g., "What should I prioritize first?")
       - Tap the → send button (or press Return)
       - User bubble appears immediately on the right
       - Typing indicator (• • •) appears briefly
       - Assistant response appears on the left referencing the report
       - Send button is disabled (greyed out) while Claude responds
       - Input cleared after sending

    5. Error handling (simulate rate limit: trigger 6 report generations):
       - Tap Regenerate 5+ times (after the initial generation) — on the 6th server-side refusal:
       - Error text "You've reached your daily limit. Try again tomorrow." appears inline below where the CTA was (the screen may re-show the CTA state with the error text under the button)

    6. Theme check:
       - No beige/warm colors anywhere on this screen
       - Dark green-black background, card surfaces are dark with subtle borders
       - Text is off-white (#E8F5EE family), not warm cream
  </how-to-verify>
  <resume-signal>Type "approved" if the flow works correctly, or describe specific issues found (e.g., "loading state does not appear", "chat send button not disabled during response", "grade badge shown for unmatched item")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit scripts → source files | grep and tsc read source files locally — no external services |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-01 | Information Disclosure | Source audit — no API key in Expo source | mitigate | grep -r "anthropic" src/ confirms zero SDK imports; Anthropic key is Edge Function secret only (verified at audit time) |
| T-18-03-02 | Elevation of Privilege | AI-06 premium gate integrity | mitigate | grep confirms isPremium gate at DashboardScreen.tsx:575 unchanged; free users reach Paywall not AIAdvisorScreen |
| T-18-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Phase 18 complete when:
1. `npx tsc --noEmit` exits 0
2. AI-06 gate confirmed at DashboardScreen.tsx:575
3. No `@anthropic-ai/sdk` import in Expo source
4. Human has approved the full flow (CTA → loading → report → chat)
5. All 6 report sections render with correct content and theming
</verification>

<success_criteria>
- tsc --noEmit exits 0 with zero errors
- Human checkpoint passed (visual verification approved)
- A/B/C evidence grade badges appear only for matched supplement/medication names
- Chat conversation works end-to-end with typing indicator and disabled send button during response
- All screens and components follow CLAUDE.md coding rules (Colors from theme, Spacing.*, StyleSheet named s)
</success_criteria>

<output>
Create `.planning/phases/18-ai-advisor-ui/18-03-SUMMARY.md` when done.
</output>
