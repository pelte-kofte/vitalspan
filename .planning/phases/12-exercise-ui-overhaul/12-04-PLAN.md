---
phase: "12"
plan: "04"
type: execute
wave: 2
depends_on:
  - "12-01"
  - "12-02"
  - "12-03"
files_modified:
  - src/screens/ExerciseDetailScreen.tsx
autonomous: true
requirements:
  - EX-01
  - EX-02
  - EX-03
  - EX-04

must_haves:
  truths:
    - "ExerciseDetailScreen renders the SVG movement illustration for the exercise matching exerciseId navigation param"
    - "ExerciseDetailScreen renders MuscleMapView with the exercise's muscleGroup as primaryMuscles and secondaryMuscles array"
    - "ExerciseDetailScreen shows formCue text if present on the exercise"
    - "ExerciseDetailScreen shows setsReps text if present on the exercise"
    - "ExerciseDetailScreen shows longevityNote text if present on the exercise"
    - "ExerciseDetailScreen shows equipment and category chips"
    - "Log this exercise CTA opens QuickLogModal bottom sheet"
    - "Screen uses warm Beige surface (Colors.Beige.bg background, Colors.Beige.card card surfaces)"
    - "Status bar is dark (readable on warm background)"
    - "tsc --noEmit passes"
  artifacts:
    - path: "src/screens/ExerciseDetailScreen.tsx"
      provides: "Full exercise detail view with illustration, muscle map, form cue, sets/reps, log CTA"
      exports: ["default ExerciseDetailScreen"]
  key_links:
    - from: "src/screens/ExerciseDetailScreen.tsx"
      to: "src/components/exercise-illustrations/index.ts"
      via: "exercise.illustrationId dynamic import resolves to SVG component"
      pattern: "illustrationId"
    - from: "src/screens/ExerciseDetailScreen.tsx"
      to: "src/components/MuscleMapView.tsx"
      via: "MuscleMapView primaryMuscles/secondaryMuscles props"
      pattern: "MuscleMapView"
    - from: "src/screens/ExerciseDetailScreen.tsx"
      to: "src/lib/exerciseService.ts"
      via: "getExercises() called once, filtered by exerciseId navigation param"
      pattern: "getExercises"
---

<objective>
Create ExerciseDetailScreen — the full-screen card showing all per-exercise visual content: SVG movement illustration, neural-dot muscle map, form cue, longevity sets/reps, equipment metadata, and a "Log this exercise" CTA that opens the existing QuickLogModal.

Purpose: This screen is the primary delivery surface for EX-01 through EX-04. Tapping any exercise row in ExerciseScreen (wired in Plan 05) navigates here.

Output: src/screens/ExerciseDetailScreen.tsx — self-contained screen, imports MuscleMapView and exercise-illustrations index.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/12-exercise-ui-overhaul/12-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/src/theme/index.ts
@/Users/bekircemkusdemir/Downloads/vitalspan/src/navigation/AppNavigator.tsx
@/Users/bekircemkusdemir/Downloads/vitalspan/src/screens/ExerciseScreen.tsx

<interfaces>
<!-- From src/navigation/AppNavigator.tsx — Plan 05 will add ExerciseDetail to this list -->
<!-- ExerciseDetailScreen must accept exerciseId via route.params -->
<!-- Plan 05 adds: ExerciseDetail: { exerciseId: string } to RootStackParamList -->
<!-- Plan 05 adds: <Stack.Screen name="ExerciseDetail" component={ExerciseDetailScreen} /> -->
<!-- ExerciseDetailScreen receives navigation params via useRoute -->

import { useRoute, RouteProp, useNavigation } from '@react-navigation/native';
import { NativeStackNavigationProp } from '@react-navigation/native-stack';
import { RootStackParamList } from '../navigation/AppNavigator';
type RouteProps = RouteProp<RootStackParamList & { ExerciseDetail: { exerciseId: string } }, 'ExerciseDetail'>;

<!-- From src/data/exercises.ts — Exercise interface after Plan 01 extends it -->
interface Exercise {
  id: string; name: string; category: ExerciseCategory; bodyPart: string;
  equipment: string; muscleGroup: string; secondaryMuscles: string[];
  target: string; instructions: string;
  illustrationId?: string; formCue?: string; setsReps?: string; longevityNote?: string;
}

<!-- From src/lib/exerciseService.ts -->
export async function getExercises(): Promise<Exercise[]>

<!-- From src/components/MuscleMapView.tsx (created in Plan 02) -->
import MuscleMapView from '../components/MuscleMapView';
Props: primaryMuscles: string[], secondaryMuscles: string[], interactive?: boolean,
       view?: 'front' | 'back', onViewToggle?: () => void

<!-- From src/components/exercise-illustrations/index.ts (created in Plan 03) -->
import * as ExerciseIllustrations from '../components/exercise-illustrations';
-- Use: const IllustrationComponent = exercise.illustrationId
     ? (ExerciseIllustrations as Record<string, React.ComponentType<{ size?: number }>>)[exercise.illustrationId]
     : null;

<!-- QuickLogModal — currently defined inside ExerciseScreen.tsx, NOT yet exported -->
<!-- ExerciseDetailScreen must replicate or inline QuickLogModal since it is not a separate file yet -->
<!-- See ExerciseScreen.tsx lines 74-178 for the full QuickLogModal implementation -->
<!-- Copy QuickLogModal implementation directly into ExerciseDetailScreen as a local component -->
<!-- This avoids creating a separate file in this wave — Plan 05 can later move to shared file if needed -->

<!-- From src/screens/ExerciseScreen.tsx — helper functions and constants to copy/reference -->
INTENSITY_OPTIONS: { key: ExerciseIntensity; label: string; met_mult: number }[]
INTENSITY_COLORS: Record<ExerciseIntensity, { bg: string; border: string; text: string }>
EQUIPMENT_SHORT: Record<string, string>
function equipShort(eq: string): string
function estimateCalories(category, durationMin, intensity, weightKg): number
function getMondayStr(date): string  — not needed in detail screen

<!-- Theme tokens -->
Colors.Beige.bg: '#EDE8DC', Colors.Beige.card: '#FFFFFF', Colors.Beige.text: '#1A1A18'
Colors.Beige.textSecondary: '#4A4A45', Colors.Beige.textMuted: '#6B6B64'
Colors.Beige.border: '#D4CFC4', Colors.Beige.bgShade: '#E4E0D4'
Colors.accent: '#5B9DBF', Colors.primary: '#2D6A4F', Colors.primaryBg: '#E8F5EE'
Spacing: xs=4, sm=8, md=12, base=16, lg=20, xl=24, xxl=32
Radius: sm=10, md=12, lg=16, xl=20, full=999
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ExerciseDetailScreen with all six layout sections and log CTA</name>
  <read_first>
    src/screens/ExerciseScreen.tsx — read full file (especially lines 74-178 QuickLogModal, lines 43-53 INTENSITY_COLORS/INTENSITY_OPTIONS, lines 30-41 EQUIPMENT_SHORT/equipShort, lines 68-72 estimateCalories) to understand the modal implementation before copying it
    src/theme/index.ts — read Colors, Spacing, Radius for the warm Beige surface styling
    src/components/MuscleMapView.tsx — read to confirm the exact props interface exported (primaryMuscles, secondaryMuscles, interactive, view, onViewToggle)
    src/components/exercise-illustrations/index.ts — read to confirm the exact export names available
    src/navigation/AppNavigator.tsx — read to understand the current RootStackParamList shape before Plan 05 extends it
  </read_first>
  <files>src/screens/ExerciseDetailScreen.tsx</files>
  <action>
    Create src/screens/ExerciseDetailScreen.tsx. The screen loads an exercise by exerciseId from navigation params and renders all Phase 12 content.

    Navigation params: The screen receives exerciseId: string via route.params. Since Plan 05 adds ExerciseDetail to RootStackParamList, use a local type assertion in this file to avoid a compile error before Plan 05 runs:
      type LocalParamList = RootStackParamList & { ExerciseDetail: { exerciseId: string } };
      const route = useRoute<RouteProp<LocalParamList, 'ExerciseDetail'>>();

    Data loading: On mount and focus, call getExercises() and find the exercise by id. Store as state. Show a loading state (spinner or blank) while loading. If exercise not found, show an error state with a back button.

    Layout (ScrollView, top to bottom per D-06):
    1. Custom back button header (TouchableOpacity with navigation.goBack(), "← {exercise.name}" truncated to 1 line). Background Colors.Beige.headerBg.
    2. SVG Illustration section: If exercise.illustrationId is set and the component exists in ExerciseIllustrations, render it at size=160 centered in a Colors.Beige.card rounded card (Radius.xl, Elevation.sm). If no illustrationId, render a placeholder View with Colors.Beige.bgShade background and a centered text "No illustration".
    3. Muscle Map section: MuscleMapView with primaryMuscles=[exercise.muscleGroup] and secondaryMuscles=exercise.secondaryMuscles, interactive=false. Render with front/back toggle (use local useState for view: 'front'|'back'). The component handles its own toggle rendering when onViewToggle is provided.
    4. Metadata chips row: equipment chip (equipShort label on Colors.Beige.bgShade), category chip (category name on Colors.Beige.bgShade). Same chip style as ExerciseScreen.
    5. Form Cue section: Only render if exercise.formCue is defined. Section label "FORM CUE" in uppercase textMuted style. formCue text in Colors.Beige.textSecondary, lineHeight 22.
    6. Sets & Reps section: Only render if exercise.setsReps is defined. Section label "LONGEVITY PRESCRIPTION" in uppercase textMuted style. setsReps text in Colors.accent bold, fontSize Typography.sizes.md.
    7. Longevity Note section: Only render if exercise.longevityNote is defined. longevityNote text in Colors.Beige.textMuted italic, fontSize Typography.sizes.sm.
    8. "Log this exercise" CTA button: Full-width TouchableOpacity, Colors.primary background, "Log this exercise" text, opens QuickLogModal (local state logModalOpen: boolean + exercise passed to modal).

    QuickLogModal: Copy the full QuickLogModal implementation from ExerciseScreen.tsx (lines 74-178) into ExerciseDetailScreen.tsx as a local component. Copy the supporting constants and helpers (INTENSITY_OPTIONS, INTENSITY_COLORS, EQUIPMENT_SHORT, equipShort, estimateCalories). Load userWeightKg from AsyncStorage '@vitalspan_user_profile' on mount. Save log entry to '@vitalspan_exercise_log' on save using the same saveLog pattern from ExerciseScreen.

    Status bar: setStatusBarStyle('dark') in useFocusEffect (warm Beige screen, per Phase 6 pattern).

    File structure: imports, type declarations (LocalParamList), helper constants (INTENSITY_OPTIONS, INTENSITY_COLORS, EQUIPMENT_SHORT, equipShort, estimateCalories), QuickLogModal component, default export ExerciseDetailScreen, StyleSheet named s at bottom.

    Component length: This screen will be longer than 200 lines due to copying QuickLogModal. That is acceptable here — the modal is being co-located temporarily. The screen's main component function itself must stay under 100 lines (the extra lines are the modal component and helpers).

    No hardcoded hex values. All colors from Colors.*. All spacing from Spacing.* (except SVG coordinates). No inline styles except dynamic ones (e.g., { color: someVar }).

    Decision references: D-05 (new ExerciseDetailScreen), D-06 (layout top-to-bottom), D-07 (QuickLogModal reused), D-04 (Colors.accent for figure), D-08 (front/back muscle map).
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/screens/ExerciseDetailScreen.tsx exists
    - tsc --noEmit passes (Note: ExerciseDetail key not yet in RootStackParamList until Plan 05; the LocalParamList type intersection handles this)
    - grep -c "MuscleMapView" src/screens/ExerciseDetailScreen.tsx returns >= 2 (import + usage)
    - grep -c "illustrationId" src/screens/ExerciseDetailScreen.tsx returns >= 1 (illustration lookup logic present)
    - grep -c "formCue" src/screens/ExerciseDetailScreen.tsx returns >= 1
    - grep -c "setsReps" src/screens/ExerciseDetailScreen.tsx returns >= 1
    - grep -c "Log this exercise" src/screens/ExerciseDetailScreen.tsx returns >= 1
    - grep -c "Colors\." src/screens/ExerciseDetailScreen.tsx returns >= 10
    - grep -v "Colors\." src/screens/ExerciseDetailScreen.tsx | grep -c "#[0-9A-Fa-f]" returns 0 (no raw hex)
    - StyleSheet named s at bottom: grep -c "const s = StyleSheet.create" src/screens/ExerciseDetailScreen.tsx returns 1
  </acceptance_criteria>
  <done>ExerciseDetailScreen created with all 6 layout sections, QuickLogModal integrated, warm Beige surface, Colors.* tokens only, tsc passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navigation params → screen | exerciseId comes from ExerciseScreen row press — internal navigation, not external input |
| AsyncStorage → QuickLogModal | userWeightKg is a numeric field from user profile; used in calorie estimation only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Information Disclosure | Exercise log saved to AsyncStorage | accept | No PII in log entries; data stays local |
| T-12-04b | Spoofing | exerciseId navigation param | accept | Internal navigation only; no external deep-link surface in this phase |
| T-12-SC | Tampering | npm/pip installs | accept | No new packages |
</threat_model>

<verification>
- tsc --noEmit passes
- No hardcoded hex values in ExerciseDetailScreen.tsx
- All 6 layout sections present (illustration, muscle map, metadata chips, form cue, sets/reps, log CTA)
- QuickLogModal local implementation matches ExerciseScreen pattern
- Screen background is Colors.Beige.bg
</verification>

<success_criteria>
ExerciseDetailScreen created with all content sections (EX-01 illustration, EX-02 muscle map, EX-03 form cue, EX-04 sets/reps), warm Beige surface, log CTA, tsc clean.
</success_criteria>

<output>
Create `.planning/phases/12-exercise-ui-overhaul/12-04-SUMMARY.md` when done
</output>
