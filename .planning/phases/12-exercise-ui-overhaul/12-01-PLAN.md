---
phase: "12"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/exercises.ts
autonomous: false
requirements:
  - EX-01
  - EX-02
  - EX-03
  - EX-04

must_haves:
  truths:
    - "Exercise interface has four new optional fields: illustrationId, formCue, setsReps, longevityNote"
    - "All 60 exercises in EXERCISES array have formCue, setsReps, longevityNote populated"
    - "All 60 exercises have illustrationId set to a camelCase name matching their SVG component"
    - "Existing Exercise fields (id, name, category, bodyPart, equipment, muscleGroup, secondaryMuscles, target, instructions) are unchanged"
    - "tsc --noEmit passes with no errors on exercises.ts"
  artifacts:
    - path: "src/data/exercises.ts"
      provides: "Extended Exercise interface and fully populated EXERCISES array"
      contains: "illustrationId?: string"
  key_links:
    - from: "src/data/exercises.ts"
      to: "src/components/exercise-illustrations/"
      via: "illustrationId field maps to SVG component filename"
      pattern: "illustrationId"
---

<objective>
Extend the Exercise interface with four new optional fields and populate every one of the 60 exercises with longevity-optimized content (formCue, setsReps, longevityNote, illustrationId). This is the data foundation Plan 04 (ExerciseDetailScreen) consumes.

Purpose: EX-01 (SVG illustration), EX-02 (muscle map), EX-03 (form cue), EX-04 (longevity sets/reps) all depend on the Exercise interface having these fields populated. The pharmacist review checkpoint in this plan ensures content quality before it reaches end users.

Output: Updated src/data/exercises.ts with extended interface and all 60 entries fully populated.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/12-exercise-ui-overhaul/12-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend Exercise interface with four optional fields</name>
  <read_first>
    src/data/exercises.ts — read the full file to understand current Exercise interface shape (lines 15-25) and all existing field names before adding new ones
  </read_first>
  <files>src/data/exercises.ts</files>
  <action>
    Add four optional fields to the Exercise interface (lines 15-25 of exercises.ts), immediately after the existing "instructions: string" line:

      illustrationId?: string;
      formCue?: string;
      setsReps?: string;
      longevityNote?: string;

    Do NOT rename, remove, or reorder any existing fields. Do NOT add these fields to ExerciseLogEntry (different type). The exerciseService.ts mapRowToExercise function maps from snake_case DB columns — since these new fields are optional, no changes to exerciseService.ts are required (absent Supabase columns will be undefined, which is valid for optional fields).

    After adding the fields, verify: run tsc --noEmit targeting exercises.ts — it must pass with no type errors. The optional modifier means existing EXERCISES array entries without the new fields remain valid TypeScript.

    Decision reference: D-14 specifies all four fields as optional for backward compatibility. D-02 specifies illustrationId maps to the named SVG component in src/components/exercise-illustrations/. D-13 specifies the Peter Attia/Outlive framework for setsReps content.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Exercise interface in exercises.ts contains all four optional fields; tsc --noEmit passes; all existing EXERCISES array entries remain valid (no new fields required on them yet)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Pharmacist review — generate content table for all 60 exercises</name>
  <read_first>
    src/data/exercises.ts — read the full EXERCISES array to get exercise names, categories, muscleGroup, secondaryMuscles, and target values for all 60 entries; these inform the content generation
    .planning/phases/12-exercise-ui-overhaul/12-CONTEXT.md — D-13 (Peter Attia/Outlive framework), D-14 (field definitions), D-02 (illustrationId naming)
  </read_first>
  <what-built>
    Generate a markdown table of proposed content for all 60 exercises in the EXERCISES array. Each row contains:
    - Exercise ID
    - Exercise name
    - category
    - illustrationId (camelCase version of the exercise name, e.g., "barbellDeadlift", "plank", "burpee")
    - formCue (1-2 sentences of technique instruction, longevity-safe focus)
    - setsReps (longevity-optimized prescription per Peter Attia/Outlive framework: compound strength 3-5x5-8, hypertrophy/longevity 3x8-12, metabolic/endurance 2-3x15-20, cardio by duration)
    - longevityNote (optional one-liner on longevity relevance; omit for stretch/mobility exercises by leaving blank)

    Content generation rules (per D-13):
    - Compound strength (deadlift, squat, press, row variants): "3–5 × 5–8 reps, controlled eccentric, 2-3 min rest"
    - Hypertrophy/longevity (isolation, machine-style): "3 × 8–12 reps, slow eccentric (3 sec), 90 sec rest"
    - Metabolic endurance (high-rep, bodyweight): "2–3 × 15–20 reps, minimal rest (30–45 sec)"
    - Cardio/conditioning: "20–40 min at Zone 2 effort (can hold a conversation)" or "3 × 30 sec on / 30 sec off"
    - Stability/core: "3 × 20–30 sec hold" or "3 × 10–15 reps, controlled"
    - Stretch/mobility: "2–3 × 30 sec hold each side"

    illustrationId naming rule: Convert exercise name to camelCase, remove special characters, e.g.:
    - "Barbell Deadlift" → "barbellDeadlift"
    - "3/4 Sit-Up" → "threeFourSitUp"
    - "Captain's Chair Straight Leg Raise" → "captainsChairStraightLegRaise"
    - "Side-to-Side Chin" → "sideToSideChin"

    Output the table in this format (output as plain markdown, NOT as code fenced):

    | ID | Name | Category | illustrationId | formCue | setsReps | longevityNote |
    |----|------|----------|----------------|---------|----------|---------------|
    | 0720 | Side-to-Side Chin | Pull / Row | sideToSideChin | ... | ... | ... |
    ...
  </what-built>
  <how-to-verify>
    Review the table output above carefully as a licensed pharmacist and longevity practitioner:

    1. Verify formCues are safe, accurate, and appropriate for longevity-focused training (no advice that increases injury risk, no hypertrophy-specific cues like "squeeze at peak contraction for maximum pump")
    2. Verify setsReps prescriptions align with the Peter Attia/Outlive framework — confirm compound lifts use 3-5x5-8, not bodybuilder ranges
    3. Verify longevityNotes are clinically accurate one-liners — remove any that are vague or marketing-sounding
    4. Check that cardio exercises (Running in Place, Burpee, etc.) use duration/Zone 2 language, not sets/reps
    5. Check that stretch/mobility exercises (Lying Side Quad Stretch, Calf Wall Stretch, Seated Wide Angle Stretch, Standing Lateral Stretch) have hold-based setsReps

    If any entry needs correction, provide the corrected row(s) in your response.
  </how-to-verify>
  <resume-signal>Type "approved" if the content table is correct and ready to commit. Or provide specific corrections and type "approved with corrections: [list corrections]"</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write approved content to all 60 EXERCISES array entries</name>
  <read_first>
    src/data/exercises.ts — read the full file again to confirm current state after Task 1 (interface extended); note the exact id values for each entry to match the checkpoint-approved content table entries correctly
  </read_first>
  <files>src/data/exercises.ts</files>
  <action>
    Using the pharmacist-approved content table from Task 2 (checkpoint output), add the four new fields (illustrationId, formCue, setsReps, and optionally longevityNote) to every entry in the EXERCISES array.

    For each entry, add the fields inline with the existing entry object. Keep entries on as few lines as is readable — do not split single entries across 20+ lines. The new fields go after "instructions: '...'" and before the closing brace.

    Naming rule for illustrationId: must be a valid JavaScript identifier in camelCase, matching what will be used as the component name in src/components/exercise-illustrations/ (e.g., the file barbellDeadlift.tsx exports default BarbellDeadlift, and illustrationId: 'barbellDeadlift'). Use the exact illustrationId values from the approved checkpoint table.

    Do NOT modify any existing field values (id, name, category, bodyPart, equipment, muscleGroup, secondaryMuscles, target, instructions). Only ADD the new fields.

    After writing, run tsc --noEmit to confirm no type errors.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | head -20 && grep -c "illustrationId:" src/data/exercises.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "illustrationId:" src/data/exercises.ts returns 60 (one per exercise entry, plus the interface declaration = 61; verify count is >= 60)
    - grep -c "formCue:" src/data/exercises.ts returns >= 60
    - grep -c "setsReps:" src/data/exercises.ts returns >= 60
    - tsc --noEmit exits with no errors
    - All existing EXERCISES array entries still have their original id, name, category, bodyPart, equipment, muscleGroup, secondaryMuscles, target, instructions values unchanged (spot-check 5 entries)
  </acceptance_criteria>
  <done>All 60 EXERCISES entries have illustrationId, formCue, setsReps, and (where applicable) longevityNote populated; tsc --noEmit passes; no existing field values were modified</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static data → UI | Exercise content (formCue, setsReps) is static, not user-controlled; no XSS/injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | formCue/setsReps content | accept | Content is pharmacist-reviewed static data; no PII; clinical accuracy enforced by checkpoint |
| T-12-SC | Tampering | npm/pip installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- tsc --noEmit passes with 0 errors after all tasks complete
- grep -c "illustrationId:" src/data/exercises.ts >= 60
- grep -c "formCue:" src/data/exercises.ts >= 60
- grep -c "setsReps:" src/data/exercises.ts >= 60
- Spot-check: entry id '0032' (Barbell Deadlift) has setsReps containing "5–8 reps" or similar compound prescription
- Spot-check: entry id '0684' (Running in Place) has setsReps containing "Zone 2" or duration-based prescription
- No hardcoded hex values introduced (exercises.ts is a data file with no UI rendering)
</verification>

<success_criteria>
Exercise interface extended with illustrationId?, formCue?, setsReps?, longevityNote? fields; all 60 EXERCISES entries populated with pharmacist-reviewed content; tsc --noEmit clean.
</success_criteria>

<output>
Create `.planning/phases/12-exercise-ui-overhaul/12-01-SUMMARY.md` when done
</output>
