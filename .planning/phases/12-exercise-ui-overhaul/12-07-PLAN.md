---
phase: "12"
plan: "07"
type: execute
wave: 4
depends_on:
  - "12-05"
  - "12-06"
files_modified: []
autonomous: false
requirements:
  - EX-01
  - EX-02
  - EX-03
  - EX-04
  - EX-05
  - EX-06

must_haves:
  truths:
    - "tsc --noEmit exits with 0 errors across the entire project"
    - "No hardcoded hex values in any Phase 12 new or modified file"
    - "ExerciseDetailScreen visually shows illustration, muscle map, form cue, sets/reps on a warm Beige surface"
    - "Tapping an exercise row in ExerciseScreen navigates to ExerciseDetailScreen"
    - "Muscle map filter panel is accessible in ExerciseScreen and filters the list correctly"
    - "Dashboard shows weekly movement summary card when logs exist"
  artifacts:
    - path: "src/screens/ExerciseDetailScreen.tsx"
      provides: "Full exercise detail screen"
    - path: "src/components/MuscleMapView.tsx"
      provides: "Neural-dot muscle map"
    - path: "src/components/exercise-illustrations/index.ts"
      provides: "Barrel export of 60 SVG illustrations"
  key_links:
    - from: "verification"
      to: "all Phase 12 files"
      via: "tsc --noEmit, grep audit, human visual check"
      pattern: "phase 12"
---

<objective>
Phase 12 final audit: TypeScript compilation check, source audit for hardcoded values, and human visual verification of ExerciseDetailScreen and muscle map selector. No code changes expected — this plan verifies the prior six plans are complete and correct.

Purpose: Ensure EX-01 through EX-06 are fully satisfied, tsc is clean, and the UI renders correctly on device before the phase is marked complete.

Output: SUMMARY.md confirming phase pass/fail; any issues documented for gap closure.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/12-exercise-ui-overhaul/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: tsc --noEmit and source audit for hardcoded hex values</name>
  <read_first>
    No files to read — this task runs CLI commands only.
  </read_first>
  <files></files>
  <action>
    Run the following audit commands in sequence and report results:

    1. TypeScript compilation check:
       npx tsc --noEmit
       Expected: 0 errors. If errors exist, report them; do NOT attempt to fix in this plan (create a gap closure instead).

    2. Hardcoded hex value audit across all Phase 12 new/modified files:
       Files to check: src/data/exercises.ts, src/components/MuscleMapView.tsx, src/screens/ExerciseDetailScreen.tsx, src/screens/ExerciseScreen.tsx, src/screens/DashboardScreen.tsx, and all 60 files in src/components/exercise-illustrations/*.tsx

       Run: grep -rn "#[0-9A-Fa-f]\{3,6\}" src/components/exercise-illustrations/ src/components/MuscleMapView.tsx src/screens/ExerciseDetailScreen.tsx --include="*.tsx" --include="*.ts"
       Then filter out false positives (color token definitions in theme/index.ts are NOT phase 12 files). Any match in the above paths is a violation.

       Also run: grep -n "#[0-9A-Fa-f]\{3,6\}" src/screens/ExerciseScreen.tsx src/screens/DashboardScreen.tsx
       (These files may have pre-existing patterns — only flag NEW hardcoded values introduced in Phase 12.)

    3. Illustration component count:
       ls src/components/exercise-illustrations/*.tsx | wc -l
       Expected: 60

    4. EXERCISES array content check:
       grep -c "illustrationId:" src/data/exercises.ts
       grep -c "formCue:" src/data/exercises.ts
       grep -c "setsReps:" src/data/exercises.ts
       Expected: each >= 60

    5. Navigation type check:
       grep "ExerciseDetail" src/navigation/AppNavigator.tsx
       Expected: at least 3 matches (import, type, Stack.Screen)

    Report all results. If any check fails, note it as a gap.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1; echo "EXIT: $?"</automated>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit exits with code 0 (0 errors)
    - grep for raw hex in exercise-illustrations/*.tsx returns empty
    - ls exercise-illustrations/*.tsx | wc -l == 60
    - grep -c "illustrationId:" src/data/exercises.ts >= 60
    - grep -c "formCue:" src/data/exercises.ts >= 60
    - grep -c "setsReps:" src/data/exercises.ts >= 60
    - grep "ExerciseDetail" AppNavigator.tsx returns >= 3 lines
  </acceptance_criteria>
  <done>All automated checks pass; results documented; any failures noted as gaps</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of ExerciseDetailScreen and muscle map</name>
  <what-built>
    Plans 12-01 through 12-06 have delivered:
    - 60 exercise SVG illustrations (neural-dot movement figures in Colors.accent)
    - MuscleMapView with front/back silhouette and highlighted muscle regions
    - ExerciseDetailScreen with 6 content sections (illustration, muscle map, form cue, sets/reps, metadata, log CTA)
    - ExerciseScreen muscle map filter panel (collapsible, dual filter with category chips)
    - Dashboard weekly movement summary card

    To build and run: npx expo start --ios (or use your existing dev build)
  </what-built>
  <how-to-verify>
    Run the app on iOS simulator or device and verify each item:

    1. EXERCISE DETAIL SCREEN:
       a. Navigate to Exercise tab → tap any exercise row
       b. Confirm ExerciseDetailScreen opens (not the old expand-in-place view)
       c. Confirm the SVG illustration appears at the top — it should be a simple neural-dot figure (dots + lines in blue-green accent color)
       d. Confirm the neural-dot muscle map appears below the illustration — shows a body silhouette with Front/Back toggle
       e. Tap the Front/Back toggle — confirm the map switches views
       f. Confirm primary muscles are highlighted in accent color (blue-green), secondary muscles are dimmer
       g. Confirm the form cue text appears (1-2 sentences)
       h. Confirm the longevity sets/reps appears (e.g., "3 × 5–8 reps" for compound lifts)
       i. Confirm the warm Beige background (cream/off-white) — not dark
       j. Confirm "Log this exercise" CTA at bottom opens the log modal
       k. Confirm back navigation works (tapping back returns to ExerciseScreen)

    2. EXERCISE LIBRARY FILTER:
       a. On ExerciseScreen, tap the "Muscle Group Filter" toggle button
       b. Confirm the muscle map panel expands below the category chips
       c. Tap a muscle region (e.g., chest area) — confirm the exercise list updates to show only chest-related exercises
       d. Confirm the filter label updates to show the selected muscle
       e. Tap "Clear filter" — confirm the full list restores
       f. Confirm category chips still work — selecting "Push" with no muscle filter shows push exercises

    3. DASHBOARD WEEKLY SUMMARY:
       a. Navigate to Home (Dashboard) tab
       b. If you have exercise logs from this week: confirm the "THIS WEEK'S MOVEMENT" card appears below the "Movement today" card
       c. Confirm it shows sessions count, total minutes, and top category
       d. If no logs from this week: confirm the card is not shown (not an empty card)

    Flag any visual issues:
    - Illustration not rendering (blank space)
    - Muscle map regions not highlighting
    - Wrong background color (should be warm beige on detail screen)
    - Layout overflow or clipping
    - Missing form cue or sets/reps text
    - Filter not working correctly
  </how-to-verify>
  <resume-signal>Type "approved" if all visual checks pass. Or describe specific issues: "issue: [description of problem seen]"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit tools → source files | Read-only grep/tsc operations; no mutation risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Information Disclosure | Source audit | accept | Audit is internal dev tooling; no secrets in exercise data files |
| T-12-SC | Tampering | npm/pip installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- tsc --noEmit exits code 0
- 60 illustration components exist
- All 60 EXERCISES entries have illustrationId, formCue, setsReps
- ExerciseDetail in AppNavigator
- Human visual check approved
</verification>

<success_criteria>
All Phase 12 plans verified: tsc clean, 60 illustrations present, source audit passes, human visual check approved — Phase 12 complete.
</success_criteria>

<output>
Create `.planning/phases/12-exercise-ui-overhaul/12-07-SUMMARY.md` when done
</output>
