---
phase: "12"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/MuscleMapView.tsx
autonomous: true
requirements:
  - EX-02
  - EX-05

must_haves:
  truths:
    - "MuscleMapView renders a front-view body silhouette with a neural-dot grid overlay"
    - "MuscleMapView renders a back-view body silhouette with a toggle button to switch views"
    - "Primary muscle regions render in Colors.accent (#5B9DBF); secondary in low-opacity accent; inactive in Colors.Beige.bgShade"
    - "When interactive=true, tapping a muscle region calls onMusclePress(muscleName)"
    - "When interactive=false, regions are non-tappable read-only highlights"
    - "Component accepts primaryMuscles string[] and secondaryMuscles string[] props"
    - "All ~13 muscle regions are present and labeled with their canonical name strings from exercises.ts"
    - "Component file is under 200 lines"
  artifacts:
    - path: "src/components/MuscleMapView.tsx"
      provides: "Front/back neural-dot muscle map with interactive and read-only modes"
      exports: ["MuscleMapView", "MuscleRegion", "MUSCLE_REGIONS"]
  key_links:
    - from: "src/components/MuscleMapView.tsx"
      to: "src/data/exercises.ts"
      via: "muscle name strings match muscleGroup and secondaryMuscles values in exercises.ts"
      pattern: "quads|hamstrings|glutes|chest|lats|traps|shoulders|biceps|triceps|core|obliques|calves|forearms"
---

<objective>
Build the MuscleMapView component — a front/back body silhouette with a neural-dot grid overlay where muscle regions are highlighted by name. Used in ExerciseDetailScreen (read-only) and ExerciseScreen filter panel (interactive/tappable).

Purpose: EX-02 (muscle map with primary/secondary highlights) and EX-05 (muscle group filter via visual map) both depend on this component. Building it standalone in Wave 1 lets ExerciseDetailScreen (Wave 2) and ExerciseScreen filter (Wave 3) import it without circular dependencies.

Output: src/components/MuscleMapView.tsx — self-contained, no network deps, uses react-native-svg (already in Expo SDK 54).
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/12-exercise-ui-overhaul/12-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/src/theme/index.ts
@/Users/bekircemkusdemir/Downloads/vitalspan/src/components/NeuralGrid.tsx

<interfaces>
<!-- From src/data/exercises.ts — canonical muscle name strings used in exercises.ts -->
<!-- MuscleMapView region names MUST match these exactly for filter logic to work -->
muscleGroup values seen in EXERCISES:
  biceps, shoulders, triceps, obliques, hip flexors, hamstrings, quadriceps, trapezius,
  traps, transverse abdominis, forearms, chest, ankles

secondaryMuscles values seen in EXERCISES (flattened, unique):
  biceps, forearms, shoulders, obliques, hamstrings, lower back, calves, chest,
  glutes, triceps, upper chest, rhomboids, core, upper back

Target regions to cover (~13, per D-10):
  chest, upper back (traps/upper back), lats, shoulders, biceps, triceps,
  forearms, abs/core (includes transverse abdominis, hip flexors), obliques,
  glutes, quads (quadriceps), hamstrings, calves

<!-- From src/theme/index.ts -->
Colors.accent: '#5B9DBF'       — primary muscle highlight
Colors.accentBg: '#E6F1FB'    — soft accent background
Colors.Beige.bgShade: '#E4E0D4' — inactive region
Colors.Beige.border: '#D4CFC4'
Colors.Beige.text: '#1A1A18'
Spacing.sm: 8, Spacing.md: 12, Spacing.base: 16
Radius.sm: 10, Radius.full: 999

<!-- From src/components/NeuralGrid.tsx — SVG dot pattern reference -->
Uses react-native-svg: Svg, Circle, Line
Dot radius: 1.2–3.0 range
Dot opacity: 0.22–0.40 range
Connection lines: thin strokes, opacity derived from distance
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MuscleMapView component with front/back silhouette and neural-dot regions</name>
  <read_first>
    src/components/NeuralGrid.tsx — read the full component for the exact SVG Circle/Line rendering pattern, dot sizing, and opacity values to replicate the neural aesthetic
    src/theme/index.ts — read Colors, Spacing, Radius tokens for use in MuscleMapView styling
    src/data/exercises.ts — scan muscleGroup and secondaryMuscles values (lines 56-132) to confirm the canonical muscle name strings the filter logic must match
  </read_first>
  <files>src/components/MuscleMapView.tsx</files>
  <action>
    Create src/components/MuscleMapView.tsx. The component renders an SVG body silhouette with neural-dot overlay.

    Props interface (export as MuscleMapViewProps):
      primaryMuscles: string[]      — muscle names to highlight in Colors.accent
      secondaryMuscles: string[]    — muscle names to highlight at 40% opacity accent
      interactive?: boolean         — when true, regions are TouchableOpacity wrapped; default false
      onMusclePress?: (muscle: string) => void  — called when a region is tapped (interactive mode only)
      view?: 'front' | 'back'       — controls which silhouette is shown; default 'front'
      onViewToggle?: () => void     — callback when front/back toggle button is tapped
      style?: object                — optional container style override

    Export: export default MuscleMapView; also export MUSCLE_REGIONS constant and MuscleRegion type.

    MUSCLE_REGIONS constant: an array of MuscleRegion objects defining each tappable area.
    MuscleRegion type: { id: string; label: string; frontVisible: boolean; backVisible: boolean; frontPath: { cx: number; cy: number; rx: number; ry: number }; backPath: { cx: number; cy: number; rx: number; ry: number } | null }

    The 13 muscle regions (id must match muscleGroup/secondaryMuscles strings in exercises.ts for filter logic):
      { id: 'chest',      label: 'Chest',      frontVisible: true,  backVisible: false }
      { id: 'shoulders',  label: 'Shoulders',  frontVisible: true,  backVisible: true  }
      { id: 'biceps',     label: 'Biceps',     frontVisible: true,  backVisible: false }
      { id: 'forearms',   label: 'Forearms',   frontVisible: true,  backVisible: true  }
      { id: 'abs/core',   label: 'Core',       frontVisible: true,  backVisible: false }
      { id: 'obliques',   label: 'Obliques',   frontVisible: true,  backVisible: true  }
      { id: 'quads',      label: 'Quads',      frontVisible: true,  backVisible: false }
      { id: 'calves',     label: 'Calves',     frontVisible: true,  backVisible: true  }
      { id: 'upper back', label: 'Upper Back', frontVisible: false, backVisible: true  }
      { id: 'lats',       label: 'Lats',       frontVisible: false, backVisible: true  }
      { id: 'triceps',    label: 'Triceps',    frontVisible: false, backVisible: true  }
      { id: 'glutes',     label: 'Glutes',     frontVisible: false, backVisible: true  }
      { id: 'hamstrings', label: 'Hamstrings', frontVisible: false, backVisible: true  }

    IMPORTANT: The filter logic in ExerciseScreen (Plan 05) will match exercises where muscleGroup OR any element in secondaryMuscles matches a region id. The region ids above must be substrings-matchable against the exercises.ts values. Implement a helper: function muscleMatches(exerciseMuscle: string, regionId: string): boolean { return exerciseMuscle.toLowerCase().includes(regionId.toLowerCase()) || regionId.toLowerCase().includes(exerciseMuscle.toLowerCase()); } — export this from MuscleMapView.tsx for use by ExerciseScreen.

    SVG layout: Use a fixed viewBox of "0 0 120 200" (front) and "0 0 120 200" (back). Draw the body silhouette as a simple series of SVG Ellipse/Path elements representing a stick-figure human form. Overlay neural dots (Circle elements, radius 2-3, Colors.accent at opacity 0.15) evenly across the viewBox in a 6x10 grid pattern for the neural aesthetic — these are static background dots, not region indicators.

    Region indicators: Draw each muscle region as an SVG Ellipse. Only render regions where frontVisible=true (front view) or backVisible=true (back view). Ellipse fill color:
      - primaryMuscles match → Colors.accent, opacity 0.75
      - secondaryMuscles match → Colors.accent, opacity 0.35
      - no match → Colors.Beige.bgShade, opacity 0.6
    Ellipse stroke: Colors.Beige.border, strokeWidth 0.5.

    When interactive=true, wrap each region in TouchableOpacity with onPress calling onMusclePress(region.id).

    Front/back toggle: Render a small toggle button below the SVG. Text: "Front" / "Back". Use Colors.accent for the active view label, Colors.Beige.textMuted for inactive. On press, calls onViewToggle(). If onViewToggle is undefined, the toggle button is not rendered.

    Position ellipses for a 120x200 coordinate space (approximate anatomical positions):
      Front view ellipses (cx, cy, rx, ry):
        chest:     60, 68,  22, 14
        shoulders: 60, 54,  30, 10   (represents both deltoid areas)
        biceps:    60, 80,  18, 10   (represents both arms, simplified)
        forearms:  60, 100, 14, 8
        abs/core:  60, 92,  18, 18
        obliques:  60, 92,  28, 16   (slightly wider than core)
        quads:     60, 140, 22, 20
        calves:    60, 170, 14, 16

      Back view ellipses (cx, cy, rx, ry):
        upper back: 60, 60,  26, 14
        lats:       60, 76,  22, 16
        triceps:    60, 80,  16, 10
        glutes:     60, 116, 26, 16
        hamstrings: 60, 140, 22, 20
        shoulders:  60, 54,  30, 10
        obliques:   60, 92,  28, 16
        forearms:   60, 100, 14, 8
        calves:     60, 170, 14, 16

    Silhouette outline: Draw a simplified human figure using SVG Path or a series of Ellipse/Rect elements on top of the region ellipses. Head: Circle cx=60 cy=18 r=12, fill=Colors.Beige.bgShade, stroke=Colors.Beige.border strokeWidth=0.5. Torso: Rect x=42 y=30 w=36 h=90 rx=10, same colors. Left arm: Rect x=24 y=32 w=16 h=68 rx=8. Right arm: Rect x=80 y=32 w=16 h=68 rx=8. Left leg: Rect x=42 y=120 w=16 h=70 rx=8. Right leg: Rect x=62 y=120 w=16 h=70 rx=8.

    File structure: imports at top, MuscleRegion type, MUSCLE_REGIONS constant, muscleMatches helper function, MuscleMapView component, StyleSheet named s at bottom. Component must be under 200 lines — if needed, extract MUSCLE_REGIONS to a separate constant block but keep in same file.

    No hardcoded hex values — all colors from Colors.*. All spacing from Spacing.* or numeric SVG coordinate values (coordinate values inside SVG viewBox are acceptable as numbers, not style values).

    Decision references: D-08 (front/back with toggle), D-09 (neural-dot grid overlay, accent primary, muted secondary), D-10 (13 muscle regions, matches muscleGroup/secondaryMuscles).
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/MuscleMapView.tsx exists
    - exports: MuscleMapView (default), MUSCLE_REGIONS (named), MuscleRegion (type), muscleMatches (named function)
    - tsc --noEmit passes with no errors
    - MUSCLE_REGIONS array has exactly 13 entries
    - grep -c "Colors\." src/components/MuscleMapView.tsx returns >= 6 (all colors use Colors.* tokens)
    - grep -v "Colors\." src/components/MuscleMapView.tsx | grep -c "#" returns 0 (no raw hex values outside Colors.* references)
    - Component is under 200 lines: wc -l src/components/MuscleMapView.tsx | awk '{print $1}' outputs a number <= 200
    - Interactive prop branch exists: grep -c "interactive" src/components/MuscleMapView.tsx returns >= 2
    - Front/back toggle branch exists: grep -c "onViewToggle" src/components/MuscleMapView.tsx returns >= 2
  </acceptance_criteria>
  <done>MuscleMapView component exists, exports correct symbols, passes tsc, has 13 regions, no hardcoded hex values, under 200 lines</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent component → MuscleMapView | primaryMuscles/secondaryMuscles are string arrays from static exercise data; no user input flows through this component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Denial of Service | MuscleMapView SVG render | accept | Static SVG with fixed node count; no dynamic computation proportional to input size |
| T-12-SC | Tampering | npm/pip installs | accept | No new packages; react-native-svg already in Expo SDK 54 |
</threat_model>

<verification>
- tsc --noEmit passes after creating MuscleMapView.tsx
- wc -l src/components/MuscleMapView.tsx reports <= 200
- All 13 MUSCLE_REGIONS entries present
- No hardcoded hex values: grep -v "Colors\." src/components/MuscleMapView.tsx | grep "#" returns empty
- muscleMatches function exported and handles substring matching both directions
</verification>

<success_criteria>
MuscleMapView component built and type-checked; 13 muscle regions defined; front/back toggle; interactive and read-only modes; neural-dot aesthetic; all colors from Colors.* tokens.
</success_criteria>

<output>
Create `.planning/phases/12-exercise-ui-overhaul/12-02-SUMMARY.md` when done
</output>
