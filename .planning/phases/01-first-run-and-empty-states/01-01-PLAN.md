---
phase: 01-first-run-and-empty-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/firstRunContent.ts
  - src/data/biomarkers.ts
  - src/components/ExplanationCard.tsx
  - src/screens/GuidedFirstRunScreen.tsx
  - src/navigation/AppNavigator.tsx
  - src/screens/OnboardingScreen.tsx
autonomous: true
requirements:
  - FIRST-01
  - FIRST-02
  - FIRST-03
  - FIRST-04

must_haves:
  truths:
    - "User completing OnboardingScreen lands on GuidedFirstRunScreen (not Main) — verified by reading line 88 of OnboardingScreen.tsx"
    - "GuidedFirstRunScreen shows Step 1 of 3 with Glucose explanation card + numeric input"
    - "User can enter a number on each step; submitting advances step counter and writes a StoredEntry to @vitalspan_biomarkers"
    - "Final step CTA writes @vitalspan_first_run_complete=true, calls nav.reset to Main, and Dashboard shows entered values"
    - "I'll do this later skip writes @vitalspan_first_run_complete=true and calls nav.reset to Main without saving"
    - "GuidedFirstRunScreen cannot be dismissed via back gesture (gestureEnabled: false)"
  artifacts:
    - path: "src/data/firstRunContent.ts"
      provides: "FIRST_RUN_CONTENT array + FIRST_RUN_CONTENT_MAP lookup for 3 biomarkers"
      exports: ["FirstRunContent", "FIRST_RUN_CONTENT", "FIRST_RUN_CONTENT_MAP"]
    - path: "src/components/ExplanationCard.tsx"
      provides: "Self-contained explanation card sub-component (icon + headline + body inside BreathingCard)"
      exports: ["ExplanationCard", "ExplanationCardProps"]
    - path: "src/screens/GuidedFirstRunScreen.tsx"
      provides: "3-step guided biomarker entry screen, uses ExplanationCard"
      min_lines: 100
    - path: "src/navigation/AppNavigator.tsx"
      provides: "GuidedFirstRun route registered in RootStackParamList with fullScreenModal presentation"
      contains: "GuidedFirstRun: undefined"
    - path: "src/screens/OnboardingScreen.tsx"
      provides: "Onboarding finish() navigates to GuidedFirstRun instead of Main"
      contains: "name: 'GuidedFirstRun'"
    - path: "src/data/biomarkers.ts"
      provides: "totalcholesterol biomarker entry (referenced by FIRST_RUN_CONTENT)"
      contains: "id: 'totalcholesterol'"
  key_links:
    - from: "src/screens/OnboardingScreen.tsx"
      to: "GuidedFirstRun route"
      via: "nav.reset on line ~88"
      pattern: "name:\\s*'GuidedFirstRun'"
    - from: "src/screens/GuidedFirstRunScreen.tsx"
      to: "@vitalspan_biomarkers AsyncStorage"
      via: "save handler on step advance/finish"
      pattern: "@vitalspan_biomarkers"
    - from: "src/screens/GuidedFirstRunScreen.tsx"
      to: "@vitalspan_first_run_complete AsyncStorage"
      via: "skip and finish handlers"
      pattern: "@vitalspan_first_run_complete"
    - from: "src/screens/GuidedFirstRunScreen.tsx"
      to: "FIRST_RUN_CONTENT_MAP"
      via: "step content lookup"
      pattern: "FIRST_RUN_CONTENT"
    - from: "src/navigation/AppNavigator.tsx"
      to: "GuidedFirstRunScreen"
      via: "Stack.Screen with fullScreenModal presentation and gestureEnabled:false"
      pattern: "name=\"GuidedFirstRun\""
---

## Phase Goal

**As a** new user who just completed onboarding, **I want to** be guided to log my first 3 biomarkers (Glucose, HbA1c, Total Cholesterol) with a plain-English explanation of why each matters, **so that** I see my Longevity Score unlock within minutes — not after hours of data entry.

<objective>
Wire the thin end-to-end happy path of the guided first-run flow: the new GuidedFirstRunScreen, its content data, its navigation registration, and the onboarding handoff. After this plan, a fresh user completing onboarding can enter all 3 biomarkers (or skip) and land on Dashboard with data visible.

Purpose: Deliver immediate clinical value — the core promise of v1. No more blank Dashboard as the first impression after onboarding.
Output: A runnable guided first-run flow that satisfies FIRST-01, FIRST-02, FIRST-03 (skip path), and FIRST-04.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-first-run-and-empty-states/01-CONTEXT.md
@.planning/phases/01-first-run-and-empty-states/01-PATTERNS.md
@.planning/phases/01-first-run-and-empty-states/01-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Existing types/exports the executor will consume. Use these verbatim. -->
<!-- No codebase exploration needed beyond <read_first> files. -->

From src/navigation/AppNavigator.tsx:
- `RootStackParamList` is an exported type with route names Landing | Onboarding | Main | BiomarkerDetail | BiomarkerEntry | InteractionChecker | LabUpload | LongevityScore | Settings | About. Add `GuidedFirstRun: undefined`.

From src/screens/BiomarkerEntryScreen.tsx:
- `export interface StoredEntry { id: string; biomarkerId: string; value: number; date: string; source: string; notes: string }`
- Import StoredEntry from './BiomarkerEntryScreen' (NOT from a separate types file — that is the project convention).

From src/data/biomarkers.ts:
- `export interface Biomarker { id, name, category, categoryLabel, unit, color, optMin, optMax, target, description, howToImprove, defaultVal, prevVal, insight, history }`
- `export const BIOMARKERS: Biomarker[]` — current IDs include fastingglucose, hba1c, hdl, ldl. There is NO `totalcholesterol` entry; this plan adds one.

From src/theme/index.ts:
- `Colors.primary='#2D6A4F'`, `Colors.primaryDark='#1C3B2A'`, `Colors.primaryBg='#E8F5EEE'`, `Colors.bg='#EDE8DC'`, `Colors.bgCard='#FFFFFF'`, `Colors.border='#D4CFC4'`, `Colors.borderLight='#E2DED6'`, `Colors.textPrimary='#1A1A18'`, `Colors.textSecondary='#4A4A45'`, `Colors.textMuted='#8A8A82'`, `Colors.status.critical='#E76F51'`
- `Spacing.xs=4, sm=8, md=12, base=16, lg=20, xl=24, xxl=32`
- `Radius.sm=10, md=12, lg=16, xl=20`
- `Typography.sizes.sm=12, base=14, body=15, lg=16, h3=18`
- `Motion.breath=4000`

From src/components/BreathingCard.tsx:
- Props: `{ children: React.ReactNode; style?: ViewStyle; glowColor?: string; period?: number }` — default glowColor `'#1C3B2A'`, default period `Motion.breath`. Pass children inside as a plain View.

From src/components/RangeBar.tsx:
- Existing range visualisation component referenced by UI-SPEC — import default export. Already used in BiomarkerEntryScreen; mirror its props from that usage. If RangeBar requires a specific prop shape that doesn't fit the guided flow context, omit it and add a TODO comment rather than fabricating props.

From CLAUDE.md AsyncStorage keys:
- `@vitalspan_user_profile`, `@vitalspan_biomarkers`, `@vitalspan_protocol`, `@vitalspan_protocol_today`, `@vitalspan_health_data`, `@vitalspan_health_permissions`
- New key introduced here: `@vitalspan_first_run_complete` (string `'true'`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add totalcholesterol biomarker entry + create firstRunContent.ts</name>
  <files>src/data/biomarkers.ts, src/data/firstRunContent.ts</files>
  <read_first>
    - src/data/biomarkers.ts (full file — observe the existing `Biomarker` interface, the `BIOMARKERS` array shape, the established convention for entries: id, name, category, categoryLabel, unit, color, optMin, optMax, target, description, howToImprove, defaultVal, prevVal, insight, history. Note that `hdl` and `ldl` exist but `totalcholesterol` does not. Mirror an existing cardio entry such as `ldl` for shape.)
    - src/theme/index.ts (Colors only — use `Colors.primaryLight` for the totalcholesterol color field to match other cardio biomarkers)
    - .planning/phases/01-first-run-and-empty-states/01-UI-SPEC.md (Copywriting Contract section — copy the 3 explanation card bodies VERBATIM into FIRST_RUN_CONTENT)
    - .planning/phases/01-first-run-and-empty-states/01-CONTEXT.md (D-06, D-07, D-08 — confirm scope is exactly 3 biomarkers, no schema-wide changes)
  </read_first>
  <action>
    Two coordinated additions.

    (A) `src/data/biomarkers.ts` — append a new Biomarker entry to the `BIOMARKERS` array with `id: 'totalcholesterol'`, `name: 'Total Cholesterol'`, `category: 'cardio'`, `categoryLabel: 'Cardiovascular'`, `unit: 'mg/dL'`, `color: Colors.primaryLight`, `optMin: 150`, `optMax: 200` (matches UI-SPEC body copy "optimal range of 150–200 mg/dL"), `target: '150–200 mg/dL'`, `defaultVal: 180`, `prevVal: 190`, plus a pharmacist-accurate `description` (one sentence on what total cholesterol measures and why it matters), `howToImprove` (one sentence on diet/exercise/statin context), `insight` (one short motivational sentence), and `history: []`. Place the new entry after the existing `ldl` entry. Add a one-line source comment `// Pharmacist-verified — longevity range from UI-SPEC §Copywriting Contract`.

    (B) `src/data/firstRunContent.ts` (NEW file) — create the data module per D-06 and PATTERNS §firstRunContent.ts. Export `FirstRunContent` interface with fields `biomarkerId: string`, `icon: string`, `headline: string`, `body: string`. Export `FIRST_RUN_CONTENT: FirstRunContent[]` containing 3 entries in this exact order: (1) `biomarkerId: 'fastingglucose'`, icon `'🍬'`, headline `'Why Fasting Glucose Matters'`, body copied VERBATIM from UI-SPEC §Copywriting Contract Glucose row; (2) `biomarkerId: 'hba1c'`, icon `'🩸'`, headline `'Why HbA1c Matters'`, body verbatim from UI-SPEC §Copywriting Contract HbA1c row; (3) `biomarkerId: 'totalcholesterol'`, icon `'💙'`, headline `'Why Total Cholesterol Matters'`, body verbatim from UI-SPEC §Copywriting Contract Cholesterol row. Also export `FIRST_RUN_CONTENT_MAP: Record<string, FirstRunContent>` built via `Object.fromEntries(FIRST_RUN_CONTENT.map(c => [c.biomarkerId, c]))` for O(1) lookup. Filename is camelCase per CLAUDE.md naming convention.

    No `any` types. No hardcoded color hex values in firstRunContent.ts (icons are emoji strings — that is allowed).
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | grep -E "firstRunContent|biomarkers" | grep -v "warning"; test $? -ne 0 || echo "TS_OK"</automated>
    Also run: `grep -c "id: 'totalcholesterol'" src/data/biomarkers.ts` MUST return 1.
    Also run: `grep -c "biomarkerId: 'fastingglucose'\|biomarkerId: 'hba1c'\|biomarkerId: 'totalcholesterol'" src/data/firstRunContent.ts` MUST return 3.
    Also run: `grep -c "FIRST_RUN_CONTENT_MAP" src/data/firstRunContent.ts` MUST return ≥ 2 (declaration + export usage).
  </verify>
  <acceptance_criteria>
    - File `src/data/firstRunContent.ts` exists and exports `FirstRunContent`, `FIRST_RUN_CONTENT`, `FIRST_RUN_CONTENT_MAP`.
    - `FIRST_RUN_CONTENT` has length exactly 3 with biomarkerIds in order: `fastingglucose`, `hba1c`, `totalcholesterol`.
    - All three body strings in `FIRST_RUN_CONTENT` match the UI-SPEC §Copywriting Contract bodies character-for-character (no paraphrasing).
    - `src/data/biomarkers.ts` contains a new entry with `id: 'totalcholesterol'`, `category: 'cardio'`, `optMin: 150`, `optMax: 200`, `unit: 'mg/dL'`.
    - `npx tsc --noEmit` reports no new errors involving the modified/created files.
    - Neither file uses the `any` type.
  </acceptance_criteria>
  <done>
    Both files exist, types are strict, TypeScript compiles, and the 3 explanation bodies in firstRunContent.ts are byte-identical to the UI-SPEC Copywriting Contract.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ExplanationCard component + GuidedFirstRunScreen.tsx (3-step guided flow)</name>
  <files>src/components/ExplanationCard.tsx, src/screens/GuidedFirstRunScreen.tsx</files>
  <read_first>
    - src/screens/OnboardingScreen.tsx (full file — primary analog per PATTERNS.md; copy the ProgressBar sub-component pattern, the early-return-per-step state machine, the SafeAreaView + ScrollView layout, the StyleSheet `s` placement, and the haptics pattern. Note that OnboardingScreen uses a 5-segment ProgressBar; GuidedFirstRunScreen uses a 3-dot indicator per UI-SPEC.)
    - src/screens/BiomarkerEntryScreen.tsx lines 1–120 (observe the StoredEntry save pattern — the executor must reuse the exact same shape `{ id, biomarkerId, value, date, source: 'Blood test', notes: '' }` and write to `@vitalspan_biomarkers`)
    - src/data/firstRunContent.ts (created in Task 1 — consume FIRST_RUN_CONTENT and reference biomarkerIds in step order)
    - src/data/biomarkers.ts lines 1–40 (Biomarker interface — used to look up unit and target for each step's input field label)
    - src/components/BreathingCard.tsx (full file — wrapper for the explanation card, glowColor + period props)
    - src/theme/index.ts (Colors, Spacing, Radius, Typography, Motion exports)
    - .planning/phases/01-first-run-and-empty-states/01-UI-SPEC.md §Component Inventory (GuidedFirstRunScreen layout) and §Interaction Contracts and §Copywriting Contract (CTAs verbatim)
    - .planning/phases/01-first-run-and-empty-states/01-PATTERNS.md §GuidedFirstRunScreen section (the step-machine, save handler, skip handler, finish handler, BreathingCard usage, KeyboardAvoidingView wrapper)
  </read_first>
  <action>
    Two coordinated file creations.

    PART A — `src/components/ExplanationCard.tsx` (NEW file, keeps GuidedFirstRunScreen under 200 lines):

    Extract the explanation card display logic into a dedicated component. This is required by CLAUDE.md's 200-line limit for components. The component is a named export (also the default export) called `ExplanationCard`.

    Export `ExplanationCardProps` interface: `{ icon: string; headline: string; body: string; glowColor?: string; period?: number; style?: ViewStyle }`.

    The component renders: `<BreathingCard glowColor={glowColor ?? Colors.primaryDark} period={period ?? Motion.breath} style={style}>` containing `<View style={s.inner}>` with three Text children — the icon (style `s.icon`), the headline (style `s.headline`), and the body (style `s.body`). StyleSheet `s` uses: `inner: { backgroundColor: Colors.bgCard, borderRadius: Radius.lg, borderWidth: 1, borderColor: Colors.borderLight, padding: Spacing.base }`, `icon: { fontSize: 32, marginBottom: Spacing.sm }`, `headline: { fontSize: Typography.sizes.h3, fontWeight: '600', color: Colors.textPrimary, marginBottom: Spacing.sm }`, `body: { fontSize: Typography.sizes.body, fontWeight: '400', color: Colors.textSecondary, lineHeight: 24 }`. No `any`. No hex values. Import `ViewStyle` from 'react-native'.

    PART B — `src/screens/GuidedFirstRunScreen.tsx`:

    Create as a new default-exported React function component following the OnboardingScreen analog.

    Imports (in this order): React + `useState` from 'react'; from 'react-native' import `View, Text, TouchableOpacity, StyleSheet, SafeAreaView, TextInput, ScrollView, KeyboardAvoidingView, Platform`; `useNavigation` from '@react-navigation/native'; `NativeStackNavigationProp` from '@react-navigation/native-stack'; `* as Haptics` from 'expo-haptics'; `AsyncStorage` from '@react-native-async-storage/async-storage'; `Colors, Spacing, Radius, Typography, Motion` from '../theme'; `RootStackParamList` from '../navigation/AppNavigator'; `StoredEntry` from './BiomarkerEntryScreen'; `BIOMARKERS` from '../data/biomarkers'; `ExplanationCard` default from '../components/ExplanationCard'; `FIRST_RUN_CONTENT` from '../data/firstRunContent'.

    Declare `type Nav = NativeStackNavigationProp<RootStackParamList>`.

    Declare a module-level constant `STEP_BIOMARKERS` as the ordered array of biomarkerIds: `['fastingglucose', 'hba1c', 'totalcholesterol'] as const`.

    Declare a module-level constant `CTA_LABELS = ['Log Glucose', 'Log HbA1c', 'Finish & See My Dashboard']` per UI-SPEC §Copywriting Contract.

    Inside the component, useState for: `step: number` (default 0), `inputValue: string` (default `''`), `inputError: string` (default `''`), `focused: boolean` (default `false`).

    Sub-component `ProgressBar({ step }: { step: number })` rendered inline above the ScrollView — renders three View dots (10px diameter for active dot using `Colors.primary`, 8px for inactive using `Colors.border`, gap `Spacing.sm`, centered in a row with `paddingTop: Spacing.xl` and a `paddingBottom: Spacing.sm`). Above the dots render a centered Text label `Step ${step + 1} of 3` in `Typography.sizes.lg, fontWeight: '600', color: Colors.textPrimary`.

    Async function `handleStepAdvance()` — read inputValue, `parseFloat`, if `isNaN(parsed) || parsed <= 0` set inputError to `'Enter a number to continue'` and return; else clear inputError, then read `@vitalspan_biomarkers` via `AsyncStorage.getItem`, parse to `StoredEntry[]` (default `[]`), push a new entry `{ id: \`${Date.now()}-${Math.random().toString(36).slice(2, 7)}\`, biomarkerId: STEP_BIOMARKERS[step], value: parsed, date: new Date().toISOString(), source: 'Blood test', notes: '' }`, write back. Call `Haptics.selectionAsync().catch(() => null)`, clear inputValue, advance `setStep(s => s + 1)`.

    Async function `handleFinish()` — same save logic as handleStepAdvance for the final entry. After save, `await AsyncStorage.setItem('@vitalspan_first_run_complete', 'true')`. Call `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success).catch(() => null)`. Then `nav.reset({ index: 0, routes: [{ name: 'Main' }] })`.

    Async function `handleSkip()` — `await AsyncStorage.setItem('@vitalspan_first_run_complete', 'true')`. Then `nav.reset({ index: 0, routes: [{ name: 'Main' }] })`. No haptic, no biomarker save.

    Render (single return — same view shell for all steps, derive content from `step`):
    - `<SafeAreaView style={s.safe}>` with `<ProgressBar step={step} />` first child.
    - Wrap the main content in `<KeyboardAvoidingView behavior={Platform.OS === 'ios' ? 'padding' : 'height'} style={{ flex: 1 }}>` containing a `<ScrollView contentContainerStyle={s.content} keyboardShouldPersistTaps="handled">`.
    - Inside ScrollView, look up `content = FIRST_RUN_CONTENT[step]` and `biomarker = BIOMARKERS.find(b => b.id === STEP_BIOMARKERS[step])`. Render: `<ExplanationCard icon={content.icon} headline={content.headline} body={content.body} style={s.explanationWrapper} />`.
    - Below ExplanationCard render the input field row: a Text label showing `${biomarker?.name} (${biomarker?.unit})` and a `<TextInput style={[s.valueInput, { borderColor: focused ? Colors.primary : Colors.border }]} keyboardType="decimal-pad" value={inputValue} onChangeText={(v) => { setInputValue(v); if (inputError) setInputError(''); }} onFocus={() => setFocused(true)} onBlur={() => setFocused(false)} placeholder={String(biomarker?.defaultVal ?? '')} placeholderTextColor={Colors.textMuted} />`. Beneath the TextInput, if `inputError` is non-empty render a Text in `Colors.status.critical, fontSize: Typography.sizes.sm` showing the error message.
    - Footer (outside ScrollView but inside KeyboardAvoidingView, in a `View style={s.footer}`): a primary `<TouchableOpacity activeOpacity={0.82} style={s.btnMain} onPress={step === 2 ? handleFinish : handleStepAdvance}>` with `<Text style={s.btnMainTxt}>{CTA_LABELS[step]}</Text>`. Below it, only when `step < 2`, render `<TouchableOpacity onPress={handleSkip} style={s.skipTouchable}><Text style={s.btnSkip}>I'll do this later</Text></TouchableOpacity>`.

    StyleSheet `s` at bottom of file. Every value from theme tokens — no hex, no magic numbers except the dot diameters (10/8) and explicit dimensions called out in the UI-SPEC (height 52 for primary CTA, height 52 for TextInput, `paddingVertical: 12` on skipTouchable for 44pt touch target). Required style keys at minimum: `safe`, `content`, `progressRow`, `progressDot`, `progressDotActive`, `progressDotInactive`, `progressLabel`, `explanationWrapper`, `inputLabel`, `valueInput`, `inputError`, `footer`, `btnMain`, `btnMainTxt`, `skipTouchable`, `btnSkip`.

    Style specifics (all from theme tokens):
    - `safe: { flex: 1, backgroundColor: Colors.bg }`
    - `content: { paddingHorizontal: Spacing.base, paddingTop: Spacing.lg, paddingBottom: Spacing.xl }`
    - `progressRow: { flexDirection: 'row', justifyContent: 'center', gap: Spacing.sm, paddingTop: Spacing.xl, paddingBottom: Spacing.sm }`
    - `progressLabel: { textAlign: 'center', fontSize: Typography.sizes.lg, fontWeight: '600', color: Colors.textPrimary, paddingTop: Spacing.xl }`
    - `progressDot: { width: 8, height: 8, borderRadius: 4 }` and active variant uses width/height 10 and `backgroundColor: Colors.primary`; inactive uses `backgroundColor: Colors.border`
    - `explanationWrapper: { marginBottom: Spacing.base }` (passed as `style` prop to ExplanationCard — the inner card styling lives in ExplanationCard's own StyleSheet)
    - `valueInput: { backgroundColor: Colors.bgCard, borderRadius: Radius.md, borderWidth: 1.5, height: 52, paddingHorizontal: Spacing.base, fontSize: Typography.sizes.h3, fontWeight: '600', color: Colors.textPrimary, marginTop: Spacing.md }`
    - `inputError: { color: Colors.status.critical, fontSize: Typography.sizes.sm, marginTop: Spacing.xs }`
    - `footer: { paddingHorizontal: Spacing.base, paddingTop: Spacing.md, paddingBottom: Spacing.xxl, backgroundColor: Colors.bg }`
    - `btnMain: { backgroundColor: Colors.primary, borderRadius: Radius.xl, height: 52, justifyContent: 'center', alignItems: 'center' }`
    - `btnMainTxt: { color: Colors.bgCard, fontSize: Typography.sizes.lg, fontWeight: '600' }`
    - `skipTouchable: { paddingVertical: 12, alignItems: 'center' }`
    - `btnSkip: { color: Colors.textMuted, fontSize: Typography.sizes.sm, textAlign: 'center' }`

    No `any`. TypeScript strict. With the explanation card logic extracted to ExplanationCard.tsx, GuidedFirstRunScreen.tsx MUST stay under 200 lines per CLAUDE.md.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | grep -E "GuidedFirstRunScreen|ExplanationCard" | grep -v "warning"; test $? -ne 0 || echo "TS_OK"</automated>
    Also run: `grep -c "@vitalspan_biomarkers" src/screens/GuidedFirstRunScreen.tsx` MUST return ≥ 1.
    Also run: `grep -c "@vitalspan_first_run_complete" src/screens/GuidedFirstRunScreen.tsx` MUST return ≥ 2 (handleSkip + handleFinish).
    Also run: `grep -c "FIRST_RUN_CONTENT" src/screens/GuidedFirstRunScreen.tsx` MUST return ≥ 1.
    Also run: `grep -c "Log Glucose\|Log HbA1c\|Finish & See My Dashboard" src/screens/GuidedFirstRunScreen.tsx` MUST return 3.
    Also run: `grep -c "I'll do this later" src/screens/GuidedFirstRunScreen.tsx` MUST return 1.
    Also run: `grep -cE "#[0-9A-Fa-f]{3,6}" src/screens/GuidedFirstRunScreen.tsx` MUST return 0 (no hardcoded hex colors).
    Also run: `grep -c ": any" src/screens/GuidedFirstRunScreen.tsx` MUST return 0.
    Also run: `wc -l src/screens/GuidedFirstRunScreen.tsx | awk '{print $1}'` MUST be ≤ 200.
    Also run: `grep -c "ExplanationCard" src/screens/GuidedFirstRunScreen.tsx` MUST return ≥ 2 (import + JSX usage).
    Also run: `grep -c "ExplanationCardProps\|export.*ExplanationCard" src/components/ExplanationCard.tsx` MUST return ≥ 2.
  </verify>
  <acceptance_criteria>
    - File `src/components/ExplanationCard.tsx` exists and exports both `ExplanationCardProps` and a default `ExplanationCard` component.
    - `ExplanationCard` renders a `BreathingCard` wrapper around icon, headline, and body Text elements. All style values from theme tokens only.
    - File `src/screens/GuidedFirstRunScreen.tsx` exists and default-exports `GuidedFirstRunScreen`.
    - GuidedFirstRunScreen imports `ExplanationCard` from `'../components/ExplanationCard'` and uses it instead of inlining the card markup.
    - GuidedFirstRunScreen is ≤ 200 lines (enforced by the extraction).
    - File imports `FIRST_RUN_CONTENT` from `'../data/firstRunContent'`.
    - File imports `StoredEntry` from `'./BiomarkerEntryScreen'`.
    - Module declares `STEP_BIOMARKERS = ['fastingglucose', 'hba1c', 'totalcholesterol']` in that order.
    - Module declares CTA labels `['Log Glucose', 'Log HbA1c', 'Finish & See My Dashboard']`.
    - The string literal `"I'll do this later"` appears in GuidedFirstRunScreen.tsx exactly once and is rendered only when `step < 2`.
    - On step advance (steps 0 and 1) the code writes to `@vitalspan_biomarkers` AsyncStorage with a `StoredEntry` having `source: 'Blood test'`.
    - On finish (step 2) and on skip the code writes `'true'` to `@vitalspan_first_run_complete` AsyncStorage.
    - Both finish and skip paths call `nav.reset({ index: 0, routes: [{ name: 'Main' }] })`.
    - The TextInput uses `keyboardType="decimal-pad"` and a dynamic `borderColor` driven by the `focused` state.
    - No hex color literals in either file's StyleSheet.
    - No `any` type in either file.
    - `npx tsc --noEmit` reports no new errors for either file.
  </acceptance_criteria>
  <done>
    ExplanationCard.tsx is a self-contained component under 200 lines. GuidedFirstRunScreen.tsx is ≤ 200 lines, compiles cleanly, follows OnboardingScreen patterns, persists entries with correct shape, gates both completion paths through `@vitalspan_first_run_complete`, and renders verbatim copy from UI-SPEC via ExplanationCard.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register GuidedFirstRun route + wire OnboardingScreen to navigate to it</name>
  <files>src/navigation/AppNavigator.tsx, src/screens/OnboardingScreen.tsx</files>
  <read_first>
    - src/navigation/AppNavigator.tsx (full file — line 23–34 is RootStackParamList; line 160–164 is the existing LongevityScore fullScreenModal Stack.Screen entry. The new GuidedFirstRun entry mirrors that block with the addition of `gestureEnabled: false`.)
    - src/screens/OnboardingScreen.tsx lines 76–89 (the `finish()` function — only line 88 changes)
    - .planning/phases/01-first-run-and-empty-states/01-PATTERNS.md §AppNavigator.tsx and §OnboardingScreen.tsx (exact change locations)
    - .planning/phases/01-first-run-and-empty-states/01-CONTEXT.md D-02, D-03, D-15 (per-decision rationale: fullScreenModal, gestureEnabled false, OnboardingScreen target change)
  </read_first>
  <action>
    Two coordinated edits.

    (A) `src/navigation/AppNavigator.tsx`:
    1. Add `import GuidedFirstRunScreen from '../screens/GuidedFirstRunScreen';` alongside the other screen imports (after the existing `AboutScreen` import on line 19).
    2. In `RootStackParamList` (lines 23–34), add the entry `GuidedFirstRun: undefined;` immediately after `LongevityScore: undefined;` and before `Settings: undefined;`.
    3. In the `<Stack.Navigator>` body, add a new `<Stack.Screen>` entry for GuidedFirstRun between the existing `LongevityScore` entry (lines 160–164) and the `Settings` entry. The new entry uses `name="GuidedFirstRun"`, `component={GuidedFirstRunScreen}`, `options={{ presentation: 'fullScreenModal', animation: 'fade_from_bottom', gestureEnabled: false }}`. The `gestureEnabled: false` flag is required per CONTEXT D-02 to prevent accidental dismissal.

    (B) `src/screens/OnboardingScreen.tsx`:
    1. On line 88 only, change `nav.reset({ index: 0, routes: [{ name: 'Main' }] });` to `nav.reset({ index: 0, routes: [{ name: 'GuidedFirstRun' }] });`. No other lines change. The Haptics.notificationAsync call on line 87 stays as-is — a celebratory haptic on onboarding completion is still appropriate.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | grep -E "AppNavigator|OnboardingScreen" | grep -v "warning"; test $? -ne 0 || echo "TS_OK"</automated>
    Also run: `grep -c "GuidedFirstRun: undefined" src/navigation/AppNavigator.tsx` MUST return 1.
    Also run: `grep -c "name=\"GuidedFirstRun\"" src/navigation/AppNavigator.tsx` MUST return 1.
    Also run: `grep -c "gestureEnabled: false" src/navigation/AppNavigator.tsx` MUST return ≥ 4 (existing Landing/Onboarding/Main + new GuidedFirstRun).
    Also run: `grep -c "import GuidedFirstRunScreen" src/navigation/AppNavigator.tsx` MUST return 1.
    Also run: `grep -c "name: 'GuidedFirstRun'" src/screens/OnboardingScreen.tsx` MUST return 1.
    Also run: `grep -v '^#' src/screens/OnboardingScreen.tsx | grep -c "name: 'Main' }\] });" | head -1` MUST return 0 (the old target line is fully replaced — but note that OnboardingScreen does not have any other Main reset, so this is exact).
  </verify>
  <acceptance_criteria>
    - `src/navigation/AppNavigator.tsx` imports `GuidedFirstRunScreen` from `'../screens/GuidedFirstRunScreen'`.
    - `RootStackParamList` includes `GuidedFirstRun: undefined`.
    - A `<Stack.Screen name="GuidedFirstRun" component={GuidedFirstRunScreen}>` exists with options `{ presentation: 'fullScreenModal', animation: 'fade_from_bottom', gestureEnabled: false }`.
    - `src/screens/OnboardingScreen.tsx` `finish()` function calls `nav.reset({ index: 0, routes: [{ name: 'GuidedFirstRun' }] })` (the previous `Main` target is gone from this file).
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>
    Onboarding completion navigates to the new fullScreenModal GuidedFirstRunScreen. The route is registered, the back gesture is disabled, and TypeScript compiles cleanly. End-to-end happy path is now wired: fresh user → Landing → Onboarding (5 steps) → GuidedFirstRun (3 steps) → Main (Dashboard with data).
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → AsyncStorage | TextInput numeric values for biomarkers cross this boundary into local persistent storage. Read back later by Dashboard and BiomarkerDetail. |
| Navigation state → app state | The fullScreenModal presentation is the only barrier preventing a user from dismissing the flow without setting `@vitalspan_first_run_complete`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | TextInput biomarker value | mitigate | `parseFloat` + `isNaN(parsed) \|\| parsed <= 0` rejection in `handleStepAdvance` and `handleFinish`. Invalid input shows inline error and does NOT write to AsyncStorage. Implausible-range copy ("That value looks unusual — double-check your lab result") is deferred — see Deferred Ideas in CONTEXT.md. |
| T-01-02 | Denial-of-Service | AsyncStorage write failure | accept | UI-SPEC §Error States says silent retry on storage write failure in first-run flow. AsyncStorage on iOS is highly reliable; surfacing a user-facing error would harm the first-impression experience more than the rare failure case. |
| T-01-03 | Information Disclosure | StoredEntry contents | accept | Local-device-only storage per Init decision "AsyncStorage only for v1 (no backend needed for single-device beta)". No network egress, no PII beyond pharmacist-relevant lab values the user knowingly entered. |
| T-01-04 | Repudiation | First-run completion flag | accept | `@vitalspan_first_run_complete` is a UX flag, not a security artifact. Reset via the existing Settings reset flow (ALL_STORAGE_KEYS) is by design. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced in this plan. All imports (`expo-haptics`, `@react-native-async-storage/async-storage`, `react-native-reanimated`, `@react-navigation/*`, `expo`) already in package.json — verified by existing OnboardingScreen.tsx import list. |

</threat_model>

<verification>
After all three tasks complete:
1. `npx tsc --noEmit` returns no new errors.
2. Visual smoke (deferred to Plan 02 verify-and-test — this plan does not run the simulator).
3. Source assertions: every grep gate in each task's `<verify>` returns the expected count.
4. The 3 explanation card body strings in `firstRunContent.ts` are byte-identical to UI-SPEC §Copywriting Contract (manual diff acceptable).
5. `wc -l src/screens/GuidedFirstRunScreen.tsx` returns ≤ 200.
</verification>

<success_criteria>
- FIRST-01 (guided flow after onboarding): satisfied — OnboardingScreen.finish() resets to GuidedFirstRunScreen which presents 3 ordered biomarker steps.
- FIRST-02 (plain-English explanation card per step): satisfied — `FIRST_RUN_CONTENT` provides icon + headline + body for each of the 3 biomarkers; GuidedFirstRunScreen renders via ExplanationCard above each step's input.
- FIRST-03 (skip path): partially satisfied — "I'll do this later" skip writes `@vitalspan_first_run_complete=true` and navigates to Main. The re-trigger from Dashboard empty state is Plan 02.
- FIRST-04 (complete → Dashboard with partial-progress state): satisfied — final step persists the third StoredEntry to `@vitalspan_biomarkers` and resets to Main. Dashboard (Plan 02) reads entries on focus; when entries exist but `biologicalAge` is still null (PhenoAge confidence 'insufficient'), FutureSelf shows the partial-progress state (checklist with logged items checked, motivational note, no biological age number). The biological age number unlocks only once 5+ PhenoAge biomarkers are logged.
</success_criteria>

<output>
Create `.planning/phases/01-first-run-and-empty-states/01-01-SUMMARY.md` when done.
</output>
