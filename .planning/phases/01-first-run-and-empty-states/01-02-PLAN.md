---
phase: 01-first-run-and-empty-states
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/screens/DashboardScreen.tsx
  - src/screens/BiomarkerDetailScreen.tsx
autonomous: true
requirements:
  - EMPTY-01
  - EMPTY-02
  - FIRST-03

must_haves:
  truths:
    - "Dashboard shows no biomarker grid when entries.length === 0 — a single empty state card with icon, heading, body, and 'Log Your First Biomarkers' CTA appears in its place"
    - "Tapping 'Log Your First Biomarkers' on Dashboard calls nav.navigate('GuidedFirstRun')"
    - "FutureSelf card stays visible above the empty state card (in locked state with no data props)"
    - "Dashboard shows normal biomarker grid when entries.length > 0"
    - "BiomarkerDetailScreen shows an empty state card at the top of the list when entries.length === 0"
    - "Tapping 'Log Your First Result' on BiomarkerDetailScreen calls nav.navigate('GuidedFirstRun')"
  artifacts:
    - path: "src/screens/DashboardScreen.tsx"
      provides: "Empty state branch replacing biomarker horizontal scroll when no entries exist"
      contains: "@vitalspan_first_run_complete"
    - path: "src/screens/BiomarkerDetailScreen.tsx"
      provides: "Empty state card in list view when entries.length === 0"
      contains: "GuidedFirstRun"
  key_links:
    - from: "src/screens/DashboardScreen.tsx"
      to: "GuidedFirstRun route"
      via: "nav.navigate('GuidedFirstRun') on empty state CTA"
      pattern: "navigate\\('GuidedFirstRun'\\)"
    - from: "src/screens/DashboardScreen.tsx"
      to: "@vitalspan_first_run_complete"
      via: "Promise.all extended with 6th key read in loadData"
      pattern: "@vitalspan_first_run_complete"
    - from: "src/screens/BiomarkerDetailScreen.tsx"
      to: "GuidedFirstRun route"
      via: "nav.navigate('GuidedFirstRun') on empty state CTA"
      pattern: "navigate\\('GuidedFirstRun'\\)"
---

## Phase Goal

**As a** new user who just completed onboarding, **I want to** be guided to log my first 3 biomarkers (Glucose, HbA1c, Total Cholesterol) with a plain-English explanation of why each matters, **so that** I see my Longevity Score unlock within minutes — not after hours of data entry.

<objective>
Replace the blank Dashboard biomarker section and the Biomarkers tab with purposeful empty states that funnel new users into the guided first-run flow. After this plan, no user ever sees a blank screen — they always see an actionable card.

Purpose: Satisfies EMPTY-01 (Dashboard empty state) and EMPTY-02 (Biomarkers tab empty state), and completes the FIRST-03 re-trigger path from Dashboard.
Output: Two modified screens with conditional empty state cards, each CTA navigating to GuidedFirstRun.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-first-run-and-empty-states/01-CONTEXT.md
@.planning/phases/01-first-run-and-empty-states/01-PATTERNS.md
@.planning/phases/01-first-run-and-empty-states/01-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Key types and contracts the executor needs. No codebase exploration required beyond read_first files. -->

From src/navigation/AppNavigator.tsx (modified by Plan 01):
- `RootStackParamList` now includes `GuidedFirstRun: undefined` — `nav.navigate('GuidedFirstRun')` is a valid typed call.

From src/screens/BiomarkerEntryScreen.tsx:
- `export interface StoredEntry { id: string; biomarkerId: string; value: number; date: string; source: string; notes: string }`

From src/screens/DashboardScreen.tsx (existing state variables at top of component):
- `const [profile, setProfile] = useState<UserProfile | null>(null);`
- `const [entries, setEntries] = useState<StoredEntry[]>([]);`
- `const [takenItems, setTakenItems] = useState<Set<string>>(new Set());`
- `const [refreshing, setRefreshing] = useState(false);`
- `const [healthData, setHealthData] = useState<HealthData | null>(null);`
- `const [exerciseLogs, setExerciseLogs] = useState<ExerciseLogEntry[]>([]);`
- Add: `const [firstRunComplete, setFirstRunComplete] = useState(false);`
- `loadData` uses `Promise.all` at lines 43–64 with 5 items. Extend to 6 items by appending `AsyncStorage.getItem('@vitalspan_first_run_complete')` and destructuring as `firstRunRaw`. After Promise.all, add `setFirstRunComplete(firstRunRaw === 'true');`.

From src/screens/DashboardScreen.tsx (existing render — the biomarker section):
- Lines 263–314 contain the biomarker section header + horizontal `ScrollView`. The section header (`View style={s.sectionHdr}`) stays unconditionally. Only the `ScrollView` block (lines 276–314) is conditionally replaced with the empty state card.

From src/screens/BiomarkerDetailScreen.tsx (existing state):
- `const [entries, setEntries] = useState<StoredEntry[]>([]);` — `entries.length === 0` is the empty state condition.
- `loadEntries` is called in `useFocusEffect`. No changes to data loading needed — the empty state is purely conditional rendering using the existing `entries` state.
- The list view `ScrollView` renders at approximately line 270. The `{entries.length === 0 && ...}` block is inserted as the first child inside the list view `ScrollView`'s content, before `CATEGORIES.map`.

From src/theme/index.ts:
- `Colors.primary='#2D6A4F'`, `Colors.bgCard='#FFFFFF'`, `Colors.bg='#EDE8DC'`, `Colors.borderLight='#E2DED6'`, `Colors.textPrimary='#1A1A18'`, `Colors.textSecondary='#4A4A45'`, `Colors.textMuted='#8A8A82'`
- `Spacing.base=16, Spacing.md=12, Spacing.lg=20, Spacing.xl=24, Spacing.sm=8`
- `Radius.lg=16, Radius.xl=20`
- `Typography.sizes.h3=18, Typography.sizes.body=15, Typography.sizes.base=14`

From .planning/phases/01-first-run-and-empty-states/01-UI-SPEC.md §Copywriting Contract:
- Dashboard empty state: icon `🧬`, heading `"Your longevity data starts here"`, body `"Log your first three biomarkers — Glucose, HbA1c, and Cholesterol — to unlock your Longevity Score and biological age projection."`, CTA `"Log Your First Biomarkers"`.
- Biomarkers tab empty state: icon `📊`, heading `"No biomarkers tracked yet"`, body `"Start with your most recent lab results. Three values unlock your biological age score."`, CTA `"Log Your First Result"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard empty state — read first_run_complete flag and replace biomarker grid</name>
  <files>src/screens/DashboardScreen.tsx</files>
  <read_first>
    - src/screens/DashboardScreen.tsx (full file — understand the current loadData Promise.all at lines 43–64; the FutureSelf usage at lines 235–241; the sectionHdr + horizontal ScrollView biomarker section at lines 263–314; the StyleSheet `s` at lines 407–475. Note: the `protoEmpty` + `protocolCard` styles at lines 463–466 are the closest visual analogs for the new empty state card styles.)
    - src/components/FutureSelf.tsx (full file — confirm the component renders in a "locked" visual state when `biologicalAge` is `undefined` and `optimality` is `0`. Do not change FutureSelf — just verify the existing props already handle locked state before relying on it.)
    - .planning/phases/01-first-run-and-empty-states/01-CONTEXT.md (D-09, D-10, D-11, D-16 — empty state replaces grid entirely; CTA navigates to GuidedFirstRun; FutureSelf stays visible in locked state above empty card; firstRunComplete read condition)
    - .planning/phases/01-first-run-and-empty-states/01-PATTERNS.md §DashboardScreen.tsx (exact Promise.all extension, empty state branch pattern, FutureSelf locked state pattern, empty state card styles)
    - .planning/phases/01-first-run-and-empty-states/01-UI-SPEC.md §Component Inventory "Modified: DashboardScreen — Empty State Branch" and §Copywriting Contract "Dashboard Empty State Card"
  </read_first>
  <action>
    Three coordinated changes to `src/screens/DashboardScreen.tsx`.

    (1) Add state variable after the existing `useState` declarations (after line 39):
    Add `const [firstRunComplete, setFirstRunComplete] = useState(false);` alongside the existing state declarations.

    (2) Extend `loadData`'s `Promise.all` — in the existing `Promise.all` at lines 43–49, add `AsyncStorage.getItem('@vitalspan_first_run_complete')` as the 6th item. Destructure the result as `firstRunRaw` alongside the existing 5 variables. After the existing `if (exerciseRaw)` assignment, add: `setFirstRunComplete(firstRunRaw === 'true');`. The entire Promise.all destructuring line changes from `const [profileRaw, entriesRaw, protocolRaw, hData, exerciseRaw] = await Promise.all([` to `const [profileRaw, entriesRaw, protocolRaw, hData, exerciseRaw, firstRunRaw] = await Promise.all([` and adds `AsyncStorage.getItem('@vitalspan_first_run_complete'),` as the last array element.

    (3) Replace the horizontal biomarker ScrollView in the render with a conditional. Locate the `<ScrollView horizontal ...>` block that renders `BIOMARKERS.slice(0, 5).map(...)` (lines 276–314). Replace ONLY this ScrollView block (the `View style={s.sectionHdr}` section header at lines 263–275 stays unconditionally). The replacement is:
    ```
    {entries.length === 0 && !firstRunComplete ? (
      <View style={s.emptyStateCard}>
        <Text style={s.emptyStateIcon}>🧬</Text>
        <Text style={s.emptyStateHeading}>Your longevity data starts here</Text>
        <Text style={s.emptyStateBody}>
          Log your first three biomarkers — Glucose, HbA1c, and Cholesterol — to unlock your Longevity Score and biological age projection.
        </Text>
        <TouchableOpacity
          activeOpacity={0.82}
          style={s.emptyStateCta}
          onPress={() => nav.navigate('GuidedFirstRun')}
        >
          <Text style={s.emptyStateCtaTxt}>Log Your First Biomarkers</Text>
        </TouchableOpacity>
      </View>
    ) : entries.length > 0 ? (
      /* existing horizontal ScrollView block — moved here unchanged */
      <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={s.bmScroll}>
        ...existing BIOMARKERS.slice(0, 5).map(...)...
      </ScrollView>
    ) : null}
    ```
    The third branch (`entries.length === 0 && firstRunComplete`) renders nothing — no grid, no empty state card. This matches D-16: if first run is complete but no entries exist (user skipped), Dashboard shows no biomarker section rather than a redundant CTA.

    Per D-11 and UI-SPEC: the FutureSelf card above the section header is already rendered unconditionally at lines 235–241. When entries are empty, pass `biologicalAge={undefined}`, `optimality={0}`, `loggedBiomarkerIds={[]}` to FutureSelf — the component already handles locked state internally with these prop values. The current code at lines 235–241 already passes `bioAge ?? undefined` for `biologicalAge`, `biomarkerOptimality` for `optimality`, and `Array.from(entryMap.keys())` for `loggedBiomarkerIds`. When `entries` is empty, these derived values are already `undefined`, `0`, and `[]` respectively — so no change is needed for FutureSelf; it already renders in locked state.

    Add new style keys to the StyleSheet `s` at the bottom of the file. Insert them after the existing `protoEmptyCtaTxt` style (line ~466):
    - `emptyStateCard`: `{ marginHorizontal: Spacing.base, backgroundColor: Colors.bgCard, borderRadius: Radius.lg, borderWidth: 1, borderColor: Colors.borderLight, padding: Spacing.xl, alignItems: 'center', marginBottom: Spacing.base }`
    - `emptyStateIcon`: `{ fontSize: 32, marginBottom: Spacing.md }`
    - `emptyStateHeading`: `{ fontSize: Typography.sizes.h3, fontWeight: '600', color: Colors.textPrimary, textAlign: 'center', marginBottom: Spacing.sm }`
    - `emptyStateBody`: `{ fontSize: Typography.sizes.body, fontWeight: '400', color: Colors.textSecondary, textAlign: 'center', lineHeight: 24, marginBottom: Spacing.lg }`
    - `emptyStateCta`: `{ backgroundColor: Colors.primary, borderRadius: Radius.xl, height: 48, paddingHorizontal: Spacing.base, justifyContent: 'center', alignItems: 'center', alignSelf: 'stretch' }`
    - `emptyStateCtaTxt`: `{ color: Colors.bgCard, fontSize: Typography.sizes.base, fontWeight: '600' }`

    No `any`. No hardcoded hex values. The existing `Colors.bgCard` is `'#FFFFFF'` — do not write that hex; use `Colors.bgCard` in the StyleSheet.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | grep "DashboardScreen" | grep -v "warning"; test $? -ne 0 || echo "TS_OK"</automated>
    Also run: `grep -c "@vitalspan_first_run_complete" src/screens/DashboardScreen.tsx` MUST return 2 (the getItem call in Promise.all + the `firstRunRaw === 'true'` read).
    Also run: `grep -c "firstRunComplete" src/screens/DashboardScreen.tsx` MUST return ≥ 3 (useState declaration + setFirstRunComplete + condition in render).
    Also run: `grep -c "navigate('GuidedFirstRun')" src/screens/DashboardScreen.tsx` MUST return 1.
    Also run: `grep -c "emptyStateCard\|emptyStateCta" src/screens/DashboardScreen.tsx` MUST return ≥ 4 (each key appears in StyleSheet + JSX).
    Also run: `grep -c "Your longevity data starts here" src/screens/DashboardScreen.tsx` MUST return 1.
    Also run: `grep -c "Log Your First Biomarkers" src/screens/DashboardScreen.tsx` MUST return 1.
  </verify>
  <acceptance_criteria>
    - `DashboardScreen.tsx` declares `const [firstRunComplete, setFirstRunComplete] = useState(false)`.
    - `loadData`'s `Promise.all` has 6 items; the 6th is `AsyncStorage.getItem('@vitalspan_first_run_complete')`.
    - After `Promise.all`, `setFirstRunComplete(firstRunRaw === 'true')` is called.
    - The conditional `entries.length === 0 && !firstRunComplete` gates the empty state card; the horizontal biomarker `ScrollView` renders only when `entries.length > 0`.
    - The empty state card renders verbatim: icon `🧬`, heading `"Your longevity data starts here"`, body `"Log your first three biomarkers — Glucose, HbA1c, and Cholesterol — to unlock your Longevity Score and biological age projection."`, CTA `"Log Your First Biomarkers"`.
    - The CTA calls `nav.navigate('GuidedFirstRun')`.
    - The FutureSelf component is NOT removed — it remains unconditional above the section header. Its props are unchanged; derived values (`bioAge ?? undefined`, `biomarkerOptimality`, `Array.from(entryMap.keys())`) handle locked state automatically when entries is empty.
    - StyleSheet `s` contains `emptyStateCard`, `emptyStateHeading`, `emptyStateBody`, `emptyStateCta`, `emptyStateCtaTxt` — all values from `Colors.*`, `Spacing.*`, `Radius.*`, `Typography.sizes.*` tokens only.
    - `npx tsc --noEmit` reports no new errors.
    - No `any` type. No hardcoded hex values in the new StyleSheet keys.
  </acceptance_criteria>
  <done>
    DashboardScreen compiles cleanly, reads @vitalspan_first_run_complete on focus, renders the empty state card with correct copy and GuidedFirstRun CTA when entries are empty and first run is incomplete, and renders the normal biomarker grid otherwise.
  </done>
</task>

<task type="auto">
  <name>Task 2: BiomarkerDetailScreen empty state — card at top of list view</name>
  <files>src/screens/BiomarkerDetailScreen.tsx</files>
  <read_first>
    - src/screens/BiomarkerDetailScreen.tsx (full file — understand where `entries` state is declared and loaded; locate the list view `ScrollView` that renders `CATEGORIES.map` (search for `CATEGORIES.map` to find the line); find the existing `emptyHistRow` + `emptyTxt` + `logCta` styles at lines ~183–195 and ~367–377 — these are visual analogs for the new empty state card styles. Confirm `entries` is the `StoredEntry[]` state used to detect empty.)
    - .planning/phases/01-first-run-and-empty-states/01-CONTEXT.md (D-12 — BiomarkerDetailScreen list view empty state, CTA to GuidedFirstRun)
    - .planning/phases/01-first-run-and-empty-states/01-PATTERNS.md §BiomarkerDetailScreen.tsx (exact insertion point: before CATEGORIES.map inside the list view ScrollView, condition `entries.length === 0`, full card JSX and style keys)
    - .planning/phases/01-first-run-and-empty-states/01-UI-SPEC.md §Component Inventory "Modified: BiomarkerDetailScreen — Empty State" and §Copywriting Contract "Biomarkers Tab Empty State Card"
  </read_first>
  <action>
    Two coordinated changes to `src/screens/BiomarkerDetailScreen.tsx`.

    (1) Insert empty state card block in the list view `ScrollView`. Locate the `ScrollView` that renders the CATEGORIES.map (the list view — not the detail view's history list). The empty state block is inserted as the FIRST child inside this ScrollView, before `CATEGORIES.map(...)`. The insertion:
    ```
    {entries.length === 0 && (
      <View style={s.emptyTabCard}>
        <Text style={s.emptyTabIcon}>📊</Text>
        <Text style={s.emptyTabHeading}>No biomarkers tracked yet</Text>
        <Text style={s.emptyTabBody}>
          Start with your most recent lab results. Three values unlock your biological age score.
        </Text>
        <TouchableOpacity
          activeOpacity={0.82}
          style={s.emptyTabCta}
          onPress={() => nav.navigate('GuidedFirstRun')}
        >
          <Text style={s.emptyTabCtaTxt}>Log Your First Result</Text>
        </TouchableOpacity>
      </View>
    )}
    ```
    The CATEGORIES.map block that follows is unchanged — it renders biomarker category sections. When entries exist, the empty state card is hidden and the full category list renders as before.

    (2) Add new style keys to the StyleSheet `s` at the bottom of the file. Insert them after the last existing style key:
    - `emptyTabCard`: `{ marginHorizontal: Spacing.base, marginTop: Spacing.base, backgroundColor: Colors.bgCard, borderRadius: Radius.lg, borderWidth: 1, borderColor: Colors.borderLight, padding: Spacing.xl, alignItems: 'center' }`
    - `emptyTabIcon`: `{ fontSize: 32, marginBottom: Spacing.md }`
    - `emptyTabHeading`: `{ fontSize: Typography.sizes.h3, fontWeight: '600', color: Colors.textPrimary, textAlign: 'center', marginBottom: Spacing.sm }`
    - `emptyTabBody`: `{ fontSize: Typography.sizes.body, fontWeight: '400', color: Colors.textSecondary, textAlign: 'center', lineHeight: 24, marginBottom: Spacing.lg }`
    - `emptyTabCta`: `{ backgroundColor: Colors.primary, borderRadius: Radius.xl, height: 48, paddingHorizontal: Spacing.base, justifyContent: 'center', alignItems: 'center', alignSelf: 'stretch' }`
    - `emptyTabCtaTxt`: `{ color: Colors.bgCard, fontSize: Typography.sizes.base, fontWeight: '600' }`

    Note: `BiomarkerDetailScreen` is also used as the `BiomarkerDetail` stack screen (for individual biomarker detail view with history) — verify that the `entries.length === 0` empty state is inserted in the LIST view branch only (the branch that renders CATEGORIES, not the detail view that renders a specific biomarker's history). The detail view has its own `emptyHistRow` empty state that must remain unchanged.

    No `any`. No hardcoded hex values.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | grep "BiomarkerDetailScreen" | grep -v "warning"; test $? -ne 0 || echo "TS_OK"</automated>
    Also run: `grep -c "navigate('GuidedFirstRun')" src/screens/BiomarkerDetailScreen.tsx` MUST return 1.
    Also run: `grep -c "emptyTabCard\|emptyTabCta" src/screens/BiomarkerDetailScreen.tsx` MUST return ≥ 4 (StyleSheet declaration + JSX usage for each).
    Also run: `grep -c "No biomarkers tracked yet" src/screens/BiomarkerDetailScreen.tsx` MUST return 1.
    Also run: `grep -c "Log Your First Result" src/screens/BiomarkerDetailScreen.tsx` MUST return 1.
    Also run: `grep -c "emptyHistRow" src/screens/BiomarkerDetailScreen.tsx` MUST return ≥ 1 (existing detail-view empty state is preserved, not replaced).
  </verify>
  <acceptance_criteria>
    - `BiomarkerDetailScreen.tsx` renders an empty state card in the list view when `entries.length === 0` — before the CATEGORIES.map block.
    - The empty state card renders verbatim: icon `📊`, heading `"No biomarkers tracked yet"`, body `"Start with your most recent lab results. Three values unlock your biological age score."`, CTA `"Log Your First Result"`.
    - The CTA calls `nav.navigate('GuidedFirstRun')`.
    - The existing detail-view empty state (`emptyHistRow` with `"No entries logged yet"` text) is NOT removed — it continues to render in the biomarker detail branch.
    - StyleSheet `s` contains `emptyTabCard`, `emptyTabHeading`, `emptyTabBody`, `emptyTabCta`, `emptyTabCtaTxt` — all values from theme tokens only.
    - `npx tsc --noEmit` reports no new errors.
    - No `any` type. No hardcoded hex values in the new StyleSheet keys.
  </acceptance_criteria>
  <done>
    BiomarkerDetailScreen list view shows a purposeful empty state card with correct copy and GuidedFirstRun CTA when no biomarkers have been logged. The existing detail-view empty state is preserved unchanged.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage → Dashboard render state | `@vitalspan_first_run_complete` is read as a string and compared to `'true'`; incorrect value would show stale empty state. |
| Nav action → GuidedFirstRun route | CTAs call `nav.navigate('GuidedFirstRun')` — route must exist in RootStackParamList (provided by Plan 01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `@vitalspan_first_run_complete` AsyncStorage read | accept | This is a UX flag, not a security boundary. The worst case is a user seeing a stale empty state after manual storage manipulation — recoverable. |
| T-02-02 | Denial-of-Service | `nav.navigate('GuidedFirstRun')` on missing route | mitigate | Route is registered in AppNavigator in Plan 01 (Wave 1 dependency). Plan 02 depends_on 01-01 — executor must verify Plan 01 complete before running this plan. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced in this plan. All imports already present in the codebase. |

</threat_model>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` returns no new errors.
2. Source assertions: every grep gate in each task's `<verify>` returns the expected count.
3. Copy assertions: heading and CTA strings in both screens match the UI-SPEC §Copywriting Contract exactly.
4. Preservation check: `grep -c "emptyHistRow" src/screens/BiomarkerDetailScreen.tsx` returns ≥ 1 (detail view empty state untouched).
</verification>

<success_criteria>
- EMPTY-01: DashboardScreen renders the empty state card with icon 🧬, heading "Your longevity data starts here", body and CTA "Log Your First Biomarkers" navigating to GuidedFirstRun when entries.length === 0 AND firstRunComplete is false.
- EMPTY-02: BiomarkerDetailScreen list view renders empty state card with icon 📊, heading "No biomarkers tracked yet", body and CTA "Log Your First Result" navigating to GuidedFirstRun when entries.length === 0.
- FIRST-03 (re-trigger path): Dashboard empty state CTA navigates to GuidedFirstRun — re-triggering the guided flow is now possible after a skip.
</success_criteria>

<output>
Create `.planning/phases/01-first-run-and-empty-states/01-02-SUMMARY.md` when done.
</output>
