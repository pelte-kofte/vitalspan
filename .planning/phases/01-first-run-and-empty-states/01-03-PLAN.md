---
phase: 01-first-run-and-empty-states
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/screens/BiomarkerEntryScreen.tsx
  - src/screens/SettingsScreen.tsx
autonomous: true
requirements:
  - FIRST-02
  - FIRST-03

must_haves:
  truths:
    - "BiomarkerEntryScreen shows a BreathingCard explanation card above the value input when biomarkerId is one of fastingglucose, hba1c, or totalcholesterol"
    - "The explanation card content (icon, headline, body) matches FIRST_RUN_CONTENT_MAP for the selected biomarker"
    - "The explanation card is NOT shown for biomarkers not in FIRST_RUN_CONTENT_MAP"
    - "@vitalspan_first_run_complete is listed in SettingsScreen ALL_STORAGE_KEYS so data reset clears the guided flow flag"
  artifacts:
    - path: "src/screens/BiomarkerEntryScreen.tsx"
      provides: "Conditional BreathingCard explanation card for supported biomarkers in step 2 (value entry)"
      contains: "FIRST_RUN_CONTENT_MAP"
    - path: "src/screens/SettingsScreen.tsx"
      provides: "@vitalspan_first_run_complete in ALL_STORAGE_KEYS array"
      contains: "@vitalspan_first_run_complete"
  key_links:
    - from: "src/screens/BiomarkerEntryScreen.tsx"
      to: "src/data/firstRunContent.ts"
      via: "import FIRST_RUN_CONTENT_MAP from '../data/firstRunContent'"
      pattern: "FIRST_RUN_CONTENT_MAP"
    - from: "src/screens/SettingsScreen.tsx"
      to: "@vitalspan_first_run_complete"
      via: "ALL_STORAGE_KEYS array"
      pattern: "@vitalspan_first_run_complete"
---

## Phase Goal

**As a** new user who just completed onboarding, **I want to** be guided to log my first 3 biomarkers (Glucose, HbA1c, Total Cholesterol) with a plain-English explanation of why each matters, **so that** I see my Longevity Score unlock within minutes — not after hours of data entry.

<objective>
Wire the explanation card into BiomarkerEntryScreen for the 3 supported biomarkers (per D-07), and register the new AsyncStorage key in SettingsScreen's reset list (per D-17). These two changes complete the FIRST-02 requirement for the non-guided path and ensure data reset keeps storage consistent.

Purpose: Users who navigate to BiomarkerEntryScreen directly (not via guided flow) also see the clinical explanation card for Glucose, HbA1c, and Cholesterol. The SettingsScreen key registration prevents a ghost flag state after "Reset all data".
Output: BiomarkerEntryScreen with conditional explanation card; SettingsScreen with complete ALL_STORAGE_KEYS array.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-first-run-and-empty-states/01-CONTEXT.md
@.planning/phases/01-first-run-and-empty-states/01-PATTERNS.md
@.planning/phases/01-first-run-and-empty-states/01-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Key types and contracts the executor needs. No codebase exploration required beyond read_first files. -->

From src/data/firstRunContent.ts (created in Plan 01 Task 1):
- `export interface FirstRunContent { biomarkerId: string; icon: string; headline: string; body: string }`
- `export const FIRST_RUN_CONTENT: FirstRunContent[]` — 3 entries with biomarkerIds: fastingglucose, hba1c, totalcholesterol.
- `export const FIRST_RUN_CONTENT_MAP: Record<string, FirstRunContent>` — O(1) lookup by biomarkerId.

From src/screens/BiomarkerEntryScreen.tsx (the key state variable for selected biomarker):
- The screen has a `selected` state of type `Biomarker | null` (the selected biomarker object). When in the "Step 2: enter value" view, `selected` is non-null. The condition to show the explanation card is `selected !== null && FIRST_RUN_CONTENT_MAP[selected.id] !== undefined`.
- The "Step 2" ScrollView is where the explanation card is inserted: ABOVE the `s.valueCard` View that contains the unit toggle, TextInput, and source selector. The insertion is the first child of the ScrollView.
- Import BreathingCard default from '../components/BreathingCard'. It is NOT currently imported — add it.
- Import `FIRST_RUN_CONTENT_MAP` from '../data/firstRunContent' — a new import.
- Import `Motion` from '../theme' — check if already imported; if not, add it alongside `Colors, Spacing, Radius, Typography`.

From src/components/BreathingCard.tsx:
- Props: `{ children: React.ReactNode; style?: ViewStyle; glowColor?: string; period?: number }`
- Default glowColor is `'#1C3B2A'` (Colors.primaryDark). Do NOT override — use default per UI-SPEC.
- Default period is `Motion.breath` (4000ms). Do NOT override — use default.

From src/theme/index.ts:
- `Colors.primaryDark='#1C3B2A'`, `Colors.bgCard='#FFFFFF'`, `Colors.borderLight='#E2DED6'`, `Colors.textPrimary='#1A1A18'`, `Colors.textSecondary='#4A4A45'`
- `Spacing.base=16, Spacing.md=12, Spacing.sm=8`
- `Radius.lg=16`
- `Typography.sizes.h3=18, Typography.sizes.body=15`
- `Motion.breath=4000`

From src/screens/SettingsScreen.tsx (lines 15–22):
- `const ALL_STORAGE_KEYS = [ '@vitalspan_user_profile', '@vitalspan_biomarkers', '@vitalspan_protocol', '@vitalspan_protocol_today', '@vitalspan_health_data', '@vitalspan_health_permissions' ];`
- The new key `'@vitalspan_first_run_complete'` is appended as the 7th item.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explanation card to BiomarkerEntryScreen for supported biomarkers</name>
  <files>src/screens/BiomarkerEntryScreen.tsx</files>
  <read_first>
    - src/screens/BiomarkerEntryScreen.tsx (full file — identify the current imports at lines 1–14; find where `selected` state is declared; locate the "Step 2" ScrollView that contains the value entry form — it renders `s.valueCard` View with the unit toggle and TextInput. The explanation card is inserted as the FIRST child of this ScrollView, before the `s.valueCard` View. Also locate the StyleSheet `s` at the bottom to understand existing style names and where to append new style keys.)
    - src/data/firstRunContent.ts (created by Plan 01 Task 1 — confirm the FIRST_RUN_CONTENT_MAP export and the three biomarkerIds: fastingglucose, hba1c, totalcholesterol)
    - src/components/BreathingCard.tsx (full file — confirm the props interface: children, style, glowColor, period)
    - .planning/phases/01-first-run-and-empty-states/01-CONTEXT.md (D-07, D-08 — explanation card shown in BiomarkerEntryScreen too; visual style is BreathingCard wrapper with clinical icon, one-line headline, 2–3 sentences body)
    - .planning/phases/01-first-run-and-empty-states/01-PATTERNS.md §BiomarkerEntryScreen.tsx (insertion point details, BreathingCard usage pattern, explanation card styles to add)
    - .planning/phases/01-first-run-and-empty-states/01-UI-SPEC.md §Component Inventory "Modified: BiomarkerEntryScreen — Explanation Card Insertion" (condition: biomarkerId is one of ['fastingglucose', 'hba1c', 'totalcholesterol'])
  </read_first>
  <action>
    Three coordinated changes to `src/screens/BiomarkerEntryScreen.tsx`.

    (1) Add new imports. After the existing `import { Colors, Spacing, Radius, Typography } from '../theme';` line, extend the theme import to also include `Motion` if not already present: `import { Colors, Spacing, Radius, Typography, Motion } from '../theme';`. Add two new import lines after the existing imports:
    - `import BreathingCard from '../components/BreathingCard';`
    - `import { FIRST_RUN_CONTENT_MAP } from '../data/firstRunContent';`

    (2) Insert explanation card in the "Step 2" ScrollView. The "Step 2" is the view that renders the value entry form with `s.valueCard`. Insert the following as the FIRST child of that ScrollView's content, before the existing `s.valueCard` View:
    ```
    {selected !== null && FIRST_RUN_CONTENT_MAP[selected.id] !== undefined && (
      <BreathingCard style={s.explanationWrapper}>
        <View style={s.explanationInner}>
          <Text style={s.explanationIcon}>{FIRST_RUN_CONTENT_MAP[selected.id].icon}</Text>
          <Text style={s.explanationHeadline}>{FIRST_RUN_CONTENT_MAP[selected.id].headline}</Text>
          <Text style={s.explanationBody}>{FIRST_RUN_CONTENT_MAP[selected.id].body}</Text>
        </View>
      </BreathingCard>
    )}
    ```
    The `BreathingCard` does not receive `glowColor` or `period` props here — both default correctly (glowColor defaults to `Colors.primaryDark`, period defaults to `Motion.breath`) per the BreathingCard component defaults. Do not override them.

    (3) Add explanation card style keys to StyleSheet `s` at the bottom of the file. Insert after the last existing style key:
    - `explanationWrapper`: `{ marginBottom: Spacing.md }`
    - `explanationInner`: `{ backgroundColor: Colors.bgCard, borderRadius: Radius.lg, borderWidth: 1, borderColor: Colors.borderLight, padding: Spacing.base }`
    - `explanationIcon`: `{ fontSize: 24, marginBottom: Spacing.sm }`
    - `explanationHeadline`: `{ fontSize: Typography.sizes.h3, fontWeight: '600', color: Colors.textPrimary, marginBottom: Spacing.sm }`
    - `explanationBody`: `{ fontSize: Typography.sizes.body, fontWeight: '400', color: Colors.textSecondary, lineHeight: 24 }`

    No `any`. No hardcoded hex values. The card renders only when `selected !== null && FIRST_RUN_CONTENT_MAP[selected.id] !== undefined` — for the 3 supported biomarkers only. For all other biomarkers, the flow is unchanged.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | grep "BiomarkerEntryScreen" | grep -v "warning"; test $? -ne 0 || echo "TS_OK"</automated>
    Also run: `grep -c "FIRST_RUN_CONTENT_MAP" src/screens/BiomarkerEntryScreen.tsx` MUST return ≥ 2 (import + usage in render).
    Also run: `grep -c "import BreathingCard" src/screens/BiomarkerEntryScreen.tsx` MUST return 1.
    Also run: `grep -c "explanationWrapper\|explanationInner" src/screens/BiomarkerEntryScreen.tsx` MUST return ≥ 4 (each key in StyleSheet + JSX).
    Also run: `grep -c ": any" src/screens/BiomarkerEntryScreen.tsx` MUST return 0.
  </verify>
  <acceptance_criteria>
    - `BiomarkerEntryScreen.tsx` imports `FIRST_RUN_CONTENT_MAP` from `'../data/firstRunContent'`.
    - `BiomarkerEntryScreen.tsx` imports `BreathingCard` default from `'../components/BreathingCard'`.
    - A `BreathingCard` explanation card is rendered in the Step 2 view when `selected !== null && FIRST_RUN_CONTENT_MAP[selected.id] !== undefined`.
    - The card renders the icon, headline, and body from `FIRST_RUN_CONTENT_MAP[selected.id]` — not hardcoded strings.
    - The card is NOT rendered for biomarkers not in `FIRST_RUN_CONTENT_MAP` (the condition gates it).
    - StyleSheet `s` contains `explanationWrapper`, `explanationInner`, `explanationIcon`, `explanationHeadline`, `explanationBody` — all values from theme tokens only.
    - The existing Step 2 form elements (`s.valueCard`, TextInput, unit toggle, source selector) are preserved unchanged.
    - `npx tsc --noEmit` reports no new errors.
    - No `any` type. No hardcoded hex values in the new StyleSheet keys.
  </acceptance_criteria>
  <done>
    BiomarkerEntryScreen compiles cleanly and shows the clinical explanation card for fastingglucose, hba1c, and totalcholesterol. For all other biomarkers the screen is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add @vitalspan_first_run_complete to SettingsScreen ALL_STORAGE_KEYS</name>
  <files>src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx lines 1–30 (the ALL_STORAGE_KEYS array at lines 15–22 — the exact array to modify; observe the existing 6 keys and the comment pattern used on other lines)
    - .planning/phases/01-first-run-and-empty-states/01-CONTEXT.md (D-17 — the new key must be added to ALL_STORAGE_KEYS so data reset clears the first-run completion flag)
    - .planning/phases/01-first-run-and-empty-states/01-PATTERNS.md §SettingsScreen.tsx (exact change: add `'@vitalspan_first_run_complete'` as the 7th entry, add trailing comment `// Phase 1: guided first-run completion flag`)
  </read_first>
  <action>
    One targeted change to `src/screens/SettingsScreen.tsx`.

    In the `ALL_STORAGE_KEYS` array (lines 15–22), append `'@vitalspan_first_run_complete'` as the 7th element. Add an inline comment `// Phase 1: guided first-run completion flag` on the same line to document the key's origin. The final array:

    ```
    const ALL_STORAGE_KEYS = [
      '@vitalspan_user_profile',
      '@vitalspan_biomarkers',
      '@vitalspan_protocol',
      '@vitalspan_protocol_today',
      '@vitalspan_health_data',
      '@vitalspan_health_permissions',
      '@vitalspan_first_run_complete',   // Phase 1: guided first-run completion flag
    ];
    ```

    No other lines in SettingsScreen.tsx change. This is a single array element addition. Do not modify ANY other part of the file.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | grep "SettingsScreen" | grep -v "warning"; test $? -ne 0 || echo "TS_OK"</automated>
    Also run: `grep -c "'@vitalspan_first_run_complete'" src/screens/SettingsScreen.tsx` MUST return 1.
    Also run: `grep -v '^[[:space:]]*//' src/screens/SettingsScreen.tsx | grep -c "ALL_STORAGE_KEYS" | head -1` MUST return ≥ 1 (the const declaration is still present).
  </verify>
  <acceptance_criteria>
    - `src/screens/SettingsScreen.tsx` ALL_STORAGE_KEYS array contains `'@vitalspan_first_run_complete'` as the 7th element.
    - The array still contains all 6 original keys — none were removed or reordered.
    - A trailing comment `// Phase 1: guided first-run completion flag` documents the new entry.
    - No other lines in the file were changed.
    - `npx tsc --noEmit` reports no new errors.
  </acceptance_criteria>
  <done>
    SettingsScreen ALL_STORAGE_KEYS contains the first-run completion flag. "Reset all data" in Settings now clears @vitalspan_first_run_complete, allowing the guided flow to re-trigger on the next app launch.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FIRST_RUN_CONTENT_MAP lookup → UI render | An undefined key access would crash. The guard `FIRST_RUN_CONTENT_MAP[selected.id] !== undefined` prevents this. |
| ALL_STORAGE_KEYS → AsyncStorage.multiRemove | SettingsScreen passes this array to AsyncStorage.multiRemove for reset. Missing the first-run key would leave a ghost flag. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | FIRST_RUN_CONTENT_MAP undefined key access | mitigate | Explicit `!== undefined` guard in the render condition prevents a runtime crash if an unexpected biomarkerId is passed. |
| T-03-02 | Repudiation | @vitalspan_first_run_complete not cleared on data reset | mitigate | Adding key to ALL_STORAGE_KEYS ensures it is cleared by the existing Settings "Reset all data" flow. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced in this plan. All imports already present in the codebase. |

</threat_model>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` returns no new errors.
2. Source assertions: every grep gate in each task's `<verify>` returns the expected count.
3. `grep -c "'@vitalspan_first_run_complete'" src/screens/SettingsScreen.tsx` returns exactly 1.
4. `grep -c "FIRST_RUN_CONTENT_MAP" src/screens/BiomarkerEntryScreen.tsx` returns ≥ 2.
</verification>

<success_criteria>
- FIRST-02 (explanation card also in BiomarkerEntry): BiomarkerEntryScreen shows the BreathingCard explanation card for fastingglucose, hba1c, totalcholesterol when those biomarkers are selected. The card content matches FIRST_RUN_CONTENT_MAP exactly.
- FIRST-03 (data reset consistency): @vitalspan_first_run_complete is in ALL_STORAGE_KEYS — "Reset all data" in Settings clears the flag, allowing the guided flow to re-trigger on next launch.
</success_criteria>

<output>
Create `.planning/phases/01-first-run-and-empty-states/01-03-SUMMARY.md` when done.
</output>
