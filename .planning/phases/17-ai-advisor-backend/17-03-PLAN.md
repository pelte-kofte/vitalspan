---
phase: 17-ai-advisor-backend
plan: 03
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - supabase/functions/ai-advisor/index.ts
autonomous: true
requirements:
  - AI-02
  - AI-03

must_haves:
  truths:
    - "A POST request with action:'report' and a valid JWT returns a LongevityReport JSON matching the 5-section schema"
    - "A POST request with action:'chat' and a valid JWT returns a plain string assistant response"
    - "A request with an invalid/missing JWT receives a 401 response"
    - "A user who has sent 5 reports today receives a 429 with the exact message from D-15 on the 6th attempt"
    - "The Anthropic API key is read from Deno.env.get('ANTHROPIC_API_KEY') — never hardcoded"
    - "CORS headers are present on every response so React Native supabase.functions.invoke() succeeds"
    - "The ai_usage counter is incremented before the Anthropic API call, so a failed Anthropic call still consumes quota"
  artifacts:
    - path: "supabase/functions/ai-advisor/index.ts"
      provides: "Deno Edge Function with report + chat dispatch"
      contains: "serve("
  key_links:
    - from: "supabase/functions/ai-advisor/index.ts"
      to: "public.ai_usage table"
      via: "Supabase service_role upsert for rate limit increment before Anthropic call"
      pattern: "ai_usage"
    - from: "supabase/functions/ai-advisor/index.ts"
      to: "Anthropic Claude API"
      via: "fetch to https://api.anthropic.com/v1/messages"
      pattern: "anthropic.com"
---

<objective>
Create the Supabase Edge Function `supabase/functions/ai-advisor/index.ts` that proxies Claude API calls server-side with JWT verification and per-user rate limiting.

Purpose: This is the security boundary that keeps the Anthropic API key off the device, enforces rate limits before every Claude call, and returns structured JSON to the Expo app (AI-02, AI-03).

Output: `supabase/functions/ai-advisor/index.ts` in Deno runtime — deployable via `supabase functions deploy ai-advisor`.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/17-ai-advisor-backend/17-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/supabase/migrations/ai_usage.sql

<interfaces>
<!-- Deno Edge Function runtime — NOT Node.js. Use URL-style imports. -->
<!-- Supabase Deno standard serve pattern: -->
  import { serve } from "https://deno.land/std@0.168.0/http/server.ts";
  import { createClient } from "https://esm.sh/@supabase/supabase-js@2";
  serve(async (req) => { ... });

<!-- Environment variables in Deno Edge Function: -->
  Deno.env.get('SUPABASE_URL')              -- injected automatically by Supabase
  Deno.env.get('SUPABASE_SERVICE_ROLE_KEY') -- injected automatically by Supabase
  Deno.env.get('ANTHROPIC_API_KEY')         -- set manually: supabase secrets set ANTHROPIC_API_KEY=sk-ant-...

<!-- JWT verification pattern: -->
  const authHeader = req.headers.get('Authorization');
  // Extract Bearer token, then use service client to verify:
  const { data: { user }, error } = await serviceClient.auth.getUser(token);
  // If error or user null → return 401

<!-- Rate limit pattern (D-13) — two sequential atomic SQL calls, pre-Anthropic: -->
  Step 1: SELECT the current count row for (user_id, todayUTC). If row exists and count >= limit → return 429.
  Step 2: If under limit → upsert to increment the counter BEFORE calling Anthropic.
         Upsert: { user_id, date: todayUTC, report_count: (currentCount ?? 0) + 1 } with onConflict: 'user_id,date'.
         This is a read-then-write with a tiny race window — acceptable at 5/20 per-day limits where simultaneous
         calls from the same user are extremely unlikely. Comment this tradeoff in the code.
  Step 3: Call Anthropic AFTER the upsert. Failed Anthropic calls consume quota (by design — prevents retry abuse).

<!-- Request body shape (from advisorContext.ts plan): -->
  interface RequestBody {
    action: 'report' | 'chat';
    context?: AdvisorContext;         // present when action:'report'
    messages?: { role: 'user' | 'assistant'; content: string }[];  // present when action:'chat'; full conversation history
    reportSummary?: string;           // present when action:'chat'; injected into system prompt
  }

<!-- Report response shape (D-01 schema contract): -->
  interface LongevityReport {
    scoreSummary: { biologicalAge: number | null; ageBand: string; headline: string; trend: string };
    priorityFindings: Array<{ finding: string; priority: 'high' | 'medium' | 'low' }>;
    biomarkerAnalysis: Array<{ name: string; status: 'Optimal' | 'Suboptimal' | 'Critical'; insight: string }>;
    supplementMedicationReview: Array<{ name: string; type: 'supplement' | 'medication'; assessment: string }>;
    recommendations: Array<{ action: string; category: string; timeframe: string }>;
  }

<!-- Claude models (D-07):
     Report: "claude-sonnet-4-6"
     Chat:   "claude-haiku-4-5-20251001"
     Note: D-07 specifies the rolling alias "claude-haiku-4-5". The versioned form used in API
     calls is "claude-haiku-4-5-20251001" — this is the explicit pinned version of that alias.
     Using the versioned form is intentional and correct for production stability. -->

<!-- Claude API call (direct fetch — no SDK): -->
  POST https://api.anthropic.com/v1/messages
  Headers:
    x-api-key: Deno.env.get('ANTHROPIC_API_KEY')
    anthropic-version: "2023-06-01"
    content-type: "application/json"
  Body:
    { model, max_tokens, system, messages: [{ role: 'user', content }] }

<!-- CORS headers (required for React Native supabase.functions.invoke): -->
  const CORS = {
    'Access-Control-Allow-Origin': '*',
    'Access-Control-Allow-Headers': 'authorization, x-client-info, apikey, content-type',
  };
  // Return CORS on OPTIONS preflight and on every response.

<!-- Rate limit constants: -->
  REPORT_LIMIT = 5   (per UTC day per user)
  CHAT_LIMIT   = 20  (per UTC day per user)

<!-- 429 message (D-15, exact string): -->
  "You've reached your daily limit. Try again tomorrow."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold supabase function directory and write ai-advisor Edge Function</name>
  <files>supabase/functions/ai-advisor/index.ts</files>
  <read_first>
    - /Users/bekircemkusdemir/Downloads/vitalspan/supabase/migrations/ai_usage.sql (table schema — confirm column names report_count, chat_count before referencing them)
    - /Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/17-ai-advisor-backend/17-CONTEXT.md (D-05 through D-16 for all function behavior decisions)
    - /Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/17-ai-advisor-backend/17-02-SUMMARY.md (AdvisorContext type shape — read if exists, else use the interfaces block above)
  </read_first>
  <action>
    Create the directory `supabase/functions/ai-advisor/` (inside the `supabase/` directory created by Plan 1).

    Write `supabase/functions/ai-advisor/index.ts` as a Deno TypeScript file. Key implementation details:

    IMPORTS (URL-style, Deno runtime):
    - `serve` from `"https://deno.land/std@0.168.0/http/server.ts"`
    - `createClient` from `"https://esm.sh/@supabase/supabase-js@2"`

    CONSTANTS:
    - `REPORT_LIMIT = 5`
    - `CHAT_LIMIT = 20`
    - CORS headers object with `Access-Control-Allow-Origin: '*'` and `Access-Control-Allow-Headers: 'authorization, x-client-info, apikey, content-type'`
    - `TODAY_UTC` computed per-request inside the handler: `new Date().toISOString().slice(0, 10)` (D-14)

    SUPABASE CLIENTS:
    - `serviceClient` = `createClient(Deno.env.get('SUPABASE_URL')!, Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!)` — used for JWT verification and ai_usage upsert (service_role bypasses RLS)

    SERVE HANDLER STRUCTURE:
    1. OPTIONS preflight → return 200 with CORS headers immediately (no auth check needed)
    2. Extract `Authorization` header → get Bearer token → call `serviceClient.auth.getUser(token)` → if error or !user → return 401 JSON `{ error: 'Unauthorized' }` with CORS headers
    3. Parse request body as JSON → extract `action`, `context`, `messages`, `reportSummary`
    4. Validate `action` is 'report' or 'chat' → if neither → return 400 `{ error: 'Invalid action' }`
    5. Compute `userId = user.id` and `todayUTC = new Date().toISOString().slice(0, 10)`
    6. Determine which counter field: `action === 'report'` → field `report_count`, limit `REPORT_LIMIT`; `action === 'chat'` → field `chat_count`, limit `CHAT_LIMIT`
    7. RATE LIMIT CHECK (Step 1 — read current count):
       - Execute: `serviceClient.from('ai_usage').select('report_count, chat_count').eq('user_id', userId).eq('date', todayUTC).maybeSingle()`
       - If the relevant counter (report_count or chat_count) in the returned row is >= limit → return 429 JSON `{ error: "You've reached your daily limit. Try again tomorrow." }` with CORS headers. Do not call Anthropic.
    8. INCREMENT COUNTER (Step 2 — upsert BEFORE calling Anthropic, per D-13):
       - Build the upsert payload: for `action === 'report'`, set `report_count` to `(currentRow?.report_count ?? 0) + 1` and `chat_count` to `currentRow?.chat_count ?? 0`. For `action === 'chat'`, set `chat_count` to `(currentRow?.chat_count ?? 0) + 1` and `report_count` to `currentRow?.report_count ?? 0`.
       - Execute: `serviceClient.from('ai_usage').upsert({ user_id: userId, date: todayUTC, report_count: newReportCount, chat_count: newChatCount }, { onConflict: 'user_id,date' })`
       - Add comment: `// Rate limit increment: counter upserted BEFORE Anthropic call per D-13. Failed Anthropic calls consume quota by design — prevents retry abuse. Race window is acceptable at 5/20 per-day limits where simultaneous calls from the same user are extremely unlikely.`
    9. GUARD: Check `ANTHROPIC_API_KEY` via `Deno.env.get('ANTHROPIC_API_KEY')` — if missing or empty → return 500 `{ error: 'Service misconfigured' }` with CORS headers immediately.
    10. CALL ANTHROPIC (after rate check and counter increment):
        - For `action === 'report'`:
          System prompt: "You are a pharmacist-trained longevity advisor reviewing an anonymized patient health snapshot. Return ONLY valid JSON — no markdown fences, no prose, no explanations outside the JSON structure. Evidence grades (A/B/C) are NOT your responsibility — do not include them. The app adds them app-side. The JSON must match this exact schema: { scoreSummary: { biologicalAge: number|null, ageBand: string, headline: string, trend: string }, priorityFindings: Array<{ finding: string, priority: 'high'|'medium'|'low' }>, biomarkerAnalysis: Array<{ name: string, status: 'Optimal'|'Suboptimal'|'Critical', insight: string }>, supplementMedicationReview: Array<{ name: string, type: 'supplement'|'medication', assessment: string }>, recommendations: Array<{ action: string, category: string, timeframe: string }> }"
          User message: stringify the `context` object with clear field labels.
          Max tokens: 2000. Model: `"claude-sonnet-4-6"`.
          Messages array: `[{ role: 'user', content: JSON.stringify(context, null, 2) }]`
        - For `action === 'chat'`:
          System prompt: "You are a pharmacist-trained longevity advisor. The following report was generated for this user:\n\n" + reportSummary + "\n\nAnswer follow-up questions referencing this report."
          Messages array: pass the `messages` array received from the client UNCHANGED. It already contains the full conversation history per D-06 — do NOT append reportSummary to any message. The reportSummary belongs in the system prompt only.
          Max tokens: 500. Model: `"claude-haiku-4-5-20251001"`. (D-07: this is the versioned form of the rolling alias "claude-haiku-4-5" — intentional for production stability.)
        - Fetch: `POST https://api.anthropic.com/v1/messages` with headers `x-api-key: Deno.env.get('ANTHROPIC_API_KEY')`, `anthropic-version: "2023-06-01"`, `content-type: "application/json"`.
        - If Anthropic returns non-2xx → log error message only (no stack trace) → return 502 `{ error: 'AI service error' }` with CORS headers.
    11. PARSE AND RETURN RESPONSE:
        - For `report`: Parse `claudeResponse.content[0].text` as JSON → return 200 with the parsed LongevityReport object and CORS headers. If JSON.parse fails → return 502 `{ error: 'Invalid report structure from AI' }` with CORS headers.
        - For `chat`: Return 200 with `{ message: claudeResponse.content[0].text }` and CORS headers.

    SYSTEM PROMPT FOR REPORT (key phrases to include verbatim):
    - "You are a pharmacist-trained longevity advisor reviewing an anonymized patient health snapshot."
    - "Return ONLY valid JSON — no markdown fences, no prose, no explanations outside the JSON structure."
    - Include the exact LongevityReport JSON schema as a schema string in the system prompt.
    - Include: "Evidence grades (A/B/C) are NOT your responsibility — do not include them. The app adds them app-side."

    ERROR HANDLING: Wrap entire handler in try/catch → return 500 `{ error: 'Internal server error' }` with CORS headers on any unhandled exception.
  </action>
  <verify>
    <automated>grep -c "serve(" /Users/bekircemkusdemir/Downloads/vitalspan/supabase/functions/ai-advisor/index.ts && grep -c "ANTHROPIC_API_KEY" /Users/bekircemkusdemir/Downloads/vitalspan/supabase/functions/ai-advisor/index.ts && grep -c "claude-sonnet-4-6" /Users/bekircemkusdemir/Downloads/vitalspan/supabase/functions/ai-advisor/index.ts && grep -c "claude-haiku-4-5-20251001" /Users/bekircemkusdemir/Downloads/vitalspan/supabase/functions/ai-advisor/index.ts && grep -c "429" /Users/bekircemkusdemir/Downloads/vitalspan/supabase/functions/ai-advisor/index.ts && grep -c "reached your daily limit" /Users/bekircemkusdemir/Downloads/vitalspan/supabase/functions/ai-advisor/index.ts</automated>
  </verify>
  <done>
    `supabase/functions/ai-advisor/index.ts` exists with: `serve()` handler, OPTIONS preflight CORS, JWT verification via `serviceClient.auth.getUser()`, rate limit SELECT-then-upsert with the upsert executed BEFORE the Anthropic call (429 returned on limit exceeded with exact D-15 message), Anthropic fetch using `Deno.env.get('ANTHROPIC_API_KEY')`, `claude-sonnet-4-6` for report, `claude-haiku-4-5-20251001` for chat (versioned form of D-07 "claude-haiku-4-5" alias), CORS headers on all responses, report JSON parsed and returned, chat text returned as `{ message }`, full messages array passed unchanged for chat with reportSummary in system prompt only.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Expo app → Edge Function | JWT in Authorization header; function must verify before any processing |
| Edge Function → Anthropic API | API key secret; must never appear in client bundle |
| Edge Function → ai_usage table | Service role write; function is sole authority on rate counts |
| Context payload → Claude | Anonymized context; no PII should reach Anthropic (enforced by advisorContext.ts) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08 | Spoofing | JWT verification | mitigate | `serviceClient.auth.getUser(token)` validates JWT signature via Supabase; invalid tokens → immediate 401 |
| T-17-09 | Tampering | Request body | mitigate | action field validated to 'report'|'chat' before processing; context is anonymized by client |
| T-17-10 | Repudiation | Rate limit bypass | mitigate | Rate check happens before Anthropic call; counter incremented before call per D-13; race window acceptable for 5/20 per-day limit |
| T-17-11 | Information Disclosure | Anthropic API key | mitigate | Key read-only via `Deno.env.get('ANTHROPIC_API_KEY')` — never in source code; set via `supabase secrets set`; never logged |
| T-17-12 | Denial of Service | Anthropic API spend | mitigate | Rate limits (5/20 per day per user) are primary spend guard; `max_tokens` cap (2000 report, 500 chat) bounds per-call cost |
| T-17-13 | Elevation of Privilege | Anonymous user access | mitigate | JWT verification blocks anonymous session IDs — per D-16, anonymous users never reach the Edge Function (blocked by premium gate in Phase 16); if they did, getUser() would return an anon user which is still a valid user — accept this risk as premium gate handles it |
| T-17-14 | Information Disclosure | Error messages | mitigate | Error responses return generic strings only — no stack traces, no Supabase internals exposed |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | Deno uses URL imports, no package manager; no install step required |
</threat_model>

<verification>
After implementation:
1. `grep -n "sk-ant\|ANTHROPIC_API_KEY.*=.*sk" supabase/functions/ai-advisor/index.ts` → zero matches (no hardcoded key)
2. `grep -c "Access-Control-Allow-Origin" supabase/functions/ai-advisor/index.ts` → at least 1 (CORS present)
3. `grep -c "getUser" supabase/functions/ai-advisor/index.ts` → at least 1 (JWT verification)
4. `grep -c "reached your daily limit" supabase/functions/ai-advisor/index.ts` → exactly 1 (D-15 message)
5. `grep -c "claude-sonnet-4-6\|claude-haiku-4-5-20251001" supabase/functions/ai-advisor/index.ts` → exactly 2 (both models)
6. Confirm increment-before-Anthropic ordering: `grep -n "upsert\|anthropic.com" supabase/functions/ai-advisor/index.ts` — upsert line number must be lower than the anthropic.com fetch line number
7. Deploy test (requires Supabase CLI): `supabase functions deploy ai-advisor --no-verify-jwt` (CI only — human deploys to production)
</verification>

<success_criteria>
`supabase/functions/ai-advisor/index.ts` implements: OPTIONS CORS preflight, JWT verification (401 on failure), rate limit SELECT-then-upsert with increment executed BEFORE the Anthropic call (429 + D-15 message when count at limit), Anthropic fetch via direct HTTP (no SDK), two action paths (report with claude-sonnet-4-6 at max_tokens:2000, chat with claude-haiku-4-5-20251001 at max_tokens:500), chat path passes full messages array unchanged with reportSummary injected into system prompt only, parsed report JSON returned, CORS headers on every response. Anthropic API key never hardcoded.
</success_criteria>

<output>
Create `.planning/phases/17-ai-advisor-backend/17-03-SUMMARY.md` when done.
</output>
