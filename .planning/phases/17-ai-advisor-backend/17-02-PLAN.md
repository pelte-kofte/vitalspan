---
phase: 17-ai-advisor-backend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/advisorContext.ts
autonomous: true
requirements:
  - AI-01

must_haves:
  truths:
    - "assembleAdvisorContext() reads all four AsyncStorage keys in parallel and returns a typed AdvisorContext object"
    - "The payload contains bucketed age (5-year band string), biological age (from computePhenoAge()), biomarker status categories (Optimal/Suboptimal/Critical), supplement names, medication names, and HealthKit aggregates"
    - "The payload contains zero PII: no user name, no exact birthdate, no raw lab values with timestamps, no Supabase user ID"
    - "When isDemoMode:true in health data, HealthKit fields are omitted and healthDataAvailable:false is present"
    - "When computePhenoAge() returns biologicalAge:null (insufficient data), the context includes biologicalAge:null without crashing"
  artifacts:
    - path: "src/lib/advisorContext.ts"
      provides: "AdvisorContext type + assembleAdvisorContext() function"
      exports:
        - "AdvisorContext"
        - "BiomarkerStatus"
        - "assembleAdvisorContext"
  key_links:
    - from: "src/lib/advisorContext.ts"
      to: "src/lib/phenoAge.ts"
      via: "computePhenoAge() call"
      pattern: "computePhenoAge"
    - from: "src/lib/advisorContext.ts"
      to: "src/data/biomarkers.ts"
      via: "BIOMARKERS range lookup for Optimal/Suboptimal/Critical bucketing"
      pattern: "BIOMARKERS"
    - from: "src/lib/advisorContext.ts"
      to: "@vitalspan_biomarkers AsyncStorage key"
      via: "AsyncStorage.getItem('@vitalspan_biomarkers')"
      pattern: "vitalspan_biomarkers"
---

<objective>
Create `src/lib/advisorContext.ts` — the anonymized health context assembler.

Purpose: Converts raw AsyncStorage data into a zero-PII payload that is safe to send to the Edge Function. All bucketing, status categorization, and exclusion logic lives here. This is the privacy boundary between raw device data and the Claude API (D-09).

Output: `src/lib/advisorContext.ts` exporting `AdvisorContext` type and `assembleAdvisorContext(): Promise<AdvisorContext>`.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/17-ai-advisor-backend/17-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/src/lib/phenoAge.ts
@/Users/bekircemkusdemir/Downloads/vitalspan/src/data/biomarkers.ts

<interfaces>
<!-- Inputs: AsyncStorage data shapes (from CLAUDE.md and 17-CONTEXT.md) -->

AsyncStorage key: @vitalspan_user_profile
Type (UserProfile — read from CLAUDE.md project instructions):
  age: number          -- exact age in years; bucket to 5-year band for context
  sex: string
  goal: string
  conditions: string[]
  medications: string[]  -- medication names; include names only (no doses)

AsyncStorage key: @vitalspan_biomarkers
Type: StoredEntry[] where StoredEntry has { id, type, value, unit, date }
  -- Take latest entry per `type`. Map `value` against BIOMARKERS[i].optMin/optMax
  -- to derive Optimal/Suboptimal/Critical (D-10)

AsyncStorage key: @vitalspan_protocol
Type (ProtocolState from CLAUDE.md):
  addedSupplements: string[]      -- supplement names from recommended list
  customSupplements: CustomSupplement[]  -- user-added; include .name only
  taken: string[]
  takenDate: string

AsyncStorage key: @vitalspan_health_data
Type (HealthData from src/lib/healthkit.ts):
  hrv: number | null
  sleepScore: number | null
  recovery: number | null
  isDemoMode: boolean
  -- If isDemoMode:true → exclude HRV/sleep/recovery, set healthDataAvailable:false (D-12)

<!-- From phenoAge.ts -->
computePhenoAge(inputs: PhenoAgeInputs): PhenoAgeResult
  returns { biologicalAge: number | null, confidence, missingCount, ... }
  -- PhenoAgeInputs requires: albumin, creatinine, glucose, crp, lymphocytePct, mcv, rdw, alkalinePhosphatase, wbc, age
  -- Map StoredEntry[] by type ID to PhenoAgeInputs using PHENO_AGE_BIOMARKER_MAP

<!-- PHENO_AGE_BIOMARKER_MAP from phenoAge.ts -->
PHENO_AGE_BIOMARKER_MAP: Record<string, keyof Omit<PhenoAgeInputs, 'age'>>
  albumin → 'albumin', creatinine → 'creatinine', fastingglucose → 'glucose',
  hscrp → 'crp', lymphocytepct → 'lymphocytePct', mcv → 'mcv', rdw → 'rdw',
  alp → 'alkalinePhosphatase', wbc → 'wbc'

<!-- From biomarkers.ts -->
BIOMARKERS: Biomarker[]  -- each has { id, name, optMin, optMax }
Biomarker status bucketing logic (D-10):
  value >= optMin && value <= optMax  → 'Optimal'
  value < optMin || (value > optMax && value <= optMax * 1.4)  → 'Suboptimal'
  otherwise  → 'Critical'
  -- Note: for biomarkers where lower is better (ApoB, hsCRP, HbA1c), Suboptimal/Critical
     boundary is above optMax. For biomarkers where the range is one-sided (e.g. optMin=0),
     apply: value <= optMax → Optimal, value <= optMax * 1.5 → Suboptimal, else Critical.
  -- Keep the bucketing logic simple and consistent: use the existing optMin/optMax as the
     Optimal band. Anything outside by less than 40% above optMax (or below optMin) is
     Suboptimal. Greater deviation is Critical.

<!-- Output type contract (for Plan 3 and Plan 4 to depend on) -->
export type BiomarkerStatus = 'Optimal' | 'Suboptimal' | 'Critical';

export interface AdvisorContext {
  ageBand: string;                   // e.g. "35–39" (D-11)
  biologicalAge: number | null;       // from computePhenoAge() (D-03)
  sex: string;
  goal: string;
  medications: string[];             // names only (D-09)
  supplements: string[];             // names only (D-09)
  biomarkers: Array<{
    name: string;
    status: BiomarkerStatus;
  }>;
  healthDataAvailable: boolean;       // false when isDemoMode:true (D-12)
  hrv?: number;                      // omit if healthDataAvailable:false
  sleepScore?: number;               // omit if healthDataAvailable:false
  recovery?: number;                 // omit if healthDataAvailable:false
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create advisorContext.ts with AdvisorContext type and assembleAdvisorContext()</name>
  <files>src/lib/advisorContext.ts</files>
  <read_first>
    - /Users/bekircemkusdemir/Downloads/vitalspan/src/lib/phenoAge.ts (computePhenoAge signature, PHENO_AGE_BIOMARKER_MAP, PhenoAgeInputs type)
    - /Users/bekircemkusdemir/Downloads/vitalspan/src/data/biomarkers.ts (BIOMARKERS array shape — optMin, optMax fields)
    - /Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/17-ai-advisor-backend/17-CONTEXT.md (D-08 through D-12 for context scope decisions)
    - /Users/bekircemkusdemir/Downloads/vitalspan/src/lib/supabase.ts (never-throws pattern reference)
  </read_first>
  <behavior>
    - Test 1: assembleAdvisorContext() returns ageBand "35–39" when userProfile.age = 37
    - Test 2: assembleAdvisorContext() returns ageBand "40–44" when userProfile.age = 42
    - Test 3: healthDataAvailable is false and hrv/sleepScore/recovery are absent when isDemoMode:true
    - Test 4: healthDataAvailable is true and hrv/sleepScore/recovery are present when isDemoMode:false
    - Test 5: biomarker with value within optMin..optMax maps to 'Optimal'
    - Test 6: biomarker with value moderately above optMax maps to 'Suboptimal'
    - Test 7: biomarker with value far above optMax maps to 'Critical'
    - Test 8: medications array contains names only, no doses
    - Test 9: supplements array merges addedSupplements + customSupplements[].name, deduped
    - Test 10: assembleAdvisorContext() never throws — returns best-effort object when AsyncStorage is empty
  </behavior>
  <action>
    Create `src/lib/advisorContext.ts`. This is a new file — no prior content to preserve.

    Exports (all TypeScript strict, no `any`):
    1. `export type BiomarkerStatus = 'Optimal' | 'Suboptimal' | 'Critical';`
    2. `export interface AdvisorContext` (shape per the interfaces block above)
    3. `export async function assembleAdvisorContext(): Promise<AdvisorContext>` — the main function

    Implementation rules:

    ASYNC STORAGE READS: Use `Promise.all([...])` to read all four keys in parallel (established pattern from existing hooks — never sequential). Each read wrapped individually in try/catch returning null on failure. Null result → treat as empty/default data.

    AGE BUCKETING (D-11): Given `age: number`, compute `lowerBound = Math.floor(age / 5) * 5`, `upperBound = lowerBound + 4`. Return `"${lowerBound}–${upperBound}"`. Example: age 37 → "35–39". Never include the exact age integer anywhere in the output object.

    BIOMARKER STATUS (D-10): For the latest entry per biomarker type, look up the corresponding `BIOMARKERS` entry by id. Compute status:
    - `value >= optMin && value <= optMax` → 'Optimal'
    - Otherwise if the deviation is less than 40% above optMax OR less than 40% below optMin → 'Suboptimal'
    - Otherwise → 'Critical'
    For biomarkers where `optMin === 0` (lower-bound only), ignore the optMin check and use: `value <= optMax` → Optimal, `value <= optMax * 1.5` → Suboptimal, else Critical.
    IMPORTANT: include only the biomarker `name` and `status` in the output — NOT the raw numeric value (D-09).

    LATEST ENTRY PER TYPE: StoredEntry[] contains historical entries. Reduce to a Map<type, StoredEntry> keeping only the entry with the latest `date` string per type. Use `localeCompare` or direct string comparison (ISO dates sort lexicographically).

    PHENO AGE: Build `PhenoAgeInputs` from latest StoredEntry map using PHENO_AGE_BIOMARKER_MAP (key = entry.type, value = PhenoAgeInputs key). Include `age: userProfile.age`. Call `computePhenoAge(inputs)`. Extract `biologicalAge` (number | null) from the result.

    SUPPLEMENTS: Merge `protocolState.addedSupplements` (string[]) with `protocolState.customSupplements.map(s => s.name)`. Deduplicate with Set. Return as string[].

    MEDICATIONS: `userProfile.medications` — already an array of name strings. Pass through directly. No doses.

    HEALTH DATA (D-12): If `healthData.isDemoMode === true` → set `healthDataAvailable: false`, omit `hrv`, `sleepScore`, `recovery` from the returned object. If demo mode is false and data is present → include them. Use optional chaining throughout; null values are omitted.

    ERROR RESILIENCE: The function must never throw. Wrap the entire body in try/catch; return a minimal valid AdvisorContext on error (empty arrays, null biologicalAge, healthDataAvailable:false).

    No imports of `@anthropic-ai/sdk` anywhere in this file. No Supabase imports in this file — it only reads AsyncStorage and calls local lib functions.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | grep -i "advisorContext" || echo "tsc: no advisorContext errors"</automated>
  </verify>
  <done>
    `src/lib/advisorContext.ts` exists and exports `BiomarkerStatus`, `AdvisorContext`, and `assembleAdvisorContext`. `tsc --noEmit` reports zero errors in this file. The function reads all four AsyncStorage keys in parallel, returns bucketed age (never exact), maps biomarker statuses without raw values, and excludes HealthKit fields when isDemoMode:true.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage → advisorContext.ts | Device-local data read into context; strip all PII before it leaves this function |
| advisorContext.ts → Edge Function | Payload sent over network; must contain zero identifiable data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Information Disclosure | assembleAdvisorContext() | mitigate | Strict PII exclusion per D-09: exact age → 5-year band, raw values → status category, no user name, no Supabase UID; userProfile.conditions array excluded (not in D-08 scope) |
| T-17-06 | Information Disclosure | isDemoMode bypass | mitigate | Explicit isDemoMode check (D-12); demo HealthKit values are synthetic and excluded to avoid misleading Claude |
| T-17-07 | Tampering | AsyncStorage manipulation | accept | Attacker with device access could alter local data; biological age is client-computed from local values — acceptable for this trust model; no financial or auth data in scope |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
After implementing:
1. `npx tsc --noEmit` exits with zero errors referencing `advisorContext.ts`
2. `grep -n "biologicalAge\|ageBand\|BiomarkerStatus\|AdvisorContext\|assembleAdvisorContext" src/lib/advisorContext.ts` confirms all exports present
3. `grep -n "user\.name\|birthdate\|user_id\|exact.*age\|rawValue" src/lib/advisorContext.ts` returns zero matches (PII check)
4. `grep -n "@anthropic-ai" src/lib/advisorContext.ts` returns zero matches
5. `grep -n "Promise\.all" src/lib/advisorContext.ts` returns at least one match (parallel reads enforced)
</verification>

<success_criteria>
`src/lib/advisorContext.ts` compiles under strict TypeScript with no errors. `assembleAdvisorContext()` returns an `AdvisorContext` object where: (1) `ageBand` is a 5-year band string, (2) `biologicalAge` is a number or null from `computePhenoAge()`, (3) `biomarkers[]` contains name+status with no raw values, (4) `supplements[]` merges both supplement sources, (5) HealthKit fields absent when isDemoMode:true. Zero PII in the payload.
</success_criteria>

<output>
Create `.planning/phases/17-ai-advisor-backend/17-02-SUMMARY.md` when done.
</output>
