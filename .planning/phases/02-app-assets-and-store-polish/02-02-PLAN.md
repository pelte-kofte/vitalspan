---
phase: 02-app-assets-and-store-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/AboutScreen.tsx
autonomous: true
requirements:
  - STORE-01
  - STORE-02
  - STORE-03
  - STORE-04

must_haves:
  truths:
    - "The VERSION constant reads from Constants.expoConfig?.version with '—' fallback — not the hardcoded string '0.1.0'"
    - "The 'About the builder' section shows 'Dr. Bekircem Kusdemir, PharmD' as the name and 'PharmD · Clinical Pharmacist' as credential subtitle"
    - "The 'Why pharmacist-built matters' section is a plain View (not TouchableOpacity) and its three bullet points are always visible without any tap"
    - "A new 'Legal' section card appears after the medical disclaimer card showing disclaimer version, acceptance date from AsyncStorage, and app version"
    - "The section order matches the spec: credential → why → mission → longevity ranges → evidence → phenoage → citations → disclaimer → legal → credits"
  artifacts:
    - path: "src/screens/AboutScreen.tsx"
      provides: "Updated About screen with dynamic version, expanded credentials, always-visible mission section, and new Legal card"
      contains: "Constants.expoConfig"
      exports: ["default"]
  key_links:
    - from: "src/screens/AboutScreen.tsx"
      to: "@vitalspan_disclaimer_accepted"
      via: "AsyncStorage.getItem in useEffect"
      pattern: "AsyncStorage\\.getItem.*vitalspan_disclaimer_accepted"
    - from: "src/screens/AboutScreen.tsx"
      to: "expo-constants"
      via: "Constants.expoConfig?.version"
      pattern: "Constants\\.expoConfig"
---

<objective>
Rewrite the AboutScreen with four targeted changes: (1) replace hardcoded VERSION with Constants.expoConfig?.version, (2) expand the founder section into a full pharmacist credential card with name and practice focus, (3) remove the expand toggle from "Why pharmacist-built matters" so it is always visible, (4) add a new "Legal" card that reads disclaimer acceptance date from AsyncStorage.

Purpose: Strengthen pharmacist credibility signals so the About screen reads as a professional clinical product rather than a generic wellness app.

Output: Updated src/screens/AboutScreen.tsx — one file changed, all changes in one plan since they all touch the same file.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/02-app-assets-and-store-polish/02-UI-SPEC.md

<interfaces>
<!-- From src/screens/AboutScreen.tsx (current state — executor must read this file before any edit): -->

<!-- VERSION constant (line 8) — current: -->
<!--   const VERSION = '0.1.0'; -->
<!-- Replace with: -->
<!--   import Constants from 'expo-constants'; -->
<!--   const VERSION = Constants.expoConfig?.version ?? '—'; -->

<!-- Founder section (lines 62–76) — current structure: -->
<!--   <View style={s.section}>                          // plain View (good) -->
<!--     <View style={s.founderRow}>                     // avatar + name row -->
<!--       <View style={s.founderAvatar}> -->
<!--         <Text style={s.founderAvatarTxt}>Rx</Text>  // keep Rx avatar -->
<!--       </View> -->
<!--       <View style={{ flex: 1 }}> -->
<!--         <Text style={s.founderName}>Founded by a Clinical Pharmacist</Text>  // REPLACE -->
<!--         <Text style={s.founderCred}>PharmD · Longevity Medicine · ...</Text> // REPLACE -->
<!--       </View> -->
<!--     </View> -->
<!--     <Text style={s.bodyTxt}>Every biomarker range...</Text>  // keep this text UNCHANGED -->
<!--   </View> -->

<!-- "Why pharmacist-built matters" section (lines 79–107) — current: TouchableOpacity with whyExpanded state -->
<!-- Change to: plain View, content always visible, remove toggle state -->

<!-- After disclaimer section (lines 189–197), before credits (lines 199–202): -->
<!-- INSERT new Legal card here -->

<!-- Existing StyleSheet styles to KEEP (do not remove): -->
<!--   s.section, s.sectionTitle, s.bodyTxt, s.founderRow, s.founderAvatar, s.founderAvatarTxt -->
<!--   s.founderName, s.founderCred, s.whyPoint, s.whyIcon, s.whyTxt, s.citationBlock -->
<!--   s.expandHeader (still used by Sources & Citations) -->
<!--   s.expandArrow (still used by Sources & Citations) -->

<!-- New StyleSheet styles needed: -->
<!--   founderPracticeFocus: Typography.sizes.xs, weight 400, Colors.textSecondary, marginTop: Spacing.sm -->
<!--   legalCard: backgroundColor Colors.bgSecondary, borderRadius Radius.lg, borderWidth 0.5, -->
<!--              borderColor Colors.border, padding Spacing.base, marginHorizontal Spacing.base, -->
<!--              marginBottom Spacing.sm, borderLeftWidth 2, borderLeftColor Colors.primaryBorder -->
<!--   legalTitle: Typography.sizes.base, fontWeight '600', Colors.textPrimary, marginBottom Spacing.sm -->
<!--   legalDisclaimerLine: Typography.sizes.xs, fontWeight '600', Colors.textSecondary -->
<!--   legalDateLine: Typography.sizes.xs, fontWeight '400', Colors.textMuted, marginTop Spacing.xs -->
<!--   legalVersionLine: Typography.sizes.xs, fontWeight '400', Colors.textMuted, marginTop Spacing.sm -->

<!-- From src/components/MedicalDisclaimer.tsx (reference only — do NOT import from here): -->
<!--   DISCLAIMER_KEY = '@vitalspan_disclaimer_accepted' (string literal — NOT imported, use directly) -->
<!--   JSON format stored: { version: string, acceptedAt: string } where acceptedAt is ISO 8601 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dynamic version, expanded credential section, and always-visible "Why" section</name>
  <files>src/screens/AboutScreen.tsx</files>
  <read_first>
    - /Users/bekircemkusdemir/Downloads/vitalspan/src/screens/AboutScreen.tsx (full file — executor must understand all current lines before editing)
    - /Users/bekircemkusdemir/Downloads/vitalspan/src/components/MedicalDisclaimer.tsx (lines 8–11 and 53–58 only — confirms AsyncStorage key format and stored JSON shape; do NOT import from this file)
    - /Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/02-app-assets-and-store-polish/02-UI-SPEC.md (Change 1 lines 200–214, Change 2 lines 219–258, Change 3 lines 262–290)
  </read_first>
  <action>
    Make three changes to `src/screens/AboutScreen.tsx`:

    CHANGE 1 — STORE-04: Dynamic version.
    Add `import Constants from 'expo-constants'` to the import block at the top. Replace `const VERSION = '0.1.0'` with `const VERSION = Constants.expoConfig?.version ?? '—'`. The two JSX references (`Version {VERSION}` in heroVersion Text and `v{VERSION}` in creditsVersion Text) remain identical — no JSX change.

    CHANGE 2 — STORE-01: Expanded "About the builder" credential section.
    The outer `<View style={s.section}>` wrapper is already correct (plain View, not TouchableOpacity). Keep it.

    Inside `founderRow`, keep the avatar (`founderAvatar` + `founderAvatarTxt` with "Rx") unchanged.

    In the `<View style={{ flex: 1 }}>` next to the avatar:
    - Replace the `founderName` Text content from `"Founded by a Clinical Pharmacist"` to `"Dr. Bekircem Kusdemir, PharmD"`.
    - Replace the `founderCred` Text content from `"PharmD · Longevity Medicine · Drug–Supplement Interactions"` to `"PharmD · Clinical Pharmacist"`. Keep `style={s.founderCred}` unchanged (marginTop: 3 is the approved optical exception — do not change).

    After the `founderRow` View (before the existing `bodyTxt`), add a new Text node using a new `founderPracticeFocus` style: content is `"Longevity medicine, metabolic health optimization, and drug–supplement interaction safety."`.

    The existing `bodyTxt` Text ("Every biomarker range...") stays immediately after the practice focus line with its current content and style unchanged. Add a new style `founderPracticeFocus` to the StyleSheet: `fontSize: Typography.sizes.xs, fontWeight: '400', color: Colors.textSecondary, marginTop: Spacing.sm`.

    CHANGE 3 — STORE-02: Remove expand toggle from "Why pharmacist-built matters".
    Remove `const [whyExpanded, setWhyExpanded] = useState(false)` from the useState declarations.
    Replace the `<TouchableOpacity style={s.section} onPress={() => setWhyExpanded(e => !e)} activeOpacity={0.8}>` wrapper with `<View style={s.section}>`.
    Replace the closing `</TouchableOpacity>` with `</View>`.
    Remove the `<View style={s.expandHeader}>` row that contains the "Why pharmacist-built matters" `sectionTitle` Text and the `expandArrow`. Instead render the `sectionTitle` Text directly as the first child of the section View, matching the pattern used by "Our mission" and other static sections.
    Remove the `{whyExpanded && (...)}` conditional wrapper — render the intro paragraph Text and all three `whyPoint` Views unconditionally.
    Do NOT remove `s.expandHeader` or `s.expandArrow` from the StyleSheet — they are still used by the "Sources & Citations" section at lines 167–186.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "VERSION = '0.1.0'" src/screens/AboutScreen.tsx` returns 0
    - `grep -c "Constants.expoConfig" src/screens/AboutScreen.tsx` returns 1
    - `grep -c "import Constants from 'expo-constants'" src/screens/AboutScreen.tsx` returns 1
    - `grep -c "Dr. Bekircem Kusdemir, PharmD" src/screens/AboutScreen.tsx` returns 1
    - `grep -c "PharmD · Clinical Pharmacist" src/screens/AboutScreen.tsx` returns 1
    - `grep -c "Longevity medicine, metabolic health optimization" src/screens/AboutScreen.tsx` returns 1
    - `grep -c "whyExpanded" src/screens/AboutScreen.tsx` returns 0
    - `grep -c "setWhyExpanded" src/screens/AboutScreen.tsx` returns 0
    - `grep -c "TouchableOpacity" src/screens/AboutScreen.tsx` returns 2 (Done button + Sources & Citations — not 3)
    - `grep -c "expandArrow" src/screens/AboutScreen.tsx` is greater than 0 (style still present, used by citations)
    - `npx tsc --noEmit` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>VERSION is dynamic from expo-constants. Credential section shows Dr. Bekircem Kusdemir PharmD with practice focus. "Why pharmacist-built matters" is always visible with no tap interaction.</done>
</task>

<task type="auto">
  <name>Task 2: Add Legal section card with AsyncStorage disclaimer date</name>
  <files>src/screens/AboutScreen.tsx</files>
  <read_first>
    - /Users/bekircemkusdemir/Downloads/vitalspan/src/screens/AboutScreen.tsx (after Task 1 changes — executor reads the post-Task-1 state of the file)
    - /Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/02-app-assets-and-store-polish/02-UI-SPEC.md (Change 5 lines 309–366 — full Legal section spec including AsyncStorage shape, display format, copy contract, and layout tokens)
  </read_first>
  <action>
    Add the "Legal" section card to `src/screens/AboutScreen.tsx` — STORE-03.

    IMPORTS: Add `AsyncStorage from '@react-native-async-storage/async-storage'` to the import block if not already present. `useEffect` is already imported from React.

    STATE: Add a new state variable inside the component function:
    `const [disclaimerInfo, setDisclaimerInfo] = useState<{ version: string; acceptedAt: string } | null>(null);`

    EFFECT: Add a new `useEffect` that runs once on mount: calls `AsyncStorage.getItem('@vitalspan_disclaimer_accepted')`, and if the raw value is truthy, calls `JSON.parse(raw)` and sets `disclaimerInfo`. The `.catch(() => {})` swallows errors silently — do not surface any error UI.

    JSX PLACEMENT: Insert the new Legal card View between the closing of the `s.disclaimer` View (after `</View>` that closes the medical disclaimer) and the opening of the `s.credits` View. The section order per spec: medical disclaimer card → Legal card → credits.

    LEGAL CARD JSX structure:
    - Outer View with new `legalCard` style
    - Text with `legalTitle` style: content `"Legal"`
    - Text with `legalDisclaimerLine` style: content is `disclaimerInfo ? \`Medical disclaimer v${disclaimerInfo.version}\` : "Medical disclaimer v1.0"`
    - Text with `legalDateLine` style: content is `disclaimerInfo ? \`Accepted: ${new Date(disclaimerInfo.acceptedAt).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}\` : "Not yet accepted"`
    - Text with `legalVersionLine` style: content is `\`App version: ${VERSION}\``

    STYLESHEET additions — add these new styles to the `s` StyleSheet.create block:
    - `legalCard`: backgroundColor Colors.bgSecondary, borderRadius Radius.lg, borderWidth 0.5, borderColor Colors.border, padding Spacing.base, marginHorizontal Spacing.base, marginBottom Spacing.sm, borderLeftWidth 2, borderLeftColor Colors.primaryBorder
    - `legalTitle`: fontSize Typography.sizes.base, fontWeight '600', color Colors.textPrimary, marginBottom Spacing.sm
    - `legalDisclaimerLine`: fontSize Typography.sizes.xs, fontWeight '600', color Colors.textSecondary
    - `legalDateLine`: fontSize Typography.sizes.xs, fontWeight '400' as const, color Colors.textMuted, marginTop Spacing.xs
    - `legalVersionLine`: fontSize Typography.sizes.xs, fontWeight '400' as const, color Colors.textMuted, marginTop Spacing.sm

    IMPORTANT: The string `'@vitalspan_disclaimer_accepted'` is used as a literal in this file — do NOT create an import from MedicalDisclaimer.tsx to get the DISCLAIMER_KEY constant. That constant is not exported from that file.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "vitalspan_disclaimer_accepted" src/screens/AboutScreen.tsx` returns 1 (the string literal in AsyncStorage.getItem)
    - `grep -c "disclaimerInfo" src/screens/AboutScreen.tsx` is at least 4 (state declaration, setDisclaimerInfo, two ternary uses in JSX)
    - `grep -c "Legal" src/screens/AboutScreen.tsx` is at least 2 (the "Legal" section title in JSX + legalCard/legalTitle style keys)
    - `grep -c "toLocaleDateString" src/screens/AboutScreen.tsx` returns 1
    - `grep -c "App version:" src/screens/AboutScreen.tsx` returns 1
    - `grep -c "Medical disclaimer v" src/screens/AboutScreen.tsx` is at least 1
    - `grep -c "Not yet accepted" src/screens/AboutScreen.tsx` returns 1
    - `grep -c "legalCard" src/screens/AboutScreen.tsx` is at least 2 (JSX use + StyleSheet definition)
    - `grep -c "import.*MedicalDisclaimer" src/screens/AboutScreen.tsx` returns 0 (no cross-import)
    - `npx tsc --noEmit` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>Legal card renders after the medical disclaimer. When disclaimer has been accepted, it shows version and formatted date. When not accepted, it shows "Not yet accepted". App version always shown from VERSION constant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage → AboutScreen UI | Disclaimer data read from device storage and rendered as text — untrusted if tampered, but display-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Information Disclosure | AsyncStorage read of disclaimer data | accept | Display-only data with no PII; JSON.parse failure caught silently; data is written by this app only |
| T-02-02-02 | Tampering | JSON.parse of disclaimer storage value | mitigate | .catch(() => {}) prevents crash; if parse fails, null state shows "Not yet accepted" gracefully |
</threat_model>

<verification>
After both tasks complete:

1. `npx tsc --noEmit` exits 0 — no TypeScript errors in AboutScreen.tsx
2. `grep -c "VERSION = '0.1.0'" src/screens/AboutScreen.tsx` returns 0
3. `grep -c "whyExpanded" src/screens/AboutScreen.tsx` returns 0
4. `grep -c "Dr. Bekircem Kusdemir, PharmD" src/screens/AboutScreen.tsx` returns 1
5. `grep -c "vitalspan_disclaimer_accepted" src/screens/AboutScreen.tsx` returns 1
6. `grep -c "Legal" src/screens/AboutScreen.tsx` is at least 2
</verification>

<success_criteria>
- VERSION reads from Constants.expoConfig?.version with '—' fallback (STORE-04)
- "About the builder" section shows Dr. Bekircem Kusdemir PharmD name, PharmD · Clinical Pharmacist subtitle, practice focus line, and existing body paragraph (STORE-01)
- "Why pharmacist-built matters" section is plain View with content always rendered (STORE-02)
- Legal card appears after medical disclaimer, reads AsyncStorage key @vitalspan_disclaimer_accepted, shows formatted date or "Not yet accepted", always shows app version (STORE-03)
- TypeScript strict mode: no errors (npx tsc --noEmit exits 0)
- No hardcoded hex values or spacing numbers added — all new styles use Colors.* and Spacing.* tokens
</success_criteria>

<output>
Create `.planning/phases/02-app-assets-and-store-polish/02-02-SUMMARY.md` when done
</output>
