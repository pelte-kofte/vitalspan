---
phase: 02-app-assets-and-store-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate-icon.js
  - scripts/generate-splash.js
  - app.json
  - assets/icon.png
  - assets/splash.png
autonomous: true
requirements:
  - ASSET-01
  - ASSET-02
user_setup: []

must_haves:
  truths:
    - "Running `node scripts/generate-icon.js` produces assets/icon.png at exactly 1024×1024 pixels"
    - "Running `node scripts/generate-splash.js` produces assets/splash.png at exactly 1284×2778 pixels"
    - "app.json splash object contains image, resizeMode, and backgroundColor fields matching the spec"
    - "The icon PNG has a solid #2D6A4F background fill — no Expo default placeholder"
    - "The splash PNG has a #EDE8DC background with centered 'Vitalspan' text and tagline"
  artifacts:
    - path: "scripts/generate-icon.js"
      provides: "Node.js script to generate 1024×1024 icon PNG"
    - path: "scripts/generate-splash.js"
      provides: "Node.js script to generate 1284×2778 splash PNG"
    - path: "assets/icon.png"
      provides: "Custom Vitalspan app icon — 1024×1024 PNG"
    - path: "assets/splash.png"
      provides: "Branded splash screen — 1284×2778 PNG"
    - path: "app.json"
      provides: "Expo config with updated splash object"
  key_links:
    - from: "scripts/generate-icon.js"
      to: "assets/icon.png"
      via: "fs.writeFileSync / canvas toBuffer"
      pattern: "writeFileSync.*icon\\.png"
    - from: "scripts/generate-splash.js"
      to: "assets/splash.png"
      via: "fs.writeFileSync / canvas toBuffer"
      pattern: "writeFileSync.*splash\\.png"
    - from: "app.json"
      to: "assets/splash.png"
      via: "splash.image field"
      pattern: "\"image\".*splash\\.png"
---

<objective>
Generate custom branded app icon and splash screen assets using Node.js canvas scripts, then update app.json to reference the splash image with correct resizeMode and background color.

Purpose: Replace the 1×1px placeholder icon and missing splash image so the app presents as a credible branded product rather than an Expo starter on the device home screen and during launch.

Output: Two generator scripts in scripts/, two PNG assets in assets/, updated app.json splash config.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/02-app-assets-and-store-polish/02-UI-SPEC.md

<interfaces>
<!-- From app.json (current splash config): -->
<!-- "splash": { "backgroundColor": "#085041" } -->
<!-- Required final state: -->
<!-- "splash": { "image": "./assets/splash.png", "resizeMode": "contain", "backgroundColor": "#EDE8DC" } -->
<!-- icon field already points to "./assets/icon.png" — no change needed -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install canvas devDependency and create icon generator script</name>
  <files>scripts/generate-icon.js, package.json</files>
  <read_first>
    - /Users/bekircemkusdemir/Downloads/vitalspan/app.json (confirms icon path is ./assets/icon.png — no app.json change needed for icon)
    - /Users/bekircemkusdemir/Downloads/vitalspan/package.json (confirm canvas is not already installed, check current devDependencies)
    - /Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/02-app-assets-and-store-polish/02-UI-SPEC.md (ASSET-01 generation script contract, lines 126–138)
  </read_first>
  <action>
    Install `canvas` as a devDependency: `npm install --save-dev canvas`. Do NOT use `npx expo install` — this is a Node.js dev-time script dependency, not an Expo runtime package.

    Create `scripts/generate-icon.js`. The script must:
    1. Require `canvas` (`createCanvas`) and `fs` — no other dependencies.
    2. Create a 1024×1024 canvas.
    3. Fill entire rect with `#2D6A4F` (Colors.primary).
    4. Set font to a serif font at 480px — use `'480px serif'` as the font string (system fallback since DM Serif Display is not available in Node.js canvas).
    5. Set `fillStyle` to `#FFFFFF`, `textAlign` to `center`, `textBaseline` to `middle`.
    6. Call `fillText('V', 512, 512)`.
    7. Write the PNG buffer to `assets/icon.png` using `fs.writeFileSync(path.join(__dirname, '..', 'assets', 'icon.png'), canvas.toBuffer('image/png'))`.
    8. Log `'Icon generated: assets/icon.png (1024x1024)'` on completion.

    After writing the script, run it: `node scripts/generate-icon.js`.
  </action>
  <verify>
    <automated>node -e "const {createCanvas}=require('canvas');const c=createCanvas(1,1);console.log('canvas ok')" 2>/dev/null && node /Users/bekircemkusdemir/Downloads/vitalspan/scripts/generate-icon.js && file /Users/bekircemkusdemir/Downloads/vitalspan/assets/icon.png</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` devDependencies contains `"canvas"` (any version)
    - `scripts/generate-icon.js` exists and is runnable with `node scripts/generate-icon.js` without error
    - `assets/icon.png` exists after running the script
    - `file assets/icon.png` output contains `PNG image data, 1024 x 1024`
    - Script source contains `fillText('V', 512, 512)` and `#2D6A4F` and `1024`
    - Script source contains `writeFileSync` writing to a path ending in `assets/icon.png`
  </acceptance_criteria>
  <done>assets/icon.png is a valid 1024×1024 PNG with green background and white V lettermark. script runs without error.</done>
</task>

<task type="auto">
  <name>Task 2: Create splash generator script, run it, and update app.json</name>
  <files>scripts/generate-splash.js, assets/splash.png, app.json</files>
  <read_first>
    - /Users/bekircemkusdemir/Downloads/vitalspan/app.json (current splash config — only has backgroundColor: "#085041", no image field)
    - /Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/02-app-assets-and-store-polish/02-UI-SPEC.md (ASSET-02 generation script contract lines 171–187, and app.json final splash config lines 162–169)
  </read_first>
  <action>
    Create `scripts/generate-splash.js`. The script must:
    1. Require `canvas` (`createCanvas`) and `fs` — no other dependencies.
    2. Create a 1284×2778 canvas.
    3. Fill entire rect with `#EDE8DC`.
    4. Draw app name: font `'300 96px serif'`, fillStyle `#1A1A18`, textAlign `center`, textBaseline `alphabetic`, `fillText('Vitalspan', 642, 1350)`.
    5. Draw tagline: font `'400 36px sans-serif'`, fillStyle `#4A4A45`, `fillText('Track your biological age', 642, 1414)`.
    6. Draw pharmacist line: font `'400 28px sans-serif'`, fillStyle `#2D6A4F`, `fillText('⚕ Built by a licensed pharmacist', 642, 1478)`.
    7. Write PNG to `assets/splash.png` using `fs.writeFileSync(path.join(__dirname, '..', 'assets', 'splash.png'), canvas.toBuffer('image/png'))`.
    8. Log `'Splash generated: assets/splash.png (1284x2778)'` on completion.

    Run the script: `node scripts/generate-splash.js`.

    Update `app.json` splash object. The current value is `{ "backgroundColor": "#085041" }`. Replace it with exactly:
    `{ "image": "./assets/splash.png", "resizeMode": "contain", "backgroundColor": "#EDE8DC" }`.
    All other fields in app.json remain unchanged.
  </action>
  <verify>
    <automated>node /Users/bekircemkusdemir/Downloads/vitalspan/scripts/generate-splash.js && file /Users/bekircemkusdemir/Downloads/vitalspan/assets/splash.png && node -e "const j=require('./app.json');const s=j.expo.splash;console.log(s.image,s.resizeMode,s.backgroundColor)" 2>/dev/null || node -e "const fs=require('fs');const j=JSON.parse(fs.readFileSync('/Users/bekircemkusdemir/Downloads/vitalspan/app.json','utf8'));const s=j.expo.splash;process.exit(s.image&&s.resizeMode==='contain'&&s.backgroundColor==='#EDE8DC'?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/generate-splash.js` exists and runs without error via `node scripts/generate-splash.js`
    - `assets/splash.png` exists after running the script
    - `file assets/splash.png` output contains `PNG image data, 1284 x 2778`
    - Script source contains `fillText('Vitalspan', 642, 1350)` and `#EDE8DC` and `1284`
    - `app.json` `expo.splash.image` equals `"./assets/splash.png"`
    - `app.json` `expo.splash.resizeMode` equals `"contain"`
    - `app.json` `expo.splash.backgroundColor` equals `"#EDE8DC"` (was `"#085041"`)
    - All other app.json fields (name, slug, version, orientation, icon, ios, extra, plugins) are unchanged
  </acceptance_criteria>
  <done>assets/splash.png is a valid 1284×2778 PNG with warm off-white background, centered Vitalspan wordmark, tagline, and pharmacist line. app.json splash config matches final spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev scripts → filesystem | Node.js scripts write PNG files to assets/ — dev-time only, no runtime exposure |
| npm install → devDependency | `canvas` installs as devDependency only, not bundled in app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | npm install canvas | mitigate | canvas is a well-established npm package (npmjs.com/package/canvas, Automattic-maintained); verify package before install |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck on canvas package legitimacy before install |
</threat_model>

<verification>
After both tasks complete:

1. `node scripts/generate-icon.js` exits 0 and produces `assets/icon.png` at 1024×1024
2. `node scripts/generate-splash.js` exits 0 and produces `assets/splash.png` at 1284×2778
3. `app.json` splash contains `image`, `resizeMode: "contain"`, `backgroundColor: "#EDE8DC"`
4. Neither script is in the app bundle: `grep -r "generate-icon\|generate-splash" src/` returns no results
</verification>

<success_criteria>
- Both generator scripts exist in scripts/ and run without error
- assets/icon.png is 1024×1024 PNG with #2D6A4F background and white V lettermark
- assets/splash.png is 1284×2778 PNG with #EDE8DC background and centered branded content
- app.json splash config has all three required fields (image, resizeMode, backgroundColor)
- canvas is installed as devDependency only — not present in expo bundle
</success_criteria>

<output>
Create `.planning/phases/02-app-assets-and-store-polish/02-01-SUMMARY.md` when done
</output>
