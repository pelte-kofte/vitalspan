---
phase: 20-protocol-schema-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/screens/ProtocolScreen.tsx
  - src/components/SupplementLibrarySection.tsx
autonomous: true
requirements:
  - PROT-01
  - PROT-02
  - PROT-03
  - PROT-04

must_haves:
  truths:
    - "ProtocolScreen loads, detects old schema by 'addedSupplements' key, migrates to supplements[], writes back immediately"
    - "All supplements (previously addedSupplements + customSupplements) appear in a single 'Your Stack' section — no 'Custom' category label"
    - "Tapping anywhere on a supplement card opens EditSupplementSheet pre-filled with item's current values"
    - "EditSupplementSheet saves personalDose and timing back to the ProtocolItem in supplements[]"
    - "Tapping anywhere on a medication card opens EditMedicationSheet with timing chips and 'Remove from view' button"
    - "Inline time chips are REMOVED from medication cards; timing is set only from EditMedicationSheet"
    - "Hidden medications (in hiddenMeds[]) are fully excluded from the UI and taken count"
    - "Remove button (✕) on supplement card removes from supplements[] and clears associated taken IDs"
    - "EMPTY_PROTOCOL and persist() use the new ProtocolState shape from src/types/protocol.ts"
    - "SupplementLibrarySection.tsx addedSupplements prop passes ProtocolItem[] not string[]"
  artifacts:
    - path: "src/screens/ProtocolScreen.tsx"
      provides: "Migrated protocol screen using new ProtocolState schema"
      contains: "migrateProtocol"
    - path: "src/components/SupplementLibrarySection.tsx"
      provides: "Updated to accept ProtocolItem[] for addedSupplements prop"
  key_links:
    - from: "src/screens/ProtocolScreen.tsx"
      to: "src/types/protocol.ts"
      via: "import { ProtocolItem, ProtocolState, TimeSlot, CustomSupplement, EMPTY_PROTOCOL }"
      pattern: "from '../types/protocol'"
    - from: "ProtocolScreen loadData"
      to: "AsyncStorage @vitalspan_protocol"
      via: "migrateProtocol() called on raw parsed data"
      pattern: "'addedSupplements' in parsed"
---

<objective>
Migrate ProtocolScreen.tsx from the split schema (addedSupplements + customSupplements) to the unified supplements[] schema. Add EditSupplementSheet and EditMedicationSheet components. Remove inline med timing chips. Wire up hidden medications. Update SupplementLibrarySection to work with new ProtocolItem[].

Purpose: This is the core schema migration. After this plan, ProtocolScreen uses the new ProtocolState from Phase 20-01 and the custom category is gone.

Output: Fully migrated ProtocolScreen.tsx + updated SupplementLibrarySection.tsx.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/20-protocol-schema-migration/20-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/20-protocol-schema-migration/20-01-SUMMARY.md

<interfaces>
<!-- Types now available from src/types/protocol.ts (created in Plan 01) -->

From src/types/protocol.ts:
```typescript
export type TimeSlot = 'morning' | 'afternoon' | 'evening' | 'night';

export interface ProtocolItem {
  id: string;
  name: string;
  dose: string;
  personalDose?: string;
  timing?: TimeSlot;
  source: 'db' | 'manual';
  addedAt: string;
}

export interface ProtocolState {
  supplements: ProtocolItem[];
  medTimes: Record<string, TimeSlot>;
  hiddenMeds: string[];
  taken: string[];
  takenDate: string;
}

export interface CustomSupplement {  // migration detection only
  id: string; name: string; dose: string;
  timing?: TimeSlot; notes?: string; addedAt: string;
}

export const EMPTY_PROTOCOL: ProtocolState;
```

From src/data/supplementTimings.ts:
```typescript
export interface SupplementInfo {
  id: string; name: string; category: string; defaultDose: string;
  timing: SupplementTiming; bestTime: BestTime; evidenceGrade: EvidenceGrade;
  shortDescription: string; ...
}
export const SUPPLEMENT_DATABASE: SupplementInfo[];
```

From src/data/medications.ts:
```typescript
export interface MedicationEntry { genericName: string; brandNames: string[]; drugClass: string; category: MedCategory; }
export const MEDICATION_DATABASE: MedicationEntry[];
```

Current AddCustomSupplementModal in ProtocolScreen.tsx (lines 126–295): bottom-sheet pattern with Modal, KeyboardAvoidingView, TouchableOpacity overlay+sheet, ms.overlay/sheet/handle/sheetTitle stylesheet keys. This is the reference implementation for EditSupplementSheet and EditMedicationSheet.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ProtocolScreen state, storage, and schema migration utility</name>
  <files>src/screens/ProtocolScreen.tsx</files>
  <read_first>
    - src/screens/ProtocolScreen.tsx (full file — current implementation to migrate from)
    - src/types/protocol.ts (types created in Plan 01)
    - src/data/supplementTimings.ts (SUPPLEMENT_DATABASE for migration lookup)
    - .planning/phases/20-protocol-schema-migration/20-CONTEXT.md (D-01 through D-10 decisions)
  </read_first>
  <action>
    Perform the following changes to src/screens/ProtocolScreen.tsx:

    IMPORTS:
    - Replace the local TimeSlot, CustomSupplement, ProtocolState type definitions with imports from '../types/protocol': ProtocolItem, ProtocolState, TimeSlot, CustomSupplement, EMPTY_PROTOCOL
    - Remove the local EMPTY_PROTOCOL const (now imported)
    - Keep all other existing imports (React Native, expo-haptics, AsyncStorage, theme, navigation, INTERACTIONS, SUPPLEMENT_DATABASE, MEDICATION_DATABASE, SupplementLibrarySection, PillIcon)

    MIGRATION FUNCTION — add migrateProtocol() after imports:
    A pure function with signature: function migrateProtocol(parsed: Record<string, unknown>): ProtocolState
    Detection: if 'addedSupplements' in parsed — old schema
    - Convert old addedSupplements: string[] → ProtocolItem[]: for each name, look up SUPPLEMENT_DATABASE.find(s => s.name.toLowerCase() === name.toLowerCase()); set source: 'db', id: `supp_migrated_${Date.now()}_${index}`, name: dbEntry?.name ?? name, dose: dbEntry?.defaultDose ?? '—', addedAt: new Date().toISOString()
    - Convert old customSupplements: CustomSupplement[] (typed) → ProtocolItem[]: for each cs, set source: 'manual', id: cs.id, name: cs.name, dose: cs.dose, timing: cs.timing, addedAt: cs.addedAt
    - Clear taken (simplifies ID remapping — resets daily anyway per D-05)
    - Return new ProtocolState with supplements: [...convertedDb, ...convertedCustom], medTimes: parsed.medTimes as Record<string, TimeSlot> ?? {}, hiddenMeds: [], taken: [], takenDate: ''
    Idempotency: if 'supplements' in parsed (new schema), cast and return as ProtocolState with EMPTY_PROTOCOL defaults for any missing fields (hiddenMeds: [], etc.)
    If neither key present, return EMPTY_PROTOCOL

    loadData FUNCTION — update to call migrateProtocol:
    After JSON.parse(protocolRaw), call migrateProtocol(JSON.parse(protocolRaw) as Record<string, unknown>)
    If migration occurred (old schema detected), immediately write the migrated state back to AsyncStorage via persist(migrated) before setting state
    Today's date reset for taken still applies (takenDate check)
    setProtocol(migrated) after potential write-back

    REPLACE addedSupplements/customSupplements STATE LOGIC:
    - Remove toggleSupplement() function (added via library toggle; now replaced by addFromLibrary() and removeFromStack())
    - Remove addCustomSupplement() and removeCustomSupplement()
    - Add addFromLibrary(name: string): creates ProtocolItem with source: 'db', id: `supp_${Date.now()}`, looks up dose from SUPPLEMENT_DATABASE
    - Add addManual(item: Omit<ProtocolItem, 'id' | 'addedAt' | 'source'>): creates ProtocolItem with source: 'manual', id: `supp_custom_${Date.now()}`, addedAt: new Date().toISOString()
    - Add removeFromStack(id: string): filters supplements[], clears taken IDs containing id, calls persist()
    - Add updateSupplementItem(id: string, updates: Partial<Pick<ProtocolItem, 'personalDose' | 'timing'>>): maps supplements[], updates matched item, calls persist()
    - Remove addedSupplements references; derive addedNames set from protocol.supplements for SupplementLibrarySection

    MEDICATION HIDDEN STATE:
    - Add hideMedication(name: string): persists { ...protocol, hiddenMeds: [...protocol.hiddenMeds, name] }
    - In medications rendering, filter medications: const visibleMeds = medications.filter(m => !protocol.hiddenMeds.includes(m))
    - totalItems and takenCount use visibleMeds.length (not medications.length)
    - medInteractionMap and medClassMap useMemo deps use visibleMeds (not medications)

    TAKEN LOGIC UPDATE:
    - toggleTaken for supplement items uses ProtocolItem.id directly (not doseId pattern for single-dose items)
    - Multi-dose items (parseDoseCount(item.dose) > 1) still use doseId(item.name, n) pattern — unchanged
    - takenCount filter: check protocol.supplements directly (supplement.id in taken)

    PROGRESS PILL:
    - totalItems: visibleMeds.length + protocol.supplements.reduce((sum, s) => sum + parseDoseCount(s.dose), 0)
    - takenCount: count of taken IDs that match a visibleMed name OR a supplement item id OR a doseId pattern

    RENDERING — "Your Stack" section:
    - Render protocol.supplements in order (no grouping by category — flat list, add order preserved per D-01)
    - Remove the groupedSupps useMemo and the category label rendering
    - Remove the separate customSupps section (the "Custom" category label disappears per D-01)
    - Each supplement card: TouchableOpacity wrapping the entire card (not just check circle) opens EditSupplementSheet with the tapped item
    - Keep ✕ remove button calling removeFromStack(item.id)
    - Display personalDose ?? item.dose in the dose badge
    - Show timing chip if item.timing is set (use TIME_SLOTS label lookup)
    - Keep dbInfo lookup for shortDescription display: look up SUPPLEMENT_DATABASE by item.name for dbInfo

    RENDERING — Medications section:
    - Each medication card: entire card is TouchableOpacity opening EditMedicationSheet for that med
    - REMOVE the cardFooter timing row (the "When" label + chip row) from medication cards — per D-10
    - Keep the check circle (toggleTaken(med)) — but the check circle tap must NOT bubble to the card tap; use stopPropagation or wrap in separate onPress handler
    - Show current medTimes[med] as a small timing chip on the card if set (read-only display, no inline edit)
    - conflictPill stays unchanged

    SUPPLEMENT LIBRARY SECTION:
    - addedSupplements prop to SupplementLibrarySection: pass a Set or array of names from protocol.supplements.map(s => s.name)
    - onToggle: call addFromLibrary(name) if not in stack, removeFromStack by finding matching id if in stack

    AddSupplementSheet: update addedSupplements prop to check protocol.supplements.map(s => s.name).includes(supp.name); onToggle calls addFromLibrary / removeFromStack

    AddCustomSupplementModal onAdd: call addManual({ name, dose, timing }) — convert CustomSupplement shape to addManual args (drop notes for DB items, keep for manual per D-08)

    REMOVE setMedTime() function — medication timing is now handled in EditMedicationSheet only. MedTimes update moves into handleSaveMedEdit below.

    Add setMedTime() equivalent inside EditMedicationSheet save handler.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | grep -i "ProtocolScreen\|protocol.ts" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - ProtocolScreen.tsx imports ProtocolItem, ProtocolState, TimeSlot, CustomSupplement, EMPTY_PROTOCOL from '../types/protocol'
    - ProtocolScreen.tsx does NOT contain local interface definitions for ProtocolState or ProtocolItem
    - ProtocolScreen.tsx contains function migrateProtocol(
    - migrateProtocol checks 'addedSupplements' in parsed for old schema detection
    - ProtocolScreen.tsx does NOT render a section with catLabel "Custom"
    - ProtocolScreen.tsx does NOT contain cardFooter timing row on medication cards (no inline "When" chips)
    - protocol.supplements.filter and protocol.hiddenMeds appear in the rendered medications block
    - tsc reports no errors in src/screens/ProtocolScreen.tsx
  </acceptance_criteria>
  <done>ProtocolScreen uses new ProtocolState schema; migration runs on load; Custom category gone; medication inline timing chips removed</done>
</task>

<task type="auto">
  <name>Task 2: Add EditSupplementSheet + EditMedicationSheet components; update SupplementLibrarySection</name>
  <files>src/screens/ProtocolScreen.tsx, src/components/SupplementLibrarySection.tsx</files>
  <read_first>
    - src/screens/ProtocolScreen.tsx (after Task 1 changes — specifically AddCustomSupplementModal lines 126–295 as the style reference for sheets)
    - src/components/SupplementLibrarySection.tsx (current addedSupplements prop type to update)
    - src/types/protocol.ts (ProtocolItem interface)
    - .planning/phases/20-protocol-schema-migration/20-CONTEXT.md (D-08, D-09, D-10 decisions for edit sheet specs)
  </read_first>
  <action>
    ADD EditSupplementSheet component to ProtocolScreen.tsx (place after AddCustomSupplementModal, before AddSupplementSheet):

    Interface EditSupplementSheetProps:
    - visible: boolean
    - item: ProtocolItem | null
    - onClose: () => void
    - onSave: (updates: { personalDose?: string; timing?: TimeSlot }) => void

    Component behavior (per D-08):
    - Uses Modal + KeyboardAvoidingView + TouchableOpacity overlay (same pattern as AddCustomSupplementModal)
    - Sheet title: item.name (name NOT editable — read-only display at top of sheet)
    - Field: "Personal dose" TextInput, pre-filled with item.personalDose ?? item.dose; placeholder "e.g. 500mg, 2 capsules"; label "Your Dose"
    - Field: "Time of day" timing chips using TIME_SLOTS array, pre-filled with item.timing; same chip style as AddCustomSupplementModal timing chips (ms.timingChip / ms.timingChipActive)
    - Field: "Notes" TextInput (multiline) — shown ONLY when item.source === 'manual' (per D-08); hidden for DB items
    - Buttons: Cancel (resets and closes) + Save (calls onSave({ personalDose: enteredDose.trim() || undefined, timing }))
    - Use local useState for personalDose, timing, notes initialized from item prop; reset on close
    - Use useEffect to sync initialValues when item prop changes

    ADD EditMedicationSheet component to ProtocolScreen.tsx (place after EditSupplementSheet):

    Interface EditMedicationSheetProps:
    - visible: boolean
    - medName: string | null
    - currentTiming: TimeSlot | undefined
    - onClose: () => void
    - onSaveTiming: (time: TimeSlot | undefined) => void
    - onHide: () => void

    Component behavior (per D-10):
    - Modal + KeyboardAvoidingView + TouchableOpacity overlay (same pattern)
    - Sheet title: medName (read-only display)
    - Section: "Time of day" timing chips (TIME_SLOTS, same chip style), pre-filled with currentTiming
    - Button: "Remove from view" — destructive style (danger color), calls Alert.alert('Hide medication?', ...) → on confirm calls onHide()
    - Buttons: Cancel + Save Timing (calls onSaveTiming(selectedTiming))

    WIRE both sheets into the ProtocolScreen main component:
    - Add state: const [editingSupplement, setEditingSupplement] = useState<ProtocolItem | null>(null)
    - Add state: const [editingMed, setEditingMed] = useState<string | null>(null)
    - Supplement cards: wrap entire card in TouchableOpacity onPress={() => setEditingSupplement(item)} (item is ProtocolItem)
      - IMPORTANT: the ✕ remove button inside must call e.stopPropagation() — use a View wrapper with its own onStartShouldSetResponder to block bubble, or restructure so removeBtn is outside the outer TouchableOpacity
    - Medication cards: wrap entire card in TouchableOpacity onPress={() => setEditingMed(med)}
      - The check circle toggle must stop propagation — use onPressIn with e.stopPropagation() on the check circle, or use separate TouchableOpacity for check circle that calls toggleTaken and does not propagate
    - Render EditSupplementSheet with visible={editingSupplement !== null}, item={editingSupplement}, onClose={() => setEditingSupplement(null)}, onSave={(updates) => { updateSupplementItem(editingSupplement.id, updates); setEditingSupplement(null); }}
    - Render EditMedicationSheet with visible={editingMed !== null}, medName={editingMed}, currentTiming={editingMed ? protocol.medTimes[editingMed] : undefined}, onClose={() => setEditingMed(null)}, onSaveTiming={(t) => { if (editingMed) setMedTimeFromSheet(editingMed, t); setEditingMed(null); }}, onHide={() => { if (editingMed) hideMedication(editingMed); setEditingMed(null); }}
    - Add setMedTimeFromSheet(med: string, time: TimeSlot | undefined): sets or clears medTimes[med]

    UPDATE SupplementLibrarySection.tsx:
    - Current addedSupplements prop is string[] — change to string[] | Set<string> or keep string[] but callers now pass protocol.supplements.map(s => s.name) which is string[]
    - This may require no type change if callers pass .map(s => s.name); verify the current prop type and update if needed to accept the name-based check
    - The onToggle prop receives a name: string — keep as-is; ProtocolScreen caller now calls addFromLibrary(name) for add and removeFromStack by finding matching item for remove

    STYLESHEET additions to ms (modal styles):
    - ms.destructiveBtn: same layout as ms.cancelBtn but with Colors.dangerBg background and Colors.danger border
    - ms.destructiveTxt: Colors.danger color, same font
    - ms.readOnlyName: ms.sheetTitle style but with Colors.onSurfaceMuted color (for read-only item name display at top of edit sheet)
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - ProtocolScreen.tsx contains function EditSupplementSheet(
    - ProtocolScreen.tsx contains function EditMedicationSheet(
    - EditSupplementSheet renders a Notes TextInput conditionally on item.source === 'manual'
    - EditMedicationSheet renders a "Remove from view" button
    - ProtocolScreen.tsx contains state variable editingSupplement
    - ProtocolScreen.tsx contains state variable editingMed
    - src/components/SupplementLibrarySection.tsx does not have TypeScript errors with new callers
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>EditSupplementSheet and EditMedicationSheet are wired; tapping supplement opens edit sheet pre-filled with personalDose and timing; tapping medication opens edit sheet with timing chips and Remove from view button; tsc passes clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage → migrateProtocol | JSON.parse output is cast as Record<string, unknown>; malformed data must not crash |
| User input → personalDose | Text field: free string, stored as-is; no validation needed beyond trim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02 | Tampering | migrateProtocol | mitigate | Wrap entire migration in try/catch; on error return EMPTY_PROTOCOL (prevent crash) |
| T-20-03 | Denial of Service | AsyncStorage write in migrateProtocol | accept | Write is fire-and-forget with .catch; migration failure degrades gracefully |
| T-20-04 | Tampering | personalDose text input | accept | Free text, user-owned data, no clinical computation uses raw personalDose in Phase 20 (Phase 22 buckets it) |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
- grep -c "migrateProtocol" src/screens/ProtocolScreen.tsx → at least 2 (definition + call in loadData)
- grep -c "addedSupplements" src/screens/ProtocolScreen.tsx → 0 (should be gone from all non-migration code)
- grep -c "customSupplements" src/screens/ProtocolScreen.tsx → 0 (should be gone from non-migration code)
- grep -c "Custom" src/screens/ProtocolScreen.tsx → 0 (no catLabel "Custom" rendered)
- grep -c "cardFooter" src/screens/ProtocolScreen.tsx → should only be in StyleSheet definition, not in medication JSX
- grep -c "EditSupplementSheet" src/screens/ProtocolScreen.tsx → at least 2 (definition + render call)
- grep -c "EditMedicationSheet" src/screens/ProtocolScreen.tsx → at least 2 (definition + render call)
- grep -c "hiddenMeds" src/screens/ProtocolScreen.tsx → at least 3 (hideMedication fn, visibleMeds filter, persist call)
- npx tsc --noEmit exits 0
</verification>

<success_criteria>
ProtocolScreen renders from protocol.supplements[]; migration runs transparently on load for users with old schema; Custom category is gone; supplement tap opens pre-filled edit sheet; medication tap opens edit sheet with timing and Remove from view; inline medication timing chips are removed; hidden medications excluded from UI and count; tsc passes.
</success_criteria>

<output>
Create `.planning/phases/20-protocol-schema-migration/20-02-SUMMARY.md` when done.
</output>
