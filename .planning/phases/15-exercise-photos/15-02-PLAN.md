---
phase: 15-exercise-photos
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - src/screens/ExerciseDetailScreen.tsx
autonomous: true
requirements:
  - EXP-01
  - EXP-02

user_setup:
  - service: expo-image
    why: "CDN-hosted exercise photo loading with disk cache"
    env_vars: []
    dashboard_config: []

must_haves:
  truths:
    - "Opening Barbell Deadlift shows a 220px-tall photo banner (CDN JPG) in the illustration area"
    - "Opening Burpee shows the existing SVG illustration unchanged at 160×160"
    - "Opening an exercise with a photoKey after navigating from a failed-photo exercise shows the photo (not SVG)"
    - "A photo that fails to load shows the SVG fallback silently — no error message"
    - "The photo banner uses contentFit='cover' (center-cropped), with a neutral grey placeholder while loading"
    - "tsc --noEmit passes with zero errors after the change"
  artifacts:
    - path: "src/screens/ExerciseDetailScreen.tsx"
      provides: "Three-tier illustration conditional: photo banner (220px) → SVG (160px) → neutral placeholder"
      contains: "illustrationCardPhoto"
    - path: "package.json"
      provides: "expo-image dependency"
      contains: "expo-image"
  key_links:
    - from: "src/screens/ExerciseDetailScreen.tsx"
      to: "cdn.jsdelivr.net/gh/yuhonas/free-exercise-db@main/exercises/{photoKey}/0.jpg"
      via: "expo-image Image source prop constructed from exercise.photoKey"
      pattern: "exercise\\.photoKey"
    - from: "src/screens/ExerciseDetailScreen.tsx"
      to: "src/data/exercises.ts"
      via: "Exercise interface photoKey field consumed in photo URL construction"
      pattern: "photoKey"
---

<objective>
Install expo-image and rewrite the illustration section of ExerciseDetailScreen.tsx to conditionally render a 220px CDN photo banner (when photoKey is set and photo loads) or the existing 160×160 SVG fallback (when photoKey is absent or photo fails).

Purpose: Delivers EXP-01 (real exercise photos) and EXP-02 (SVG fallback intact) using the photoKey field populated in Plan 01.

Output: ExerciseDetailScreen with three-tier illustration logic, expo-image installed, s.illustrationCardPhoto style added, photoError state + reset useEffect wired.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/15-exercise-photos/15-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/15-exercise-photos/15-RESEARCH.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/15-exercise-photos/15-01-SUMMARY.md

<interfaces>
From src/data/exercises.ts (as updated by Plan 01):
```typescript
export interface Exercise {
  id: string;
  name: string;
  category: ExerciseCategory;
  bodyPart: string;
  equipment: string;
  muscleGroup: string;
  secondaryMuscles: string[];
  target: string;
  instructions: string;
  illustrationId?: string;
  photoKey?: string;       // yuhonas/free-exercise-db folder name; e.g. "Barbell_Deadlift"
  formCue?: string;
  setsReps?: string;
  longevityNote?: string;
}
```

From src/screens/ExerciseDetailScreen.tsx (current state — will be modified):
- IllustrationComponent lookup: `(ExerciseIllustrations as Record<string, React.ComponentType<{ size?: number }>>)[exercise.illustrationId]` at line 65
- Current single illustration block (lines 79–88): single `illustrationCard` View wrapping `IllustrationComponent` or placeholder — this gets replaced with the three-tier conditional
- Existing style at line 159: `illustrationCard: { backgroundColor: Colors.surface, borderRadius: Radius.xl, borderWidth: 0.5, borderColor: Colors.borderLight, alignItems: 'center', paddingVertical: Spacing.xl, ...Elevation.sm }`
- useFocusEffect callback already uses `[exerciseId]` dep array; the new photoError reset useEffect also uses `[exerciseId]`
- Existing imports: React, useState, useCallback from 'react'; useEffect NOT currently imported — must be added
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install expo-image via Expo SDK resolver</name>
  <files>package.json</files>

  <read_first>
    - package.json — verify expo-image is not already present before running install
    - .planning/phases/15-exercise-photos/15-RESEARCH.md — Package Legitimacy Audit confirms expo-image is an official Expo package (github.com/expo/expo); install command is `npx expo install expo-image`
  </read_first>

  <action>
    Run the Expo SDK-aware install command from the project root:
      npx expo install expo-image

    This uses the Expo SDK version resolver (SDK 54) to install expo-image@~3.0.11. Do NOT use `npm install expo-image` — that installs the latest version (SDK 56 compat) which causes native module version mismatch on SDK 54.

    After install, verify package.json contains the expo-image entry.
  </action>

  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep '"expo-image"' package.json</automated>
    Expected output: a line like `"expo-image": "~3.0.11"` (version may vary slightly per SDK resolver; the key requirement is version 3.x.x, not 4.x or 5.x).
  </verify>

  <acceptance_criteria>
    - package.json contains "expo-image" key with a 3.x version string
    - node_modules/expo-image directory exists
  </acceptance_criteria>

  <done>expo-image installed at the SDK-54-compatible version via the Expo resolver.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite ExerciseDetailScreen illustration section with photo banner + error fallback</name>
  <files>src/screens/ExerciseDetailScreen.tsx</files>

  <read_first>
    - src/screens/ExerciseDetailScreen.tsx — read the full file before editing; pay attention to: (a) the imports block at top, (b) the state declarations (lines 31–34), (c) the useFocusEffect block (lines 36–44), (d) the illustration block (lines 79–88), (e) the StyleSheet at lines 148–173
    - src/data/exercises.ts — verify that photoKey?: string now exists in the Exercise interface after Plan 01 completion
    - .planning/phases/15-exercise-photos/15-RESEARCH.md — Architecture Patterns section and Common Pitfalls (especially Pitfall 3: overflow: 'hidden' and Pitfall 4: photoError reset) are the authoritative implementation guide
  </read_first>

  <action>
    Make the following targeted changes to ExerciseDetailScreen.tsx. Read the file first, then apply each change precisely.

    CHANGE 1 — Imports: Add `useEffect` to the existing React import. Add `Image` from 'expo-image' as a new import line immediately after the React import line. Add `StyleSheet` from react-native if not already imported (it is currently imported at line 3). The Image import must be at the file top, not conditional.

    CHANGE 2 — State: After the existing `const [logModalOpen, setLogModalOpen] = useState(false);` line, add:
      const [photoError, setPhotoError] = useState(false);

    CHANGE 3 — useEffect for photoError reset: After the useFocusEffect block (which ends around line 44), add a new useEffect that resets photoError when exerciseId changes. This prevents a broken-photo error from persisting when the user navigates from one exercise to another:
      useEffect(() => { setPhotoError(false); }, [exerciseId]);

    CHANGE 4 — Photo URL construction: After the IllustrationComponent lookup (line 65 area), add the photoUrl derivation:
      const photoUrl = exercise.photoKey && !photoError
        ? `https://cdn.jsdelivr.net/gh/yuhonas/free-exercise-db@main/exercises/${exercise.photoKey}/0.jpg`
        : null;

    CRITICAL: The CDN URL is `exercises/{photoKey}/0.jpg` — NOT `exercises/{photoKey}/images/0.jpg`. No `images/` subdirectory. Using `images/` returns HTTP 404 and all photos silently fall back to SVG.

    CHANGE 5 — Replace illustration block: Replace the existing `{/* 1. SVG Illustration */}` block (currently lines 79–88, which is a single `<View style={s.illustrationCard}>` containing IllustrationComponent or placeholder) with the three-tier conditional below. Maintain the `{/* 1. ... */}` JSX comment:

    {/* 1. Illustration — photo banner (if photoKey) or SVG (if illustrationId) or placeholder */}
    {photoUrl ? (
      <View style={s.illustrationCardPhoto}>
        <Image
          source={photoUrl}
          style={StyleSheet.absoluteFill}
          contentFit="cover"
          transition={200}
          placeholder={{ color: Colors.surfaceElevated }}
          onError={() => setPhotoError(true)}
        />
      </View>
    ) : (
      <View style={s.illustrationCard}>
        {IllustrationComponent ? (
          <IllustrationComponent size={160} />
        ) : (
          <View style={s.illustrationPlaceholder}>
            <Text style={s.illustrationPlaceholderTxt}>No illustration</Text>
          </View>
        )}
      </View>
    )}

    Notes on the JSX block:
    - The existing `s.illustrationCard` style (160×160 square with paddingVertical Spacing.xl) is UNCHANGED — do not modify it
    - StyleSheet.absoluteFill is already imported via react-native (it is a StyleSheet property, no new import needed)
    - `transition={200}` is a number prop (200ms crossfade from placeholder to photo)
    - `placeholder={{ color: Colors.surfaceElevated }}` shows the neutral grey background while loading

    CHANGE 6 — Add s.illustrationCardPhoto to StyleSheet: In the `const s = StyleSheet.create({...})` block at the bottom of the file, after the existing `illustrationCard` style definition, add a new entry:
      illustrationCardPhoto: {
        backgroundColor: Colors.surfaceElevated,
        borderRadius: Radius.xl,
        borderWidth: 0.5,
        borderColor: Colors.borderLight,
        height: 220,
        overflow: 'hidden',
        ...Elevation.sm,
      },

    The `overflow: 'hidden'` is REQUIRED — without it, the photo will not be clipped by borderRadius on iOS, rendering as a full rectangle.

    IMPORTANT constraints from CLAUDE.md:
    - The 220px height must be in the StyleSheet entry, not as an inline style
    - No fenced code blocks in action (this is directive prose)
    - No `any` types — Image from expo-image is typed; use it directly
  </action>

  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep -c "illustrationCardPhoto" src/screens/ExerciseDetailScreen.tsx</automated>
    Expected: 2 (one JSX usage in the render, one style definition in StyleSheet)

    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep "overflow: 'hidden'" src/screens/ExerciseDetailScreen.tsx</automated>
    Expected: prints the overflow: 'hidden' line inside the illustrationCardPhoto style

    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep "images/0.jpg" src/screens/ExerciseDetailScreen.tsx | wc -l</automated>
    Expected: 0 — the wrong CDN path with `/images/` subdirectory must not appear

    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep "exercises/\${exercise.photoKey}/0.jpg" src/screens/ExerciseDetailScreen.tsx</automated>
    Expected: prints the correct CDN URL template literal

    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep "photoError" src/screens/ExerciseDetailScreen.tsx | wc -l</automated>
    Expected: 4 or more (useState declaration, useEffect reset, photoUrl derivation, onError callback)

    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | tail -5</automated>
    Expected: exits with no TypeScript errors.
  </verify>

  <acceptance_criteria>
    - ExerciseDetailScreen.tsx imports Image from 'expo-image' at the file top (unconditional import)
    - ExerciseDetailScreen.tsx imports useEffect from 'react' alongside useState and useCallback
    - photoError state: `const [photoError, setPhotoError] = useState(false);` exists
    - useEffect with dep [exerciseId] resets photoError to false on exercise change
    - photoUrl derivation uses the exact URL: `https://cdn.jsdelivr.net/gh/yuhonas/free-exercise-db@main/exercises/${exercise.photoKey}/0.jpg` — no `/images/` subdirectory
    - The JSX conditional renders a View with s.illustrationCardPhoto when photoUrl is truthy, and the existing illustrationCard structure when photoUrl is null
    - s.illustrationCardPhoto style contains: height: 220, overflow: 'hidden', borderRadius: Radius.xl, borderWidth: 0.5, borderColor: Colors.borderLight, backgroundColor: Colors.surfaceElevated, and ...Elevation.sm
    - s.illustrationCard style (existing) is unchanged — still has alignItems: 'center' and paddingVertical: Spacing.xl
    - Behavior: opening Barbell Deadlift (photoKey: 'Barbell_Deadlift') shows the 220px photo banner; opening Burpee (no photoKey) shows SVG unchanged
    - Behavior: a photo load failure sets photoError to true, causing photoUrl to be null, causing the SVG to render — no error text shown to user
    - npx tsc --noEmit exits with zero errors
  </acceptance_criteria>

  <done>ExerciseDetailScreen renders CDN photo banners for 46 mapped exercises (EXP-01) and SVG fallback for unmapped exercises (EXP-02). tsc passes. photo-error fallback works silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExerciseDetailScreen → jsDelivr CDN | App makes outbound HTTPS GET request to cdn.jsdelivr.net; no user data transmitted; URL segments are hardcoded strings from exercises.ts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Spoofing | CDN image content | accept | jsDelivr serves content pinned to yuhonas/free-exercise-db@main; no user-controlled URL segment; content integrity is the source repo owner's responsibility |
| T-15-03 | Information Disclosure | HTTPS CDN request | accept | expo-image uses iOS ATS (App Transport Security) which enforces HTTPS-only by default; no HTTP downgrade path |
| T-15-04 | Tampering | photoKey → URL construction | accept | photoKey values are static string literals from exercises.ts — no user input flows into the URL; string interpolation is safe |
| T-15-SC | Tampering | npm install (expo-image) | mitigate | expo-image verified as official Expo org package (github.com/expo/expo, packages/expo-image); Package Legitimacy Audit in RESEARCH.md confirms approved; use `npx expo install` not `npm install` to get SDK-pinned version |
</threat_model>

<verification>
- package.json contains "expo-image" at a 3.x version
- ExerciseDetailScreen.tsx imports Image from 'expo-image'
- Three-tier conditional renders in illustration area: photo banner → SVG → neutral placeholder
- s.illustrationCardPhoto has height: 220 and overflow: 'hidden'
- CDN URL uses /exercises/{photoKey}/0.jpg (no /images/ subdirectory)
- photoError state resets on exerciseId change
- npx tsc --noEmit passes with zero errors
</verification>

<success_criteria>
- Opening Barbell Deadlift (mapped) renders a 220px CDN photo banner (EXP-01 satisfied)
- Opening Burpee (unmapped) shows Phase 12 SVG illustration unchanged (EXP-02 satisfied)
- 46/60 = 77% of exercises have photoKey (EXP-03 satisfied — per Plan 01)
- Zero TypeScript errors
- Photo load failure shows SVG silently (D-07 honored)
- Neutral grey placeholder during load (D-06 honored)
- No overlay on photo (D-03 honored)
</success_criteria>

<output>
Create `.planning/phases/15-exercise-photos/15-02-SUMMARY.md` when done
</output>
