---
phase: 07-reference-data-and-exercise-screen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/seed_exercises.sql
  - src/db/seed_biomarker_definitions.sql
  - src/lib/exerciseService.ts
  - src/lib/biomarkerService.ts
autonomous: true
requirements:
  - SUPA-04
  - SUPA-05
must_haves:
  truths:
    - "exerciseService.ts exports getExercises() that returns all 59 exercises from Supabase when online"
    - "exerciseService.ts falls back to static EXERCISES array when Supabase throws or returns empty"
    - "biomarkerService.ts exports getBiomarkers() that returns merged Biomarker[] from Supabase when online"
    - "biomarkerService.ts falls back to static BIOMARKERS array when Supabase throws or returns empty"
    - "seed_exercises.sql seeds all 59 exercises into the exercises table with correct schema"
    - "seed_biomarker_definitions.sql seeds all 51 biomarker definitions with longevity-optimized ranges"
  artifacts:
    - path: "src/db/seed_exercises.sql"
      provides: "SQL seed for exercises table — all 59 rows from EXERCISES array"
      contains: "CREATE TABLE IF NOT EXISTS exercises"
    - path: "src/db/seed_biomarker_definitions.sql"
      provides: "SQL seed for biomarker_definitions table — 51 rows from BIOMARKERS array"
      contains: "CREATE TABLE IF NOT EXISTS biomarker_definitions"
    - path: "src/lib/exerciseService.ts"
      provides: "Service function for exercise library with Supabase + static fallback"
      exports: ["getExercises"]
    - path: "src/lib/biomarkerService.ts"
      provides: "Service function for biomarker definitions with Supabase merge + static fallback"
      exports: ["getBiomarkers"]
  key_links:
    - from: "src/lib/exerciseService.ts"
      to: "src/lib/supabase.ts"
      via: "import { supabase } from './supabase'"
      pattern: "supabase\\.from\\('exercises'\\)"
    - from: "src/lib/biomarkerService.ts"
      to: "src/lib/supabase.ts"
      via: "import { supabase } from './supabase'"
      pattern: "supabase\\.from\\('biomarker_definitions'\\)"
---

<objective>
Create the Supabase reference table seed files and service layer that fetches from Supabase with static fallback.

Purpose: Biomarker definitions and exercise library become remotely updatable without an app release. The service layer abstracts the data source — screens call the service function and receive the same type shape regardless of whether Supabase or static data is used.

Output:
- `src/db/` directory with two SQL seed files (run once via Supabase SQL editor)
- `src/lib/exerciseService.ts` — exports `getExercises(): Promise<Exercise[]>`
- `src/lib/biomarkerService.ts` — exports `getBiomarkers(): Promise<Biomarker[]>`
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md

<interfaces>
<!-- Types the executor needs. Extracted from src/data/exercises.ts and src/data/biomarkers.ts. -->

From src/data/exercises.ts:
```typescript
export type ExerciseCategory =
  | 'Cardio' | 'Legs' | 'Push' | 'Pull / Row'
  | 'Core' | 'Shoulders' | 'Arms' | 'Calves';

export interface Exercise {
  id: string;
  name: string;
  category: ExerciseCategory;
  bodyPart: string;
  equipment: string;
  muscleGroup: string;
  secondaryMuscles: string[];
  target: string;
  instructions: string;
}

export const EXERCISES: Exercise[];  // 59 entries
```

From src/data/biomarkers.ts:
```typescript
export interface Biomarker {
  id: string;
  name: string;
  category: 'cardio' | 'metabolic' | 'inflammation' | 'hormones' | 'vitamins'
    | 'cbc' | 'metabolicPanel' | 'thyroid' | 'liver' | 'kidney' | 'longevity';
  categoryLabel: string;
  unit: string;
  color: string;         // UI only — not in Supabase table
  optMin: number;
  optMax: number;
  target: string;
  description: string;
  howToImprove: string;  // UI only — not in Supabase table
  defaultVal: number;    // UI only — not in Supabase table
  prevVal: number;       // UI only — not in Supabase table
  insight: string;       // UI only — not in Supabase table
  history: { date: string; value: number }[];  // UI only — not in Supabase table
}

export const BIOMARKERS: Biomarker[];  // 51 biomarker entries (BIOMARKERS array, not INTERACTIONS)
```

From src/lib/supabase.ts:
```typescript
export const supabase: SupabaseClient;  // singleton — import directly
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SQL seed files</name>
  <files>src/db/seed_exercises.sql, src/db/seed_biomarker_definitions.sql</files>

  <read_first>
    - src/data/exercises.ts — all 59 Exercise objects; the SQL rows mirror the Exercise interface exactly
    - src/data/biomarkers.ts — the BIOMARKERS array (lines 21–967, before INTERACTIONS at line 968); 51 entries; only these fields go in the table: id, name, unit, opt_min, opt_max, category, target, description
  </read_first>

  <action>
    Create directory src/db/ and two SQL seed files.

    seed_exercises.sql schema and logic:
    - CREATE TABLE IF NOT EXISTS exercises (id text PRIMARY KEY, name text NOT NULL, category text NOT NULL, body_part text, equipment text, muscle_group text, secondary_muscles text[], target text, instructions text)
    - Enable RLS: ALTER TABLE exercises ENABLE ROW LEVEL SECURITY
    - CREATE POLICY "public read" ON exercises FOR SELECT TO anon USING (true)
    - INSERT all 59 exercises using INSERT INTO exercises (id, name, category, body_part, equipment, muscle_group, secondary_muscles, target, instructions) VALUES (...) ON CONFLICT (id) DO NOTHING
    - Map Exercise fields to SQL columns: bodyPart → body_part, muscleGroup → muscle_group, secondaryMuscles → secondary_muscles (as PostgreSQL array literal e.g. ARRAY['biceps','forearms'])
    - Every Exercise in EXERCISES must have a corresponding INSERT row — verify count is 59

    seed_biomarker_definitions.sql schema and logic:
    - CREATE TABLE IF NOT EXISTS biomarker_definitions (id text PRIMARY KEY, name text NOT NULL, unit text, opt_min numeric, opt_max numeric, category text, target text, description text)
    - Enable RLS: ALTER TABLE biomarker_definitions ENABLE ROW LEVEL SECURITY
    - CREATE POLICY "public read" ON biomarker_definitions FOR SELECT TO anon USING (true)
    - INSERT all 51 BIOMARKERS entries with fields: id, name, unit, opt_min (from optMin), opt_max (from optMax), category, target, description
    - Fields NOT in the table (color, howToImprove, defaultVal, prevVal, insight, history) are intentionally excluded — they are static and UI-only
    - Use INSERT ... ON CONFLICT (id) DO NOTHING for idempotency
    - Escape single quotes in text values by doubling them (e.g., 'It''s' not 'It\'s')
  </action>

  <verify>
    <automated>grep -c "INSERT INTO exercises" /Users/bekircemkusdemir/Downloads/vitalspan/src/db/seed_exercises.sql</automated>
    <automated>grep -c "INSERT INTO biomarker_definitions" /Users/bekircemkusdemir/Downloads/vitalspan/src/db/seed_biomarker_definitions.sql</automated>
  </verify>

  <acceptance_criteria>
    - src/db/seed_exercises.sql contains CREATE TABLE IF NOT EXISTS exercises with columns: id, name, category, body_part, equipment, muscle_group, secondary_muscles, target, instructions
    - src/db/seed_exercises.sql contains exactly 59 INSERT INTO exercises rows (one per exercise in EXERCISES array)
    - src/db/seed_exercises.sql contains ALTER TABLE exercises ENABLE ROW LEVEL SECURITY and CREATE POLICY "public read" ON exercises FOR SELECT TO anon USING (true)
    - src/db/seed_biomarker_definitions.sql contains CREATE TABLE IF NOT EXISTS biomarker_definitions with columns: id, name, unit, opt_min, opt_max, category, target, description
    - src/db/seed_biomarker_definitions.sql contains exactly 51 INSERT INTO biomarker_definitions rows
    - src/db/seed_biomarker_definitions.sql contains ALTER TABLE biomarker_definitions ENABLE ROW LEVEL SECURITY and CREATE POLICY "public read" ON biomarker_definitions FOR SELECT TO anon USING (true)
    - All INSERT statements use ON CONFLICT (id) DO NOTHING
    - No single-quote escaping errors (use doubled single quotes for apostrophes in text)
  </acceptance_criteria>

  <done>Both SQL seed files exist in src/db/, are valid PostgreSQL syntax, include RLS + anon SELECT policy, and have correct row counts for all entries from the static arrays.</done>
</task>

<task type="auto">
  <name>Task 2: Create exerciseService.ts and biomarkerService.ts</name>
  <files>src/lib/exerciseService.ts, src/lib/biomarkerService.ts</files>

  <read_first>
    - src/lib/supabase.ts — supabase client export; import pattern is: import { supabase } from './supabase'
    - src/data/exercises.ts — Exercise interface and EXERCISES array; getExercises() must return Exercise[] matching this interface exactly
    - src/data/biomarkers.ts — Biomarker interface, BIOMARKERS array; getBiomarkers() merges Supabase data field-by-field into BIOMARKERS shape
    - src/components/NeuralGrid.tsx — Reanimated pattern reference (for code style context only)
  </read_first>

  <action>
    Create src/lib/exerciseService.ts:
    - Import: supabase from './supabase', EXERCISES and Exercise from '../data/exercises'
    - Export async function getExercises(): Promise&lt;Exercise[]&gt;
    - Implementation: query supabase.from('exercises').select('*') — map result rows to Exercise interface (body_part → bodyPart, muscle_group → muscleGroup, secondary_muscles → secondaryMuscles). If the query errors, data is null, or data.length === 0, return EXERCISES (static fallback). Never throw.
    - Catch block must return EXERCISES, not rethrow.
    - TypeScript strict: map each row field explicitly; do not use `as Exercise` on raw row without mapping.

    Create src/lib/biomarkerService.ts:
    - Import: supabase from './supabase', BIOMARKERS and Biomarker from '../data/biomarkers'
    - Export async function getBiomarkers(): Promise&lt;Biomarker[]&gt;
    - Implementation: query supabase.from('biomarker_definitions').select('id, name, unit, opt_min, opt_max, category, target, description'). On success, merge field-by-field into BIOMARKERS: for each static biomarker, find the Supabase row matching by id, then override name, unit, optMin (from opt_min), optMax (from opt_max), category, target, description if the Supabase row exists. UI-only fields (color, howToImprove, defaultVal, prevVal, insight, history, categoryLabel) are always taken from the static array. Return the merged Biomarker[] array.
    - On error or empty result: return BIOMARKERS unchanged (static fallback).
    - Catch block must return BIOMARKERS, not rethrow.
    - The return type is always Biomarker[] — the merged shape is identical to the existing Biomarker interface.

    Both service files follow project conventions:
    - TypeScript strict — no `any`
    - No default exports — only named exports
    - No console.log in production paths; use console.warn for fallback logging (e.g., console.warn('[exerciseService] Supabase fetch failed, using static fallback'))
  </action>

  <verify>
    <automated>npx tsc --noEmit --project /Users/bekircemkusdemir/Downloads/vitalspan/tsconfig.json 2>&1 | grep -E "exerciseService|biomarkerService" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - src/lib/exerciseService.ts exports async function getExercises(): Promise&lt;Exercise[]&gt;
    - getExercises() calls supabase.from('exercises').select('*') and maps body_part → bodyPart, muscle_group → muscleGroup, secondary_muscles → secondaryMuscles
    - getExercises() returns EXERCISES (static array) when Supabase data is null, empty, or throws
    - src/lib/biomarkerService.ts exports async function getBiomarkers(): Promise&lt;Biomarker[]&gt;
    - getBiomarkers() queries supabase.from('biomarker_definitions').select('id, name, unit, opt_min, opt_max, category, target, description')
    - getBiomarkers() merges Supabase rows into BIOMARKERS by id — only overriding: name, unit, optMin, optMax, category, target, description; all UI-only fields come from static array
    - getBiomarkers() returns BIOMARKERS unchanged when Supabase errors or returns empty
    - Both files have no TypeScript errors (tsc --noEmit passes for these files)
    - Neither file uses `any` type
  </acceptance_criteria>

  <done>Both service files exist, export the correct async functions, implement Supabase-first with static fallback, perform field mapping, and pass tsc --noEmit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase → app | Data returned from anon SELECT; executor does not control what Supabase returns |
| SQL seed files → Supabase | Seed files run via Supabase dashboard SQL editor by a trusted developer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | seed_exercises.sql / seed_biomarker_definitions.sql | accept | Files run by trusted developer in Supabase dashboard; not executed by app at runtime |
| T-07-02 | Information Disclosure | biomarker_definitions RLS | mitigate | RLS: anon role gets SELECT only; no insert/update/delete policies created — verified in seed file |
| T-07-03 | Information Disclosure | exercises RLS | mitigate | RLS: anon role gets SELECT only; no insert/update/delete policies created — verified in seed file |
| T-07-04 | Spoofing | biomarkerService Supabase response | mitigate | Merge strategy applies Supabase values field-by-field onto static type; unknown fields are dropped by TypeScript mapping |
| T-07-05 | Denial of Service | exerciseService offline | accept | Static fallback ensures app works offline; no network dependency for core functionality |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — RNGH and Reanimated already in package.json |
</threat_model>

<verification>
1. `grep -c "INSERT INTO exercises" src/db/seed_exercises.sql` returns 59
2. `grep -c "INSERT INTO biomarker_definitions" src/db/seed_biomarker_definitions.sql` returns 51
3. `npx tsc --noEmit` passes with zero errors referencing exerciseService.ts or biomarkerService.ts
4. `grep "any" src/lib/exerciseService.ts src/lib/biomarkerService.ts` returns no matches
5. Both service files import `{ supabase }` from `'./supabase'` (not re-initializing the client)
</verification>

<success_criteria>
- src/db/ directory exists with two valid PostgreSQL seed files
- seed_exercises.sql: 59 INSERT rows, RLS enabled, anon SELECT policy
- seed_biomarker_definitions.sql: 51 INSERT rows, RLS enabled, anon SELECT policy
- src/lib/exerciseService.ts exports getExercises(): Promise&lt;Exercise[]&gt; with Supabase-first + static fallback
- src/lib/biomarkerService.ts exports getBiomarkers(): Promise&lt;Biomarker[]&gt; with Supabase-first merge + static fallback
- tsc --noEmit passes — zero TypeScript errors in both service files
</success_criteria>

<output>
Create `.planning/phases/07-reference-data-and-exercise-screen/07-01-SUMMARY.md` when done
</output>
