---
phase: 21-exercise-routine-and-history
plan: 05
type: execute
wave: 3
depends_on:
  - 21-01
  - 21-02
  - 21-03
files_modified:
  - src/screens/ExerciseDetailScreen.tsx
autonomous: true
requirements:
  - OVLD-03

must_haves:
  truths:
    - "ExerciseDetailScreen shows an 8-week progressive overload sparkline below the exercise description"
    - "Sparkline uses react-native-chart-kit LineChart with 8 data points (most recent 8 ISO weeks)"
    - "Weeks with no logs show 0 on the chart (no gap/crash)"
    - "Chart only renders when 2 or more data points are non-zero (otherwise shows a placeholder)"
    - "Chart reads from @vitalspan_exercise_log filtered by exerciseId on useFocusEffect"
    - "tsc --noEmit exits 0"
  artifacts:
    - path: "src/screens/ExerciseDetailScreen.tsx"
      provides: "8-week progressive overload sparkline (LineChart from react-native-chart-kit)"
      contains: "LineChart"
  key_links:
    - from: "ExerciseDetailScreen useFocusEffect"
      to: "AsyncStorage @vitalspan_exercise_log"
      via: "loadOverloadData reads and filters by exerciseId"
      pattern: "vitalspan_exercise_log"
    - from: "LineChart data"
      to: "weekly max weight per ISO week"
      via: "computeWeeklyMaxes(logs, exerciseId)"
      pattern: "LineChart"
---

<objective>
Add an 8-week progressive overload sparkline chart to ExerciseDetailScreen below the exercise description.

Purpose: OVLD-03. Users can see at a glance whether their load is progressing over the last 8 weeks. The chart reads from the same @vitalspan_exercise_log data that the routine cards use for trend badges, giving a detailed historical view.
Output: Updated `ExerciseDetailScreen.tsx` with LineChart rendering 8 weeks of max-weight data per exercise.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/21-exercise-routine-and-history/21-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/21-exercise-routine-and-history/21-01-SUMMARY.md

<interfaces>
<!-- react-native-chart-kit LineChart API (already installed: ^6.12.0 + react-native-svg 15.12.1) -->
import { LineChart } from 'react-native-chart-kit';
import { Dimensions } from 'react-native';

LineChart props used:
  data: { labels: string[]; datasets: [{ data: number[] }] }
  width: Dimensions.get('window').width - (Spacing.base * 2) - (Spacing.md * 2)  // card inner width
  height: 120
  chartConfig: {
    backgroundColor: Colors.surface,
    backgroundGradientFrom: Colors.surface,
    backgroundGradientTo: Colors.surface,
    decimalPlaces: 0,
    color: (opacity = 1) => `rgba(45, 106, 79, ${opacity})`,   // Colors.primary in rgba
    labelColor: (opacity = 1) => `rgba(107, 107, 100, ${opacity})`,  // Colors.onSurfaceMuted
    propsForDots: { r: '3', strokeWidth: '1', stroke: Colors.primary },
    propsForBackgroundLines: { stroke: Colors.borderLight },
  }
  bezier
  style: { borderRadius: Radius.lg }
  withShadow={false}
  withInnerLines={true}
  withOuterLines={false}

<!-- Current ExerciseDetailScreen state -->
From src/screens/ExerciseDetailScreen.tsx:
  State: exercise (Exercise | null), loading (bool), logModalOpen (bool), photoError (bool)
  useFocusEffect: loads exercise from getExercises(), sets loading
  AsyncStorage: not currently read in this screen (only exercises DB)

<!-- New state needed -->
  overloadData: number[]  — 8 values, most-recent week first, then reversed for chart (oldest → newest)
  overloadLabels: string[] — 8 week-start labels, e.g. "Jun 9", "Jun 16" etc.

<!-- getMondayStr already defined in ExerciseScreen.tsx — duplicate inline or import -->
Must NOT import from ExerciseScreen.tsx (would create circular or wrong import).
Define getMondayStr locally in ExerciseDetailScreen.tsx or extract to a shared util.
For Phase 21: define inline in ExerciseDetailScreen.tsx (same body as in ExerciseScreen.tsx).

<!-- computeWeeklyMaxes: produces 8-week array -->
function computeWeeklyMaxes(logs: ExerciseLogEntry[], exerciseId: string): { data: number[]; labels: string[] }
  - Compute the Monday of the current week
  - Generate 8 week ranges: week[0] = 7 weeks ago Monday, week[7] = current week Monday
  - For each week: filter logs by exerciseId AND date >= weekMonday AND date < nextWeekMonday
    If entries have setsData: max of all set.weightKg values (ignore undefined/0)
    If entries have sets but no setsData: use 0 (no weight data)
    If no entries: 0
  - labels: short date string for each week Monday, e.g. `${d.getDate()} ${MONTH_ABBR[d.getMonth()]}`
  - data: 8 numbers, oldest to newest (so index 0 = 7 weeks ago, index 7 = current week)

<!-- D-07 chart guard: render only when 2+ data points are non-zero -->
const nonZeroCount = overloadData.filter(v => v > 0).length;
if (nonZeroCount < 2) → show placeholder: "Log this exercise 2+ weeks to see progress trend"

<!-- Placement: below longevityNote section, above the spacer View at end of ScrollView -->
<!-- The longevityNote section ends around line 133 in current ExerciseDetailScreen -->

<!-- ExerciseLogEntry import -->
Already imported: Exercise from '../data/exercises'
Add to import: ExerciseLogEntry, SetRecord  (SetRecord needed for typing)

<!-- AsyncStorage import -->
Already imported: not present in current ExerciseDetailScreen.tsx
Add: import AsyncStorage from '@react-native-async-storage/async-storage';
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add overload data loading and computation to ExerciseDetailScreen</name>
  <files>src/screens/ExerciseDetailScreen.tsx</files>
  <read_first>
    - src/screens/ExerciseDetailScreen.tsx — read fully; note all existing imports, state, useFocusEffect, JSX structure, StyleSheet entries
    - src/data/exercises.ts — confirm ExerciseLogEntry and SetRecord are exported after Plan 21-01
    - src/theme/index.ts — confirm Colors.primary, Colors.onSurfaceMuted, Colors.surface, Colors.borderLight, Radius.lg values
  </read_first>
  <action>
    Add progressive overload data loading and chart rendering to ExerciseDetailScreen:

    1. IMPORTS: Add to existing imports:
       - `AsyncStorage` from `'@react-native-async-storage/async-storage'`
       - `ExerciseLogEntry` from `'../data/exercises'` (add to existing Exercise import)
       - `LineChart` from `'react-native-chart-kit'`
       - `Dimensions` from `'react-native'` (add to existing react-native import destructure)

    2. LOCAL HELPERS (add above the component function):

       `const MONTH_ABBR = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];`

       `function getMondayStr(date: Date): string` — identical body to ExerciseScreen.tsx version:
         const d = new Date(date); const day = d.getDay(); const diff = day === 0 ? -6 : 1 - day; d.setDate(d.getDate() + diff); return d.toISOString().slice(0, 10);

       `function computeWeeklyMaxes(logs: ExerciseLogEntry[], exerciseId: string): { data: number[]; labels: string[] }`:
         - currentMonday: getMondayStr(new Date()) → parse as Date
         - Build 8 week-start Dates: weeks[i] = currentMonday minus (7-i)*7 days, for i in 0..7 (so weeks[0] = 7 weeks ago, weeks[7] = current week)
         - For each week index i:
             weekStart = weeks[i].toISOString().slice(0,10)
             weekEnd = weeks[i+1]?.toISOString().slice(0,10) ?? new Date(weeks[i].getTime() + 7*86400000).toISOString().slice(0,10)
             weekLogs = logs.filter(l => l.exerciseId === exerciseId && l.date >= weekStart && l.date < weekEnd)
             if weekLogs is empty: data[i] = 0
             else:
               maxWeight = Math.max(0, ...weekLogs.flatMap(l => l.setsData?.map(s => s.weightKg ?? 0) ?? [0]))
               data[i] = maxWeight
             label[i] = `${weeks[i].getDate()} ${MONTH_ABBR[weeks[i].getMonth()]}`
         - return { data, labels }

    3. NEW STATE (add alongside existing state):
       - `overloadData: number[]` — default: Array(8).fill(0)
       - `overloadLabels: string[]` — default: Array(8).fill('')

    4. EXTEND useFocusEffect: Inside the existing useFocusEffect callback (which currently loads the exercise), add an AsyncStorage read for overload data:
       After `getExercises()` resolves and sets `exercise`:
         `AsyncStorage.getItem('@vitalspan_exercise_log').then(raw => { const allLogs: ExerciseLogEntry[] = raw ? JSON.parse(raw) : []; const { data, labels } = computeWeeklyMaxes(allLogs, exerciseId); setOverloadData(data); setOverloadLabels(labels); }).catch(() => null);`
       This is a fire-and-forget sub-chain — if it fails, overloadData stays at all-zeros and chart shows placeholder.

    5. CHART JSX: Insert a new section in the ScrollView, after the `longevityNote` section (the `<Text style={s.longevityNoteTxt}>`) and before `<View style={{ height: Spacing.xxl }} />`:

       ```
       Compute: const nonZeroCount = overloadData.filter(v => v > 0).length;
       const chartWidth = Dimensions.get('window').width - Spacing.base * 2 - Spacing.md * 2;
       ```

       Render:
       `<View style={s.sectionCard}>`:
         `<Text style={s.sectionLabel}>PROGRESSIVE OVERLOAD — 8 WEEKS</Text>`
         `{nonZeroCount >= 2 ? (`
           `<LineChart`
             `data={{ labels: overloadLabels, datasets: [{ data: overloadData }] }}`
             `width={chartWidth}`
             `height={120}`
             `chartConfig={{ backgroundColor: Colors.surface, backgroundGradientFrom: Colors.surface, backgroundGradientTo: Colors.surface, decimalPlaces: 0, color: (opacity = 1) => \`rgba(45, 106, 79, \${opacity})\`, labelColor: (opacity = 1) => \`rgba(107, 107, 100, \${opacity})\`, propsForDots: { r: '3', strokeWidth: '1', stroke: Colors.primary }, propsForBackgroundLines: { stroke: Colors.borderLight } }}`
             `bezier`
             `style={{ borderRadius: Radius.lg }}`
             `withShadow={false}`
             `withOuterLines={false}`
           `/>`
         `) : (`
           `<Text style={s.overloadPlaceholder}>Log this exercise in 2 or more weeks to see your progress trend.</Text>`
         `)}`
       `</View>`

       Do NOT place fenced code blocks in `<action>` — the above uses backtick inline code in prose. Write the actual JSX without the prose wrapping.

    6. NEW STYLESHEET ENTRIES (append to existing `s`):
       - `overloadPlaceholder`: fontSize: Typography.sizes.sm, color: Colors.onSurfaceMuted, fontStyle: 'italic', textAlign: 'center', paddingVertical: Spacing.md, lineHeight: 20
       Note: `sectionCard` and `sectionLabel` already exist in `s` — reuse them, do not duplicate.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "LineChart" src/screens/ExerciseDetailScreen.tsx` returns at least 2 matches (import + JSX)
    - `grep "computeWeeklyMaxes\|overloadData\|overloadLabels" src/screens/ExerciseDetailScreen.tsx` returns matches
    - `grep "vitalspan_exercise_log" src/screens/ExerciseDetailScreen.tsx` returns a match (loaded in useFocusEffect)
    - `grep "PROGRESSIVE OVERLOAD" src/screens/ExerciseDetailScreen.tsx` returns a match (section label)
    - `grep "Log this exercise in 2 or more weeks" src/screens/ExerciseDetailScreen.tsx` returns a match (placeholder text)
    - `grep "nonZeroCount" src/screens/ExerciseDetailScreen.tsx` returns a match (chart guard)
    - `npx tsc --noEmit` exits 0
    - No new packages are installed (react-native-chart-kit is already in package.json)
  </acceptance_criteria>
  <done>ExerciseDetailScreen shows 8-week sparkline when 2+ weeks of data exist; placeholder shown otherwise; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| @vitalspan_exercise_log JSON parse | Corrupt or large stored data must not crash ExerciseDetailScreen |
| chart-kit LineChart with all-zero data | LineChart with dataset of all zeros must not produce NaN errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-10 | Denial of Service | JSON.parse on large @vitalspan_exercise_log | accept | Fire-and-forget with .catch(() => null); screen still loads exercise data; chart shows placeholder |
| T-21-11 | Tampering | LineChart with all-zero data array | mitigate | nonZeroCount < 2 guard prevents LineChart from rendering with degenerate data; placeholder shown instead |
| T-21-12 | Tampering | computeWeeklyMaxes on corrupt setsData entries | mitigate | setsData?.map(s => s.weightKg ?? 0) with nullish coalescing — undefined/null weight maps to 0 cleanly |
</threat_model>

<verification>
- Run: `grep "LineChart" src/screens/ExerciseDetailScreen.tsx` — must return 2 matches
- Run: `grep "computeWeeklyMaxes" src/screens/ExerciseDetailScreen.tsx` — must return match
- Run: `npx tsc --noEmit` — must exit 0
- Behavioral: Open any exercise detail after logging it for 2+ weeks → sparkline renders
- Behavioral: Open exercise detail with 0 or 1 week of data → placeholder text shown
</verification>

<success_criteria>
- 8-week sparkline chart appears below the Longevity Note section in ExerciseDetailScreen
- Chart labels: short week-start dates (e.g. "Jun 9", "Jun 16")
- Chart data: max weightKg per week; 0 for weeks with no logs or bodyweight-only logs
- Guard: chart hidden (placeholder shown) when fewer than 2 weeks have non-zero values
- react-native-chart-kit used (already installed — no new packages)
- tsc --noEmit clean
</success_criteria>

<output>
Create `.planning/phases/21-exercise-routine-and-history/21-05-SUMMARY.md` when done
</output>
