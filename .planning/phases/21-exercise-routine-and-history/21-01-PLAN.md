---
phase: 21-exercise-routine-and-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/exercises.ts
  - package.json
autonomous: true
requirements:
  - HIST-04
  - ROUT-01
  - ROUT-02
  - ROUT-03
  - ROUT-04
  - ROUT-05

must_haves:
  truths:
    - "ExerciseLogEntry accepts setsData?: SetRecord[] alongside existing legacy fields without breaking old entries"
    - "SetRecord interface is exported from src/data/exercises.ts"
    - "Drag-to-reorder dependency is available (react-native-draggable-flatlist or resolved alternative)"
    - "tsc --noEmit exits 0 after data model changes"
  artifacts:
    - path: "src/data/exercises.ts"
      provides: "SetRecord interface + setsData field on ExerciseLogEntry"
      contains: "export interface SetRecord"
    - path: "package.json"
      provides: "drag-to-reorder package dependency resolved"
  key_links:
    - from: "ExerciseLogEntry"
      to: "SetRecord[]"
      via: "setsData?: SetRecord[] optional field"
      pattern: "setsData\\?:\\s*SetRecord\\[\\]"
---

<objective>
Lay the data model and package foundation that all subsequent Phase 21 plans depend on.

Purpose: Two non-UI changes that unblock every downstream plan — (1) extend the exercise data types with the per-set weight/reps model, and (2) resolve the drag-to-reorder dependency. Neither change has UI risk; both are purely additive.
Output: `SetRecord` exported from exercises.ts, `setsData?` on `ExerciseLogEntry`, drag library installed or alternative decided.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/21-exercise-routine-and-history/21-CONTEXT.md

<interfaces>
<!-- Current ExerciseLogEntry (src/data/exercises.ts lines 34-47) -->
<!-- All existing fields must be PRESERVED — setsData is purely additive -->
From src/data/exercises.ts:
```
export interface ExerciseLogEntry {
  id: string;
  exerciseId: string;
  exerciseName: string;
  category: ExerciseCategory;
  date: string;
  sets?: number;
  reps?: number;
  durationMin?: number;
  intensity?: ExerciseIntensity;
  caloriesEstimated?: number;
  notes?: string;
  loggedAt: string;
}
```

New interface to ADD (D-04 from CONTEXT.md):
```
export interface SetRecord {
  reps: number;
  weightKg?: number;
}
```

New field to ADD to ExerciseLogEntry (D-04):
  setsData?: SetRecord[];
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SetRecord interface and setsData field to exercises.ts</name>
  <files>src/data/exercises.ts</files>
  <read_first>
    - src/data/exercises.ts — read fully; understand existing interface layout before editing (do NOT rearrange existing fields)
  </read_first>
  <action>
    Insert `export interface SetRecord` immediately before the `ExerciseLogEntry` interface definition (around line 34). The interface has two fields: `reps: number` and `weightKg?: number` (optional, omitted for bodyweight exercises).

    Then add `setsData?: SetRecord[]` as the last optional field on `ExerciseLogEntry`, after `notes?: string` and before `loggedAt: string`.

    Do NOT modify any other field on `ExerciseLogEntry`. Do NOT remove or rename any existing export. Do NOT change `ExerciseIntensity`, `Exercise`, `ExerciseCategory`, `EXERCISE_CATEGORIES`, `CATEGORY_MET`, or the `EXERCISES` array.

    The new field `setsData` is additive — existing log entries stored in AsyncStorage lack this field and must still deserialize without error (TypeScript optional field, so no runtime migration needed).
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/data/exercises.ts exports `interface SetRecord` with fields `reps: number` and `weightKg?: number`
    - `ExerciseLogEntry` has field `setsData?: SetRecord[]` (grep: `setsData\?:\s*SetRecord\[\]` matches in exercises.ts)
    - All pre-existing fields on `ExerciseLogEntry` (id, exerciseId, exerciseName, category, date, sets, reps, durationMin, intensity, caloriesEstimated, notes, loggedAt) are still present and unchanged
    - `npx tsc --noEmit` exits with code 0 (zero TypeScript errors)
  </acceptance_criteria>
  <done>SetRecord and setsData are exported, all legacy fields preserved, tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve drag-to-reorder dependency</name>
  <files>package.json</files>
  <read_first>
    - package.json — check if react-native-draggable-flatlist is already present under dependencies or devDependencies
    - node_modules/react-native-draggable-flatlist/package.json — check peerDependencies if the directory exists
  </read_first>
  <action>
    Step 1 — Check if `react-native-draggable-flatlist` is already in package.json. If it is, skip to Step 3.

    Step 2 — Determine compatibility: `react-native-draggable-flatlist` v4.x requires `react-native-reanimated` >=2 and `react-native-gesture-handler` >=2. The project has reanimated ~4.1.1 and gesture-handler ~2.28.0. Run: `npm info react-native-draggable-flatlist peerDependencies` to confirm. If peer deps are satisfied, install with: `npx expo install react-native-draggable-flatlist`. Do NOT use `npm install` directly — use `npx expo install` to ensure Expo SDK compatibility resolution.

    Step 3 — If the install fails or peer deps conflict with Reanimated 4 (v4.x uses a different Animated API than Reanimated 2/3), document the conflict in a comment at the top of the SUMMARY.md and choose the fallback: implement drag-to-reorder in Plan 21-04 using `react-native-gesture-handler` `LongPressGestureHandler` + React Native core `Animated` (not Reanimated). This approach is always compatible. If the fallback is chosen, do NOT add draggable-flatlist to package.json.

    The outcome of this task must be ONE of:
    A) `react-native-draggable-flatlist` present in package.json and node_modules
    B) SUMMARY.md note: "draggable-flatlist incompatible with Reanimated 4 — Plan 21-04 uses LongPressGestureHandler + Animated fallback"
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && (grep -q "draggable-flatlist" package.json && echo "INSTALLED" || echo "FALLBACK — check SUMMARY")</automated>
  </verify>
  <acceptance_criteria>
    - Either: `package.json` contains `"react-native-draggable-flatlist"` in dependencies AND `node_modules/react-native-draggable-flatlist` exists
    - Or: A clear decision is documented in SUMMARY.md stating the fallback approach (LongPressGestureHandler + Animated) that Plan 21-04 will implement
    - `npx tsc --noEmit` still exits 0 after any package changes (no broken types introduced)
  </acceptance_criteria>
  <done>Drag-to-reorder dependency is either installed or fallback approach is documented — Plan 21-04 has an unambiguous implementation path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry | Package install fetches from npm; MITM or supply-chain risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | npm install react-native-draggable-flatlist | mitigate | verify package exists on npmjs.com before install; use `npx expo install` (Expo SDK resolver, not raw npm) |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck — verify react-native-draggable-flatlist is the canonical package (npmjs.com/package/react-native-draggable-flatlist) before installing |
</threat_model>

<verification>
- `grep "export interface SetRecord" src/data/exercises.ts` returns a match
- `grep "setsData" src/data/exercises.ts` returns a match on `ExerciseLogEntry`
- `npx tsc --noEmit` exits 0
- `grep "react-native-draggable-flatlist" package.json` returns match OR SUMMARY.md documents fallback
</verification>

<success_criteria>
- SetRecord interface exported from src/data/exercises.ts
- setsData?: SetRecord[] added to ExerciseLogEntry (additive, no migration)
- All 13 existing ExerciseLogEntry fields preserved
- Drag-to-reorder dependency resolved (installed or fallback documented)
- tsc --noEmit clean
</success_criteria>

<output>
Create `.planning/phases/21-exercise-routine-and-history/21-01-SUMMARY.md` when done
</output>
