---
phase: 21-exercise-routine-and-history
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - src/components/QuickLogModal.tsx
autonomous: true
requirements:
  - HIST-04

must_haves:
  truths:
    - "Log modal shows Sets, Reps per set, and Weight (kg) numeric inputs instead of intensity chips"
    - "Weight input is optional — left blank for bodyweight exercises"
    - "On save, setsData is built as Array(sets).fill({ reps: repsPerSet, weightKg: weight || undefined }) and written to ExerciseLogEntry"
    - "Existing modal structure (KeyboardAvoidingView, title, notes field, save/cancel buttons) is preserved"
    - "tsc --noEmit exits 0"
  artifacts:
    - path: "src/components/QuickLogModal.tsx"
      provides: "Sets/Reps/Weight log form replacing intensity chips"
      contains: "setsData"
  key_links:
    - from: "QuickLogModal handleSave"
      to: "ExerciseLogEntry.setsData"
      via: "Array(setsNum).fill({ reps: repsNum, weightKg })"
      pattern: "setsData"
---

<objective>
Update QuickLogModal to capture per-set weight and reps data, replacing the intensity chip row.

Purpose: This is the data entry point for HIST-04. New log entries built by this modal will populate `setsData`, enabling progressive overload computation in OVLD-01/02/03.
Output: Updated `QuickLogModal.tsx` with Sets/Reps/Weight inputs; `setsData` written on save.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/21-exercise-routine-and-history/21-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/21-exercise-routine-and-history/21-01-SUMMARY.md

<interfaces>
<!-- Current QuickLogModal structure (src/components/QuickLogModal.tsx) -->
From src/components/QuickLogModal.tsx:
- State: sets ('3'), reps ('12'), duration ('30'), intensity (ExerciseIntensity), notes, userWeightKg
- Props: QuickLogModalProps { exercise: Exercise | null; visible: boolean; onClose: () => void }
- Save: builds ExerciseLogEntry with sets/reps/durationMin/intensity/caloriesEstimated; writes to @vitalspan_exercise_log
- Structure: Modal > overlay > sheet > sheetHandle + title + cat + logFields + intensityRow + calEstimate + saveBtn + cancelBtn
- Intensity chips section: <Text intensityLabel> + <View intensityRow> with three TouchableOpacity chips

<!-- After Phase 21-01, ExerciseLogEntry gains: -->
setsData?: SetRecord[];   // where SetRecord = { reps: number; weightKg?: number }

<!-- D-05 from CONTEXT.md (MUST follow exactly): -->
Replace intensity chip row with three numeric inputs:
  Sets (numeric TextInput, default '3')
  Reps per set (numeric TextInput, default '10')
  Weight kg (numeric TextInput, placeholder 'optional', default '')
On save: build setsData = Array(parseInt(sets)).fill({ reps: parseInt(repsPerSet), weightKg: parseFloat(weightKg) || undefined })
Keep: KeyboardAvoidingView structure, title, notes field, save/cancel buttons, duration field for cardio
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace intensity chips with Sets/Reps/Weight inputs in QuickLogModal</name>
  <files>src/components/QuickLogModal.tsx</files>
  <read_first>
    - src/components/QuickLogModal.tsx — read fully before editing; note exact JSX structure, state variables, StyleSheet keys, and imports
    - src/data/exercises.ts — confirm SetRecord and setsData field exist after Plan 21-01
    - src/theme/index.ts — confirm Colors, Spacing, Radius, Typography token names (already imported in modal)
  </read_first>
  <action>
    Make the following targeted changes to QuickLogModal.tsx:

    1. IMPORTS: Add `SetRecord` to the import from `../data/exercises`. Remove `ExerciseIntensity` from the import only if it is no longer used anywhere in the file (it may still be needed for INTENSITY_COLORS/INTENSITY_OPTIONS exports that other files consume — if those exports exist, keep `ExerciseIntensity` imported and keep those exports but remove the UI that renders chips).

    2. STATE: Add two new state variables alongside existing `sets` and `reps`:
       - `repsPerSet` (string, default '10')
       - `weightKg` (string, default '') — empty string means bodyweight
       Remove `intensity` state variable. Keep `sets`, `reps` (reps is used by legacy entries; keep for backward compat but the new UI will use repsPerSet as the per-set value), `duration`, `notes`, `userWeightKg`.

    3. RESET on exercise change (useEffect on exercise?.id): reset `repsPerSet` to '10', `weightKg` to '', keep other resets unchanged.

    4. UI — Replace the intensity section (the `<Text style={s.intensityLabel}>` + `<View style={s.intensityRow}>` block) with a "Weight (kg)" row in the logFields group:
       - Add a new `<View style={[s.fieldRow, s.fieldRowBorder]}>` row between the Reps row and the Duration row (or after Duration for non-cardio):
         - Label: "Reps / set"
         - Input: TextInput for `repsPerSet`, keyboardType="decimal-pad", selectTextOnFocus
       - Add a new `<View style={[s.fieldRow, s.fieldRowBorder]}>` row:
         - Label: "Weight (kg)"
         - Input: TextInput for `weightKg`, keyboardType="decimal-pad", placeholder="optional", placeholderTextColor={Colors.onSurfaceMuted}
       - Remove the `<Text style={s.intensityLabel}>Intensity</Text>` and the `<View style={s.intensityRow}>` chip block entirely.
       - The `calEstimate` display can be removed as well since calorie estimation depended on intensity.

    5. SAVE (handleSave): After the existing entry construction, add `setsData` to the ExerciseLogEntry object:
       - `const setsNum = parseInt(sets) || 1;`
       - `const repsNum = parseInt(repsPerSet) || 0;`
       - `const weightNum = parseFloat(weightKg) || undefined;`
       - `setsData: Array(setsNum).fill({ reps: repsNum, weightKg: weightNum })`
       - Keep existing fields (sets, reps, durationMin, intensity: undefined, caloriesEstimated) for backward compat with old entries — new entries will have setsData populated; old entries won't.

    6. STYLE: The `intensityLabel`, `intensityRow`, `intensityChip`, `intensityTxt`, `calEstimate` StyleSheet entries can be removed from `s` if no longer referenced in the JSX. Do NOT remove `fieldRow`, `fieldRowBorder`, `fieldLabel`, `fieldInput` — those are reused by the new Weight row.

    7. EXPORTS: `INTENSITY_OPTIONS` and `INTENSITY_COLORS` are exported constants — keep them exported (they may be imported in other files like ExerciseScreen). Do not remove those exports even though the chip UI is gone.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "setsData" src/components/QuickLogModal.tsx` returns at least one match (in handleSave)
    - `grep "repsPerSet" src/components/QuickLogModal.tsx` returns matches (state variable + TextInput)
    - `grep "weightKg" src/components/QuickLogModal.tsx` returns matches (state + TextInput + setsData)
    - `grep "intensityRow\|intensityChip\|intensityLabel" src/components/QuickLogModal.tsx` returns no JSX usage (style entries may remain as dead style if desired, but no rendered JSX)
    - `npx tsc --noEmit` exits 0
    - `INTENSITY_OPTIONS` and `INTENSITY_COLORS` are still exported (grep: `export const INTENSITY_OPTIONS` and `export const INTENSITY_COLORS` both match)
    - Modal still has sets field, duration field, notes field, save button, cancel button
  </acceptance_criteria>
  <done>QuickLogModal saves setsData on every new log entry; intensity chip row is removed; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User numeric input | Sets/reps/weight values are free-form numeric text inputs; invalid input must not crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02 | Tampering | weightKg TextInput → parseFloat | accept | `parseFloat(weightKg) || undefined` — NaN/empty maps to undefined gracefully; no PII or security risk |
| T-21-03 | Denial of Service | Array(setsNum).fill() with huge setsNum | mitigate | `parseInt(sets) || 1` with implicit cap — user typing "999" sets would create a large array but AsyncStorage write is the natural limit; add explicit cap: `Math.min(parseInt(sets) || 1, 20)` |
</threat_model>

<verification>
- Run: `grep "setsData" src/components/QuickLogModal.tsx` — must return match
- Run: `grep "repsPerSet\|weightKg" src/components/QuickLogModal.tsx` — must return matches
- Run: `npx tsc --noEmit` — must exit 0
- Manual check: QuickLogModal still exports INTENSITY_OPTIONS and INTENSITY_COLORS
</verification>

<success_criteria>
- QuickLogModal UI shows Sets, Reps/set, Weight (kg) inputs in logFields group
- handleSave writes setsData: SetRecord[] to ExerciseLogEntry
- Weight input accepts empty string (bodyweight — setsData entries get weightKg: undefined)
- tsc --noEmit clean
- INTENSITY_OPTIONS + INTENSITY_COLORS still exported (downstream consumers unbroken)
</success_criteria>

<output>
Create `.planning/phases/21-exercise-routine-and-history/21-02-SUMMARY.md` when done
</output>
