---
phase: 21-exercise-routine-and-history
plan: 04
type: execute
wave: 3
depends_on:
  - 21-01
  - 21-02
  - 21-03
files_modified:
  - src/screens/ExerciseScreen.tsx
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
  - ROUT-03
  - ROUT-04
  - ROUT-05
  - HIST-01
  - HIST-02
  - HIST-03
  - OVLD-01
  - OVLD-02

must_haves:
  truths:
    - "Rutinim/Kesfet segmented control appears below the header row in ExerciseScreen"
    - "Smart default: open to Rutinim if @vitalspan_exercise_routine has items; open to Kesfet if empty"
    - "Rutinim view shows exercise cards with drag handle, thumbnail, last-session summary, and trend badge"
    - "Kesfet view shows full library with existing category chips and an add-to-routine button per card"
    - "Max 10 exercises enforced on add with Alert message; already-in-routine shows checkmark/disabled"
    - "Drag-to-reorder updates @vitalspan_exercise_routine array and persists"
    - "Remove exercise from routine removes it from the array and persists"
    - "Edit sheet opens when tapping a log row (via onEdit prop on SwipeableLogRow)"
    - "Edit sheet allows changing setsData (Sets/Reps/Weight) and saves to @vitalspan_exercise_log"
    - "tsc --noEmit exits 0"
  artifacts:
    - path: "src/screens/ExerciseScreen.tsx"
      provides: "Rutinim/Kesfet toggle, routine CRUD, edit log sheet, drag-to-reorder"
      contains: "vitalspan_exercise_routine"
  key_links:
    - from: "ExerciseScreen Rutinim tab"
      to: "AsyncStorage @vitalspan_exercise_routine"
      via: "loadData reads routine IDs; add/remove/reorder write back"
      pattern: "vitalspan_exercise_routine"
    - from: "SwipeableLogRow onEdit"
      to: "EditLogSheet state"
      via: "setEditingLog(log)"
      pattern: "editingLog"
---

<objective>
Rebuild ExerciseScreen with the Rutinim/Kesfet toggle, routine management (add/reorder/remove), Rutinim exercise cards with last-session data and trend badges, and the edit-log bottom sheet.

Purpose: This is the central deliverable of Phase 21 for the routine and history requirements. Brings ROUT-01 through ROUT-05, HIST-02/03, and OVLD-01/02 to life in one screen.
Output: Fully rebuilt `ExerciseScreen.tsx` with Rutinim view, Kesfet view, and edit-log sheet.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/21-exercise-routine-and-history/21-CONTEXT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/21-exercise-routine-and-history/21-01-SUMMARY.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/21-exercise-routine-and-history/21-02-SUMMARY.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/21-exercise-routine-and-history/21-03-SUMMARY.md

<interfaces>
<!-- Key imports already in ExerciseScreen.tsx -->
From src/screens/ExerciseScreen.tsx (current top section):
- Nav type: NativeStackNavigationProp<RootStackParamList>
- Existing state: selectedCat, logModal, logs, refreshing, exercises, selectedMuscle, muscleMapOpen, muscleMapView
- Key helpers: getMondayStr(date: Date): string (already defined in file — reuse for overload computation)
- loadData: reads @vitalspan_exercise_log + getExercises() — expand to also read @vitalspan_exercise_routine
- deleteLog(id): filters logs, writes @vitalspan_exercise_log
- Sections: Today, This Week, History (keep these in both views; the log sections are always shown regardless of Rutinim/Kesfet)

<!-- New AsyncStorage key (D-08) -->
@vitalspan_exercise_routine — string[] of exercise IDs in display order (max 10)

<!-- Drag-to-reorder implementation (from 21-01-SUMMARY) -->
Read 21-01-SUMMARY.md for the drag approach chosen:
- If react-native-draggable-flatlist was installed: import DraggableFlatList, ScaleDecorator from it
- If fallback (LongPressGestureHandler + Animated): implement in-place drag with state-based reorder
  The fallback approach: LongPressGestureHandler on each card detects long-press to enter drag mode;
  PanGestureHandler tracks Y position and determines target index; on release, update routine array order.
  Use GestureHandlerRootView already in the app (check AppNavigator.tsx for existing wrapping).

<!-- SetRecord from 21-01 -->
SetRecord: { reps: number; weightKg?: number }
ExerciseLogEntry.setsData?: SetRecord[]

<!-- SwipeableLogRow onEdit prop from 21-03 -->
onEdit?: (log: ExerciseLogEntry) => void

<!-- Overload computation (D-07) -->
getWeeklyMaxWeight(logs: ExerciseLogEntry[], exerciseId: string, weekMonday: string): number | null
  - Filter logs by exerciseId AND date >= weekMonday AND date < next monday
  - For entries with setsData: take max of all set weightKg values
  - For entries without setsData: 0 (no weight data)
  - If no entries for the week: null

getTrendBadge(currentWeekMax: number | null, prevWeekMax: number | null): '↑' | '–' | '↓' | null
  - currentWeekMax === null → null (hidden)
  - currentWeekMax > prevWeekMax (or prevWeekMax === null and currentWeekMax > 0) → '↑'
  - currentWeekMax === prevWeekMax → '–'
  - currentWeekMax < prevWeekMax → '↓'

getTrendColor(badge: '↑' | '–' | '↓' | null): string
  - '↑' → Colors.status.optimal (green)
  - '↓' → Colors.status.critical (red)
  - '–' → Colors.onSurfaceMuted (grey)
  - null → 'transparent'

<!-- Last-session summary (D-02, specifics section) -->
getLastSessionSummary(logs: ExerciseLogEntry[], exerciseId: string): string | null
  - Find most recent log for exerciseId (by loggedAt or date descending)
  - If setsData exists and length > 0:
      weightKg defined: `${setsData.length} × ${setsData[0].reps} @ ${setsData[0].weightKg}kg`
      weightKg undefined: `${setsData.length} × ${setsData[0].reps} reps`
  - Else if sets defined: `${sets} × ${reps}`
  - Else: null

<!-- Edit log sheet (D-06) -->
State: editingLog: ExerciseLogEntry | null (null = closed)
Fields: editSets (string), editRepsPerSet (string), editWeightKg (string)
Structure: Modal + overlay + sheet (same as QuickLogModal) with:
  - Title: "Edit Log"
  - Subtitle: log.exerciseName + formatLogDate(log.date) (date read-only per D-06)
  - Three fieldRows: Sets, Reps / set, Weight (kg) — same inputs as updated QuickLogModal
  - Save button: update the log entry's setsData in @vitalspan_exercise_log, update local `logs` state
  - Delete button: call existing deleteLog(id) then close sheet
  - Cancel button: close sheet
  On open: prefill editSets from setsData.length or log.sets or '3',
            editRepsPerSet from setsData[0]?.reps or log.reps or '10',
            editWeightKg from setsData[0]?.weightKg or ''

<!-- Rutinim card UI (D-02) -->
Each card: exercise name + category + trend badge + last-session summary + drag handle on left
Tap card body → nav.navigate('ExerciseDetail', { exerciseId: ex.id })
Remove button: small "×" on the right edge or swipe — use a simple "×" TouchableOpacity for Phase 21

<!-- Kesfet add-to-routine button (D-03) -->
Add "+" button to each exercise row in the library (Kesfet view only)
If exercise.id in routine: show "✓" (filled/disabled state)
If routine.length >= 10: Alert.alert('Routine full', 'You can add up to 10 exercises. Remove one to add another.')
Else: append exercise.id to routine array, write to AsyncStorage, update state

<!-- Empty state (ROUT-05) -->
When routine is empty and view is Rutinim:
  RunnerIcon (already imported) + "Build your routine" headline + "Add exercises from the library" body
  + CTA button "Explore Exercises" that switches view to 'kesset'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Rutinim/Kesfet state, routine CRUD, overload helpers, and edit-log state to ExerciseScreen</name>
  <files>src/screens/ExerciseScreen.tsx</files>
  <read_first>
    - src/screens/ExerciseScreen.tsx — read fully; note all existing state variables, helper functions, useFocusEffect calls, and StyleSheet keys
    - src/data/exercises.ts — confirm SetRecord, ExerciseLogEntry.setsData types are present after Plan 21-01
    - src/components/SwipeableLogRow.tsx — confirm onEdit prop signature after Plan 21-03
    - src/components/QuickLogModal.tsx — confirm no interface changes that affect the import in ExerciseScreen
    - package.json / 21-01-SUMMARY.md — check whether react-native-draggable-flatlist was installed or the fallback approach was chosen
    - src/theme/index.ts — confirm Colors.status.optimal, Colors.status.critical, Colors.onSurfaceMuted values
  </read_first>
  <action>
    This task wires all state, data logic, and pure helper functions. The JSX rendering is in Task 2.

    1. NEW STATE (add after existing state declarations):
       - `activeTab: 'rutinim' | 'kesset'` — default determined at load time (see loadData)
       - `routine: string[]` — exercise IDs in order, loaded from @vitalspan_exercise_routine
       - `editingLog: ExerciseLogEntry | null` — null = sheet closed
       - `editSets: string` — prefilled from editingLog when sheet opens
       - `editRepsPerSet: string` — prefilled from editingLog
       - `editWeightKg: string` — prefilled from editingLog

    2. EXTEND loadData (existing useCallback):
       Add a third Promise: `AsyncStorage.getItem('@vitalspan_exercise_routine')`
       Parse it as `string[]` (default `[]` if null).
       After all three resolve:
         - Set `routine` from parsed array
         - Set `activeTab`: if parsed routine has length > 0, set 'rutinim'; else set 'kesset'
         - Keep existing logs and exercises state updates unchanged

    3. PURE HELPERS (add above component, after getMondayStr):

       `function getWeeklyMaxWeight(logs: ExerciseLogEntry[], exerciseId: string, weekMonday: string): number | null`
         - Filter: log.exerciseId === exerciseId AND log.date >= weekMonday AND log.date < (next monday, computed as adding 7 days to weekMonday)
         - If filtered is empty: return null
         - Otherwise: return Math.max(...filtered.flatMap(l => l.setsData?.map(s => s.weightKg ?? 0) ?? (l.sets ? [0] : [0]))) or null if all zeros and no setsData — return 0 if there are logs but no weight data

       `function getTrendBadge(curr: number | null, prev: number | null): '↑' | '–' | '↓' | null`
         - curr === null → return null
         - prev === null: curr > 0 → '↑'; curr === 0 → null (no data to compare)
         - curr > prev → '↑'; curr === prev → '–'; curr < prev → '↓'

       `function getTrendColor(badge: '↑' | '–' | '↓' | null): string`
         - '↑' → Colors.status.optimal; '↓' → Colors.status.critical; '–' → Colors.onSurfaceMuted; null → 'transparent'

       `function getLastSessionSummary(logs: ExerciseLogEntry[], exerciseId: string): string | null`
         - Find most recent log: logs.filter(l => l.exerciseId === exerciseId).sort((a,b) => b.loggedAt.localeCompare(a.loggedAt))[0]
         - If not found: return null
         - If setsData exists and length > 0:
             weightKg defined on setsData[0]: return `${setsData.length}×${setsData[0].reps} @ ${setsData[0].weightKg}kg`
             weightKg undefined: return `${setsData.length}×${setsData[0].reps} reps`
         - Else if sets: return `${log.sets}×${log.reps}`
         - Else: return null

    4. ROUTINE CRUD FUNCTIONS:

       `async function addToRoutine(exerciseId: string)`:
         - If routine.includes(exerciseId): return (already added)
         - If routine.length >= 10: Alert.alert('Routine full', 'You can add up to 10 exercises to your routine. Remove one to add another.'); return
         - const updated = [...routine, exerciseId]
         - setRoutine(updated)
         - await AsyncStorage.setItem('@vitalspan_exercise_routine', JSON.stringify(updated))

       `async function removeFromRoutine(exerciseId: string)`:
         - const updated = routine.filter(id => id !== exerciseId)
         - setRoutine(updated)
         - await AsyncStorage.setItem('@vitalspan_exercise_routine', JSON.stringify(updated))

       `async function reorderRoutine(from: number, to: number)`:
         - const updated = [...routine]
         - const [moved] = updated.splice(from, 1)
         - updated.splice(to, 0, moved)
         - setRoutine(updated)
         - await AsyncStorage.setItem('@vitalspan_exercise_routine', JSON.stringify(updated))

    5. EDIT LOG FUNCTIONS:

       `function openEditSheet(log: ExerciseLogEntry)`:
         - setEditingLog(log)
         - setEditSets(String(log.setsData?.length ?? log.sets ?? 3))
         - setEditRepsPerSet(String(log.setsData?.[0]?.reps ?? log.reps ?? 10))
         - setEditWeightKg(log.setsData?.[0]?.weightKg != null ? String(log.setsData[0].weightKg) : '')

       `async function saveEditLog()`:
         - if (!editingLog) return
         - const setsNum = Math.min(parseInt(editSets) || 1, 20)
         - const repsNum = parseInt(editRepsPerSet) || 0
         - const weightNum = parseFloat(editWeightKg) || undefined
         - const updated: ExerciseLogEntry = { ...editingLog, setsData: Array(setsNum).fill({ reps: repsNum, weightKg: weightNum }) }
         - const newLogs = logs.map(l => l.id === editingLog.id ? updated : l)
         - setLogs(newLogs)
         - await AsyncStorage.setItem('@vitalspan_exercise_log', JSON.stringify(newLogs))
         - setEditingLog(null)
         - Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success).catch(() => null)

       `async function deleteAndCloseSheet()`:
         - if (!editingLog) return
         - setEditingLog(null) first
         - then call existing deleteLog(editingLog.id)
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "vitalspan_exercise_routine" src/screens/ExerciseScreen.tsx` returns at least 3 matches (read in loadData, write in add/remove/reorder)
    - `grep "activeTab\|rutinim\|kesset" src/screens/ExerciseScreen.tsx` returns matches (state declared and set)
    - `grep "getWeeklyMaxWeight\|getTrendBadge\|getLastSessionSummary" src/screens/ExerciseScreen.tsx` returns matches (helpers defined)
    - `grep "editingLog\|openEditSheet\|saveEditLog" src/screens/ExerciseScreen.tsx` returns matches
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All state, helpers, and CRUD functions are in place; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build Rutinim JSX, Kesfet add button, Rutinim empty state, and edit-log sheet</name>
  <files>src/screens/ExerciseScreen.tsx</files>
  <read_first>
    - src/screens/ExerciseScreen.tsx — read the current JSX return (post-Task 1) to understand where to add the toggle, Rutinim section, and edit sheet
    - src/components/QuickLogModal.tsx — reference sheet Modal structure (Modal+overlay+sheet+sheetHandle+title+logFields+saveBtn+cancelBtn) for edit-log sheet
    - src/theme/index.ts — Colors, Spacing, Radius, Typography for new StyleSheet entries
    - 21-01-SUMMARY.md — drag implementation approach (DraggableFlatList or fallback)
  </read_first>
  <action>
    Add JSX for the Rutinim/Kesfet toggle, Rutinim card list, Kesfet add buttons, and edit-log sheet:

    1. SEGMENTED CONTROL (D-01): Insert immediately below the existing `<View style={s.topBar}>` and above the activityCard:
       A `<View style={s.segmentedControl}>` containing two `<TouchableOpacity>` segments:
         - "Rutinim" | "Kesfet"
         - Active segment: backgroundColor: Colors.primary, text: Colors.primaryBg
         - Inactive segment: backgroundColor: Colors.surfaceElevated, text: Colors.onSurfaceMuted
         - onPress: setActiveTab('rutinim') / setActiveTab('kesset') + Haptics.selectionAsync()
       Category chips and muscle map filter toggle are conditionally rendered: `{activeTab === 'kesset' && (...)}`.

    2. RUTINIM VIEW (only when activeTab === 'rutinim'):
       Replace the exercise library ScrollView content with the Rutinim section. The log sections (Today, This Week, History) and activityCard always show regardless of tab.
       
       Structure inside the ScrollView, gated by `activeTab === 'rutinim'`:
       
       a. EMPTY STATE (ROUT-05): `{routine.length === 0 && activeTab === 'rutinim' && (...)}`:
          `<View style={s.emptyStateCard}>` + RunnerIcon + `<Text style={s.emptyStateHeadline}>Build your routine</Text>` + `<Text style={s.emptyStateBody}>Add exercises from Kesfet to build your personal routine.</Text>` + CTA TouchableOpacity "Explore Exercises" that calls `setActiveTab('kesset')`.

       b. RUTINIM CARDS (when routine.length > 0): Render a `<View style={s.card}>` with routine.map((exerciseId, index) => ...).
          For each exercise, resolve the exercise object from `exercises.find(e => e.id === exerciseId)`.
          If exercise not found (deleted from DB): skip render (return null).
          
          Card content (use DRAG implementation from 21-01-SUMMARY):
          - IF DraggableFlatList installed: use DraggableFlatList with renderItem receiving drag props; add `renderItem={({ item, drag, isActive }) => ...}` and onDragEnd callback calling `reorderRoutine`.
          - IF fallback (LongPressGestureHandler + Animated): render as a standard FlatList or .map(); add long-press state to detect drag initiation; on long-press, show a visual indicator and allow drag via PanGestureHandler — the executor must implement a simplified swap-on-release drag (no live visual drag animation required for Phase 21; just detect long-press then tap to swap with adjacent items, or use a simpler approach: each card has up/down reorder arrows as a Phase 21 fallback if animated drag is too complex). Document the chosen approach in SUMMARY.md.

          Each routine card (regardless of drag implementation):
          `<TouchableOpacity style={s.routineCard} onPress={() => nav.navigate('ExerciseDetail', { exerciseId })} activeOpacity={0.75}>`:
            - Left: drag handle text "⠿" (Unicode braille pattern, serves as visual drag handle) in a `<View style={s.dragHandle}>`
            - Middle: `<View style={{ flex: 1 }}>`:
                `<Text style={s.routineCardName}>{exercise.name}</Text>`
                `<Text style={s.routineCardMeta}>{exercise.category} · {exercise.equipment}</Text>`
                lastSession = getLastSessionSummary(logs, exerciseId); if lastSession: `<Text style={s.routineLastSession}>{lastSession}</Text>`
            - Right: trend badge + remove button stacked vertically:
                currentMonday = getMondayStr(new Date())
                prevMonday = getMondayStr(new Date(new Date(currentMonday).getTime() - 7 * 24 * 60 * 60 * 1000).toISOString())
                currMax = getWeeklyMaxWeight(logs, exerciseId, currentMonday)
                prevMax = getWeeklyMaxWeight(logs, exerciseId, prevMonday)
                badge = getTrendBadge(currMax, prevMax)
                `{badge && <Text style={[s.trendBadge, { color: getTrendColor(badge) }]}>{badge}</Text>}`
                `<TouchableOpacity onPress={() => removeFromRoutine(exerciseId)} style={s.removeBtn}>` `<Text style={s.removeBtnTxt}>×</Text>` `</TouchableOpacity>`

    3. KESFET VIEW — ADD BUTTON (D-03): In the existing exercise library rows (the `filtered.map` block), add an add-to-routine "+" button on the right side of each exercise row.
       The existing row has: exercise name text + equipment badge. Add after the existing content:
       `<TouchableOpacity style={s.addToRoutineBtn} onPress={() => addToRoutine(ex.id)} activeOpacity={0.75} disabled={routine.includes(ex.id)}>`:
         - `<Text style={s.addToRoutineTxt}>{routine.includes(ex.id) ? '✓' : '+'}</Text>`
       This button should only render when `activeTab === 'kesset'`. The exercise row already handles navigation to ExerciseDetail on press; the add button sits alongside and uses stopPropagation by being a separate TouchableOpacity.
       When `activeTab === 'rutinim'`, do not show the library rows at all (they are gated by the tab).

    4. EDIT-LOG SHEET (D-06): Add a Modal at the bottom of the return (just before the existing QuickLogModal):
       Controlled by `editingLog !== null`.
       Structure mirrors QuickLogModal: `<Modal visible={editingLog !== null} transparent animationType="slide" onRequestClose={() => setEditingLog(null)}>`:
         - `<TouchableOpacity style={s.overlay} onPress={() => setEditingLog(null)} activeOpacity={1} />`
         - `<View style={s.sheet}>` + sheetHandle + `<Text style={s.sheetTitle}>Edit Log</Text>` + subtitle showing `{editingLog?.exerciseName} · {formatLogDate(editingLog?.date ?? '')}` (reuse or inline the date formatter from SwipeableLogRow — add `function formatLogDate(dateStr: string)` above the component if not already in scope)
         - logFields group with: Sets fieldRow + Reps/set fieldRow + Weight (kg) fieldRow (same pattern as QuickLogModal's updated logFields)
         - `<TouchableOpacity style={s.saveBtn} onPress={saveEditLog}>` `<Text style={s.saveBtnTxt}>Save Changes</Text>` `</TouchableOpacity>`
         - `<TouchableOpacity style={s.deleteBtn} onPress={deleteAndCloseSheet}>` `<Text style={s.deleteBtnTxt}>Delete Entry</Text>` `</TouchableOpacity>`
         - `<TouchableOpacity style={s.cancelBtn} onPress={() => setEditingLog(null)}>` `<Text style={s.cancelBtnTxt}>Cancel</Text>` `</TouchableOpacity>`
       The `s.deleteBtn` style: backgroundColor: Colors.status.criticalBg, border: Colors.status.criticalBorder, borderWidth: 1, borderRadius: Radius.xl, padding: 14, alignItems: 'center', marginBottom: Spacing.sm.
       The `s.deleteBtnTxt` style: color: Colors.status.criticalText, fontWeight: '600'.

    5. SWIPEABLELOGROW onEdit WIRING: Pass `onEdit={openEditSheet}` to all three `<SwipeableLogRow>` callsites (Today, This Week, History sections).

    6. NEW STYLESHEET ENTRIES (append to existing `s = StyleSheet.create({...})`):
       - `segmentedControl`: flexDirection 'row', gap: Spacing.xs, marginHorizontal: Spacing.base, marginBottom: Spacing.sm, backgroundColor: Colors.surfaceElevated, borderRadius: Radius.full, padding: 3
       - `segment`: flex 1, paddingVertical: Spacing.sm, borderRadius: Radius.full, alignItems: 'center'
       - `segmentActive`: backgroundColor: Colors.primary
       - `segmentTxt`: fontSize: Typography.sizes.sm, fontWeight: '600', color: Colors.onSurfaceMuted
       - `segmentTxtActive`: color: Colors.primaryBg
       - `routineCard`: flexDirection 'row', alignItems: 'center', padding: Spacing.md, gap: Spacing.sm, backgroundColor: Colors.surface
       - `dragHandle`: paddingHorizontal: Spacing.sm, paddingVertical: Spacing.md
       - `dragHandleTxt`: fontSize: Typography.sizes.lg, color: Colors.onSurfaceMuted
       - `routineCardName`: fontSize: Typography.sizes.base, fontWeight: '600', color: Colors.onSurface
       - `routineCardMeta`: fontSize: Typography.sizes.xs, color: Colors.onSurfaceMuted, marginTop: 2
       - `routineLastSession`: fontSize: Typography.sizes.sm, color: Colors.primaryLight, fontWeight: '500', marginTop: 2
       - `trendBadge`: fontSize: Typography.sizes.base, fontWeight: '700'
       - `removeBtn`: padding: Spacing.sm
       - `removeBtnTxt`: fontSize: Typography.sizes.md, color: Colors.status.critical, fontWeight: '700'
       - `addToRoutineBtn`: padding: Spacing.sm, minWidth: 32, alignItems: 'center'
       - `addToRoutineTxt`: fontSize: Typography.sizes.lg, fontWeight: '700', color: Colors.primary
       - `sheet` / `overlay` / `sheetHandle` / `sheetTitle` / `saveBtn` / `saveBtnTxt` / `cancelBtn` / `cancelBtnTxt` / `deleteBtn` / `deleteBtnTxt`: match QuickLogModal's existing sheet style patterns (Colors.surface bg, Radius.xxl corners, etc.)
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "segmentedControl\|activeTab\|rutinim\|kesset" src/screens/ExerciseScreen.tsx` returns multiple matches (toggle rendered + state used in conditions)
    - `grep "vitalspan_exercise_routine" src/screens/ExerciseScreen.tsx` returns at least 3 matches (loadData read, addToRoutine write, removeFromRoutine write)
    - `grep "routineCard\|dragHandle\|trendBadge" src/screens/ExerciseScreen.tsx` returns matches (Rutinim card styles defined)
    - `grep "editingLog\|openEditSheet\|saveEditLog\|Edit Log" src/screens/ExerciseScreen.tsx` returns matches (edit sheet JSX present)
    - `grep "onEdit={openEditSheet}" src/screens/ExerciseScreen.tsx` returns 3 matches (all SwipeableLogRow callsites wired)
    - `grep "addToRoutineBtn\|addToRoutine(" src/screens/ExerciseScreen.tsx` returns matches (Kesfet add button)
    - `grep "Build your routine\|Explore Exercises" src/screens/ExerciseScreen.tsx` returns matches (empty state)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Rutinim/Kesfet toggle renders; routine cards show last-session + trend; edit sheet opens from log rows; add-to-routine works in Kesfet; empty state shown when routine is empty; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage @vitalspan_exercise_routine | Persisted JSON string[]; corrupt data must not crash the screen |
| User-supplied edit form inputs | Sets/reps/weight text inputs must not produce NaN or negative values that break computation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-06 | Tampering | Parsing @vitalspan_exercise_routine as string[] | mitigate | Wrap JSON.parse in try/catch; default to [] on failure |
| T-21-07 | Tampering | Max 10 routine cap bypass | mitigate | Enforce in addToRoutine: if routine.length >= 10 → Alert and return; check is done before state update |
| T-21-08 | Tampering | editSets parseInt overflow | mitigate | Math.min(parseInt(editSets) || 1, 20) — caps at 20 sets max |
| T-21-09 | Information Disclosure | Routine stored in plain AsyncStorage | accept | No PII; exercise IDs are non-sensitive; AsyncStorage is device-local |
</threat_model>

<verification>
- Run: `npx tsc --noEmit` — must exit 0
- Run: `grep "vitalspan_exercise_routine" src/screens/ExerciseScreen.tsx` — must return multiple matches
- Run: `grep "activeTab" src/screens/ExerciseScreen.tsx` — must return matches
- Run: `grep "onEdit={openEditSheet}" src/screens/ExerciseScreen.tsx` — must return 3 matches
- Behavioral: App loads with Rutinim tab visible (empty state when routine is empty, exercise list when populated)
- Behavioral: Tapping "+" in Kesfet adds to routine; tapping "✓" is disabled; > 10 shows Alert
</verification>

<success_criteria>
- Rutinim/Kesfet segmented control visible below header
- Smart default: Kesfet on empty routine, Rutinim on populated routine (D-01)
- Rutinim cards show exercise name, category, last-session summary, trend badge (↑/–/↓), remove button (D-02)
- Kesfet library shows "+" add button per exercise; "✓" when already in routine; Alert on max 10 (D-03)
- Drag-to-reorder implemented via chosen approach (DraggableFlatList or fallback)
- Routine persists in @vitalspan_exercise_routine across focus cycles
- Edit log sheet opens from SwipeableLogRow onEdit, saves setsData, or deletes entry (D-06)
- Rutinim empty state shows with "Explore Exercises" CTA (ROUT-05)
- tsc --noEmit clean
</success_criteria>

<output>
Create `.planning/phases/21-exercise-routine-and-history/21-04-SUMMARY.md` when done
</output>
