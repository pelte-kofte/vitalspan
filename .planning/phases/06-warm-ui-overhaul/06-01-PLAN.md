---
phase: 06-warm-ui-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/theme/index.ts
  - src/screens/SettingsScreen.tsx
  - src/screens/AboutScreen.tsx
autonomous: true
requirements:
  - THEME-02
  - THEME-03
  - THEME-04
  - THEME-05

must_haves:
  truths:
    - "Colors.Beige.textMuted resolves to #6B6B64 (WCAG AA compliant on cream backgrounds)"
    - "SettingsScreen renders with Colors.Beige.bg background, Colors.Beige.card card surfaces, Colors.Beige.* text tokens"
    - "AboutScreen renders with Colors.Beige.bg background, Colors.Beige.card section cards, Colors.Beige.* text tokens"
    - "SettingsScreen and AboutScreen set status bar to dark on focus via useFocusEffect"
    - "No hardcoded hex values exist in SettingsScreen.tsx or AboutScreen.tsx"
    - "tsc --noEmit passes with zero errors"
  artifacts:
    - path: "src/theme/index.ts"
      provides: "Corrected textMuted token"
      contains: "textMuted: '#6B6B64'"
    - path: "src/screens/SettingsScreen.tsx"
      provides: "Warm-themed settings modal"
      contains: "Colors.Beige.bg"
    - path: "src/screens/AboutScreen.tsx"
      provides: "Warm-themed about modal"
      contains: "Colors.Beige.bg"
  key_links:
    - from: "src/screens/SettingsScreen.tsx"
      to: "src/theme/index.ts"
      via: "import Colors.Beige.*"
      pattern: "Colors\\.Beige\\."
    - from: "src/screens/AboutScreen.tsx"
      to: "src/theme/index.ts"
      via: "import Colors.Beige.*"
      pattern: "Colors\\.Beige\\."
---

<objective>
Fix the Colors.Beige.textMuted token value in the theme, then apply warm Beige token migration to SettingsScreen and AboutScreen — the two lightest modal screens with no empty-state work.

Purpose: The textMuted fix must land in Wave 1 before any other screen consumes the token. Settings and About are self-contained modals that establish the migration pattern with low risk.

Output: theme/index.ts with corrected textMuted, SettingsScreen.tsx and AboutScreen.tsx fully migrated to Colors.Beige.* tokens with Elevation.sm spread and useFocusEffect status bar wiring.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/06-warm-ui-overhaul/06-UI-SPEC.md

<interfaces>
<!-- Token values from src/theme/index.ts — use these exact names -->
Colors.Beige.bg = '#EDE8DC'          -- page background
Colors.Beige.card = '#FFFFFF'         -- card / sheet surfaces
Colors.Beige.headerBg = '#F5F0E8'    -- navigation header
Colors.Beige.bgShade = '#E4E0D4'     -- inset fields, icon wrap bg
Colors.Beige.border = '#D4CFC4'      -- card border (borderWidth: 0.5)
Colors.Beige.borderLight = '#E2DED6' -- subtle separators
Colors.Beige.divider = '#C8C0B0'     -- list row separators
Colors.Beige.text = '#1A1A18'        -- primary text
Colors.Beige.textSecondary = '#4A4A45' -- secondary / body text
Colors.Beige.textMuted = '#8A8A82'   -- CURRENTLY WRONG; fix to '#6B6B64'

Elevation.sm = { shadowColor: '#000', shadowOffset: { width: 0, height: 2 },
                 shadowOpacity: 0.06, shadowRadius: 8, elevation: 2 }

Radius.xl = 20   (card border radius per card spec)
Radius.md = 12   (icon wrap, unit toggle)
Spacing.base = 16, Spacing.md = 12, Spacing.sm = 8, Spacing.xs = 4
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Colors.Beige.textMuted in theme</name>
  <files>src/theme/index.ts</files>
  <read_first>
    - src/theme/index.ts (read current Beige block to locate textMuted line)
    - .planning/phases/06-warm-ui-overhaul/06-UI-SPEC.md (Color Contract section — WCAG AA rationale)
  </read_first>
  <action>
    In src/theme/index.ts, locate the Colors.Beige block (lines ~93-105). Change the single line:
      textMuted: '#8A8A82'
    to:
      textMuted: '#6B6B64'
    This is a one-line targeted edit. Do not change any other token. The legacy Colors.textMuted at the top of the file (line ~18) is intentionally left at '#8A8A82' — it is a different token used by dark screens and must not be changed.
  </action>
  <verify>
    <automated>grep -n "textMuted" /Users/bekircemkusdemir/Downloads/vitalspan/src/theme/index.ts</automated>
  </verify>
  <done>
    Colors.Beige.textMuted = '#6B6B64' in theme/index.ts. Colors.textMuted at the root level is still '#8A8A82'. No other token changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate SettingsScreen to warm Beige tokens</name>
  <files>src/screens/SettingsScreen.tsx</files>
  <read_first>
    - src/screens/SettingsScreen.tsx (read full file — ~312 lines — to see all token usages)
    - src/theme/index.ts (verify exact token names)
    - .planning/phases/06-warm-ui-overhaul/06-UI-SPEC.md (Card Spec, Section Header Spec, Status Bar Contract, Screen-by-Screen Token Mapping)
  </read_first>
  <action>
    Apply the following changes to SettingsScreen.tsx:

    IMPORTS: Add Elevation to the theme import. Change:
      import { Colors, Spacing, Radius, Typography } from '../theme';
    to:
      import { Colors, Spacing, Radius, Typography, Elevation } from '../theme';

    Add these imports (not currently present):
      import { useFocusEffect } from '@react-navigation/native';
      import { setStatusBarStyle } from 'expo-status-bar';
      import { useCallback } from 'react';  (add to React import destructuring)

    STATUS BAR: Inside the SettingsScreen component function body, add useFocusEffect hook after state declarations:
      useFocusEffect(useCallback(() => { setStatusBarStyle('dark'); return () => {}; }, []));

    STYLESHEET — replace all legacy token references in the StyleSheet.create block at the bottom.
    Apply these substitutions throughout the StyleSheet (and any inline dynamic styles):
      Colors.bg        → Colors.Beige.bg
      Colors.bgCard    → Colors.Beige.card
      Colors.bgSecondary → Colors.Beige.bgShade
      Colors.textPrimary → Colors.Beige.text
      Colors.textSecondary → Colors.Beige.textSecondary
      Colors.textMuted → Colors.Beige.textMuted
      Colors.border    → Colors.Beige.border
      Colors.borderLight → Colors.Beige.borderLight

    CARD STYLE: The `card` style currently has manual shadow props and borderRadius: 20.
    Replace the entire set of shadow props with ...Elevation.sm spread. Also:
      - Change borderRadius from 20 to Radius.xl
      - Add: borderWidth: 0.5, borderColor: Colors.Beige.border

    SECTION LABEL: The `sectionLabel` style currently references Colors.textMuted. Change to Colors.Beige.textMuted. Confirm fontWeight is '600'.

    HEADER: The header View has no explicit background — add backgroundColor: Colors.Beige.headerBg to the `header` style.

    ROW BORDER: `rowBorder` uses Colors.border — change to Colors.Beige.divider.

    ROW ICON WRAP: `rowIconWrap` uses Colors.bgSecondary — change to Colors.Beige.bgShade.

    DISCLAIMER CARD: Uses Colors.bgCard and Colors.primaryBorder — keep Colors.primaryBorder (semantic color, not Beige), but change Colors.bgCard to Colors.Beige.card.

    UNIT TOGGLE: Uses Colors.bg for background — change to Colors.Beige.bgShade. Uses Colors.border — change to Colors.Beige.border.

    Switch thumbColor references Colors.textMuted — change to Colors.Beige.textMuted.

    Remove the hardcoded '#000' from shadowColor if it remains after the Elevation.sm spread replaces the shadow props.

    Do NOT change: Colors.primary, Colors.primaryBorder, Colors.primaryBg, Colors.danger, Colors.dangerBg, Colors.warning*, Colors.status.* — these are semantic colors that must remain unchanged per THEME-03 rules.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep -n "Colors\\.bg[^S^C]" src/screens/SettingsScreen.tsx | grep -v "Colors\.bgCard\|Colors\.bgSecondary\|Colors\.Beige\|Colors\.primaryBg\|Colors\.warningBg\|Colors\.dangerBg\|Colors\.accentBg\|Colors\.status"</automated>
  </verify>
  <acceptance_criteria>
    - SettingsScreen.tsx contains `Colors.Beige.bg` (safe background)
    - SettingsScreen.tsx contains `Colors.Beige.card` (card backgrounds)
    - SettingsScreen.tsx contains `Colors.Beige.headerBg` (header background)
    - SettingsScreen.tsx contains `Colors.Beige.textMuted` (section labels, row subtitles)
    - SettingsScreen.tsx contains `...Elevation.sm` (card shadow)
    - SettingsScreen.tsx contains `setStatusBarStyle('dark')` (status bar)
    - SettingsScreen.tsx contains `useFocusEffect` (status bar lifecycle)
    - SettingsScreen.tsx does NOT contain `Colors.bg,` or `Colors.bgCard,` or `Colors.bgSecondary,` or `Colors.textPrimary,` or `Colors.textMuted,` as standalone references (these must all be replaced with Beige equivalents)
    - grep -c '#[0-9A-Fa-f]\{3,6\}' src/screens/SettingsScreen.tsx returns 0 (no hardcoded hex)
    - Elevation import is present in theme import line
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    SettingsScreen uses warm Beige tokens throughout. Status bar sets to 'dark' on focus. No hardcoded hex values. Elevation.sm spread replaces manual shadow props. tsc passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate AboutScreen to warm Beige tokens</name>
  <files>src/screens/AboutScreen.tsx</files>
  <read_first>
    - src/screens/AboutScreen.tsx (read full file — ~338 lines)
    - src/theme/index.ts (verify token names)
    - .planning/phases/06-warm-ui-overhaul/06-UI-SPEC.md (Card Spec, Screen-by-Screen Token Mapping, Status Bar Contract)
  </read_first>
  <action>
    Apply the following changes to AboutScreen.tsx:

    IMPORTS: Add Elevation and change:
      import { Colors, Spacing, Radius, Typography } from '../theme';
    to:
      import { Colors, Spacing, Radius, Typography, Elevation } from '../theme';

    Add these imports (not currently present):
      import { useFocusEffect } from '@react-navigation/native';
      import { setStatusBarStyle } from 'expo-status-bar';
      import { useCallback } from 'react';  (add to React import destructuring — useEffect already imported)

    STATUS BAR: Inside the AboutScreen component, after the useState/useEffect declarations, add:
      useFocusEffect(useCallback(() => { setStatusBarStyle('dark'); return () => {}; }, []));

    STYLESHEET token substitutions throughout the s = StyleSheet.create block:
      Colors.bg        → Colors.Beige.bg
      Colors.bgCard    → Colors.Beige.card
      Colors.bgSecondary → Colors.Beige.bgShade
      Colors.textPrimary → Colors.Beige.text
      Colors.textSecondary → Colors.Beige.textSecondary
      Colors.textMuted → Colors.Beige.textMuted
      Colors.border    → Colors.Beige.border
      Colors.borderLight → Colors.Beige.borderLight

    HEADER: Add backgroundColor: Colors.Beige.headerBg to the `header` style.

    SECTION CARDS: The `section` style uses Colors.bgCard and borderRadius: Radius.lg.
    Change:
      - backgroundColor: Colors.bgCard → Colors.Beige.card
      - borderRadius: Radius.lg → Radius.xl
      - The existing borderWidth: 0.5, borderColor: Colors.border lines → borderColor: Colors.Beige.border
      - Add ...Elevation.sm spread (remove any pre-existing manual shadow props if present; note the `section` style currently has no shadow — add Elevation.sm spread)

    LEGAL CARD: `legalCard` uses Colors.bgSecondary → Colors.Beige.bgShade. Change borderColor: Colors.border → Colors.Beige.border.

    CITATION BLOCK: `citationBlock` uses Colors.bgSecondary → Colors.Beige.bgShade.

    RANGE TABLE: `rangeTable` uses Colors.border → Colors.Beige.border. `rangeRow` uses Colors.border and Colors.bgCard → Colors.Beige.border, Colors.Beige.card.

    INLINE JSX dynamic styles: The rangeHeaderRow rows in JSX use inline `{ color: Colors.textPrimary }` and `{ color: Colors.textMuted }`. Change these inline dynamic color references to Colors.Beige.text and Colors.Beige.textMuted respectively.

    EVIDENCE_GRADES constant at file top: The Grade 'C' entry references `Colors.textMuted` and `Colors.bgSecondary` and `Colors.border`. Change these to Colors.Beige.textMuted, Colors.Beige.bgShade, Colors.Beige.border.

    Do NOT change: Colors.primary, Colors.primaryBg, Colors.primaryBorder, Colors.primaryDark, Colors.warning*, Colors.warningBg, Colors.warningBorder, Colors.warningText — these remain as semantic colors.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep -n "Colors\.bg[^e]" src/screens/AboutScreen.tsx | grep -v "Colors\.Beige\|Colors\.primaryBg\|Colors\.warningBg\|Colors\.dangerBg"</automated>
  </verify>
  <acceptance_criteria>
    - AboutScreen.tsx contains `Colors.Beige.bg` (safe background)
    - AboutScreen.tsx contains `Colors.Beige.card` (section card backgrounds)
    - AboutScreen.tsx contains `Colors.Beige.headerBg` (header background)
    - AboutScreen.tsx contains `Colors.Beige.textMuted` (captions, subtitles)
    - AboutScreen.tsx contains `...Elevation.sm` (section card shadow)
    - AboutScreen.tsx contains `setStatusBarStyle('dark')` (status bar)
    - AboutScreen.tsx contains `useFocusEffect` (lifecycle)
    - AboutScreen.tsx does NOT contain `Colors.bgCard` or `Colors.bgSecondary` or `Colors.textPrimary` or `Colors.textMuted,` as standalone token references
    - grep -c '#[0-9A-Fa-f]\{3,6\}' src/screens/AboutScreen.tsx returns 0 (no hardcoded hex)
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    AboutScreen uses warm Beige tokens throughout including section cards at Radius.xl, Elevation.sm spread, and status bar set to 'dark' on focus. No hardcoded hex. tsc passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage → UI | User-stored profile/data rendered in AboutScreen legal card — read-only display only, no eval path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | SettingsScreen export data handler | accept | Existing Share.share call — user-initiated, no new exposure introduced by this plan |
| T-06-SC | Tampering | npm/pip installs | accept | No package installs in this plan — pure token migration |
</threat_model>

<verification>
Run after all three tasks complete:

```bash
cd /Users/bekircemkusdemir/Downloads/vitalspan

# 1. Verify textMuted fix
grep "textMuted" src/theme/index.ts

# 2. Verify no legacy tokens in migrated files
grep -n "Colors\.textPrimary\|Colors\.textMuted,\|Colors\.bgCard\|Colors\.bgSecondary\b" \
  src/screens/SettingsScreen.tsx src/screens/AboutScreen.tsx

# 3. Verify no hardcoded hex
grep -n "'#[0-9A-Fa-f]" src/screens/SettingsScreen.tsx src/screens/AboutScreen.tsx

# 4. Verify Beige tokens present
grep -c "Colors\.Beige\." src/screens/SettingsScreen.tsx
grep -c "Colors\.Beige\." src/screens/AboutScreen.tsx

# 5. TypeScript check
npx tsc --noEmit 2>&1 | head -20
```
</verification>

<success_criteria>
1. Colors.Beige.textMuted is '#6B6B64' in theme/index.ts (was '#8A8A82')
2. SettingsScreen.tsx: Colors.Beige.bg, Colors.Beige.card, Colors.Beige.headerBg, Elevation.sm, setStatusBarStyle('dark') — all present
3. AboutScreen.tsx: Colors.Beige.bg, Colors.Beige.card, Colors.Beige.headerBg, Elevation.sm, setStatusBarStyle('dark') — all present
4. Zero hardcoded hex values in either migrated screen file
5. tsc --noEmit exits 0
</success_criteria>

<output>
Create `.planning/phases/06-warm-ui-overhaul/06-01-SUMMARY.md` when done
</output>
