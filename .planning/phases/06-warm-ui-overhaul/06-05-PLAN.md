---
phase: 06-warm-ui-overhaul
plan: 05
type: execute
wave: 3
depends_on:
  - 06-03
  - 06-04
files_modified:
  - src/screens/ProfileScreen.tsx
autonomous: false
requirements:
  - THEME-02
  - THEME-03
  - THEME-04
  - THEME-05
  - THEME-06

must_haves:
  truths:
    - "ProfileScreen renders with Colors.Beige.bg background and Colors.Beige.card surfaces"
    - "ProfileScreen shows motivating empty state when !profile: 👤 icon, 'Your health story starts here.' headline, correct body, 'Complete Onboarding' CTA navigating to Onboarding"
    - "ProfileScreen sets status bar to dark on focus via useFocusEffect"
    - "Dark screens (Dashboard, LongevityScore, Landing) are visually unchanged — no beige bleed"
    - "No hardcoded hex values in ProfileScreen.tsx"
    - "tsc --noEmit passes with zero errors across all 7 migrated screens"
  artifacts:
    - path: "src/screens/ProfileScreen.tsx"
      provides: "Warm-themed profile screen with motivating empty state"
      contains: "Colors.Beige.bg"
    - path: "src/screens/ProfileScreen.tsx"
      provides: "Profile empty state"
      contains: "Your health story starts here."
  key_links:
    - from: "ProfileScreen empty state"
      to: "nav.navigate('Onboarding')"
      via: "CTA onPress"
      pattern: "navigate.*Onboarding"
    - from: "src/screens/ProfileScreen.tsx"
      to: "src/theme/index.ts"
      via: "Elevation import"
      pattern: "Elevation\\.sm"
---

<objective>
Migrate ProfileScreen to warm Beige tokens, replace its minimal two-line empty state with the full motivating anatomy, and perform a final end-to-end visual checkpoint across all 7 warm screens.

Purpose: ProfileScreen is the last warm screen. Wave 3 ensures all prior migrations are complete before the human checkpoint verifies the full Phase 6 goal: warm beige on all list/data screens, dark neural untouched on immersive screens.

Output: ProfileScreen.tsx fully migrated. Empty state delivers 👤 icon, "Your health story starts here." headline, body, "Complete Onboarding" CTA. Human checkpoint confirms visual correctness of the entire phase.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/06-warm-ui-overhaul/06-UI-SPEC.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/06-warm-ui-overhaul/06-03-SUMMARY.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/06-warm-ui-overhaul/06-04-SUMMARY.md

<interfaces>
<!-- Token values post Plan 01 -->
Colors.Beige.bg = '#EDE8DC'
Colors.Beige.card = '#FFFFFF'
Colors.Beige.bgShade = '#E4E0D4'
Colors.Beige.border = '#D4CFC4'
Colors.Beige.divider = '#C8C0B0'
Colors.Beige.text = '#1A1A18'
Colors.Beige.textSecondary = '#4A4A45'
Colors.Beige.textMuted = '#6B6B64'
Colors.Beige.headerBg = '#F5F0E8'

Elevation.sm = { shadowColor: '#000', shadowOffset: { width: 0, height: 2 },
                 shadowOpacity: 0.06, shadowRadius: 8, elevation: 2 }

<!-- Profile empty state spec from UI-SPEC.md -->
Trigger: !profile (no onboarding data found — AsyncStorage returned null)
Icon: '👤' at fontSize: 40
Headline: "Your health story starts here."
Body: "Complete your profile so Vitalspan can personalise your biomarker targets and flag relevant drug interactions."
CTA label: "Complete Onboarding"
CTA action: nav.navigate('Onboarding')

<!-- ProfileScreen current empty state — lines ~104-113 -->
Current: SafeAreaView > View style={s.empty} > Text "No profile found." + Text "Complete onboarding..."
Replace this entire early-return block with the motivating anatomy.

<!-- ProfileScreen current card analysis — lines ~378-387 -->
Current card: backgroundColor: Colors.bgCard, borderRadius: Radius.xl (correct!), overflow: 'hidden',
              manual shadow props (shadowOpacity: 0.06, shadowRadius: 12), no borderWidth/borderColor
Need: add borderWidth: 0.5, borderColor: Colors.Beige.border, replace manual shadow with ...Elevation.sm
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ProfileScreen stylesheet and replace empty state</name>
  <files>src/screens/ProfileScreen.tsx</files>
  <read_first>
    - src/screens/ProfileScreen.tsx (read full file — ~469 lines)
    - src/theme/index.ts (verify Elevation.sm, Radius.xl)
    - .planning/phases/06-warm-ui-overhaul/06-UI-SPEC.md (Card Spec, Section Header Spec, Status Bar Contract, Empty State Specification — Profile section)
  </read_first>
  <action>
    Apply to ProfileScreen.tsx:

    IMPORTS: Add Elevation to theme import:
      from: import { Colors, Spacing, Radius, Typography } from '../theme';
      to:   import { Colors, Spacing, Radius, Typography, Elevation } from '../theme';

    STATUS BAR: useFocusEffect is already imported from '@react-navigation/native'. Add:
      import { setStatusBarStyle } from 'expo-status-bar';

    Inside ProfileScreen function body, after the loadProfile callback, add status bar hook:
      useFocusEffect(useCallback(() => { setStatusBarStyle('dark'); return () => {}; }, []));

    NOTE: useCallback is already imported. The loadProfile useFocusEffect is at line 57. Add the status bar useFocusEffect as a separate call after it.

    PROFILE EMPTY STATE: The current empty state is an early return block (lines ~104-113):
      if (!profile) { return (<SafeAreaView><View style={s.empty}><Text>No profile found.</Text><Text>Complete onboarding...</Text></View></SafeAreaView>); }

    Replace the entire early-return block with a full motivating state:
      if (!profile) {
        return (
          <SafeAreaView style={s.safe}>
            <View style={s.topBar}>
              <Text style={s.screenTitle}>Profile</Text>
            </View>
            <View style={s.emptyStateCard}>
              <Text style={s.emptyStateIcon}>👤</Text>
              <Text style={s.emptyStateHeadline}>Your health story starts here.</Text>
              <Text style={s.emptyStateBody}>
                Complete your profile so Vitalspan can personalise your biomarker targets and flag relevant drug interactions.
              </Text>
              <TouchableOpacity
                style={s.emptyStateCta}
                onPress={() => nav.navigate('Onboarding')}
                activeOpacity={0.82}
              >
                <Text style={s.emptyStateCtaTxt}>Complete Onboarding</Text>
              </TouchableOpacity>
            </View>
          </SafeAreaView>
        );
      }

    STYLESHEET token substitutions throughout s = StyleSheet.create:
      Colors.bg          → Colors.Beige.bg
      Colors.bgCard      → Colors.Beige.card
      Colors.bgSecondary → Colors.Beige.bgShade
      Colors.textPrimary → Colors.Beige.text
      Colors.textSecondary → Colors.Beige.textSecondary
      Colors.textMuted   → Colors.Beige.textMuted
      Colors.border      → Colors.Beige.border

    CARD STYLE: Current `card` already has borderRadius: Radius.xl and overflow: 'hidden' — these are correct. Add:
      - borderWidth: 0.5
      - borderColor: Colors.Beige.border
      - Replace manual shadow props (shadowColor: '#000', shadowOffset: ..., shadowOpacity: 0.06, shadowRadius: 12, elevation: 2) with ...Elevation.sm spread
      - Change backgroundColor: Colors.bgCard → Colors.Beige.card

    SETTINGS CARD and ABOUT CARD: Both use manual shadow props and Colors.bgCard. Replace manual shadows with ...Elevation.sm, add borderWidth: 0.5 and borderColor: Colors.Beige.border, change Colors.bgCard → Colors.Beige.card.

    ROW BORDER: `rowBorder` uses Colors.border → Colors.Beige.divider.

    SECTION LABEL: Change Colors.textMuted → Colors.Beige.textMuted.

    HEADER / TOP BAR: Add backgroundColor: Colors.Beige.bg to `topBar` and `editHeader` styles.

    AVATAR: backgroundColor: Colors.primaryBg, borderColor: Colors.primaryBorder — keep (semantic).

    AGE BUTTON: `ageBtn` uses Colors.bg and Colors.border → Colors.Beige.bg (or bgShade) and Colors.Beige.border.

    SEX BUTTON: `sexBtn` uses Colors.bg, Colors.border → Colors.Beige.bgShade, Colors.Beige.border.

    CONDITION BUTTONS: `condBtn` uses Colors.bgCard, Colors.border → Colors.Beige.card, Colors.Beige.border.

    EDIT NOTE: Uses Colors.bgCard, Colors.border → Colors.Beige.card, Colors.Beige.border.

    EMPTY styles (the OLD s.empty, s.emptyTxt, s.emptySub) — these are now replaced by the motivating state. Remove these three old styles from the StyleSheet to avoid dead code. Add the new emptyState* styles instead.

    NEW STYLES to add to StyleSheet:
      emptyStateCard: {
        marginHorizontal: Spacing.base,
        marginTop: Spacing.xl,
        backgroundColor: Colors.Beige.card,
        borderRadius: Radius.xl,
        borderWidth: 0.5,
        borderColor: Colors.Beige.border,
        ...Elevation.sm,
        padding: Spacing.xl,
        alignItems: 'center',
        overflow: 'hidden',
      },
      emptyStateIcon: {
        fontSize: 40,
        marginBottom: Spacing.md,
      },
      emptyStateHeadline: {
        fontSize: 18,
        fontWeight: '600',
        color: Colors.Beige.text,
        textAlign: 'center',
        lineHeight: 24,
        marginBottom: Spacing.sm,
      },
      emptyStateBody: {
        fontSize: 14,
        fontWeight: '400',
        color: Colors.Beige.textSecondary,
        textAlign: 'center',
        lineHeight: 22,
        marginBottom: Spacing.lg,
      },
      emptyStateCta: {
        backgroundColor: Colors.primary,
        borderRadius: Radius.xl,
        minHeight: 44,
        alignSelf: 'stretch',
        justifyContent: 'center',
        alignItems: 'center',
        paddingHorizontal: Spacing.base,
      },
      emptyStateCtaTxt: {
        color: Colors.Beige.card,
        fontSize: 14,
        fontWeight: '600',
      },

    INLINE STYLES in JSX: ProfileScreen has an inline `{ fontSize: 18 }` for the settings gear icon emoji — leave this as-is (decorative icon size, not a spacing/color value). The `{ color: Colors.primary }` on the biological age value row is an inline dynamic style — change Colors.primary to Colors.primary (already correct — keep it).

    Do NOT change: Colors.primary, Colors.primaryBg, Colors.primaryBorder, Colors.primaryDark, Colors.primaryLight, Colors.danger, Colors.status.*.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep -c "Colors\.Beige\." src/screens/ProfileScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ProfileScreen.tsx contains `Colors.Beige.bg`
    - ProfileScreen.tsx contains `Colors.Beige.card`
    - ProfileScreen.tsx contains `Colors.Beige.textMuted`
    - ProfileScreen.tsx contains `...Elevation.sm`
    - ProfileScreen.tsx contains `setStatusBarStyle('dark')`
    - ProfileScreen.tsx contains `"Your health story starts here."`
    - ProfileScreen.tsx contains `"Complete Onboarding"` as CTA label
    - ProfileScreen.tsx contains `nav.navigate('Onboarding')` in empty state CTA
    - ProfileScreen.tsx does NOT contain `Colors.bgCard` or `Colors.bgSecondary` or `Colors.textPrimary` as standalone references
    - ProfileScreen.tsx does NOT contain the old `s.emptyTxt` or `s.emptySub` styles or JSX
    - grep -c "'#[0-9A-Fa-f]" src/screens/ProfileScreen.tsx returns 0
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    ProfileScreen uses Beige tokens throughout. Old two-line empty state replaced with full motivating anatomy. Status bar dark on focus. No hardcoded hex. tsc passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full phase verification — tsc + grep audit across all 7 screens</name>
  <files></files>
  <read_first>
    - .planning/phases/06-warm-ui-overhaul/06-UI-SPEC.md (Success Criteria section and Dark Screens exclusion table)
  </read_first>
  <action>
    Run a comprehensive automated verification pass across the entire Phase 6 scope.

    Verify all 7 warm screens have Colors.Beige.* tokens and no legacy token leaks.
    Verify dark screens are untouched (THEME-03 compliance).
    Run TypeScript compilation.
    Grep audit for hardcoded hex values across all modified files.

    This is a verification-only task — no code changes. If any verification fails, fix the specific file before proceeding to the human checkpoint.
  </action>
  <verify>
    <automated>
      cd /Users/bekircemkusdemir/Downloads/vitalspan &&
      echo "=== Beige token coverage ===" &&
      for f in src/screens/SettingsScreen.tsx src/screens/AboutScreen.tsx src/screens/ProtocolScreen.tsx src/screens/ExerciseScreen.tsx src/screens/ProfileScreen.tsx src/screens/BiomarkerDetailScreen.tsx src/screens/BiomarkerEntryScreen.tsx; do
        echo "$f: $(grep -c 'Colors\.Beige\.' $f) Beige refs";
      done &&
      echo "=== Legacy token check (should be 0 lines each) ===" &&
      grep -rn "Colors\.bgCard\|Colors\.bgSecondary\|Colors\.textPrimary\b\|Colors\.textMuted," \
        src/screens/SettingsScreen.tsx src/screens/AboutScreen.tsx src/screens/ProtocolScreen.tsx \
        src/screens/ExerciseScreen.tsx src/screens/ProfileScreen.tsx \
        src/screens/BiomarkerDetailScreen.tsx src/screens/BiomarkerEntryScreen.tsx || echo "PASS: no legacy tokens" &&
      echo "=== Hardcoded hex check (should be 0 lines each) ===" &&
      grep -rn "'#[0-9A-Fa-f]" \
        src/screens/SettingsScreen.tsx src/screens/AboutScreen.tsx src/screens/ProtocolScreen.tsx \
        src/screens/ExerciseScreen.tsx src/screens/ProfileScreen.tsx \
        src/screens/BiomarkerDetailScreen.tsx src/screens/BiomarkerEntryScreen.tsx || echo "PASS: no hex" &&
      echo "=== Dark screen unchanged check ===" &&
      grep -c "Colors\.Beige\." src/screens/DashboardScreen.tsx 2>/dev/null || echo "PASS: Dashboard untouched" &&
      grep -c "Colors\.Beige\." src/screens/LongevityScoreScreen.tsx 2>/dev/null || echo "PASS: LongevityScore untouched" &&
      echo "=== TypeScript ===" &&
      npx tsc --noEmit 2>&1 | head -30
    </automated>
  </verify>
  <acceptance_criteria>
    - All 7 warm screens have at least 5 Colors.Beige.* references each
    - grep for Colors.bgCard, Colors.bgSecondary, Colors.textPrimary in warm screen files returns zero results
    - grep for hardcoded hex in warm screen files returns zero results
    - DashboardScreen.tsx and LongevityScoreScreen.tsx have 0 Colors.Beige.* references (dark screens untouched)
    - tsc --noEmit exits 0 (zero TypeScript errors)
  </acceptance_criteria>
  <done>
    Automated verification confirms: all 7 warm screens use Beige tokens, zero legacy token leaks, zero hardcoded hex, dark screens untouched, TypeScript clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 7 warm screens migrated to Beige tokens across Plans 01-05:
    1. SettingsScreen — Beige bg, card, status bar dark
    2. AboutScreen — Beige bg, card, status bar dark
    3. ProtocolScreen — Beige bg, upgraded empty state, status bar dark
    4. BiomarkerDetailScreen — Beige bg, card spec normalized
    5. BiomarkerEntryScreen — Beige bg, status bar added
    6. ExerciseScreen — Beige bg, new empty state "Move daily. Live longer."
    7. ProfileScreen — Beige bg, new empty state "Your health story starts here."

    Dark screens (Dashboard, LongevityScore, Landing) are untouched.
  </what-built>
  <how-to-verify>
    Run on iOS simulator (or device):

    1. Open the app. Navigate to Dashboard tab — confirm it still shows the dark neural aesthetic with dark background and neural grid. No beige elements.

    2. Navigate to Biomarkers tab (BiomarkerDetailScreen) — confirm warm cream/beige background, white card surfaces, dark text.

    3. Navigate to Protocol tab — confirm warm beige background. If no medications/supplements added, confirm the empty state shows: 💊 icon, "Build your longevity stack." headline, "Get Started" button.

    4. Navigate to Exercise tab — confirm warm beige background. If no logs exist, confirm the empty state shows: 🏃 icon, "Move daily. Live longer." headline, "Log a Workout" button. Tap "Log a Workout" to confirm it opens the exercise log modal.

    5. Navigate to Profile tab. If no profile found, confirm: 👤 icon, "Your health story starts here." headline, "Complete Onboarding" button. If profile exists, confirm warm beige background, white card surfaces.

    6. From Profile, tap the settings gear → Settings modal. Confirm: warm beige background, white cards, "Done" button in green.

    7. From Settings, tap "About Vitalspan" → About modal. Confirm: warm beige background, white section cards.

    8. Tap back to Dashboard. Confirm it immediately returns to dark neural aesthetic (status bar style should be light/white).

    9. Navigate to LongevityScore (tap the score card on Dashboard) — confirm dark immersive modal (not beige).

    Expected: All warm screens display cream/beige backgrounds. Dark screens retain dark neural aesthetic. Status bar text is dark (readable) on warm screens, light on dark screens.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all screens look correct.
    Describe any issues found (e.g. "Protocol empty state missing period in headline" or "Dashboard has beige tint on card").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage → ProfileScreen | Profile data rendered in read view; edit mode writes validated fields back to storage |
| nav.navigate('Onboarding') | Empty state CTA navigates to onboarding — uses existing navigation param list; no new param injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | Profile name/age TextInput in edit mode | accept | Pre-existing validation (name.trim() check); no new input paths |
| T-06-06 | Spoofing | Empty state CTA nav.navigate('Onboarding') | accept | Navigation within app stack only; no external URL; route is pre-registered in RootStackParamList |
| T-06-SC | Tampering | npm installs | accept | No package installs |
</threat_model>

<verification>
Run after Task 2 automation and before human checkpoint:

```bash
cd /Users/bekircemkusdemir/Downloads/vitalspan

# Full phase audit
echo "=== Warm screen Beige coverage ==="
for f in \
  src/screens/SettingsScreen.tsx \
  src/screens/AboutScreen.tsx \
  src/screens/ProtocolScreen.tsx \
  src/screens/ExerciseScreen.tsx \
  src/screens/ProfileScreen.tsx \
  src/screens/BiomarkerDetailScreen.tsx \
  src/screens/BiomarkerEntryScreen.tsx; do
  count=$(grep -c 'Colors\.Beige\.' "$f" 2>/dev/null || echo 0)
  echo "  $f: $count refs"
done

echo "=== Dark screens untouched ==="
grep -c "Colors\.Beige\." src/screens/DashboardScreen.tsx 2>/dev/null && echo "FAIL: Dashboard has Beige" || echo "PASS: Dashboard untouched"
grep -c "Colors\.Beige\." src/screens/LongevityScoreScreen.tsx 2>/dev/null && echo "FAIL: LongevityScore has Beige" || echo "PASS: LongevityScore untouched"

echo "=== textMuted fix ==="
grep "textMuted" src/theme/index.ts

echo "=== TypeScript ==="
npx tsc --noEmit 2>&1 | head -30
```
</verification>

<success_criteria>
Phase 6 complete when:
1. ProfileScreen uses Colors.Beige.* throughout with empty state "Your health story starts here."
2. All 7 warm screens pass the grep audit: zero Colors.bgCard, Colors.textPrimary, hardcoded hex
3. DashboardScreen.tsx and LongevityScoreScreen.tsx have zero Colors.Beige.* references
4. tsc --noEmit exits 0 across the entire project
5. Human checkpoint approved: visual appearance confirmed correct on all warm and dark screens
</success_criteria>

<output>
Create `.planning/phases/06-warm-ui-overhaul/06-05-SUMMARY.md` when done
</output>
