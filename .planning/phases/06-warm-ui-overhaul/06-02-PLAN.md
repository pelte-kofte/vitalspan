---
phase: 06-warm-ui-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/ProtocolScreen.tsx
autonomous: true
requirements:
  - THEME-02
  - THEME-04
  - THEME-05
  - THEME-06

must_haves:
  truths:
    - "ProtocolScreen renders with Colors.Beige.bg background and Colors.Beige.card card surfaces"
    - "ProtocolScreen card has borderWidth: 0.5, Radius.xl, and Elevation.sm spread (no manual shadow props)"
    - "Protocol empty state (totalItems === 0) shows full anatomy: 💊 icon, correct headline/body/CTA copy, Elevation.sm card"
    - "ProtocolScreen sets status bar to dark on focus via useFocusEffect"
    - "No hardcoded hex values in ProtocolScreen.tsx"
    - "tsc --noEmit passes"
  artifacts:
    - path: "src/screens/ProtocolScreen.tsx"
      provides: "Warm-themed Protocol tab with upgraded empty state"
      contains: "Colors.Beige.bg"
    - path: "src/screens/ProtocolScreen.tsx"
      provides: "Elevation.sm card spec"
      contains: "Elevation.sm"
    - path: "src/screens/ProtocolScreen.tsx"
      provides: "Empty state with correct anatomy"
      contains: "Move daily. Live longer."
  key_links:
    - from: "src/screens/ProtocolScreen.tsx"
      to: "src/theme/index.ts"
      via: "Elevation import"
      pattern: "Elevation\\.sm"
    - from: "emptyScreenCard style"
      to: "totalItems === 0 JSX branch"
      via: "s.emptyScreenCard"
      pattern: "emptyScreenCard"
---

<objective>
Migrate ProtocolScreen to warm Beige tokens and upgrade its existing empty state to match the full anatomy spec from UI-SPEC.md.

Purpose: ProtocolScreen is the most complex warm screen — it has a modal sheet, custom supplement modal, and an existing empty state card that needs style normalization. Parallelized with Plan 01 since the only shared dependency is theme/index.ts (which Plan 01 fixes, but ProtocolScreen can be planned against the corrected token values without waiting).

Output: ProtocolScreen.tsx fully migrated. Empty state card at Radius.xl, Elevation.sm, Spacing.xl padding, correct copy, full-width 44px-tall CTA. Both the main `s` and modal `ms` StyleSheet blocks updated.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/06-warm-ui-overhaul/06-UI-SPEC.md

<interfaces>
<!-- Token values — exact names to use -->
Colors.Beige.bg = '#EDE8DC'
Colors.Beige.card = '#FFFFFF'
Colors.Beige.bgShade = '#E4E0D4'
Colors.Beige.border = '#D4CFC4'
Colors.Beige.divider = '#C8C0B0'
Colors.Beige.text = '#1A1A18'
Colors.Beige.textSecondary = '#4A4A45'
Colors.Beige.textMuted = '#6B6B64'   -- after Plan 01 fix

Elevation.sm = { shadowColor: '#000', shadowOffset: { width: 0, height: 2 },
                 shadowOpacity: 0.06, shadowRadius: 8, elevation: 2 }

Card spec: backgroundColor: Colors.Beige.card, borderRadius: Radius.xl (20),
           borderWidth: 0.5, borderColor: Colors.Beige.border,
           ...Elevation.sm, overflow: 'hidden'

Empty state card: same card spec + padding: Spacing.xl (24), alignItems: 'center', marginHorizontal: Spacing.base

Empty state CTA button: backgroundColor: Colors.primary, borderRadius: Radius.xl,
                         minHeight: 44, alignSelf: 'stretch', justifyContent: 'center', alignItems: 'center'

<!-- Protocol empty state copy (from UI-SPEC Copywriting Contract) -->
Icon: '💊' at fontSize 40
Headline: "Build your longevity stack."
Body: "Add your medications and pharmacist-curated supplements to track your daily protocol and check for interactions."
CTA label: "Get Started"
CTA action: setShowRecommendedSheet(true)
Trigger: totalItems === 0
</interfaces>

Note: This plan CAN be executed before Plan 01 completes since it writes different files. However, run it after Plan 01 has fixed Colors.Beige.textMuted in theme/index.ts to ensure the token value is correct when verifying.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ProtocolScreen main stylesheet to Beige tokens</name>
  <files>src/screens/ProtocolScreen.tsx</files>
  <read_first>
    - src/screens/ProtocolScreen.tsx (read full file — ~929 lines — with focus on StyleSheet block at lines ~733-868 and modal ms block at lines ~871-928)
    - src/theme/index.ts (verify Elevation.sm, Radius.xl, token names)
    - .planning/phases/06-warm-ui-overhaul/06-UI-SPEC.md (Card Spec, Section Header Spec, Status Bar Contract)
  </read_first>
  <action>
    Apply to ProtocolScreen.tsx:

    IMPORTS: Change theme import to add Elevation:
      from: import { Colors, Spacing, Radius, Typography } from '../theme';
      to:   import { Colors, Spacing, Radius, Typography, Elevation } from '../theme';

    STATUS BAR: useFocusEffect is already imported from '@react-navigation/native'. Add:
      import { setStatusBarStyle } from 'expo-status-bar';
      import { useCallback } from 'react';  (add to React destructure if not already there — useCallback is already imported; just add setStatusBarStyle import)

    Inside ProtocolScreen function body, after the loadData callback definition, add:
      useFocusEffect(useCallback(() => { setStatusBarStyle('dark'); return () => {}; }, []));

    Note: useFocusEffect is already called on line 353 for data loading — add a second separate useFocusEffect call for the status bar. Both calls are valid.

    MAIN STYLESHEET (s = StyleSheet.create) — token substitutions:
      Colors.bg          → Colors.Beige.bg
      Colors.bgCard      → Colors.Beige.card
      Colors.bgSecondary → Colors.Beige.bgShade
      Colors.textPrimary → Colors.Beige.text
      Colors.textSecondary → Colors.Beige.textSecondary
      Colors.textMuted   → Colors.Beige.textMuted
      Colors.border      → Colors.Beige.border (for card borders and separators)

    CARD STYLE: The `card` style has borderRadius: Radius.lg (wrong), borderWidth: 1 (wrong), padding: Spacing.md, and manual shadow props. Replace with:
      card: {
        marginHorizontal: Spacing.base,
        backgroundColor: Colors.Beige.card,
        borderRadius: Radius.xl,
        borderWidth: 0.5,
        borderColor: Colors.Beige.border,
        ...Elevation.sm,
        overflow: 'hidden',
        padding: Spacing.md,
      }
    Remove all shadowColor, shadowOffset, shadowOpacity, shadowRadius, elevation manual props.

    ROW BORDER: Change `rowBorder` to use Colors.Beige.divider (not Colors.Beige.border) since these are row separators.

    SECTION LABEL: `sectionLabel` fontWeight is '500' — change to '600' per spec. Change color to Colors.Beige.textMuted.

    REMOVE BUTTONS / CHIPS: `removeBtn` uses Colors.bgSecondary → Colors.Beige.bgShade. Colors.border → Colors.Beige.border.

    TIME CHIPS: `timeChip` uses Colors.bgSecondary → Colors.Beige.bgShade. Colors.border → Colors.Beige.border.

    MODAL STYLESHEET (ms = StyleSheet.create) — same substitutions:
      Colors.bgCard      → Colors.Beige.card
      Colors.bgSecondary → Colors.Beige.bgShade
      Colors.textPrimary → Colors.Beige.text
      Colors.textMuted   → Colors.Beige.textMuted
      Colors.border      → Colors.Beige.border

    MODAL SHEET: The `sheet` style uses Colors.bgCard → Colors.Beige.card.
    MODAL HANDLE: The `handle` style uses Colors.border → Colors.Beige.border.
    MODAL INPUT: The `input` style uses Colors.bgSecondary → Colors.Beige.bgShade. Colors.border → Colors.Beige.border. Colors.textPrimary → Colors.Beige.text.
    MODAL DB RESULTS: `dbResults` uses Colors.bgCard → Colors.Beige.card. Colors.border → Colors.Beige.border.
    TIMING CHIPS: `timingChip` uses Colors.bgSecondary → Colors.Beige.bgShade. Colors.border → Colors.Beige.border.
    CANCEL BTN in modal: Uses Colors.bgSecondary → Colors.Beige.bgShade. Colors.border → Colors.Beige.border. Colors.textMuted → Colors.Beige.textMuted.

    Do NOT change: Colors.primary, Colors.primaryBg, Colors.primaryBorder, Colors.primaryLight, Colors.primaryDark, Colors.warning*, Colors.danger, Colors.dangerBg — these are semantic colors.

    The placeholderTextColor in JSX TextInput props (inline dynamic styles) must also be updated:
      Colors.textMuted → Colors.Beige.textMuted
    (these are inline styles which are exempt from StyleSheet-only rule since they are dynamic prop values passed to native components)
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep -c "Colors\.Beige\." src/screens/ProtocolScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ProtocolScreen.tsx contains `Colors.Beige.bg`
    - ProtocolScreen.tsx contains `Colors.Beige.card`
    - ProtocolScreen.tsx contains `Colors.Beige.textMuted`
    - ProtocolScreen.tsx contains `...Elevation.sm`
    - ProtocolScreen.tsx contains `borderWidth: 0.5` in the `card` style (not borderWidth: 1)
    - ProtocolScreen.tsx contains `borderRadius: Radius.xl` in the `card` style (not Radius.lg)
    - ProtocolScreen.tsx contains `setStatusBarStyle('dark')`
    - ProtocolScreen.tsx does NOT contain `Colors.bgCard` or `Colors.bgSecondary` or `Colors.textPrimary` or standalone `Colors.textMuted,` (verify no unreplaced legacy references)
    - grep -c "'#[0-9A-Fa-f]" src/screens/ProtocolScreen.tsx returns 0 (no hardcoded hex)
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    ProtocolScreen main stylesheet and modal stylesheet fully migrated to Beige tokens. Card spec normalized to Radius.xl / borderWidth: 0.5 / Elevation.sm. Status bar dark on focus. No hardcoded hex.
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade Protocol empty state card to full anatomy spec</name>
  <files>src/screens/ProtocolScreen.tsx</files>
  <read_first>
    - src/screens/ProtocolScreen.tsx (focus on the emptyScreenCard JSX block ~lines 544-559 and emptyScreenCard StyleSheet styles ~lines 820-860)
    - .planning/phases/06-warm-ui-overhaul/06-UI-SPEC.md (Empty State Specification — Protocol section and Shared Empty State Anatomy)
  </read_first>
  <action>
    The Protocol empty state is triggered when totalItems === 0 and renders a View with style s.emptyScreenCard. The copy is already correct. What needs updating is the styles and one copy detail.

    JSX CHANGES: The existing empty state JSX (lines ~544-559):
    1. The emoji icon Text currently has inline style `{ fontSize: 32, textAlign: 'center', marginBottom: Spacing.md }` — change fontSize from 32 to 40 per spec.
    2. The CTA button text currently reads "Get started →" — change to "Get Started" (capital S, no arrow) per Copywriting Contract.
    3. The headline text currently reads "Build your longevity stack" (no period) — ensure it reads "Build your longevity stack." (with period per Copywriting Contract).

    STYLE CHANGES — `emptyScreenCard` style block:
    Current state has: borderRadius: Radius.lg (wrong), borderWidth: 1 (wrong), borderColor: Colors.border (replace), backgroundColor: Colors.bgCard (replace).
    Replace the entire emptyScreenCard style with:
      emptyScreenCard: {
        marginHorizontal: Spacing.base,
        backgroundColor: Colors.Beige.card,
        borderRadius: Radius.xl,
        borderWidth: 0.5,
        borderColor: Colors.Beige.border,
        ...Elevation.sm,
        padding: Spacing.xl,
        alignItems: 'center',
        marginBottom: Spacing.base,
        overflow: 'hidden',
      }

    STYLE CHANGES — `emptyScreenHeadline`:
    Current: fontSize: Typography.sizes.h3 (which resolves to 18 — correct per spec)
    Change color: Colors.textPrimary → Colors.Beige.text

    STYLE CHANGES — `emptyScreenSubtext`:
    Change color: Colors.textSecondary → Colors.Beige.textSecondary
    Set lineHeight: 22 explicitly.

    STYLE CHANGES — `emptyScreenCta`:
    Replace with: backgroundColor: Colors.primary, borderRadius: Radius.xl, minHeight: 44, height: 44, justifyContent: 'center', alignItems: 'center', alignSelf: 'stretch', paddingHorizontal: Spacing.base

    STYLE CHANGES — `emptyScreenCtaTxt`:
    Change color: Colors.primaryBg → Colors.Beige.card (white text on green button). Keep fontWeight: '600'.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep -A5 "emptyScreenCta:" src/screens/ProtocolScreen.tsx | head -10</automated>
  </verify>
  <acceptance_criteria>
    - ProtocolScreen.tsx emptyScreenCard style contains `borderRadius: Radius.xl` (not Radius.lg)
    - ProtocolScreen.tsx emptyScreenCard style contains `borderWidth: 0.5` (not 1)
    - ProtocolScreen.tsx emptyScreenCard style contains `...Elevation.sm`
    - ProtocolScreen.tsx emptyScreenCard style contains `padding: Spacing.xl`
    - ProtocolScreen.tsx emptyScreenCard style contains `Colors.Beige.card`
    - The empty state icon text has fontSize 40 (not 32)
    - The CTA button text reads "Get Started" (capital S, no arrow)
    - The headline contains the period: "Build your longevity stack."
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    Protocol empty state card matches the full anatomy spec: 💊 icon at fontSize 40, correct headline with period, correct body copy, Elevation.sm card, full-width 44px CTA button reading "Get Started".
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage → Protocol state | Protocol reads user medications and supplement list — display only |
| Alert dialogs | Remove supplement and interaction checker alerts are pre-existing; no new trust boundaries added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | Protocol supplement modal TextInput | accept | Name/dose fields are local state only; no server writes in this phase |
| T-06-SC | Tampering | npm installs | accept | No package installs — pure token migration |
</threat_model>

<verification>
Run after both tasks complete:

```bash
cd /Users/bekircemkusdemir/Downloads/vitalspan

# 1. Verify Beige tokens present
grep -c "Colors\.Beige\." src/screens/ProtocolScreen.tsx

# 2. Check card spec
grep -A8 "^  card:" src/screens/ProtocolScreen.tsx | grep "borderWidth\|borderRadius\|Elevation"

# 3. Verify empty state copy
grep -n "Get Started\|longevity stack\." src/screens/ProtocolScreen.tsx

# 4. Verify no legacy tokens
grep -n "Colors\.bgCard\|Colors\.bgSecondary\|Colors\.textPrimary\b\|Colors\.textMuted," src/screens/ProtocolScreen.tsx

# 5. Verify no hardcoded hex
grep -n "'#[0-9A-Fa-f]" src/screens/ProtocolScreen.tsx

# 6. TypeScript
npx tsc --noEmit 2>&1 | head -20
```
</verification>

<success_criteria>
1. ProtocolScreen uses Colors.Beige.* throughout — zero legacy Colors.bg, Colors.bgCard, Colors.textPrimary references
2. Card spec: Radius.xl, borderWidth: 0.5, Elevation.sm spread, Colors.Beige.card, Colors.Beige.border
3. Empty state: fontSize 40 icon, "Build your longevity stack." headline, "Get Started" CTA, Elevation.sm card
4. Status bar: setStatusBarStyle('dark') in useFocusEffect
5. Zero hardcoded hex values
6. tsc --noEmit passes
</success_criteria>

<output>
Create `.planning/phases/06-warm-ui-overhaul/06-02-SUMMARY.md` when done
</output>
