---
phase: 06-warm-ui-overhaul
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/screens/BiomarkerDetailScreen.tsx
  - src/screens/BiomarkerEntryScreen.tsx
autonomous: true
requirements:
  - THEME-02
  - THEME-04
  - THEME-05

must_haves:
  truths:
    - "BiomarkerDetailScreen renders with Colors.Beige.bg background and Colors.Beige.card surfaces"
    - "BiomarkerEntryScreen renders with Colors.Beige.bg background and Colors.Beige.card surfaces"
    - "Both screens set status bar to dark on focus via useFocusEffect"
    - "Card styles use Radius.xl, borderWidth: 0.5, Elevation.sm (no manual shadow props)"
    - "No hardcoded hex values in either file"
    - "tsc --noEmit passes"
  artifacts:
    - path: "src/screens/BiomarkerDetailScreen.tsx"
      provides: "Warm-themed biomarker list and detail view"
      contains: "Colors.Beige.bg"
    - path: "src/screens/BiomarkerEntryScreen.tsx"
      provides: "Warm-themed biomarker entry modal"
      contains: "Colors.Beige.bg"
  key_links:
    - from: "src/screens/BiomarkerDetailScreen.tsx"
      to: "src/theme/index.ts"
      via: "Elevation import"
      pattern: "Elevation\\.sm"
    - from: "src/screens/BiomarkerEntryScreen.tsx"
      to: "src/theme/index.ts"
      via: "Elevation import"
      pattern: "Elevation\\.sm"
---

<objective>
Migrate BiomarkerDetailScreen and BiomarkerEntryScreen to warm Beige tokens. Both are Wave 2 because they depend on the textMuted fix landing in Wave 1 (Plan 01). BiomarkerDetailScreen is a tab screen with list + detail views; BiomarkerEntryScreen is a modal with a biomarker search + entry form.

Purpose: After Plans 01 and 02 establish the migration pattern, this plan applies the same process to the two biomarker screens. BiomarkerEntryScreen needs useFocusEffect added (currently absent) for status bar management.

Output: Both screen files fully migrated. BiomarkerDetailScreen card spec normalized. BiomarkerEntryScreen gets useFocusEffect status bar wiring.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/06-warm-ui-overhaul/06-UI-SPEC.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/06-warm-ui-overhaul/06-01-SUMMARY.md

<interfaces>
<!-- Confirmed token values after Plan 01 -->
Colors.Beige.bg = '#EDE8DC'
Colors.Beige.card = '#FFFFFF'
Colors.Beige.bgShade = '#E4E0D4'
Colors.Beige.border = '#D4CFC4'
Colors.Beige.divider = '#C8C0B0'
Colors.Beige.text = '#1A1A18'
Colors.Beige.textSecondary = '#4A4A45'
Colors.Beige.textMuted = '#6B6B64'   -- fixed by Plan 01
Colors.Beige.headerBg = '#F5F0E8'

Elevation.sm = { shadowColor: '#000', shadowOffset: { width: 0, height: 2 },
                 shadowOpacity: 0.06, shadowRadius: 8, elevation: 2 }

Radius.xl = 20 (card border radius)

<!-- BiomarkerDetailScreen existing card analysis -->
Current card: borderRadius: 20 (hardcoded), shadowColor: '#000' (manual shadow), padding: Spacing.md
Need: borderRadius: Radius.xl, borderWidth: 0.5, borderColor: Colors.Beige.border, ...Elevation.sm, overflow: 'hidden'

<!-- BiomarkerEntryScreen status bar gap -->
Missing: useFocusEffect, setStatusBarStyle — must be added
Currently imports: useNavigation, useRoute from '@react-navigation/native'
Must add: useFocusEffect to @react-navigation/native import
Must add: import { setStatusBarStyle } from 'expo-status-bar'
Must add: useCallback to React import (if not already there — check file)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate BiomarkerDetailScreen to warm Beige tokens</name>
  <files>src/screens/BiomarkerDetailScreen.tsx</files>
  <read_first>
    - src/screens/BiomarkerDetailScreen.tsx (read full file — ~478 lines — with focus on StyleSheet at lines ~374-477)
    - src/theme/index.ts (verify Elevation.sm, Radius.xl)
    - .planning/phases/06-warm-ui-overhaul/06-UI-SPEC.md (Card Spec, Status Bar Contract, Screen-by-Screen Token Mapping)
  </read_first>
  <action>
    Apply to BiomarkerDetailScreen.tsx:

    IMPORTS: Add Elevation to theme import:
      from: import { Colors, Spacing, Radius, Typography } from '../theme';
      to:   import { Colors, Spacing, Radius, Typography, Elevation } from '../theme';

    STATUS BAR: useFocusEffect is already imported from '@react-navigation/native'. Add:
      import { setStatusBarStyle } from 'expo-status-bar';

    useCallback is already imported. Inside BiomarkerDetailScreen, add a useFocusEffect call for status bar. Place it after the loadEntries callback, before the saveEdit function:
      useFocusEffect(useCallback(() => { setStatusBarStyle('dark'); return () => {}; }, []));

    STYLESHEET token substitutions throughout s = StyleSheet.create:
      Colors.bg          → Colors.Beige.bg
      Colors.bgCard      → Colors.Beige.card
      Colors.bgSecondary → Colors.Beige.bgShade
      Colors.textPrimary → Colors.Beige.text
      Colors.textSecondary → Colors.Beige.textSecondary
      Colors.textMuted   → Colors.Beige.textMuted
      Colors.border      → Colors.Beige.border (for card borders)

    CARD STYLE: Current `card` has hardcoded borderRadius: 20, shadowColor: '#000' with manual shadow props, padding: Spacing.md. Replace shadow props with ...Elevation.sm spread, change borderRadius to Radius.xl, add borderWidth: 0.5 and borderColor: Colors.Beige.border:
      card: {
        marginHorizontal: Spacing.base,
        backgroundColor: Colors.Beige.card,
        borderRadius: Radius.xl,
        borderWidth: 0.5,
        borderColor: Colors.Beige.border,
        ...Elevation.sm,
        overflow: 'hidden',
        padding: Spacing.md,
      }

    ROW BORDER: `rowBorder` uses Colors.border — change to Colors.Beige.divider (list row separators).

    SECTION LABEL `sectionLabel` and `catLabel` both use Colors.textMuted → Colors.Beige.textMuted.

    LIST HEADER: `listHeader` has no background — add backgroundColor: Colors.Beige.bg. (Header is a View in the list view.)

    DETAIL HEADER: Same — add backgroundColor: Colors.Beige.bg to `detailHeader` style.

    INSIGHT CARD: `insightCard` uses Colors.status.optimalBg — this is a semantic color, do NOT change.

    UPLOAD BUTTON: `uploadBtn` uses Colors.bgCard and Colors.border → Colors.Beige.card, Colors.Beige.border.

    TARGET BADGE: `targetBadge` uses Colors.border and Colors.bgCard → Colors.Beige.border, Colors.Beige.card.

    EDIT CONTROLS: `editInput` uses Colors.bgSecondary → Colors.Beige.bgShade. Uses Colors.primaryBorder — keep (semantic). `editPencilBtn` uses Colors.bgSecondary and Colors.border → Colors.Beige.bgShade, Colors.Beige.border. `editCancelBtn` same.

    EMPTY TAB CARD: The `emptyTabCard` currently uses Colors.bgCard, Colors.borderLight. Change to:
      - backgroundColor: Colors.Beige.card
      - borderColor: Colors.Beige.borderLight (note: Colors.Beige.borderLight = '#E2DED6')
      - borderRadius: Radius.xl (currently Radius.lg — change)
      - borderWidth: 0.5 (currently 1 — change)
      - Add ...Elevation.sm

    INLINE STYLES in JSX: The histRow/rangeHeaderRow inline colors referencing Colors.textPrimary or Colors.textMuted — change to Colors.Beige.text, Colors.Beige.textMuted.

    BACK BUTTON text: `back` style uses Colors.primaryLight — keep (semantic).

    Do NOT change: Colors.status.*, Colors.primary, Colors.primaryBg, Colors.primaryBorder, Colors.primaryLight, Colors.primaryDark, Colors.warning*, Colors.danger.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep -c "Colors\.Beige\." src/screens/BiomarkerDetailScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - BiomarkerDetailScreen.tsx contains `Colors.Beige.bg`
    - BiomarkerDetailScreen.tsx contains `Colors.Beige.card`
    - BiomarkerDetailScreen.tsx contains `Colors.Beige.textMuted`
    - BiomarkerDetailScreen.tsx contains `...Elevation.sm`
    - BiomarkerDetailScreen.tsx contains `borderWidth: 0.5` on the main `card` style
    - BiomarkerDetailScreen.tsx contains `borderRadius: Radius.xl` on the main `card` style
    - BiomarkerDetailScreen.tsx contains `setStatusBarStyle('dark')`
    - BiomarkerDetailScreen.tsx does NOT contain `Colors.bgCard` or `Colors.bgSecondary` or `Colors.textPrimary` as standalone references
    - grep -c "'#[0-9A-Fa-f]" src/screens/BiomarkerDetailScreen.tsx returns 0
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    BiomarkerDetailScreen uses Beige tokens throughout both the list and detail view. Card spec normalized. Status bar dark on focus. No hardcoded hex. tsc passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate BiomarkerEntryScreen to warm Beige tokens + add status bar</name>
  <files>src/screens/BiomarkerEntryScreen.tsx</files>
  <read_first>
    - src/screens/BiomarkerEntryScreen.tsx (read full file — ~345 lines — with focus on StyleSheet at lines ~294-344)
    - src/theme/index.ts (verify token names)
    - .planning/phases/06-warm-ui-overhaul/06-UI-SPEC.md (Card Spec, Status Bar Contract — BiomarkerEntry is a modal that presents over dark Dashboard)
  </read_first>
  <action>
    Apply to BiomarkerEntryScreen.tsx:

    IMPORTS: Add Elevation and useFocusEffect:
      Change theme import from:
        import { Colors, Spacing, Radius, Typography, Motion } from '../theme';
      to:
        import { Colors, Spacing, Radius, Typography, Motion, Elevation } from '../theme';

      Change @react-navigation/native import from:
        import { useNavigation, useRoute, RouteProp } from '@react-navigation/native';
      to:
        import { useNavigation, useRoute, RouteProp, useFocusEffect } from '@react-navigation/native';

      Add new import line:
        import { setStatusBarStyle } from 'expo-status-bar';

      Add useCallback to React import destructuring (currently imports React and useState):
        import React, { useState, useCallback } from 'react';

    STATUS BAR: Inside BiomarkerEntryScreen function body, after state declarations, add:
      useFocusEffect(useCallback(() => { setStatusBarStyle('dark'); return () => {}; }, []));

    STYLESHEET token substitutions throughout s = StyleSheet.create:
      Colors.bg          → Colors.Beige.bg
      Colors.bgCard      → Colors.Beige.card
      Colors.bgSecondary → Colors.Beige.bgShade
      Colors.textPrimary → Colors.Beige.text
      Colors.textSecondary → Colors.Beige.textSecondary
      Colors.textMuted   → Colors.Beige.textMuted
      Colors.border      → Colors.Beige.border
      Colors.borderLight → Colors.Beige.borderLight

    VALUE CARD: `valueCard` has borderRadius: Radius.lg, borderWidth: 0.5, borderColor: Colors.border. Change:
      - borderRadius: Radius.lg → Radius.xl
      - borderColor: Colors.border → Colors.Beige.border
      - backgroundColor: Colors.bgCard → Colors.Beige.card
      - Add ...Elevation.sm (currently has no shadow — add for consistency with card spec)

    PICK ROWS (biomarker selection list): `pickRow` uses Colors.bgCard → Colors.Beige.card.
    `pickRowBorder` uses Colors.border → Colors.Beige.divider.

    SEARCH INPUT: `searchInput` uses Colors.bgCard, Colors.border → Colors.Beige.card, Colors.Beige.border.

    CHIPS: `chip` uses Colors.border, Colors.bgCard → Colors.Beige.border, Colors.Beige.card.

    EXPLANATION CARD (BreathingCard wrapper): `explanationInner` uses Colors.bgCard, Colors.borderLight → Colors.Beige.card, Colors.Beige.borderLight.

    NOTES INPUT / DATE INPUT: `notesInput`, `customDateInput` use Colors.bgCard, Colors.border → Colors.Beige.card, Colors.Beige.border.

    UNIT CHIPS: `unitChip` uses Colors.border, Colors.bgCard → Colors.Beige.border, Colors.Beige.card.

    CAT BADGE: `catBadge` uses Colors.primaryBg, Colors.primaryBorder — keep (semantic).

    INLINE JSX placeholderTextColor references: Colors.textMuted → Colors.Beige.textMuted.

    Do NOT change: Colors.primary, Colors.primaryBg, Colors.primaryBorder, Colors.primaryDark, Colors.primaryLight, Colors.warning*, Colors.danger, Colors.dangerBg, Colors.status.*.

    Note: The cancel text `s.cancel` currently uses Colors.primaryLight — this may be changed to Colors.primary per the Navigation Header Spec (tint color = Colors.primary). Check UI-SPEC Navigation Header section — "Tint color (buttons, links): Colors.primary". Change Colors.primaryLight to Colors.primary for the cancel/back text.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep -c "Colors\.Beige\." src/screens/BiomarkerEntryScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - BiomarkerEntryScreen.tsx contains `Colors.Beige.bg`
    - BiomarkerEntryScreen.tsx contains `Colors.Beige.card`
    - BiomarkerEntryScreen.tsx contains `Colors.Beige.textMuted`
    - BiomarkerEntryScreen.tsx contains `setStatusBarStyle('dark')`
    - BiomarkerEntryScreen.tsx contains `useFocusEffect`
    - BiomarkerEntryScreen.tsx contains `...Elevation.sm` (on valueCard)
    - BiomarkerEntryScreen.tsx does NOT contain `Colors.bgCard` or `Colors.bgSecondary` or `Colors.textPrimary` as standalone references
    - grep -c "'#[0-9A-Fa-f]" src/screens/BiomarkerEntryScreen.tsx returns 0
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    BiomarkerEntryScreen uses Beige tokens throughout. Status bar wired to dark via new useFocusEffect hook. No hardcoded hex. tsc passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → AsyncStorage | BiomarkerEntryScreen writes float-parsed value to storage; parseFloat validation already exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | BiomarkerEntry value input | accept | Pre-existing parseFloat + NaN guard; no new input paths added |
| T-06-SC | Tampering | npm installs | accept | No package installs |
</threat_model>

<verification>
Run after both tasks complete:

```bash
cd /Users/bekircemkusdemir/Downloads/vitalspan

# 1. Verify Beige token coverage
grep -c "Colors\.Beige\." src/screens/BiomarkerDetailScreen.tsx
grep -c "Colors\.Beige\." src/screens/BiomarkerEntryScreen.tsx

# 2. Verify no legacy tokens
grep -n "Colors\.bgCard\|Colors\.bgSecondary\|Colors\.textPrimary\b" \
  src/screens/BiomarkerDetailScreen.tsx src/screens/BiomarkerEntryScreen.tsx

# 3. Verify no hardcoded hex
grep -n "'#[0-9A-Fa-f]" \
  src/screens/BiomarkerDetailScreen.tsx src/screens/BiomarkerEntryScreen.tsx

# 4. Verify status bar on both screens
grep -n "setStatusBarStyle" \
  src/screens/BiomarkerDetailScreen.tsx src/screens/BiomarkerEntryScreen.tsx

# 5. TypeScript
npx tsc --noEmit 2>&1 | head -20
```
</verification>

<success_criteria>
1. Both screens use Colors.Beige.* tokens throughout — no legacy Colors.bgCard, Colors.textPrimary
2. BiomarkerDetailScreen: card Radius.xl / borderWidth 0.5 / Elevation.sm
3. BiomarkerEntryScreen: useFocusEffect + setStatusBarStyle('dark') now present
4. Zero hardcoded hex in either file
5. tsc --noEmit passes
</success_criteria>

<output>
Create `.planning/phases/06-warm-ui-overhaul/06-03-SUMMARY.md` when done
</output>
