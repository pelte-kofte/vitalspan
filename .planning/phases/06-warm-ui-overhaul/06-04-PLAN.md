---
phase: 06-warm-ui-overhaul
plan: 04
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/screens/ExerciseScreen.tsx
autonomous: true
requirements:
  - THEME-02
  - THEME-04
  - THEME-05
  - THEME-06

must_haves:
  truths:
    - "ExerciseScreen renders with Colors.Beige.bg background and Colors.Beige.card surfaces"
    - "ExerciseScreen card has Radius.xl, borderWidth: 0.5, Elevation.sm (no manual shadow props)"
    - "ExerciseScreen shows motivating empty state when logs.length === 0: 🏃 icon, 'Move daily. Live longer.' headline, correct body, 'Log a Workout' CTA"
    - "ExerciseScreen sets status bar to dark on focus via useFocusEffect"
    - "No hardcoded hex values in ExerciseScreen.tsx"
    - "tsc --noEmit passes"
  artifacts:
    - path: "src/screens/ExerciseScreen.tsx"
      provides: "Warm-themed exercise screen with motivating empty state"
      contains: "Colors.Beige.bg"
    - path: "src/screens/ExerciseScreen.tsx"
      provides: "Exercise empty state"
      contains: "Move daily. Live longer."
  key_links:
    - from: "ExerciseScreen empty state JSX"
      to: "logs.length === 0 condition"
      via: "conditional render above activityCard"
      pattern: "logs\\.length === 0"
    - from: "src/screens/ExerciseScreen.tsx"
      to: "src/theme/index.ts"
      via: "Elevation import"
      pattern: "Elevation\\.sm"
---

<objective>
Migrate ExerciseScreen to warm Beige tokens, normalize the card spec, and add the motivating empty state (new feature — does not currently exist).

Purpose: ExerciseScreen currently has no empty state for logs.length === 0, only an "activityCard" that shows a placeholder message. The empty state is a new THEME-06 deliverable. This plan also wires the status bar (useFocusEffect already exists for data loading — add a second call for status bar).

Output: ExerciseScreen.tsx with Beige tokens, Elevation.sm cards, and a full empty state card with the specified copy and CTA.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/06-warm-ui-overhaul/06-UI-SPEC.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/06-warm-ui-overhaul/06-01-SUMMARY.md

<interfaces>
<!-- Token values after Plan 01 fix -->
Colors.Beige.bg = '#EDE8DC'
Colors.Beige.card = '#FFFFFF'
Colors.Beige.bgShade = '#E4E0D4'
Colors.Beige.border = '#D4CFC4'
Colors.Beige.divider = '#C8C0B0'
Colors.Beige.text = '#1A1A18'
Colors.Beige.textSecondary = '#4A4A45'
Colors.Beige.textMuted = '#6B6B64'

Elevation.sm = { shadowColor: '#000', shadowOffset: { width: 0, height: 2 },
                 shadowOpacity: 0.06, shadowRadius: 8, elevation: 2 }

<!-- Exercise empty state spec from UI-SPEC.md -->
Trigger: logs.length === 0 (no exercise logs at all)
Container: Colors.Beige.card, Radius.xl, ...Elevation.sm, marginHorizontal: Spacing.base, padding: Spacing.xl, alignItems: 'center'
Icon: Text emoji '🏃' at fontSize: 40, marginBottom: Spacing.md
Headline: "Move daily. Live longer." — fontSize: 18, fontWeight: '600', color: Colors.Beige.text, lineHeight: 24, textAlign: 'center', marginBottom: Spacing.sm
Body: "Log your first workout to start tracking your movement. Consistency compounds — even a 20-minute walk counts." — fontSize: 14, color: Colors.Beige.textSecondary, lineHeight: 22, textAlign: 'center', marginBottom: Spacing.lg
CTA: "Log a Workout" — backgroundColor: Colors.primary, borderRadius: Radius.xl, minHeight: 44, alignSelf: 'stretch', justifyContent: 'center', alignItems: 'center'
CTA action: Opens QuickLogModal with the first exercise in filtered list (EXERCISES[0]) OR sets selectedCat to 'All' and scrolls to library

<!-- ExerciseScreen current card analysis -->
Current main card: borderRadius: 20 (hardcoded), manual shadow props (shadowOpacity: 0.06, shadowRadius: 12), no borderWidth/borderColor
Current activityCard: manual shadow props (shadowOpacity: 0.05, shadowRadius: 12), borderRadius: 20 hardcoded, no border

<!-- Status bar -->
useFocusEffect already imported and used for loadLogs() on line 172.
Need to add a second useFocusEffect for status bar ONLY.
setStatusBarStyle import must be added.
useCallback already imported.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ExerciseScreen stylesheet to Beige tokens</name>
  <files>src/screens/ExerciseScreen.tsx</files>
  <read_first>
    - src/screens/ExerciseScreen.tsx (read full file — ~585 lines — with focus on StyleSheet at lines ~388-584)
    - src/theme/index.ts (verify Elevation.sm, Radius.xl)
    - .planning/phases/06-warm-ui-overhaul/06-UI-SPEC.md (Card Spec, Section Header Spec)
  </read_first>
  <action>
    Apply to ExerciseScreen.tsx:

    IMPORTS: Add Elevation to theme import:
      from: import { Colors, Spacing, Radius, Typography } from '../theme';
      to:   import { Colors, Spacing, Radius, Typography, Elevation } from '../theme';

    Add status bar import:
      import { setStatusBarStyle } from 'expo-status-bar';

    STATUS BAR: useFocusEffect is already imported. Inside ExerciseScreen function, after the loadLogs callback, add a separate useFocusEffect for status bar:
      useFocusEffect(useCallback(() => { setStatusBarStyle('dark'); return () => {}; }, []));
    This is a second useFocusEffect call, independent of the data-loading one at line 172.

    STYLESHEET token substitutions throughout s = StyleSheet.create:
      Colors.bg          → Colors.Beige.bg
      Colors.bgCard      → Colors.Beige.card
      Colors.bgSecondary → Colors.Beige.bgShade
      Colors.textPrimary → Colors.Beige.text
      Colors.textSecondary → Colors.Beige.textSecondary
      Colors.textMuted   → Colors.Beige.textMuted
      Colors.border      → Colors.Beige.border

    CARD STYLE: Current `card` has hardcoded borderRadius: 20, manual shadow props (shadowOpacity: 0.06, shadowRadius: 12), no borderWidth/borderColor. Replace with:
      card: {
        marginHorizontal: Spacing.base,
        backgroundColor: Colors.Beige.card,
        borderRadius: Radius.xl,
        borderWidth: 0.5,
        borderColor: Colors.Beige.border,
        ...Elevation.sm,
        overflow: 'hidden',
      }

    ACTIVITY CARD: `activityCard` has hardcoded borderRadius: 20, manual shadow props (shadowOpacity: 0.05, shadowRadius: 12). Replace with:
      activityCard: {
        marginHorizontal: Spacing.base,
        marginBottom: Spacing.sm,
        backgroundColor: Colors.Beige.card,
        borderRadius: Radius.xl,
        borderWidth: 0.5,
        borderColor: Colors.Beige.border,
        ...Elevation.sm,
        padding: Spacing.md,
      }

    ROW BORDER: `rowBorder` uses Colors.border → Colors.Beige.divider.

    SECTION LABEL: `sectionLabel` uses Colors.textMuted → Colors.Beige.textMuted. fontWeight is already '600' — keep.

    CATEGORY TABS: `tab` uses Colors.bgCard and Colors.border → Colors.Beige.card, Colors.Beige.border. `tabTxt` uses Colors.textSecondary → Colors.Beige.textSecondary.

    EQUIP CHIP: `equipChip` uses Colors.bgSecondary → Colors.Beige.bgShade. `equipChipTxt` uses Colors.textMuted → Colors.Beige.textMuted.

    ACTIVITY LABEL: `activityLabel` uses Colors.textMuted → Colors.Beige.textMuted.
    ACTIVITY EMPTY: `activityEmpty` uses Colors.textMuted → Colors.Beige.textMuted.
    ACTIVITY DIVIDER: `activityDivider` uses Colors.border → Colors.Beige.border.

    LOG ROWS: `logName` uses Colors.textPrimary → Colors.Beige.text. `logMeta` uses Colors.textMuted → Colors.Beige.textMuted.

    EXERCISE ROWS: `exName` uses Colors.textPrimary → Colors.Beige.text. `exTarget` uses Colors.textMuted → Colors.Beige.textMuted.

    MODAL SHEET: `sheet` uses Colors.bgCard → Colors.Beige.card. `sheetHandle` uses Colors.border → Colors.Beige.border. `logFields` uses Colors.bg → Colors.Beige.bg.

    INTENSITY CHIPS: `intensityChip` uses Colors.bgSecondary → Colors.Beige.bgShade. Colors.border → Colors.Beige.border.

    FIELD ROW BORDER: `fieldRowBorder` uses Colors.border → Colors.Beige.border.

    INLINE placeholderTextColor in QuickLogModal TextInput: Colors.textMuted → Colors.Beige.textMuted.

    Do NOT change: Colors.primary, Colors.primaryBg, Colors.primaryBorder, Colors.primaryLight, Colors.status.*, Colors.danger.

    Note: `saveBtn` has its own shadowColor: Colors.primary — this is intentional green glow on the save button, not a card shadow. Do NOT replace this with Elevation.sm. Keep it as a manual green shadow.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep -c "Colors\.Beige\." src/screens/ExerciseScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ExerciseScreen.tsx contains `Colors.Beige.bg`
    - ExerciseScreen.tsx contains `Colors.Beige.card`
    - ExerciseScreen.tsx contains `Colors.Beige.textMuted`
    - ExerciseScreen.tsx contains `...Elevation.sm`
    - ExerciseScreen.tsx contains `borderWidth: 0.5` on the `card` style
    - ExerciseScreen.tsx contains `borderRadius: Radius.xl` on the `card` style
    - ExerciseScreen.tsx contains `setStatusBarStyle('dark')`
    - ExerciseScreen.tsx does NOT contain `Colors.bgCard` or `Colors.bgSecondary` or `Colors.textPrimary` as standalone references
    - grep -c "'#[0-9A-Fa-f]" src/screens/ExerciseScreen.tsx returns 0
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    ExerciseScreen stylesheet fully migrated to Beige tokens. Cards at Radius.xl / borderWidth 0.5 / Elevation.sm. Status bar dark on focus. No hardcoded hex. tsc passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add motivating empty state to ExerciseScreen</name>
  <files>src/screens/ExerciseScreen.tsx</files>
  <read_first>
    - src/screens/ExerciseScreen.tsx (focus on the JSX return block ~lines 223-385 — specifically the ScrollView and activityCard section; and the existing StyleSheet block at the bottom)
    - .planning/phases/06-warm-ui-overhaul/06-UI-SPEC.md (Empty State Specification — Exercise section, Shared Empty State Anatomy, Copywriting Contract)
  </read_first>
  <action>
    The exercise screen currently has NO screen-level empty state for logs.length === 0. The "activityCard" shows "Log a workout below to track today's movement" when todayLogs.length === 0 — but this is not the motivating empty state spec.

    PLACEMENT: Inside the main ScrollView (the outer ScrollView that wraps the exercise library, starting around line 288), add the empty state card as the first child, shown conditionally when `logs.length === 0` (total logs, not todayLogs). Place it ABOVE the existing `{todayLogs.length > 0 && (...)}` block.

    JSX: Add this block inside the ScrollView, before the todayLogs section:
      {logs.length === 0 && (
        <View style={s.emptyStateCard}>
          <Text style={s.emptyStateIcon}>🏃</Text>
          <Text style={s.emptyStateHeadline}>Move daily. Live longer.</Text>
          <Text style={s.emptyStateBody}>
            Log your first workout to start tracking your movement. Consistency compounds — even a 20-minute walk counts.
          </Text>
          <TouchableOpacity
            style={s.emptyStateCta}
            onPress={() => {
              Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Light).catch(() => null);
              setLogModal(EXERCISES[0]);
            }}
            activeOpacity={0.82}
          >
            <Text style={s.emptyStateCtaTxt}>Log a Workout</Text>
          </TouchableOpacity>
        </View>
      )}

    Note: EXERCISES is already imported at the top of the file from '../data/exercises'. EXERCISES[0] is the first exercise in the static list — this opens QuickLogModal for the user to log it.

    STYLES: Add these styles to the s = StyleSheet.create block at the bottom of the file:
      emptyStateCard: {
        marginHorizontal: Spacing.base,
        backgroundColor: Colors.Beige.card,
        borderRadius: Radius.xl,
        borderWidth: 0.5,
        borderColor: Colors.Beige.border,
        ...Elevation.sm,
        padding: Spacing.xl,
        alignItems: 'center',
        marginBottom: Spacing.base,
        overflow: 'hidden',
      },
      emptyStateIcon: {
        fontSize: 40,
        marginBottom: Spacing.md,
      },
      emptyStateHeadline: {
        fontSize: 18,
        fontWeight: '600',
        color: Colors.Beige.text,
        textAlign: 'center',
        lineHeight: 24,
        marginBottom: Spacing.sm,
      },
      emptyStateBody: {
        fontSize: 14,
        fontWeight: '400',
        color: Colors.Beige.textSecondary,
        textAlign: 'center',
        lineHeight: 22,
        marginBottom: Spacing.lg,
      },
      emptyStateCta: {
        backgroundColor: Colors.primary,
        borderRadius: Radius.xl,
        minHeight: 44,
        alignSelf: 'stretch',
        justifyContent: 'center',
        alignItems: 'center',
        paddingHorizontal: Spacing.base,
      },
      emptyStateCtaTxt: {
        color: Colors.Beige.card,
        fontSize: 14,
        fontWeight: '600',
      },

    COMPONENT SIZE CHECK: After adding the empty state, verify the ExerciseScreen component body is within the 200-line limit per CLAUDE.md rules. The file is currently ~585 lines total including StyleSheet. The main component function body is well under 200 lines — no extraction needed.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep -n "Move daily\|Log a Workout\|emptyStateCard" src/screens/ExerciseScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ExerciseScreen.tsx contains `"Move daily. Live longer."` (exact copy including period)
    - ExerciseScreen.tsx contains `"Log a Workout"` (exact CTA label)
    - ExerciseScreen.tsx contains `"Consistency compounds"` (body copy presence)
    - ExerciseScreen.tsx contains `logs.length === 0` as the trigger condition
    - ExerciseScreen.tsx contains `emptyStateCard` style with `borderRadius: Radius.xl` and `...Elevation.sm`
    - ExerciseScreen.tsx contains `emptyStateCta` style with `minHeight: 44`
    - ExerciseScreen.tsx contains `EXERCISES[0]` as the CTA target (opens QuickLogModal)
    - grep -c "'#[0-9A-Fa-f]" src/screens/ExerciseScreen.tsx returns 0 (no hex in new styles)
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    Exercise screen shows motivating empty state when logs.length === 0: 🏃 icon at fontSize 40, "Move daily. Live longer." headline, correct body copy, full-width "Log a Workout" CTA button opening QuickLogModal for EXERCISES[0].
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage → exercise log | loadLogs reads JSON from storage; existing JSON.parse with no sanitization — pre-existing pattern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | Exercise log TextInputs in QuickLogModal | accept | parseInt/parseFloat validation already present; no new input vectors |
| T-06-SC | Tampering | npm installs | accept | No package installs |
</threat_model>

<verification>
Run after both tasks complete:

```bash
cd /Users/bekircemkusdemir/Downloads/vitalspan

# 1. Verify Beige coverage
grep -c "Colors\.Beige\." src/screens/ExerciseScreen.tsx

# 2. Verify empty state copy
grep -n "Move daily\|Log a Workout\|Consistency compounds" src/screens/ExerciseScreen.tsx

# 3. Verify card spec
grep -A8 "^  card:" src/screens/ExerciseScreen.tsx | grep "Radius\|borderWidth\|Elevation"

# 4. Verify no legacy tokens
grep -n "Colors\.bgCard\|Colors\.bgSecondary\|Colors\.textPrimary\b" src/screens/ExerciseScreen.tsx

# 5. Verify no hardcoded hex
grep -n "'#[0-9A-Fa-f]" src/screens/ExerciseScreen.tsx

# 6. TypeScript
npx tsc --noEmit 2>&1 | head -20
```
</verification>

<success_criteria>
1. ExerciseScreen uses Colors.Beige.* throughout — no legacy Colors.bgCard or Colors.textPrimary
2. Card spec: Radius.xl / borderWidth 0.5 / Elevation.sm on both `card` and `activityCard`
3. Empty state: logs.length === 0 trigger, 🏃 icon, "Move daily. Live longer." headline, "Log a Workout" CTA
4. Status bar: setStatusBarStyle('dark') in useFocusEffect
5. Zero hardcoded hex
6. tsc --noEmit passes
</success_criteria>

<output>
Create `.planning/phases/06-warm-ui-overhaul/06-04-SUMMARY.md` when done
</output>
