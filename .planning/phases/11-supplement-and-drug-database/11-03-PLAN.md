---
phase: 11-supplement-and-drug-database
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - src/screens/ProtocolScreen.tsx
  - src/components/SupplementLibrarySection.tsx
autonomous: true
requirements:
  - SUPP-01
  - SUPP-02

must_haves:
  truths:
    - "Protocol screen shows a 'Supplement Library' section below the active protocol with categorized supplements"
    - "Library section has a search bar that filters entries by name, shortDescription, and mechanismOfAction"
    - "Categories are collapsible with category headers showing name and item count"
    - "Each supplement row shows: name, evidence grade badge, shortDescription, and rxLabel badge (if set)"
    - "Tapping a row expands inline to show mechanismOfAction, longevityRelevance, dose, timing, and an Add/In Stack button"
    - "'Add to protocol' button calls existing toggleSupplement — no new AsyncStorage keys created"
    - "Component extracted to SupplementLibrarySection.tsx to keep ProtocolScreen component under 200 lines"
    - "All colors from Colors.Beige.* (warm screen convention); no hardcoded hex values; tsc --noEmit passes"
  artifacts:
    - path: "src/components/SupplementLibrarySection.tsx"
      provides: "Supplement Library section component with search, category grouping, inline expansion, Add to protocol"
      min_lines: 50
      contains: "SupplementLibrarySection"
    - path: "src/screens/ProtocolScreen.tsx"
      provides: "Protocol screen with Library section wired below active protocol"
      contains: "SupplementLibrarySection"
  key_links:
    - from: "SupplementLibrarySection.onToggle"
      to: "ProtocolScreen.toggleSupplement"
      via: "prop callback"
      pattern: "onToggle.*toggleSupplement"
    - from: "SupplementLibrarySection"
      to: "SUPPLEMENT_DATABASE"
      via: "direct import in component"
      pattern: "import.*SUPPLEMENT_DATABASE"
---

<objective>
Create src/components/SupplementLibrarySection.tsx with a searchable, categorized library of all supplements from SUPPLEMENT_DATABASE, and wire it into ProtocolScreen below the active protocol section.

Purpose: Delivers the browsable supplement library (D-06, D-07). Users can discover any of the ~65 supplements, read evidence-graded summaries, and add them to their protocol — without touching the existing BASE_SUPPLEMENTS/GOAL_SUPPLEMENTS protocol logic.

Output: SupplementLibrarySection.tsx component + ProtocolScreen wiring. ProtocolScreen component function stays under 200 lines.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/STATE.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/11-supplement-and-drug-database/11-01-SUMMARY.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/11-supplement-and-drug-database/11-02-SUMMARY.md
</context>

<interfaces>
<!-- ProtocolScreen toggleSupplement function (lines 401–408) — call this unchanged from Library -->
```typescript
// src/screens/ProtocolScreen.tsx — existing function, do NOT modify:
function toggleSupplement(name: string) {
  const isAdded = protocol.addedSupplements.includes(name);
  const addedSupplements = isAdded
    ? protocol.addedSupplements.filter(s => s !== name)
    : [...protocol.addedSupplements, name];
  const taken = isAdded ? protocol.taken.filter(t => t !== name) : protocol.taken;
  persist({ ...protocol, addedSupplements, taken });
}
```

<!-- ProtocolScreen ProtocolState shape (from CLAUDE.md) -->
```typescript
interface ProtocolState {
  medTimes: Record<string, TimeSlot>;
  addedSupplements: string[];   // names — Library "Add to protocol" pushes here
  customSupplements: CustomSupplement[];
  taken: string[];
  takenDate: string;
}
```

<!-- SupplementInfo interface (from supplementTimings.ts, extended by Plan 11-01) -->
```typescript
export interface SupplementInfo {
  id: string;
  name: string;
  category: 'nad' | 'mitochondrial' | 'cardiovascular' | 'metabolic' | 'sleep' | 'antioxidant' | 'mineral' | 'vitamin' | 'adaptogen' | 'amino_acid' | 'nootropic' | 'senolytic' | 'prescription_only';
  defaultDose: string;
  timing: SupplementTiming;
  bestTime: BestTime;
  avoidWith: string[];
  separateFromMeds: { drug: string; hours: number; reason: string }[];
  reason: string;
  evidenceGrade: EvidenceGrade;
  shortDescription: string;
  contraindications?: string[];
  prescriptionOnly?: boolean;
  rxNote?: string;
  mechanismOfAction?: string;    // Added by 11-01
  longevityRelevance?: string;   // Added by 11-01
  rxLabel?: string;              // Added by 11-01
}
```

<!-- Theme tokens used in ProtocolScreen (warm beige convention) -->
<!-- All styles in ProtocolScreen use Colors.Beige.* namespace -->
<!-- Key tokens: Colors.Beige.bg, Colors.Beige.card, Colors.Beige.text, Colors.Beige.textMuted, Colors.Beige.border -->
<!-- Colors.primary, Colors.primaryBg, Colors.primaryBorder, Colors.warning, Colors.warningBg -->
<!-- Spacing.*, Radius.*, Typography.sizes.* -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create SupplementLibrarySection.tsx component</name>
  <read_first>
    src/screens/ProtocolScreen.tsx
    src/data/supplementTimings.ts
    src/theme/index.ts
  </read_first>
  <files>src/components/SupplementLibrarySection.tsx</files>
  <action>
    Read ProtocolScreen.tsx first to understand AddCustomSupplementModal (lines 113–198) — this is the closest analog. Read supplementTimings.ts to understand the current SUPPLEMENT_DATABASE shape including the new fields from 11-01. Read theme/index.ts to understand Colors.Beige.* token names.

    Create src/components/SupplementLibrarySection.tsx as a new component. This file must stay under 200 lines.

    Props interface:
    - addedSupplements: string[] — names currently in user's protocol
    - onToggle: (name: string) => void — called when user taps Add/Remove

    Internal state:
    - libSearch: string (useState(''))
    - libExpanded: Set string (useState(new Set()))

    Category order constant (module level):
    const LIBRARY_CATEGORY_ORDER: SupplementInfo['category'][] = [
      'nad', 'mitochondrial', 'senolytic', 'adaptogen', 'nootropic',
      'vitamin', 'mineral', 'antioxidant', 'amino_acid', 'metabolic',
      'cardiovascular', 'prescription_only',
    ];

    Category label map (module level):
    const LIBRARY_CATEGORY_LABELS: Record<SupplementInfo['category'], string> = {
      nad: 'NAD+ Pathway', mitochondrial: 'Mitochondrial', senolytic: 'Senolytics',
      adaptogen: 'Adaptogens', nootropic: 'Nootropics', vitamin: 'Vitamins',
      mineral: 'Minerals', antioxidant: 'Antioxidants & Polyphenols',
      amino_acid: 'Amino Acids', metabolic: 'Metabolic', cardiovascular: 'Cardiovascular',
      prescription_only: 'Prescription / Drug Classes',
    };

    Filtering logic (useMemo):
    - Filter SUPPLEMENT_DATABASE by libSearch (case-insensitive substring match on name, shortDescription, and mechanismOfAction)
    - If libSearch is empty, show all entries

    Rendering structure:
    1. TextInput search bar — placeholder "Search supplements...", value=libSearch, onChangeText=setLibSearch
    2. For each category in LIBRARY_CATEGORY_ORDER where at least one filtered entry belongs:
       a. Collapsible category header row: label + item count + chevron (▾/▸)
         - Toggle via libExpanded Set (use key = category string)
       b. When expanded: render each supplement row with:
         - Evidence grade badge (circle, 26x26, A=Colors.primaryBg/Colors.primary, B/C=Colors.warningBg/Colors.warning)
         - Name (Typography.sizes.base, fontWeight '500', Colors.Beige.text)
         - shortDescription (Typography.sizes.xs, Colors.Beige.textMuted)
         - rxLabel badge if present (9px uppercase bold, Colors.warning, Colors.warningBg)
         - Chevron indicating row expansion state
       c. When row is expanded (key = 'detail_' + info.id in libExpanded):
         - mechanismOfAction if present: "How: " + text
         - longevityRelevance if present: "Why: " + text
         - dose + timing line
         - Add/Remove button: calls Haptics.selectionAsync() then onToggle(info.name)
           Button text: isAdded ? '✓ In your stack' : '+ Add to protocol'
           Button style: when added use Colors.primary background, else Colors.primaryBg background with Colors.primaryBorder border

    StyleSheet: named 's' at bottom of file. All colors via Colors.Beige.* or Colors.* tokens (no hardcoded hex). All spacing via Spacing.*. All radii via Radius.*. All font sizes via Typography.sizes.*.

    Required imports: React, useState, useMemo, useCallback from 'react'; View, Text, TextInput, TouchableOpacity, StyleSheet from 'react-native'; Haptics from 'expo-haptics'; Colors, Spacing, Radius, Typography from '../theme'; SUPPLEMENT_DATABASE, SupplementInfo from '../data/supplementTimings'.

    The component must be a default export named SupplementLibrarySection.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | grep "SupplementLibrary" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/SupplementLibrarySection.tsx exists
    - grep "export default function SupplementLibrarySection" src/components/SupplementLibrarySection.tsx returns 1 match
    - grep "onToggle" src/components/SupplementLibrarySection.tsx returns >= 2 matches (prop declaration + usage)
    - grep "Colors.Beige" src/components/SupplementLibrarySection.tsx returns >= 3 matches (warm screen convention)
    - grep -c "^" src/components/SupplementLibrarySection.tsx returns <= 200 (line count under 200)
    - grep "Colors.primaryBg\|Colors.primary" src/components/SupplementLibrarySection.tsx returns >= 2 matches (grade badge + add button colors)
    - No hardcoded hex values (grep "#[0-9a-fA-F]" src/components/SupplementLibrarySection.tsx returns 0)
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>SupplementLibrarySection.tsx created with search, category grouping, inline expansion, Add button — under 200 lines, zero TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SupplementLibrarySection into ProtocolScreen</name>
  <read_first>
    src/screens/ProtocolScreen.tsx
    src/components/SupplementLibrarySection.tsx
  </read_first>
  <files>src/screens/ProtocolScreen.tsx</files>
  <action>
    Read ProtocolScreen.tsx fully to understand current structure before modifying. The file is 942 lines — read it to find:
    - The section label render pattern (sectionLabel style) around line 718
    - The addStackBtn TouchableOpacity (the "Add supplement" button around line 718)
    - The ScrollView close tag and bottom spacer (around line 850)

    Make two changes to ProtocolScreen.tsx:

    Change 1 — Add import at top of file (with other component imports):
    import SupplementLibrarySection from '../components/SupplementLibrarySection';

    Change 2 — Add the Library section to the JSX. Insert AFTER the "addStackBtn" TouchableOpacity (the "+ Add supplement" button) and BEFORE the final bottom spacer View. Insert:

    {/* Supplement Library */}
    <View style={s.libDivider} />
    <Text style={s.sectionLabel}>Supplement Library</Text>
    <SupplementLibrarySection
      addedSupplements={protocol.addedSupplements}
      onToggle={toggleSupplement}
    />

    Change 3 — Add the libDivider style to the StyleSheet 's' at the bottom of ProtocolScreen.tsx:
    libDivider: { height: 1, backgroundColor: Colors.Beige.border, marginHorizontal: Spacing.base, marginVertical: Spacing.lg },

    Do NOT modify toggleSupplement, the ProtocolState shape, BASE_SUPPLEMENTS, GOAL_SUPPLEMENTS, or any existing protocol logic. The Library section is additive only.

    After the change, verify that the ProtocolScreen component function (the function body starting around line 328) does not exceed 200 lines by counting the component function lines, not the full file.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep "SupplementLibrarySection" src/screens/ProtocolScreen.tsx && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "import SupplementLibrarySection" src/screens/ProtocolScreen.tsx returns 1 match
    - grep "SupplementLibrarySection" src/screens/ProtocolScreen.tsx returns >= 2 matches (import + JSX usage)
    - grep "addedSupplements={protocol.addedSupplements}" src/screens/ProtocolScreen.tsx returns 1 match
    - grep "onToggle={toggleSupplement}" src/screens/ProtocolScreen.tsx returns 1 match
    - Existing toggleSupplement function is unchanged (grep toggleSupplement returns same occurrences as before)
    - grep "Supplement Library" src/screens/ProtocolScreen.tsx returns >= 1 match (section header text)
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>SupplementLibrarySection wired into ProtocolScreen below active protocol; existing protocol logic unchanged; zero TypeScript errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User text input (search bar) → Array.filter | Search query is a plain string; only used with .toLowerCase().includes() — no regex injection possible |
| Library "Add to protocol" → toggleSupplement → AsyncStorage | onToggle calls existing tested function; no new storage keys created |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering | Library "Add to protocol" button | accept | Calls existing toggleSupplement function which already validates ProtocolState; no new code path introduced |
| T-11-06 | Denial of Service | Search input on 65-entry array | accept | Array.filter on ~65 static items is negligible; useMemo prevents re-computation until libSearch changes |
| T-11-SC | Tampering | npm/pip installs | accept | No new packages installed |
</threat_model>

<verification>
After both tasks complete:
1. src/components/SupplementLibrarySection.tsx exists and has <= 200 lines
2. grep "SupplementLibrarySection" src/screens/ProtocolScreen.tsx returns >= 2 matches
3. No hardcoded hex values in SupplementLibrarySection.tsx
4. npx tsc --noEmit exits with code 0
</verification>

<success_criteria>
- SupplementLibrarySection.tsx created with search + category grouping + inline expand + Add button
- Wired into ProtocolScreen below active protocol
- All colors from Colors.Beige.* (per warm screen convention)
- Component file under 200 lines
- Existing protocol logic (BASE_SUPPLEMENTS, GOAL_SUPPLEMENTS, toggleSupplement) unchanged
- tsc --noEmit exits 0
</success_criteria>

<output>
Create .planning/phases/11-supplement-and-drug-database/11-03-SUMMARY.md when done
</output>
