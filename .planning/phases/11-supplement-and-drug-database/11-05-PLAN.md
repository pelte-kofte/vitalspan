---
phase: 11-supplement-and-drug-database
plan: 05
type: execute
wave: 4
depends_on:
  - 11-03
  - 11-04
files_modified: []
autonomous: false
requirements:
  - SUPP-01
  - SUPP-02
  - SUPP-03
  - SUPP-04

must_haves:
  truths:
    - "tsc --noEmit passes with zero TypeScript errors across the entire codebase"
    - "No hardcoded hex values in any modified file (supplementTimings.ts, biomarkers.ts, InteractionCheckerScreen.tsx, ProtocolScreen.tsx, SupplementLibrarySection.tsx)"
    - "No Supabase URL or anon key present in source files"
    - "Supplement Library section is visible and functional in the Protocol tab"
    - "Interaction Checker auto-populates from user's existing protocol stack on first open"
    - "Categorized chip sections appear and are expandable in the Interaction Checker"
    - "INTERACTIONS lookup returns results for new pairs (e.g., searching CBD + Warfarin shows the high-risk flag)"
  artifacts:
    - path: "src/data/supplementTimings.ts"
      provides: "Extended SupplementInfo interface and ~65 entries"
      contains: "mechanismOfAction"
    - path: "src/data/biomarkers.ts"
      provides: "50+ interaction pairs"
      contains: "cbd-warfarin"
    - path: "src/components/SupplementLibrarySection.tsx"
      provides: "Library component"
      contains: "SupplementLibrarySection"
    - path: "src/screens/ProtocolScreen.tsx"
      provides: "Protocol screen with Library wired in"
      contains: "SupplementLibrarySection"
    - path: "src/screens/InteractionCheckerScreen.tsx"
      provides: "Auto-populate + categorized chips"
      contains: "CATEGORY_TO_DRUG_CLASS"
  key_links:
    - from: "All modified files"
      to: "TypeScript compiler"
      via: "tsc --noEmit"
      pattern: "exit code 0"
---

<objective>
Run the full verification suite for Phase 11: TypeScript compilation, source audit (no secrets), and an on-device UI checkpoint confirming the Library section and Interaction Checker work end-to-end.

Purpose: Gates the phase as complete and surfaces any integration issues before moving to Phase 12.

Output: Phase 11 verified as production-ready. All SUPP-01 through SUPP-04 requirements satisfied and auditable.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/STATE.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/11-supplement-and-drug-database/11-01-SUMMARY.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/11-supplement-and-drug-database/11-02-SUMMARY.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/11-supplement-and-drug-database/11-03-SUMMARY.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/11-supplement-and-drug-database/11-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript compilation + source audit</name>
  <read_first>
    src/data/supplementTimings.ts
    src/data/biomarkers.ts
    src/screens/InteractionCheckerScreen.tsx
    src/screens/ProtocolScreen.tsx
    src/components/SupplementLibrarySection.tsx
  </read_first>
  <files></files>
  <action>
    Run all source audits. Report results for each check. If any check fails, identify the specific file and line for remediation.

    Audit 1 — TypeScript compilation:
    Run: npx tsc --noEmit
    Expected: exit code 0, no errors. If errors exist, fix them before proceeding.

    Audit 2 — No hardcoded hex values in Phase 11 modified files:
    Run: grep -n "#[0-9a-fA-F]\{3,6\}" src/data/supplementTimings.ts src/data/biomarkers.ts src/screens/InteractionCheckerScreen.tsx src/screens/ProtocolScreen.tsx src/components/SupplementLibrarySection.tsx 2>/dev/null
    Expected: zero matches. If any found, replace with Colors.* token.

    Audit 3 — No Supabase secrets in source:
    Run: grep -r "supabase.co\|eyJhbGci" src/ --include="*.ts" --include="*.tsx" 2>/dev/null | grep -v ".env"
    Expected: zero matches.

    Audit 4 — SUPPLEMENT_DATABASE entry count:
    Run: grep -c "id:" src/data/supplementTimings.ts
    Expected: >= 65 (was 47, added 18 new supplements + 4 drug classes)

    Audit 5 — INTERACTIONS pair count:
    Run: grep -c "severity:" src/data/biomarkers.ts
    Expected: >= 50 (was 30, added 23 new pairs)

    Audit 6 — SupplementLibrarySection file size:
    Run: wc -l src/components/SupplementLibrarySection.tsx
    Expected: <= 200 lines.

    Audit 7 — SAFE_COMBOS count:
    Run: grep -c "pair:" src/screens/InteractionCheckerScreen.tsx
    Expected: 11.

    Audit 8 — SUPPLEMENTS const is removed:
    Run: grep "const SUPPLEMENTS" src/screens/InteractionCheckerScreen.tsx
    Expected: zero matches.

    Audit 9 — Required new fields present in interface:
    Run: grep -c "mechanismOfAction\|longevityRelevance\|rxLabel" src/data/supplementTimings.ts
    Expected: >= 3 (one per field in interface declaration).

    Report results in a table. Fix any failures before marking this task done.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && npx tsc --noEmit 2>&1 | tail -3 && echo "TSC_EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits with code 0 (zero TypeScript errors)
    - grep "#[0-9a-fA-F]" across 5 modified files returns 0 matches in new style blocks
    - grep -r "supabase.co\|eyJhbGci" src/ --include="*.ts" --include="*.tsx" returns 0 matches
    - grep -c "id:" src/data/supplementTimings.ts returns >= 65
    - grep -c "severity:" src/data/biomarkers.ts returns >= 50
    - wc -l src/components/SupplementLibrarySection.tsx reports <= 200
    - grep -c "pair:" src/screens/InteractionCheckerScreen.tsx returns 11
    - grep "const SUPPLEMENTS" src/screens/InteractionCheckerScreen.tsx returns 0 matches
  </acceptance_criteria>
  <done>All 9 audits pass; zero TypeScript errors; zero secrets; entry counts meet targets.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 11 implementation complete. Claude has:
    1. Extended SupplementInfo interface with mechanismOfAction, longevityRelevance, rxLabel fields
    2. Added 18 new supplement entries + 4 drug class entries to SUPPLEMENT_DATABASE (~65 total)
    3. Expanded INTERACTIONS from 30 to ~53 pharmacist-reviewed pairs
    4. Expanded SAFE_COMBOS from 4 to 11 synergistic pairs
    5. Created SupplementLibrarySection.tsx — searchable, categorized, expandable library
    6. Wired Library section into ProtocolScreen below active protocol
    7. Upgraded InteractionChecker with auto-populate from user's stack + categorized chip sections
  </what-built>
  <how-to-verify>
    Run the app: npx expo start --ios (or use your existing Expo Go setup)

    Verify the following user flows:

    FLOW 1 — Protocol Tab / Supplement Library:
    1. Open the Protocol tab
    2. Scroll below the active protocol section
    3. Confirm "Supplement Library" section header is visible
    4. Confirm search bar is visible and functional (type "NMN" — should filter to NAD+ entries)
    5. Tap a category header (e.g., "NAD+ Pathway") — it should expand/collapse
    6. Tap a supplement row — it should expand inline showing "How: " and "Why: " text
    7. Confirm rxLabel badge appears on prescription-only entries (Rapamycin, Metformin, drug classes)
    8. Tap "+ Add to protocol" on a supplement — confirm it appears in your active protocol

    FLOW 2 — Interaction Checker auto-populate:
    1. Add at least one supplement to your protocol in Flow 1
    2. Navigate to the Interaction Checker (from Protocol tab or wherever it is accessible)
    3. Confirm the supplement you added appears as a pre-populated chip in the "Checking interactions for" section
    4. If you have medications in your profile, confirm they appear as drug chips (resolved to drug class)

    FLOW 3 — Interaction Checker categorized chips:
    1. In the Interaction Checker "Supplements" section, confirm chip rows are grouped by category
    2. Confirm NAD+ Pathway and Mitochondrial categories start expanded
    3. Tap a collapsed category header — it should expand showing supplement chips
    4. Tap a supplement chip — it should add to the "Checking interactions for" section
    5. Add CBD and Warfarin — confirm the high-risk CBD+Warfarin interaction flag appears with recommendation text

    FLOW 4 — Safe Combos tab:
    1. Tap the "Safe combos" tab in Interaction Checker
    2. Confirm >= 11 synergistic pairs listed
    3. Confirm "NMN + Apigenin", "Urolithin A + NMN", "GlyNAC" entries appear

    Report any issues found. If all flows work, type "approved".
  </how-to-verify>
  <resume-signal>
    Type "approved" if all four flows work correctly.
    Or describe any issues found (e.g., "Library section categories not collapsing", "Auto-populate not firing").
    After fixes are applied, type "approved" to complete Phase 11.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code → deployed app | Verification confirms no secrets leaked into production build paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Information Disclosure | Supabase secrets in source | mitigate | Audit 3 in Task 1 explicitly greps for supabase.co and eyJhbGci patterns |
| T-11-SC | Tampering | npm/pip installs | accept | No new packages in this phase; no package legitimacy concern |
</threat_model>

<verification>
Phase 11 is complete when:
1. Task 1 all 9 audits pass with zero failures
2. Human checkpoint (Task 2) approved
3. All SUPP-01 through SUPP-04 requirements verified by pharmacist user
</verification>

<success_criteria>
- tsc --noEmit exits 0 (SUPP-01, SUPP-02)
- SUPPLEMENT_DATABASE has >= 65 entries with mechanismOfAction, longevityRelevance fields (SUPP-01, SUPP-02)
- INTERACTIONS has >= 50 pairs with recommendation fields (SUPP-03, SUPP-04)
- SupplementLibrarySection renders in Protocol tab with search + categories + inline expand (SUPP-01, SUPP-02)
- InteractionChecker auto-populates from user's stack and shows categorized chips (SUPP-03)
- All interaction flags have actionable recommendations — no flag without "what to do" (SUPP-04)
- Zero secrets in source; zero hardcoded hex values in new code
- Human UI checkpoint approved
</success_criteria>

<output>
Create .planning/phases/11-supplement-and-drug-database/11-05-SUMMARY.md when done
</output>
