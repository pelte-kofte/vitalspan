---
phase: 11-supplement-and-drug-database
plan: 04
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - src/screens/InteractionCheckerScreen.tsx
autonomous: true
requirements:
  - SUPP-03
  - SUPP-04

must_haves:
  truths:
    - "InteractionChecker auto-populates from ProtocolState.addedSupplements on first open"
    - "InteractionChecker auto-populates from UserProfile.medications via MEDICATION_DATABASE category lookup on first open"
    - "Medication → drug class resolution: 'statin'→'Statin', 'nsaid'→'Ibuprofen', 'thyroid'→'Levothyroxine', 'diabetes'→'Metformin', 'anticoagulant'→'Warfarin'"
    - "Auto-populate runs once per session (autoPopulated guard) — navigating away and back does not re-append items"
    - "Hardcoded SUPPLEMENTS const (8 names) replaced with chips derived from SUPPLEMENT_DATABASE grouped by category"
    - "Category chip sections are collapsible with expand/collapse toggle"
    - "NAD+ and Mitochondrial categories start expanded by default"
    - "tsc --noEmit passes with zero errors"
  artifacts:
    - path: "src/screens/InteractionCheckerScreen.tsx"
      provides: "Auto-populate on mount + categorized expandable chip sections"
      contains: "CATEGORY_TO_DRUG_CLASS"
  key_links:
    - from: "useFocusEffect auto-populate"
      to: "AsyncStorage @vitalspan_protocol + @vitalspan_user_profile"
      via: "Promise.all with AsyncStorage.getItem"
      pattern: "@vitalspan_protocol.*@vitalspan_user_profile"
    - from: "drug class resolution"
      to: "MEDICATION_DATABASE.category"
      via: "CATEGORY_TO_DRUG_CLASS map lookup"
      pattern: "CATEGORY_TO_DRUG_CLASS"
    - from: "chip render"
      to: "SUPPLEMENT_DATABASE"
      via: "chipsByCategory useMemo"
      pattern: "chipsByCategory"
---

<objective>
Upgrade InteractionCheckerScreen to auto-populate the user's current stack on open (from AsyncStorage ProtocolState + UserProfile) and replace the hardcoded 8-supplement SUPPLEMENTS const with categorized, expandable chip sections derived from SUPPLEMENT_DATABASE.

Purpose: Delivers SUPP-03 — the interaction checker evaluates the user's actual stack, not a manually curated list. Auto-populate means the first result the user sees is personalized, not generic.

Output: InteractionCheckerScreen.tsx with useFocusEffect auto-populate, CATEGORY_TO_DRUG_CLASS resolution, chipsByCategory grouped chip render — all within the existing dark-background screen's Colors.* token convention.
</objective>

<execution_context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/PROJECT.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/ROADMAP.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/STATE.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/11-supplement-and-drug-database/11-01-SUMMARY.md
@/Users/bekircemkusdemir/Downloads/vitalspan/.planning/phases/11-supplement-and-drug-database/11-02-SUMMARY.md
</context>

<interfaces>
<!-- Current InteractionCheckerScreen state and structure — lines 31–67 -->
```typescript
// Existing state declarations (keep all, add new ones after):
const [tab, setTab] = useState(0);
const [items, setItems] = useState<{ name: string; type: 'drug' | 'supp' }[]>([]);
const [expanded, setExpanded] = useState<string | null>(null);
const [rxnavResults, setRxnavResults] = useState<DrugInteractionResult[]>([]);
const [rxnavLoading, setRxnavLoading] = useState(false);

// Existing addItem function (do NOT modify — chip onPress still calls this):
function addItem(name: string, type: 'drug' | 'supp') {
  if (!items.find(i => i.name.toLowerCase() === name.toLowerCase())) {
    setItems(prev => [...prev, { name, type }]);
  }
}
```

<!-- MedicationEntry type from medications.ts — needed for resolution logic -->
```typescript
// src/data/medications.ts — MedicationEntry type (relevant fields):
interface MedicationEntry {
  genericName: string;
  brandNames: string[];
  category: string;  // e.g., 'statin', 'nsaid', 'thyroid', 'diabetes', 'anticoagulant'
  // ... other fields
}
```

<!-- ProtocolState shape (from CLAUDE.md) -->
```typescript
interface ProtocolState {
  addedSupplements: string[];  // supplement names (strings)
  // ... other fields
}
// AsyncStorage key: '@vitalspan_protocol'
```

<!-- UserProfile shape (medications field) -->
```typescript
interface UserProfile {
  medications: string[];  // medication names as strings
  // ... other fields
}
// AsyncStorage key: '@vitalspan_user_profile'
```

<!-- Existing chip render at lines 109–116 (to be replaced): -->
```typescript
// LINES 109–116 — REMOVE THIS BLOCK entirely:
<Text style={s.sectionLbl}>Supplements</Text>
<View style={s.chipRow}>
  {SUPPLEMENTS.map(d => (
    <TouchableOpacity key={d} style={[s.chip, s.chipSupp]} onPress={() => addItem(d, 'supp')}>
      <Text style={s.chipSuppTxt}>+ {d}</Text>
    </TouchableOpacity>
  ))}
</View>
```

<!-- InteractionCheckerScreen uses Colors.* (dark bg) — NOT Colors.Beige.* -->
<!-- catHeader/catLabel styles must use Colors.textMuted, Colors.bg -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add imports, module-level constants, and auto-populate useFocusEffect</name>
  <read_first>
    src/screens/InteractionCheckerScreen.tsx
    src/data/medications.ts
  </read_first>
  <files>src/screens/InteractionCheckerScreen.tsx</files>
  <action>
    Read the full InteractionCheckerScreen.tsx file. Then read src/data/medications.ts to understand MedicationEntry type and the category field values.

    Make the following changes in order:

    Step 1 — Update imports at lines 1–9:
    - Change React import to: import React, { useState, useMemo, useEffect, useCallback } from 'react';
    - Add after existing imports (after line 9):
      import { useFocusEffect } from '@react-navigation/native';
      import AsyncStorage from '@react-native-async-storage/async-storage';
      import { SUPPLEMENT_DATABASE } from '../data/supplementTimings';
      import { MEDICATION_DATABASE } from '../data/medications';

    Step 2 — Remove the hardcoded SUPPLEMENTS const at line 11:
    Remove the entire line: const SUPPLEMENTS = ['NMN', 'Omega-3', 'Berberine', 'Resveratrol', 'CoQ10', 'Vitamin K2', 'Magnesium', 'Vitamin D'];

    Step 3 — Add two module-level constants after the existing SAFE_COMBOS constant (between SAFE_COMBOS and SEVERITY_CONFIG):

    const CATEGORY_TO_DRUG_CLASS: Record<string, string> = {
      statin: 'Statin',
      nsaid: 'Ibuprofen',
      thyroid: 'Levothyroxine',
      diabetes: 'Metformin',
      anticoagulant: 'Warfarin',
    };

    const CATEGORY_LABELS: Record<string, string> = {
      nad: 'NAD+ Pathway',
      mitochondrial: 'Mitochondrial',
      senolytic: 'Senolytics',
      adaptogen: 'Adaptogens',
      nootropic: 'Nootropics',
      vitamin: 'Vitamins',
      mineral: 'Minerals',
      antioxidant: 'Antioxidants',
      amino_acid: 'Amino Acids',
      metabolic: 'Metabolic',
      cardiovascular: 'Cardiovascular',
      prescription_only: 'Prescription / Drug Classes',
      sleep: 'Sleep',
    };

    Step 4 — Inside the component function, add new state after the existing state declarations (after line 36, after rxnavLoading state):

    const [autoPopulated, setAutoPopulated] = useState(false);
    const [expandedCategories, setExpandedCategories] = useState<Set<string>>(new Set(['nad', 'mitochondrial']));

    Step 5 — Add chipsByCategory useMemo after the state declarations and before the addItem function:

    const chipsByCategory = useMemo(() => {
      const map = new Map<string, typeof SUPPLEMENT_DATABASE>();
      for (const supp of SUPPLEMENT_DATABASE) {
        if (!map.has(supp.category)) map.set(supp.category, []);
        map.get(supp.category)!.push(supp);
      }
      return map;
    }, []);

    Step 6 — Add auto-populate useFocusEffect after the chipsByCategory useMemo, before the existing useEffect (the one that calls checkDrugInteractions):

    useFocusEffect(useCallback(() => {
      if (autoPopulated) return;
      let active = true;

      async function autoPopulate() {
        const [protocolRaw, profileRaw] = await Promise.all([
          AsyncStorage.getItem('@vitalspan_protocol'),
          AsyncStorage.getItem('@vitalspan_user_profile'),
        ]);
        if (!active) return;

        const newItems: { name: string; type: 'drug' | 'supp' }[] = [];

        if (protocolRaw) {
          const protocol: { addedSupplements?: string[] } = JSON.parse(protocolRaw);
          for (const suppName of (protocol.addedSupplements ?? [])) {
            newItems.push({ name: suppName, type: 'supp' });
          }
        }

        if (profileRaw) {
          const profile: { medications?: string[] } = JSON.parse(profileRaw);
          for (const medName of (profile.medications ?? [])) {
            const entry = MEDICATION_DATABASE.find(m =>
              m.genericName.toLowerCase() === medName.toLowerCase() ||
              m.brandNames.some((b: string) => b.toLowerCase() === medName.toLowerCase())
            );
            const resolvedClass = entry
              ? (CATEGORY_TO_DRUG_CLASS[entry.category] ?? medName)
              : medName;
            newItems.push({ name: resolvedClass, type: 'drug' });
          }
        }

        if (newItems.length > 0) {
          setItems(prev => {
            const existingNames = new Set(prev.map(i => i.name.toLowerCase()));
            return [
              ...prev,
              ...newItems.filter(i => !existingNames.has(i.name.toLowerCase())),
            ];
          });
        }
        setAutoPopulated(true);
      }

      void autoPopulate();
      return () => { active = false; };
    }, [autoPopulated]));

    After these changes, run tsc --noEmit to verify zero errors before proceeding to Task 2.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep "CATEGORY_TO_DRUG_CLASS" src/screens/InteractionCheckerScreen.tsx | wc -l && grep "autoPopulated" src/screens/InteractionCheckerScreen.tsx | wc -l && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "CATEGORY_TO_DRUG_CLASS" src/screens/InteractionCheckerScreen.tsx returns >= 2 matches (declaration + usage)
    - grep "autoPopulated" src/screens/InteractionCheckerScreen.tsx returns >= 3 matches (state declaration + guard + setter)
    - grep "chipsByCategory" src/screens/InteractionCheckerScreen.tsx returns >= 1 match (useMemo declaration)
    - grep "SUPPLEMENTS" src/screens/InteractionCheckerScreen.tsx returns 0 matches (old const fully removed)
    - grep "useFocusEffect" src/screens/InteractionCheckerScreen.tsx returns >= 2 matches (import + usage)
    - grep "SUPPLEMENT_DATABASE" src/screens/InteractionCheckerScreen.tsx returns >= 1 match (import)
    - grep "MEDICATION_DATABASE" src/screens/InteractionCheckerScreen.tsx returns >= 1 match (import)
    - grep "@vitalspan_protocol" src/screens/InteractionCheckerScreen.tsx returns 1 match
    - grep "@vitalspan_user_profile" src/screens/InteractionCheckerScreen.tsx returns 1 match
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>Imports updated, SUPPLEMENTS const removed, CATEGORY_TO_DRUG_CLASS and CATEGORY_LABELS added, auto-populate useFocusEffect implemented, chipsByCategory useMemo added; zero TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Replace flat chip render with categorized expandable sections</name>
  <read_first>
    src/screens/InteractionCheckerScreen.tsx
  </read_first>
  <files>src/screens/InteractionCheckerScreen.tsx</files>
  <action>
    Read the current InteractionCheckerScreen.tsx (after Task 1's changes) to locate the chip render block at approximately lines 109–116 and the StyleSheet 's' at the bottom.

    Step 1 — Replace the Supplements chip section in JSX.
    Find the block that starts with "Supplements" section label and the chipRow View containing the SUPPLEMENTS.map. Replace the entire block (from the Supplements sectionLbl Text to the closing </View> of chipRow) with:

    <Text style={s.sectionLbl}>Supplements</Text>
    {Array.from(chipsByCategory.entries()).map(([cat, supps]) => (
      <View key={cat}>
        <TouchableOpacity
          style={s.catHeader}
          onPress={() => {
            setExpandedCategories(prev => {
              const next = new Set(prev);
              if (next.has(cat)) next.delete(cat); else next.add(cat);
              return next;
            });
          }}
        >
          <Text style={s.catLabel}>{CATEGORY_LABELS[cat] ?? cat}</Text>
          <Text style={s.catChevron}>{expandedCategories.has(cat) ? '▾' : '▸'}</Text>
        </TouchableOpacity>
        {expandedCategories.has(cat) && (
          <View style={s.chipRow}>
            {supps.map(supp => (
              <TouchableOpacity
                key={supp.id}
                style={[s.chip, s.chipSupp]}
                onPress={() => addItem(supp.name, 'supp')}
              >
                <Text style={s.chipSuppTxt}>+ {supp.name}</Text>
              </TouchableOpacity>
            ))}
          </View>
        )}
      </View>
    ))}

    Step 2 — Add new styles to the existing StyleSheet 's' at the bottom of the file (add after the last existing style, before the closing bracket):

    catHeader: {
      flexDirection: 'row',
      justifyContent: 'space-between',
      alignItems: 'center',
      paddingHorizontal: Spacing.base,
      paddingVertical: Spacing.sm,
    },
    catLabel: {
      fontSize: 11,
      fontWeight: '600' as const,
      color: Colors.textMuted,
      textTransform: 'uppercase' as const,
      letterSpacing: 1.5,
    },
    catChevron: {
      fontSize: Typography.sizes.sm,
      color: Colors.textMuted,
    },

    After these changes, run tsc --noEmit to verify zero errors.
  </action>
  <verify>
    <automated>cd /Users/bekircemkusdemir/Downloads/vitalspan && grep "chipsByCategory" src/screens/InteractionCheckerScreen.tsx | wc -l && grep "catHeader" src/screens/InteractionCheckerScreen.tsx | wc -l && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "chipsByCategory" src/screens/InteractionCheckerScreen.tsx returns >= 2 matches (useMemo + JSX usage)
    - grep "catHeader" src/screens/InteractionCheckerScreen.tsx returns >= 2 matches (JSX usage + StyleSheet definition)
    - grep "catLabel" src/screens/InteractionCheckerScreen.tsx returns >= 2 matches (JSX + StyleSheet)
    - grep "SUPPLEMENTS.map" src/screens/InteractionCheckerScreen.tsx returns 0 matches (old flat render fully removed)
    - grep "expandedCategories.has" src/screens/InteractionCheckerScreen.tsx returns >= 2 matches (chevron + conditional render)
    - No hardcoded hex values in new style additions (grep "#[0-9a-fA-F]" src/screens/InteractionCheckerScreen.tsx returns 0 for new catHeader/catLabel/catChevron styles)
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>Flat SUPPLEMENTS chip row replaced with categorized expandable sections; NAD+ and Mitochondrial categories start expanded; zero TypeScript errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AsyncStorage data → component state | Protocol and profile data from AsyncStorage is parsed with JSON.parse; typed with inline interface to prevent runtime errors |
| medication names → drug class resolution | MEDICATION_DATABASE lookup uses toLowerCase for case-insensitive matching; unresolved medications fall back to raw name (no crash) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Tampering | autoPopulated guard | accept | Guard prevents duplicate items on re-focus; existing addItem dedup provides a second layer of protection |
| T-11-08 | Information Disclosure | Auto-populated drug class names shown as chips | accept | Drug class names (e.g., 'Statin') are clinical category labels, not PII; user explicitly added medications to their profile |
| T-11-09 | Denial of Service | useMemo chipsByCategory with 65 entries | accept | One-time computation on static array; no performance concern |
| T-11-SC | Tampering | npm/pip installs | accept | No new packages installed |
</threat_model>

<verification>
After both tasks complete:
1. grep "CATEGORY_TO_DRUG_CLASS" src/screens/InteractionCheckerScreen.tsx returns >= 2 matches
2. grep "SUPPLEMENTS" src/screens/InteractionCheckerScreen.tsx returns 0 (old const fully gone)
3. grep "chipsByCategory" src/screens/InteractionCheckerScreen.tsx returns >= 2 matches
4. grep -c "pair:" src/screens/InteractionCheckerScreen.tsx returns 11 (SAFE_COMBOS from Plan 02 untouched)
5. npx tsc --noEmit exits with code 0
</verification>

<success_criteria>
- Auto-populate reads ProtocolState.addedSupplements and UserProfile.medications on first open
- CATEGORY_TO_DRUG_CLASS resolves 5 medication categories to drug class names per D-09
- autoPopulated guard prevents duplicate items on subsequent focus events
- Hardcoded SUPPLEMENTS const replaced with chipsByCategory from SUPPLEMENT_DATABASE
- Category chip sections are collapsible; NAD+ and Mitochondrial start expanded
- All styles use Colors.* (dark screen convention) — no Colors.Beige.* crossover
- tsc --noEmit exits 0
</success_criteria>

<output>
Create .planning/phases/11-supplement-and-drug-database/11-04-SUMMARY.md when done
</output>
